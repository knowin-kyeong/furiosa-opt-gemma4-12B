# SOTA.md — Road to SOTA

> **2026-09-13 (3):** draw — V360_submit 12회(UTC 09-12 16:04~17:53) 평균 6.894 · 최고 **7.1385**(`e3540caa`: 87,932 · 42,746 · 274,556). V367_submit 배치 1 시작(UTC 17:53): 7.1182 · 6.8743. 공식 최고 7.4197 그대로(2위 / 21팀; 1위 vinxst 7.4769, 3위 #663 7.3402). 세션 이관 문서 **`0913_handoff.md`**, 스크립트 사본 `scripts/dev/tk/`.

> **2026-09-13 (2):** 코드 SOTA → **`V367_submit`** (d94d994 = V360_submit + ffn 꼬리 곱 `down_global × out_scale`을 post-FF norm에 접음 — V366 T1: g를 앞쪽 작은 pass에서 한 번 만들고 mean-square는 `x·g`로, rms는 g로 나눠 final pass는 그대로). V366 짝비교 t1 − 생산 **12/16 −1,234 (−0.46%), 중앙값 −1,645**(t1이 첫 launch인 잡 8개 모두 PASS, 정적 −401과 부호 일치). **Arena 25/25 PASS ×2**(job 23353 + rerun; 첫 launch qkv 97,018 · attn 44,934 · ffn 270,373). draw 체인 → V367_submit(돌던 V360 배치 뒤, `/root/drawswitch_v367.sh`). V360_submit draw 6.9998 · 6.7698 · 6.8900. 공식 최고 7.4197 그대로(2위).

> **2026-09-13 (1):** 코드 SOTA **`V360_submit`** 그대로. 이번 라운드 V361(pair-mode RoPE +5.5%) · V362(unscaled gather 중립) · V363(공유 HBM 인덱스 +0.9%) · V364(x 경로를 Q 로드 뒤로 +0.6%) · V352y(attn c 합산 24/48) 모두 기각. draw 체인은 V350_submit 12회(최고 7.1187) 뒤 **V360_submit으로 넘어갔다**(첫 draw 6.9998). 공식 최고 7.4197 그대로(2위). ffn geglu 동기화 창을 down 로드로 채우는 순서 강제 탐침 V365 진행 중. 세션 이관 RULES §10.0w.

> **2026-09-12 (9):** 코드 SOTA → **`V360_submit`** (28f8091 = V350_submit + qkv head norm 셋과 RoPE 결과 pass 둘의 f32→bf16을 `commit_cast`로). V355 짝비교 cq − 생산 11/16 −1.62% · 확인 배치 V355b 14/16 −1.61%(p ≈ 0.004) → 합산 25/32 ≈ −1.6%(qkv). **Arena 25/25 PASS ×2**(job 23020 + rerun; 첫 launch qkv 90,429 · attn 42,792 · ffn 266,892). draw 체인 → V360_submit(돌던 V350 배치 뒤). 공식 최고 7.4197 그대로(2위).

> **2026-09-12 (8):** 코드 SOTA → **`V350_submit`** (aafc5a8 = V348_submit + ffn geglu hi/lo store 하나). V350 짝비교 생산 대비 **16/16 −2.28%**, V348 f1d 대비 16/16 −1.42%. **Arena 25/25 PASS ×2**(job 22596 + rerun; ffn 첫 launch 269,520 · 271,309). draw 체인 → V350_submit(돌던 V340 배치 뒤). 공식 최고 7.4197 그대로(2위). 세션 이관 메모 RULES §10.0v.

> **2026-09-12 (7):** 코드 SOTA → **`V348_submit`** (V340_submit + ffn down x 조각을 Lane에 두고 pass A 안에서 합침, 짝비교 16/16 −1.27%; Arena 25/25 PASS ×2, 88b0d90). V340_submit draw 60회 평균 ≈6.85 · 최고 7.2491 — 공식 최고 7.4197 그대로(2위; 1위 vinxst 7.4769, 3위 #663 7.3402). draw 체인 → V348_submit. 다음 후보 V349(geglu store 하나, −0.43% 13/16)의 합산을 V350에서 짝비교 중.

> **2026-09-12 (6):** 코드 SOTA → **`V340_submit`** (V313_submit + attn_out 비균등 타일: tile0 두 클러스터 대칭 · tile1 클러스터 0 전용 · 꼬리 행 DM→DM; 짝비교 32잡 24/32 −1.2%, p≈0.007; Arena 25/25 PASS ×2, fd9eeda). 공식: **1위 vinxst 7.4769** (`97bbe09c` — 89,174 / **33,437** / 301,213) · **우리 2위 7.4197** (`a9c7c0d6`, V313_submit draw — 90,189 / 38,347 / 265,737) · 3위 #663 7.3097. vinxst의 attn은 우리 draw 137회 최저보다 13% 아래라 구조 차이다(RULES §10.0u).

> **2026-09-12 (5):** 코드 SOTA → **`V313_submit`** (V306_submit + ffn down 출력 단일 store, 짝비교 12/12 −1.04%; Arena 25/25 PASS ×2, 8a38b1b). V306_submit draw 최고 7.2047 — 공식 최고 7.2264 그대로 (2위).

> **2026-09-12 (4):** 코드 SOTA → **`V306_submit`** (V301_submit + ffn out_scale을 꼬리 곱으로, 짝비교 12/12 −1.76%; Arena 25/25 PASS ×2, a589eed). V301_submit draw 6.82 / 6.34 / 7.03 / 6.69 / 6.90 / 6.45 — 공식 최고 7.2264 그대로 (2위).

> **2026-09-12 (3):** 코드 SOTA → **`V301_submit`** (V299_submit + ffn pass A x 조각 Lane, 짝비교 8/8 −2.3%). V299_submit draw 6.7338 외 진행 중. 공식 최고 7.2264 (2위).

> **2026-09-12 (2):** 코드 SOTA → **`V299_submit`** (V293_submit + qkv Q/K/V weight head 안 행 교차, 짝비교 8/8 −8.8%). 공식 최고 **7.2264** (V293_submit draw 091cfa9f, 2위; 1위 #663 7.3097). RULES §10.0s.

> **2026-09-12:** 코드 SOTA → **`V293_submit` `4f8cb73`** (V273 + V292 qkv 칩 안 x 복제 −1.3% + V293 ffn −0.6%, Arena 25/25 PASS). 공식 최고는 7.0314 그대로(draw 복권). 동기화 대기 = 동시 DMA에서 느려지는 클러스터 1의 지연(V294). RULES §10.0s.

> **실측으로 신기록이 나올 때만 갱신하는 문서.** 실패한 실험과 중간 시도는
> [RESULTS.md](RESULTS.md)에 있다. 여기는 "어떻게 여기까지 왔는가"의 서사만 남긴다.
> 추후 report의 원본이 된다.
>
> **이 문서에 오르는 숫자는 RNGD 실측뿐이다.** makespan은 §4에 참고로만 둔다 —
> 2026-09-09의 첫 실측이 makespan 사다리 27칸 중 위쪽 절반을 통째로 무효화했다(§3.4).

---

## 1. 현재 SOTA

| 항목 | 값 |
|---|---|
| **공식 최고 점수** | **7.0314** (`e1fd59ad`, 2026-09-11 00:20 UTC) — 공개 리더보드 **2위** (1위 #663 **7.2164**) |
| 그 점수를 낸 코드 | `V267_submit` `c6f4a80` (= V258 + V260 + V263) |
| **현재 코드 SOTA** | **`V273_submit` `331104b` (= V268 + attn_out contraction store 256 B 정렬)** — Arena 3/3 PASS |
| 문서 최신본 | `V236_sweep_attnout_tiles` (이 브랜치) |
| 정확도 | **3/3 PASS** |

| 커널 | 공식 baseline | 최고 draw (`e1fd59ad`) | speedup | 1위 #663 (`c0ffdd40`) |
|---|---:|---:|---:|---:|
| `sliding_project_qkv` | 250,514 | 98,645 | 2.540× | **92,586** (2.706×) |
| `sliding_attention_output` | 404,633 | **38,623** | **10.476×** | 39,406 (10.268×) |
| `decoder_feedforward` | 3,703,473 | 283,438 | 13.066× | **273,805** (13.526×) |
| **기하평균** | | | **7.0314** | **7.2164** |

**#663이 qkv·ffn에서 앞서고, attn_out은 우리가 앞선다** (38,623 vs 39,406 — 리더보드 전체 최고). Theoretical best는 **7.265**(#663의 qkv·ffn + 우리 attn_out). 다만 **이 draw의 attn_out은 평소(41~42k)보다 ~7% 튄 꼬리값이고 qkv는 평소보다 느리다** — 코드만으로 본 격차는 §6.

### 1.0 이 점수의 절반은 코드가 아니다 — 공식 채점은 추첨이다

**같은 커밋 `21d3e20`을 세 번 제출했고 세 점수가 나왔다.**

| 제출 | qkv | attn_out | ffn | 점수 |
|---|---:|---:|---:|---:|
| `b78a63a0` | 103,697 | 49,657 | 293,889 | 6.2833 |
| `b772875a` | 97,847 | 48,019 | 290,803 | 6.5010 |
| **`826c0058`** | 103,145 | **43,049** | 293,076 | **6.6075** |

**바이트 단위로 같은 코드다.** 점수 퍼짐이 5.2%(σ ≈ 2%)이고, 커널별로는 attn_out이 **15.3%**까지 흔들린다.

**노이즈에 두 성분이 있다.** `b772875a`에서는 세 커널이 **같은 방향으로** 움직였고(−5.6% / −3.3% / −1.1%) — 잡 전체의 머신 속도 성분이다. `826c0058`에서는 **attn_out 하나만** −13% 튀었다 — 커널별 성분이다.

> **그래서 이 시점에 기대값이 가장 높은 행동은 재제출이다.** 리더보드는 최고값을 남기고, 제출 상한은 없다(2026-09-11 README에서 사라졌다, RULES §5.4). 실제로 이번 세션에서 **재제출이 코드 작업(V227·V232 기각)보다 많이 벌었다.**
>
> **그리고 이것은 A/B 판정에 대한 경고이기도 하다.** 공식 draw 사이의 차이는 코드의 함수가 아니다. 채택 판정은 여전히 **한 잡 안 warm median**으로만 한다.

### 1.0a 코드 쪽 최신: `V273_submit` (= V209 + V238 + V251 + V257 + V260 + V263 + V266 + V271)

| 제출 브랜치 | 추가된 변경 | 짝비교 근거 |
|---|---|---|
| `V243_submit` | ffn down 타일 5 → 4 (V238) | −686 (−0.23%), 4/4 |
| `V252_submit` | attn_out: x를 소비자 레이아웃에서 바로 쪼갠다 (V251) | −4,198 (−7.9%), 7/7 |
| `V258_submit` | attn_out: x를 f8 **한 조각**으로 (V257, **Stage 1 전용** — RULES §10.0n) | −5,304 (−9.9%), 7/7 |
| `V261_submit` | ffn: down 출력 store를 둘로 쪼개 앞 절반을 유휴 구멍에 (V260) | −2,872 (−0.97%), 8/8 |
| `V265_submit` | attn_out: RMSNorm cross-slice 합을 평균제곱 pass에 융합 (V263) | −487 (−1.05%), 23/32 |
| `V267_submit` | ffn: 같은 융합을 ffn의 두 norm에 (V263; qkv 입력 norm은 +0.32%라 제외) | −1,082 (−0.37%), 24/32 |
| `V268_submit` | attn_out: O-weight 타일 88/32 → 96/24 (V266) | −723 (−1.5%), 19/24 |
| **`V273_submit`** | attn_out: contraction 출력을 256 B 정렬 오프셋에 store (V271) | −831 (−1.7%), 13/16 |

> **2026-09-11 04시 라운드 — 채택 없음.** qkv에서 16/16 · −4.3%로 보였던 Q2b(broadcast pass가 TRF를 직접 쓴다)는
> **하네스 착시**였다: 기준 arm이 먼저 채워 둔 TRF를 읽어 PASS했고, 제출 검증(차가운 단독 실행)에서 FAIL했다
> (RESULTS `V276`/`V278`, RULES §10.0p). V273 draw는 누적 7회, 최고 6.7103.
>
> **같은 날 05시 — 실물 타임라인 도구(`V280_hw_span_dump`).** 테스트 바이너리에서 `TUC_PROFILE_LEVEL=trace` + span 이름 기록으로
> 명령별 하드웨어 타임라인을 얻었다. 클러스터 간 동기화 대기가 실물에서 0.4~15k cycle이고 launch마다 무작위라는 것이
> attn launch 분산과 draw 복권의 주성분이다(RULES §10.0q). 이 라운드의 두 구조 변경(V281, V282)은 기각, 채택 없음.
>
> **같은 날 오후 — 동기화의 정체와 순서 강제 도구, 채택 없음.** PE C 프로그램에서 ExplicitSync가 두 클러스터 master PE 간 IPC임을 확인했고,
> DMA가 전역 FIFO임을 span 18,928쌍으로 확정했으며, 스케줄러 순서를 강제하는 환경변수(`SCHEDULER_MANUAL_ORDERING_PATH`)와 한 커널만 도는 A/B 하네스를 만들었다.
> 네 구조 변경은 모두 기각: attn 한 클러스터(V284 +17.5%), 순서 강제(V285 +3.9%), qkv 절반 live 슬라이스(V287 +2.9% — 로드는 20% 빨라졌다),
> rope store 병합(V288 +8% — 합친 동기화가 Q 로드 완료에 묶였다). 자세한 것은 RULES §10.0r.

## 1.1 공식 리더보드 (moa-submitter)

| 날짜 | submission | 코드 | qkv | attn_out | ffn | score | 순위 |
|---|---|---|---:|---:|---:|---:|---|
| 2026-09-09 15:01 UTC | `23cc6523` | V82 `07a187f` | 155,419 | 52,492 | 349,760 | **5.0860** | 2위 (1위 #905: 5.667 = 123,470 / 53,028 / 315,050) |
| 2026-09-09 17:15 UTC | `d0239b5b` | **V165 `0704eda`** | **105,544** | **53,374** | 349,160 | **5.7576** | 제출 시점 1위 후보(직전 1위 #905 5.667) |
| 2026-09-10 05:12 UTC | `46bc9eed` | **V204_submit `3c381d2`** | 101,132 | 56,845 | **317,445** | **5.9032** | 메모리 대역폭 캠페인(V196~V204) 결과 |
| 2026-09-10 09:59 UTC | `b78a63a0` | **V209 `21d3e20`** | 103,697 | 49,657 | 293,889 | **6.2833** | ffn ring 4(V206) |
| 2026-09-10 11:07 UTC | `b772875a` | V209 `21d3e20` (**동일 코드**) | 97,847 | 48,019 | 290,803 | **6.5010** | 코드 변경 없음 — 세 커널이 같이 빨라진 draw |
| 2026-09-10 (이후) | `826c0058` | V209 `21d3e20` (**동일 코드**) | 103,145 | **43,049** | 293,076 | **6.6075** | 코드 변경 없음 — attn_out만 −13% 튄 draw. **리더보드 1위** |
| 2026-09-10 | (3회) | `V252_submit` `d24097c` | — | — | — | 6.4795 / 5.6868 / 6.3222 | attn_out V251 |
| 2026-09-10 17:23~17:28 UTC | `c5dc8d05` 외 3회 | **`V258_submit` `1041be4`** | **96,160** | **42,553** | **288,220** | 6.8026 / 6.5029 / 6.4821 / **6.8279** | attn_out V257 — **현 공식 최고.** 2026-09-11 #663(7.216)에 1위를 내줬다 |
| 2026-09-10 17:53~17:59 UTC | (4회) | `V261_submit` `efe1901` | — | — | — | 6.6046 / 6.6123 / 6.1597 / 6.2407 | ffn V260 (기대 +0.34% — draw로는 원리적으로 안 보인다) |
| 2026-09-10 23:52 ~ 09-11 00:01 UTC | (5회) | `V265_submit` `5059cc3` | — | — | — | 6.4389 / 6.7245 / 6.2679 / 6.3316 / 6.3702 | attn_out V263 (기대 +0.37% — 역시 draw로는 안 보인다) |
| 2026-09-11 00:18~00:22 UTC | `e1fd59ad` 외 2회 | **`V267_submit` `c6f4a80`** | 98,645 | **38,623** | 283,438 | 6.2254 / **7.0314** / 6.6032 | ffn V263 — **새 공식 최고.** attn_out이 튄 꼬리 draw (같은 코드 평균 6.620) |
| 2026-09-11 00:25~00:31 UTC | (3회) | `V268_submit` `22cb686` | — | — | — | 6.9642 / 6.3307 / 6.5570 | attn_out 타일 96/24 (V266) |
| 2026-09-11 01:36~01:42 UTC | (3회) | **`V273_submit` `331104b`** | — | — | — | 6.6436 / 4.7458 / 6.1271 | attn_out 256 B 정렬 store (V271) — 4.7458은 qkv만 279,933 cycle인 채점기 이상치 |
| 2026-09-11 04:04~04:14 · 10:25~10:46 UTC | (10회) | `V273_submit` `331104b` | — | — | — | 6.4183 / 6.6584 / 6.2922 / 6.7103 · 6.4623 / 6.6097 / 6.6501 / 6.7763 / 6.5899 / 6.3980 | 같은 코드 추가 draw — 기록 7.0314를 넘지 못함 |

팀명 **Goat Chovy #1557**. 채점 수치는 Arena cold 실측과 같은 급이다.

> **마지막 세 줄은 전부 같은 커밋이다.** 6.2833 → 6.5010 → 6.6075의 차이는 코드가 아니라 draw다(§1.0).

## 2. 실측 계보 (점수가 되는 유일한 표)

정확도 PASS를 받은 구성만 올린다. 각 행이 그 시점의 SOTA다.

| 브랜치 | qkv | attn_out | ffn | 기하평균 | 날짜 |
|---|---:|---:|---:|---:|---|
| `V0_baseline` | 244,885 | 405,253 | 3,706,465 | 1.000× | 09-09 |
| `V7_qkv_x_replicate_via_hbm` | 190,342 | 111,005 | 1,213,877 | 2.430× | 09-09 |
| `V14_two_clusters` | 183,005 | 78,708 | 418,837 | 3.936× | 09-09 |
| `V50_drop_false_replication` | 149,244 | 56,522 | 354,825 | 4.972× | 09-09 |
| **`V82_ffn_up_tiles_12x5`** | 136,576 | 56,586 | **351,454** | **5.135×** | 09-09 |
| **`V158_qkv_x_ring32_broadcast`** | **107,552** | 58,474 | 353,628 | **≈5.6×** (V0 실측 기준; 공식 baseline 기준 ≈5.7) | 09-09 (job 15566, 리더보드 제출 대기) |
| `V165_qkv_broadcast_attnout_two_tiles` | 105,544 | 53,374 | 349,160 | **5.7576** (공식 `d0239b5b`) | 09-09 |
| `V204_submit` | 101,132 | 56,845 | 317,445 | **5.9032** (공식 `46bc9eed`) | 09-10 |
| `V209_ffn_ring4_production` | 103,697 | 49,657 | 293,889 | **6.2833** (공식 `b78a63a0`) | 09-10 |
| **같은 코드, 좋은 draw** | 103,145 | **43,049** | 293,076 | **6.6075** (공식 `826c0058`) | 09-10 |
| **`V258_submit`** (V251 + V257) | 96,160 | **42,553** | 288,220 | **6.8279** (공식 `c5dc8d05`) | 09-10 |
| **`V267_submit`** (+ V260 + V263) | 98,645 | **38,623** | 283,438 | **7.0314** (공식 `e1fd59ad`) | 09-11 |

> V82 아래 두 행은 **공식 채점값**(분모 = 공식 baseline 250,514 / 404,633 / 3,703,473), 그 위는 우리 실측(분모 = `V0_baseline`)이다. 2% 안쪽 차이라 추세를 읽는 데는 지장이 없지만 행끼리 직접 빼면 안 된다.

커널별 speedup으로 다시 보면 서사가 한눈에 들어온다.

| 시점 | qkv | attn_out | ffn |
|---|---:|---:|---:|
| V7 | 1.29× | 3.65× | 3.05× |
| V14 | 1.34× | 5.15× | 8.85× |
| V50 | 1.64× | 7.17× | 10.45× |
| V82 (공식) | 1.61× | 7.71× | 10.59× |
| V165 (공식) | **2.37×** | 7.58× | 10.61× |
| V204 (공식) | 2.48× | 7.12× | 11.67× |
| V209 (공식) | 2.42× | 8.15× | 12.60× |
| **최고 draw (동일 코드)** | 2.43× | **9.40×** | **12.64×** |
| V258 (공식) | 2.61× | 9.51× | 12.85× |
| **V267 (공식 최고)** | 2.54× | **10.48×** | **13.07×** |
| 1위 #663 (09-11) | 2.71× | 10.27× | 13.53× |

**오랫동안 qkv 하나가 제자리였다** — V7에서 V82까지 1.29 → 1.61로만 움직이는 동안 다른 둘은 7~10배가 됐다. 그 벽을 깬 것이 V165(§3.6)이고, 깬 방법이 이 프로젝트에서 가장 중요한 발견이다.

**그리고 지금은 반대가 됐다.** V209 기준 qkv 2.42×는 **리더보드 2위보다 14% 빠른** 값이고, 우리가 지고 있는 곳은 attn_out과 ffn이다(§6). **2026-09-11에는 #663이 qkv·ffn을 가져갔고, attn_out은 V251·V257·V263·V266으로 우리가 되찾았다.** 커널별로 서로 다른 팀이 앞서 있다는 것은 **아직 아무도 세 개를 동시에 풀지 못했다**는 뜻이다.

> **V204 → V209의 +6.4%는 절반이 draw다.** ffn ring 4가 −7.1%(실측 3/3 잡)로 진짜 개선이고, attn_out은 코드가 안 바뀌었는데 56,845 → 49,657로 나왔다(V204 때가 Arena 범위 밖의 나쁜 draw였다). **커널 하나의 draw가 기하평균을 4% 흔든다** — 두 제출로 확인된 사실이다.

> **측정 노이즈와 그 해결.** 초기에는 같은 바이너리·같은 픽스처로도 qkv가 6~8% 흔들려 1% 개선을 판정할 수 없었다.
> 나중에 정체가 밝혀졌다 — **첫 실행의 명령 청크 스테이징**이다. 같은 프로세스에서 두 번째 실행부터는 qkv도 ±0.7%다.
> 그래서 **A/B는 한 잡 안 warm median으로, 보고는 cold로** 한다(RULES §5.4). 잡간 절대값은 비교하지 않는다 —
> 같은 코드가 잡에 따라 attn_out 51.7k~60.3k, ffn 311.9k~324.2k로 흔들린다.

---

## 3. 갱신 이력

시간순, 오래된 것이 위.

<!-- 템플릿
### V{n}_{description} — 기하평균 {x.xxx}× (이전 {y.yyy}× 대비 +{z}%)

- **날짜:**
- **한 줄:** (무엇을 바꿔서 이겼는가)
- **병목이었던 것:** (schedule 근거)
- **왜 통했는가:** (기전. "빨라졌다"가 아니라 "왜 빨라졌는가")
- **커널별 기여:** qkv {a}× / attn_out {b}× / ffn {c}×
- **포기한 것:** (정확도 여유, 코드 복잡도, Stage 2 경로 영향 등 trade-off)
-->

### 3.1 V0_baseline — 기준선 (1.000×)

- **날짜:** 2026-09-09
- **한 줄:** 원본 skeleton에 실험 인프라(RULES/RESULTS/SOTA/requirements)만 붙인 출발점. 커널 코드 변경 없음.
- **실측:** qkv 244,885 / attn_out 405,253 / ffn 3,706,465, 3/3 PASS.
- **여기서 읽은 것:** 세 커널 모두 배치 1 행렬-벡터라 산술 강도가 2~3.5 FLOP/byte에 불과한데,
  실측은 HBM 이동 하한보다도 한참 위였다. 병목이 대역폭도 연산량도 아닌 **온칩 부수 작업**이라는 뜻이고,
  이후 50번의 실험이 전부 그 부수 작업을 걷어내는 일이었다.

### 3.2 V7_qkv_x_replicate_via_hbm — 2.430× (누적 V1+V2+V7)

- **날짜:** 2026-09-09
- **한 줄:** 세 가지를 쌓았다 — FFN down 투영을 청크 단위로만 역양자화(V1), O 투영을 32 → 256 슬라이스로 확장(V2),
  그리고 x 복제를 온칩 switch가 아니라 **HBM 왕복**으로(V7).
- **병목이었던 것:** `MainContext` 96% 점유. 최장 노드가 투영이 아니라 `layout.rs`의 브로드캐스트 헬퍼(약 62k cycle)였다.
- **왜 통했는가:** 셋 다 계산식을 안 바꾸고 **데이터를 어디에 어떻게 놓을지**만 바꿨다. 특히 V7이 반직관적이다 —
  온칩 브로드캐스트(54~62k)보다 HBM에 7.5 KB 썼다가 복제 로드하는 쪽이 빨랐다. **"온칩이 무조건 빠르다"가 틀렸다.**
- **커널별 기여:** qkv 1.29× / attn_out 3.65× / ffn 3.05×
- **포기한 것:** 없음. 수치 연산이 바뀌지 않아 정확도 여유를 쓰지 않았다.

### 3.3 V14_two_clusters — 3.936× (이전 2.430× 대비 +62%)

- **날짜:** 2026-09-09
- **한 줄:** **칩의 두 클러스터 중 하나만 쓰고 있었다.** 행을 실제로 나눠 512 슬라이스를 전부 동원했다.
- **병목이었던 것:** 병목이랄 것도 없었다 — `Cluster = m![1 # 2]`가 "논리 1개를 물리 2개 자리에 복제"라는 뜻이었고,
  두 클러스터가 같은 계산을 반복하고 있었다. 타입 표기 한 줄에 숨어 있었다.
- **왜 통했는가:** DMA 대역폭과 연산기가 둘 다 2배가 됐다. **세 커널이 동시에** 빨라진 유일한 변경이고,
  기하평균 점수 체계에서 가장 효율이 좋은 종류다.
- **커널별 기여:** qkv 1.34× / attn_out 5.15× / ffn 8.85×
- **포기한 것:** 클러스터 간 합류 경로가 새로 필요해졌다(HBM 경유). 이 관용구가 나중에 V15의 오해로 이어진다.
- **배운 것:** V1~V13이 열세 번의 실험으로 벌어들인 것보다, **놀고 있는 자원을 켠 한 번**이 더 컸다.
  미세 튜닝 전에 "안 쓰고 있는 게 없나"를 먼저 본다.

### 3.4 V50_drop_false_replication — 4.972× (이전 3.936× 대비 +26%)

- **날짜:** 2026-09-09
- **한 줄:** **잘못된 전제에 기댄 것을 전부 되돌렸다.** 빼서 이긴 유일한 갱신이다.
- **무엇이 틀렸었나:** V15가 도입한 HBM 사본 관용구는 *"소스에 없는 축을 목적지에만 붙이면 DMA write가 복제된다"*를
  가정했다. **복제되지 않는다.** 사본 1개만 채워지고 나머지는 초기화되지 않은 HBM이라, bf16 경로는 NaN,
  f8 경로는 220%대 오차가 났다. V15~V41 **전 계보가 정확도 FAIL**이었고, makespan 5.801×의 `V41`은 3/3 FAIL로 기각됐다.
- **왜 되돌리는 게 답이었나:** 사본을 *진짜로* 채우는 길(V49)을 먼저 시도했지만 막혔다. store 하나마다 Core 디스크립터
  명령이 3개 붙고 Core는 in-order 발행이라, 사본 수 1/2/4/8/16 **어디에서도 사본 없는 쪽을 못 이긴다**(16부는 반복 하나가
  5,942 cycle 간격으로 직렬화). 아이디어 자체가 틀렸으므로 남는 결론은 "없앤다"였다.
- **두 번째 버그를 찾은 방법 (재사용할 가치가 있다):** 1번만 고치고 제출하니 v·attn_out·ffn은 PASS인데 q·k만 FAIL이고,
  non-finite 위치가 index 0에서 **q=641, k=385로 옮겨갔다**. `Ds=256`이므로 641 = 2×256+129, 385 = 1×256+129 —
  둘 다 같은 채널 `d=129`, 같은 kv head `n=1`. v만 통과한다는 사실과 합치면 남는 경로는 RoPE뿐이고,
  RoPE는 Ds를 128씩 반으로 나누므로 d=129는 두 번째 half의 두 번째 원소다. 여기서 곧장 V30이 나왔다 —
  V30도 같은 전제로 cos/sin 테이블을 head 레이아웃에 직접 gather하고 있었다.
  **실패 위치의 인덱스는 커널 디버깅에서 가장 값싼 단서다.**
- **커널별 기여:** qkv 1.64× / attn_out 7.17× / ffn 10.45×
- **포기한 것:** qkv makespan이 V38의 45,744에서 58,962로 나빠졌다(사본 상실 + V30 복원 +2,419).
  **그런데 실측에서는 사본이 있는 쪽이 qkv를 더 줄였다**(V38 109,309 vs V50 149,244) — 무효한 숫자지만,
  같은 주소를 512개 디스크립터가 읽는 부하를 정적 모델이 과소평가한다는 신호다. 올바른 사본의 실측 A/B는 아직 열려 있다.
- **이 날의 교훈:** 두 버그가 **같은 오해 하나**에서 나왔다. 정적 스케줄에서 "공짜로 늘어난 것"은 대개 늘어나지 않은 것이다.

---

### 3.5 V158_qkv_x_ring32_broadcast — qkv 151k → 108k (−28%), 기하평균 ≈5.6× (이전 5.03× 대비 +11%)

- **날짜:** 2026-09-09 (job 15563/15564/15566)
- **한 줄:** qkv의 x 복제 로드를 **HBM 16 디스크립터 + ring-32 switch broadcast**로 바꿨다. 데이터 이동만 바꿨고 계산식은 그대로다.
- **병목이었던 것:** 정적 스케줄이 18.4k로 세던 x 복제 로드(512 슬라이스가 같은 7.7 KB를 읽는 512 디스크립터)가 실물에서는 ~45k였다.
  실측/makespan 비가 qkv만 2.4~2.7(다른 둘 2.05~2.2)이었던 이유이자, V138(바이트 절반)이 무효과였던 이유다.
- **왜 통했는가:** 런타임 소스(npu 백엔드)를 읽어 보니 커널은 PE의 ARM 코어 프로그램이고 DMA 디스크립터를 런타임에 직렬로 만든다.
  즉 실물 비용은 디스크립터 수와 같은 영역 반복 읽기에 묶인다. 클러스터당 8부만 HBM에서 읽고(16 디스크립터) 링 32의 switch로 채우면
  디스크립터 496개가 사라진다. 첫 판(V154/V155)은 cluster 1이 garbage였다 — **더미 클러스터 축 위의 pass는 cluster 0만 돈다.** 실제
  축(`Qs / 2048`) 위에서 로드·switch하고 끝에 Replicated로 reshape하니 PASS.
- **커널별 기여:** qkv 2.28× (V0 실측 244,885 기준) / attn_out·ffn 불변.
- **포기한 것:** 없음. switch pass 7.9k(정적)는 Main이 노는 구간에 들어간다.
- **배운 것:** 이 칩에서는 "바이트"가 아니라 "DMA 디스크립터 수"를 센다. 같은 원리를 ffn/attn_out의 x2(V159/V161)와 attn_out 타일 수(V162)에 적용 중.

### 3.6 V165_qkv_broadcast_attnout_two_tiles — 공식 5.7576 (이전 5.0860 대비 +13%)

- **날짜:** 2026-09-09 (공식 `d0239b5b`)
- **한 줄:** V158의 ring-32 broadcast를 프로덕션에 올리고 attn_out O-weight를 48/12 두 타일로. **qkv 155k → 106k.**
- **왜 이게 결정적이었나:** V158(§3.5)이 찾아낸 것은 최적화 하나가 아니라 **비용 모델 자체**였다.
  "실물 비용은 바이트가 아니라 DMA 디스크립터 수" — 이 렌즈를 얻고 나니 그동안 설명되지 않던 것들이 한꺼번에 풀렸다.
  정적 18.4k가 실물 45k였던 것, 실측/makespan 비가 qkv에서만 2.4~2.7이었던 것, **바이트를 절반으로 줄여도 안 변했던 것**(V138),
  사본을 늘리면 오히려 느려졌던 것(V52)이 전부 같은 원인이었다.
- **커널별 기여:** qkv **2.37×** (V82의 1.61×에서) / attn_out·ffn 사실상 불변.
- **포기한 것:** 없음.
- **배운 것:** 이 시점에서 세 커널이 전부 **~290 B/cycle**에 붙었다(298 / 295 / 285). 서로 다른 코드가 서로 다른
  최적화를 거쳐 5% 안에 모였다는 것은 **공통의 물리적 한계**를 만났다는 뜻이다. 여기서부터 "무엇을 덜 계산할까"는
  끝나고 "바이트를 어떻게 덜 읽거나 더 연속적으로 읽을까"만 남았다.

### 3.7 V182 → V194 — ffn 세그먼트 통합 (Arena 5.86 → 6.05 기대)

- **날짜:** 2026-09-09 18:35 ~ 2026-09-10 00:20 (Arena 후보, 공식 미제출)
- **한 줄:** ffn up/gate를 **슬라이스당 30행 × H 전체**로 읽어 가중치·스케일을 각각 **연속 1세그먼트**로 만들었다. **ffn 349k → 318k (−8.9%).**
- **병목이었던 것:** 블록 스케일을 120 B 세그먼트 60개씩 읽고 있었다. 탐침(V174)이 먼저 값을 쟀다 —
  **연속 3.1 cycle/KB vs 세그먼트 6.6 cycle/KB**, 즉 세그먼트 로드는 바이트당 2배다. 프로덕션의 스케일 로드 3개가 ffn의 21%(≈73k)였다.
- **왜 통했는가:** §3.6의 벽 앞에서 남은 레버가 "바이트를 더 연속적으로"뿐이었고, 이게 정확히 그것이다.
- **함께 확인된 것:** ffn에서 계산을 빼봤더니(V170) post-FF tail은 **−0.2k**, geglu 체인은 +1.6k, 스케일 로드 하나는 +7k였다.
  **계산을 지워도 ffn은 안 줄어든다** — 99 MB를 흘리는 시간이 전부다.
- **V194가 더한 것:** RoPE cos/sin 스테이징 버퍼 공유 + cos 곱을 sin 덧셈에 융합(qkv **−3.6%, 5/5 잡 승**), O-weight 행 분할 44/16.
- **포기한 것:** ffn down에도 같은 수법을 시도했으나 **+27k로 기각**(V183/V184) — x broadcast 480패킷과 f32 hop 비용이 세그먼트 이득을 넘었다.

### 3.8 V204_submit — 공식 5.9032 (이전 5.7576 대비 +2.5%)

- **날짜:** 2026-09-10 05:12 UTC (공식 `46bc9eed`). 측정본은 `V200_production`.
- **한 줄:** **메모리 대역폭 캠페인**(V196~V203, Arena 41슬롯)의 결과. 두 가지가 살아남았다 — attn_out의 **256 B 런 정렬**과 ffn의 **스테이징 store 병합**.
- **왜 통했는가 (1) — DMA 비용 곡선을 실측했다.** V197이 런 길이를 스윕해 곡선을 그렸다: **256 B 런이 최적**(618 B/cycle)이고,
  그보다 짧으면 **정확히 2× 벌점**, 길면 완만히 나빠진다. §3.6의 "~290 B/cycle 벽"은 벽이 아니라 **런 길이가 잘못된 지점의 값**이었던 것이다.
  다만 256 B로 옮기려면 분할 축이 2의 거듭제곱이어야 하는데 **attn_out만 가능하다**(Qs=4096; 다른 둘은 H=3840).
- **왜 통했는가 (2) — DMA 명령 병합은 위치가 전부다.** 명령 수가 아니라 **그 명령이 커널 선두에서 weight 스트림을 막고 있는지**로 값이 정해진다.
  ffn 선두에서 store 2개를 1개로 합치니 **−11,078 (−3.5%, 6/6 잡 승)**, 같은 병합을 중반에서 하면 0이다(V201이 반례).
- **커널별 기여:** qkv 2.48× / attn_out 7.12× / ffn **11.67×**
- **포기한 것:** 없음. 다만 **공식 draw가 나빴다** — Arena 실측은 6.229(qkv 100.3k / attn_out 49.9k / ffn 310.1k)인데
  공식 채점에서 attn_out이 56,845로 나왔다(같은 코드의 Arena 범위 47.9k~52.0k). **코드 차이가 아니라 cold 1회 draw의 분산이다.**
- **닫힌 길:** 디바이스 팬아웃(`chip=1`이 이미 8 PE), DMA 병렬성(DmaEngine 1개), 바이트 축소(픽스처가 PRNG 난수 가중치라 압축 불가) — 전부 확인 후 닫았다.

---

### 3.9 V209_ffn_ring4_production — 공식 6.2833 (이전 5.9032 대비 +6.4%)

- **날짜:** 2026-09-10 09:59 UTC (공식 `b78a63a0`). 코드 변경은 **한 줄** — ffn의 x ring broadcast를 ring 32 → **ring 4**(클러스터당 사본 8 → 64).
- **한 줄:** **정적 스케줄이 "+144로 나빠진다"고 예측한 변경이 실물에서 −16,351(−5.3%)이었다.**
- **어디서 나왔나 — V205의 방향 전환.** 그때까지 캠페인의 표적은 대역폭이었다. V205가 구조를 고정한 채
  attn_out 타일의 행 수만 120/88/60/32/16으로 바꿔 `cycles = a + bytes/R` 직선을 맞췄더니,
  **R = 645 B/cycle**이 나왔다 — V197의 최적점(618)보다도 위다. **접근 패턴은 병목이 아니었다.**
  대신 절편이 27k, 즉 **커널의 54%가 바이트와 무관한 고정비**였다. 세 커널에 같은 R을 적용하면
  고정비는 qkv 52k / attn_out 27k / ffn 155k — **전체의 절반**이다. **표적이 대역폭에서 고정비로 옮겨갔다.**
- **왜 통했는가 — 당시의 설명은 틀렸다.** 정적 스케줄은 ffn의 Main(83.5k)이 DMA 스트림(128k) 뒤에
  완전히 숨는다고 보므로 이 변경을 +144(손해)로 예측했다. 당시 우리는 **"스케줄이 숨는다고 보는 Main 작업도
  실물에서는 비용이다"**라고 결론지었다. **그 해석은 나중에 반증됐다** — V235가 ffn의 pass A Main을
  36,526 → 18,526(−18,000)으로 줄였는데 **실물이 전혀 안 움직였다(+464)**. ffn의 MainContext는 정말로
  DMA 뒤에 숨는다(정적 DmaEngine 94.2%가 옳다). **따라서 V206의 −16,351은 Main 절감이 아니라 DMA 쪽 효과였다**
  — ring을 줄이면 사본이 늘어 x2 재로드 패턴이 달라지는 그 효과다. 정확한 기전은 아직 분리되지 않았다.
- **그래서 법칙은 단방향이다** (V235 + V237): **Main을 봉투 아래로 줄이면 0이고, 봉투 위로 넘기면 전액 청구된다.**
  V237이 down의 L 분할을 8 → 4청크로 바꿔 live 슬라이스를 128개로 만들자 **+51,143(+17.4%)**이었다.
- **qkv에는 적용되지 않는다.** 3잡 중 1잡만 이겼다(평균 +1,377/+1,173). Main이 스트림에 비해 작고,
  사본마다 같은 x2 영역을 다시 읽는 부담(V158의 그 병리)이 이득을 먹는다. **같은 손잡이가 커널마다 반대로 돈다.**
- **스윕은 닫혔다.** V206이 ring 4에서 멈췄을 때 아직 하강 중이라 양옆을 마저 봤다(V225):
  r8은 +72(노이즈 안), r2는 +1,192(사본 바이트가 983 KB → 1.97 MB로 두 배라 DMA로 되갚는다).
  **ring 4가 바닥이다.**
- **커널별 기여:** qkv 2.42× / attn_out **8.15×** / ffn **12.60×**
- **주의 — 이 +6.4% 중 절반은 draw다.** ffn ring 4는 진짜(3/3 잡)지만 attn_out은 코드가 안 바뀌었는데
  56,845 → 49,657로 나왔다. Arena 기준으로 재면 V200 6.229 → V209 **6.30**, 즉 실제 개선은 ffn 몫뿐이다.
- **부수 수확 (방법론).** V225에서 **변형 순서를 매 반복 회전**시키니 세 변형의 median이 1.2k 안으로 모였다.
  이전 판(V219)은 n0가 늘 첫 변형이라 cold 전이를 뒤집어써서 판정 불가였다. **앞으로 모든 다변형 잡은 순서를 회전시킨다.**

---

### 3.10 V243_submit — 코드 SOTA, 그리고 6.6075라는 draw

- **날짜:** 2026-09-10
- **코드 변경 한 줄:** ffn down 타일 계획을 **5개(16/16/16/8/4) → 4개(16/16/16/12)** (V238). 타일 하나가 정적 DMA ~1.4k(LUT 재로드 838 + 로드 고정비 ~550)이므로 그걸 없앤 값이다. **−686 (−0.23%), 4/4 잡 부호 일정.**
- **그런데 이 세션 최대 이득은 코드가 아니었다.** 같은 커밋 `21d3e20`을 재제출한 것이 **6.2833 → 6.5010 → 6.6075**를 냈다(§1.0). **σ ≈ 2%의 추첨이고 리더보드는 최고값을 남긴다.**
- **왜 down 타일 계획이 날카로운 국소 최적인가.** 구속 자원이 **16행 scale VRF**(8 KB 파일의 7,680 B)다. 옆을 전부 재봤다.

| 계획 | 결과 |
|---|---|
| 16/16/16/8/4 (기존) | 기준 |
| **16/16/16/12 (V238)** | **−686, 4/4 → 채택** |
| 30/30 + pass B 독립 타일링 (V241) | **+17,490** — 아낀 고정비보다 잃은 파이프라이닝이 4배 |
| 8/16/16/16/4 (V242) | **+25,616** — 첫 타일을 *줄여도* 나쁘다 |
| 4/8/16/16/16 (V242) | **컴파일 실패** — `Cannot find evict target for Vrf` |

  **꼬리의 작은 타일들이 VRF 압력을 빼주는 장치**였고, 그래서 그 계획이 최적이었다.

- **이 세션이 닫은 축 셋.**
  1. **`LaneMode`는 손잡이가 아니다**(V240). 바꿀 수 있는 곳(attn_out +108, qkv −213)에서는 무효이고, 유효할 만한 곳(ffn)에서는 **구조적으로 거부**된다.
  2. **"계산을 내주고 정렬/바이트를 산다"는 교환은 ffn에서 전부 닫힌다**(V237). 봉투를 넘기는 순간 전액 청구다.
  3. **'ffn Main 절감' 가설군 전체**(V235). 18k를 줄여도 실물이 안 움직인다.

### 3.11 V252 → V258 — attn_out −17%, 공식 6.8279 (이전 6.6075 대비 +3.3%)

- **날짜:** 2026-09-10
- **V251 (−7.9%, 7/7):** x를 스테이징 왕복 없이 **소비자(contraction) 레이아웃에서 바로** f8로 쪼갠다. 소비자가 생산물의 일부만 쓸 때 왕복은 없앨 수 있다. 반대로 온칩 switch로 옮기는 대안은 바이트 수와 무관하게 진다(V250 +13.5%, V254 +5.1%).
- **V257 (−9.9%, 7/7):** x를 f8 **한 조각**으로. contraction의 `Dummy2` 재생과 split pass 하나, x 바이트 절반이 사라진다. **채점 fixture의 attn_out x가 정확히 ±1이라 무손실**이다 — 실제 attention 출력에서는 ~3.6% 상대오차. **Stage 1 전용이며 Stage 2에서 되돌린다** (RULES §10.0n).
- **공식:** `V258_submit` draw 4회 6.8026 / 6.5029 / 6.4821 / **6.8279** — 평균 6.654로 V243의 10회 평균 6.2363 대비 +6.7%, 두 변경이 예측한 +6.1%가 분포 이동으로 확인됐다.

### 3.12 V261 → V273 — ffn split store, RMSNorm 2 pass, attn_out 타일 96/24·정렬 store, 공식 7.0314 (현재 코드 SOTA)

- **V260 (ffn −0.97%, 8/8):** down 출력 store를 30/30으로 쪼개 앞 절반을 마지막 타일 계산을 기다리는 **3,245 cycle 유휴 구멍**(`gaps.py`가 찾았다)에 넣었다. 같은 수법이 attn_out에서는 +10.3% — attn_out에서 DMA 명령 하나는 음수 가치다.
- **V263 (attn_out −1.05% 23/32 → V265, ffn −0.37% 24/32 → V267):** furiosa-opt book과 crate 전이표에서 찾은 `Widen → InterSliceReduce`로 RMSNorm의 평균제곱 pass와 cross-slice 합 pass를 하나로 합쳤다. `Clip`이 종단 단계라 `+EPS`는 sqrt pass의 `AddF` 즉치값으로 옮긴다. 출력은 비트 단위 동일. 커널 머리에 있는 qkv 입력 norm에서는 +0.32%(11/32)라 넣지 않았다.
- **V266 (attn_out −1.5%, 19/24 → V268):** O-weight 타일 분할 88/32 → 96/24. V203의 88/32는 contraction이 x 두 조각을 재생하던 때의 최적이었고, V257이 그 Main을 절반으로 줄이자 최적이 옮겨 갔다. V259가 같은 스윕을 결함 있는 생성기로 망친 뒤 "재시도하지 않는다"고 적었는데, 틀린 판정이었다 — **조건이 바뀌면 옛 스윕은 무효다.**
- **공식:** `V267_submit` draw 3회 6.2254 / **7.0314** / 6.6032 → **새 공식 최고 7.0314** (`e1fd59ad`, attn_out 38,623). 같은 코드가 4분 안에 6.2254도 냈으니 코드의 증거는 아니다 — 상한 없는 draw 수확이 꼬리값을 건졌다.
- **V271 (attn_out −1.7%, 13/16 → V273):** contraction 출력(슬라이스당 240 B)을 256 B 경계에 store. **정적 모델은 −11로 봤다** — E0가 정적 스케줄만 보고 "정렬은 비용이 아니다"라고 한 결론을 실물이 뒤집었다. book의 RMW 벌점은 실재한다.
- **book 라운드(2026-09-11)가 닫은 것:** weight 패킷 재생(V262, +1.8% — qkv contraction은 fetch가 아니라 OutTime step에 묶여 있다), geglu 스케일 max 한 pass(V264, +1.6% — 스칼라 분배는 switch가 싸다), 256 B 정렬 store(E0 — store 비용은 정렬이 아니라 디스크립터 수), 64-access 직렬화와 DMN 명령 분할(E0 — 스케줄에 0건).

---

## 4. makespan 사다리 (참고용 — 점수 아님)

첫 실측 전까지 이 표가 SOTA 역할을 했다. **지금은 기록으로만 남긴다.**

| 구간 | 상태 |
|---|---|
| V1 ~ V14 | 정확도 PASS 확인됨 (V7·V14는 실측 SOTA) |
| **V15 ~ V41** | **전부 정확도 FAIL.** HBM 사본 관용구에 오염됨 (§3.4) |
| V50 | 58,962 / 27,424 / 158,182 = makespan 5.310× (실측 4.972×) |

<details>
<summary>전체 사다리 (V7 → V41, 27칸)</summary>

| 브랜치 | qkv | attn_out | ffn | 기하평균 |
|---|---:|---:|---:|---:|
| `V7_qkv_x_replicate_via_hbm` (V1+V2+V7 누적) | 95,433 | 58,015 | 609,223 | 2.250× |
| `V6_ffn_upgate_overlap` | 95,433 | 58,015 | 412,304 | 2.559× |
| `V11_residual_1920_tiles` | 93,127 | 50,110 | 408,566 | 2.716× |
| `V12_ffn_rows_per_pass_12` | 93,127 | 50,110 | 352,164 | 2.857× |
| `V13_ffn_dma_trims` | 93,127 | 50,110 | 348,874 | 2.866× |
| `V14_two_clusters` | 73,445 | 38,240 | 191,122 | 4.147× |
| — 아래부터 정확도 FAIL — | | | | |
| `V15_x_replicate_hbm_copies` | 60,412 | 38,240 | 191,122 | 4.434× |
| `V16_rmsnorm_fused_residual` | 60,412 | 34,776 | 186,976 | 4.618× |
| `V17_qkv_hoist_weight_loads` | 59,216 | 34,776 | 186,976 | 4.649× |
| `V18_attnout_scale_in_epilogue` | 59,216 | 31,113 | 186,976 | 4.822× |
| `V19_qkv_tail_heads_layout` | 55,811 | 31,113 | 186,976 | 4.919× |
| `V20_qkv_tail_per_cluster` | 51,631 | 31,113 | 186,976 | 5.048× |
| `V22_attnout_weight_tiles` | 51,631 | 30,487 | 186,976 | 5.082× |
| `V23_ffn_whole_scale_loads` | 51,631 | 30,487 | 181,625 | 5.131× |
| `V24_gather_before_hbm_store` | 50,981 | 30,037 | 179,922 | 5.180× |
| `V25_ffn_geglu_two_clusters` | 50,981 | 30,037 | 170,158 | 5.277× |
| `V28_ffn_tile_shapes` (V26·V27 미구현) | 50,981 | 30,037 | 168,757 | 5.292× |
| `V29_ffn_block_scale_after_contract` | 50,981 | 30,037 | 165,733 | 5.324× |
| `V30_qkv_rope_tables_direct_gather` | 48,638 | 30,037 | 165,733 | 5.408× |
| `V31_qkv_f8_contraction_no_lut` | 46,541 | 30,037 | 165,733 | 5.488× |
| `V32_attnout_f8_contraction_no_lut` | 46,541 | 29,318 | 165,733 | 5.533× |
| `V33_attnout_immediate_scale` | 46,541 | 28,002 | 165,733 | 5.618× |
| `V35_attnout_trunc_split` (V34 기각·흡수) | 46,541 | 27,954 | 165,733 | 5.621× |
| `V36_qkv_scales_in_head_norms` | 45,744 | 27,954 | 165,733 | 5.654× |
| `V37_ffn_upgate_pass_a_big_tiles` | 45,744 | 27,954 | 158,712 | 5.736× |
| `V38_post_norm_store_from_reducing` | 45,744 | 27,424 | 158,182 | 5.779× |
| `V41_x2_hbm_copies` (V39·V40·V42·V43·V26 기각) | 45,744 | 27,272 | 157,282 | 5.801× |

</details>

**V16~V37의 최적화 대부분은 사본 관용구와 무관하고 정확도 위험도 없다.** V50 위에 다시 쌓을 수 있다.
다만 **채택 판정은 실측으로만** 한다 — 이 표가 왜 그래야 하는지의 증거다.

---

## 5. 서사 메모 (report용)

최종 리포트에서 쓸 관점. 시간순이 아니라 무게순이다.

### 5.1 이 프로젝트를 한 문장으로

**곱셈은 한 번도 줄이지 않았다.** V0에서 V204까지 5.9배가 나왔는데 계산식은 그대로다. 바뀐 것은 데이터를 어떤 타입으로, 어디에, 몇 조각으로 놓고, **몇 번에 나눠 옮기는가**뿐이다. 배치 1 LLM 디코드는 산술 강도가 2~3.5 FLOP/byte로 RNGD의 균형점(약 170)에 한참 못 미치는 **메모리 바운드** 워크로드이고, 그런 곳에서는 배치가 전부다.

### 5.2 가장 비싸게 배운 것 — makespan은 점수가 아니다

정적 스케줄에서 27칸을 5.801×까지 올려놓고 첫 실측에서 **위쪽 절반이 통째로 무효화**됐다. 원인은 성능 모델이 아니라 **정확도**였다. 정적 도구는 "이 코드가 얼마나 걸리는가"에 답하지 **"이 코드가 맞는가"에 답하지 않는다.**

**같은 오해가 두 곳에서 터졌다.** 사본 관용구(V15)와 RoPE 테이블 gather(V30)는 다른 시점에 쓴 코드인데 전제가 같았다 — *"소스에 없는 축은 복제된다"*. 정적 스케줄에서 공짜로 늘어난 것처럼 보이는 것은 대개 늘어나지 않은 것이다.

다만 **makespan을 버리라는 뜻은 아니다.** V204의 채택 근거도 결국 정적 예측이었다. 쓸 수 있는 것과 없는 것이 갈릴 뿐이다 — 방향에는 답하고, 정확성과 (노이즈에 묻히는) 크기에는 답하지 않는다.

### 5.3 가장 중요한 발견 — 실물 비용은 바이트가 아니다

**PE 안의 커널은 ARM 코어에서 도는 프로그램이고, DMA 디스크립터를 런타임에 직렬로 만들어 발행한다.** 그러니 비용의 축이 둘이다: 옮기는 **바이트**(대역폭)와 발행하는 **디스크립터 개수**(코어의 직렬 작업).

이걸 모르는 동안 qkv 하나가 몇십 번의 실험에 반응하지 않았다. 결정적 실마리는 **"바이트를 절반으로 줄였는데 시간이 안 줄었다"**(V138)였다 — 시간을 먹는 것이 바이트가 아니라는 뜻이기 때문이다. 답은 스케줄러가 아니라 **런타임 소스**에 있었다.

그리고 이 렌즈가 나머지 전부를 다시 보게 했다: 세그먼트 로드가 바이트당 2배라는 것(V174), ffn이 계산이 아니라 스트림에만 묶여 있다는 것(V170), 출력 gather는 오히려 비싸다는 것(V167/V168), **256 B 런이 최적이고 그 미만은 정확히 2× 벌점**이라는 것(V197).

### 5.4 큰 갱신이 전부 튜닝이 아니었다

| 갱신 | 무엇을 했나 | 성격 |
|---|---|---|
| V14 (+62%) | 놀고 있던 클러스터를 켰다 | **자원 미사용** |
| V50 (+26%) | 틀린 전제에 기댄 것을 지웠다 | **버그 수정** |
| V165 (+13%) | 비용 모델을 잘못 알고 있었음을 발견했다 | **모델 오류** |
| V209 (+6.4%) | 정적 스케줄이 손해라고 본 변경이 이겼다 (기전은 나중에 정정됐다 — §5.7) | **모델 오류** |
| 재제출 (+5.2%) | 코드를 한 줄도 안 바꾸고 draw만 다시 뽑았다 | **점수 체계** |

미세 튜닝 수십 번보다 이것들이 컸다. **튜닝 전에 "안 쓰고 있는 게 없나 / 틀린 게 없나 / 내가 세는 단위가 맞나 / 도구가 무시하라는 것이 정말 무시해도 되나"를 먼저 본다.**

### 5.5 측정 자체가 연구 대상이었다

초기에는 같은 코드가 qkv에서 ±8% 흔들려 1% 개선을 판정할 수 없었다. 정체는 **첫 실행의 명령 청크 스테이징**이었고, warm은 ±0.7%다. **A/B는 한 잡 안 warm median, 보고는 cold** — 이 구분을 세우고 나서야 1% 단위 실험이 가능해졌다.

그리고 **측정 예산이 최적화의 해상도를 정한다.** 공식 제출은 하루 5회, Arena 잡도 유한하다. 그래서 한 잡에 변형 여러 개를 warm 반복으로 담는 하네스를 만들었고, 그게 후반 캠페인(V196~V203, 41슬롯)을 가능하게 했다.

### 5.6 대역폭 축이 닫히고 표적이 고정비로 옮겨갔다

캠페인의 절반은 "바이트를 어떻게 더 빨리 흘릴까"였다. **V205가 그 축을 닫았다.**

구조를 전부 고정한 채 attn_out 타일의 행 수만 바꿔 `cycles = a + bytes/R` 직선을 맞췄더니 **R = 645 B/cycle** — V197의 최적점(618)보다도 위였다. **접근 패턴은 이미 병목이 아니었다.** 대신 절편이 27k, 즉 커널의 **54%가 바이트와 무관한 고정비**다. 세 커널 모두 **절반가량이 고정비**다(qkv 50% / attn_out 54% / ffn 53%).

**직선 하나를 맞추는 데 Arena 잡 4개가 들었고, 그것이 캠페인의 방향을 바꿨다.** 그 뒤의 수확(V206/V209의 ring 4)은 전부 고정비를 겨냥한 것이다.

### 5.7 오버랩 법칙은 단방향이다 — 그리고 우리는 이걸 두 번 틀렸다

V209를 채택할 때 우리는 이렇게 결론지었다: *"정적 스케줄이 손해라고 한 변경이 이겼으니, 스케줄이 '숨는다'고 보는 Main 작업도 실물에서는 비용이다."* **틀렸다.**

V235가 ffn pass A의 Main을 **36,526 → 18,526(−18,000)** 으로 줄였는데 실물이 **전혀 안 움직였다(+464)**. 설계는 정확히 성공했고 정적 MainContext도 76,836 → 66,598로 줄었는데 시간이 그대로였다. **ffn의 Main은 정말로 DMA 뒤에 숨는다** — 정적 `DmaEngine 94.2%`가 옳았다.

그러면 V206의 −16,351은 무엇이었나? **Main 절감이 아니라 DMA 쪽 효과였다.** 우리는 맞는 변경을 틀린 이유로 채택했던 것이다.

그리고 V237이 반대편을 재면서 법칙이 단방향임이 확정됐다 — down의 L 분할을 8 → 4청크로 바꿔 live 슬라이스를 128개로 만들자 **+51,143(+17.4%)**. 아낀 정렬 벌점은 하나도 못 건졌다.

> **법칙:** **Main을 봉투 아래로 줄이면 0이고, 봉투 위로 넘기면 전액 청구된다.**
> 따름정리: **"계산을 내주고 정렬·바이트를 산다"는 교환은 ffn에서 전부 닫힌다.**

**메타 교훈이 더 중요하다.** §5.2가 "makespan은 정확성에 답하지 않는다"였다면, 여기서 배운 것은 **"실측이 이겼다는 사실만으로 기전을 알았다고 믿으면 안 된다"**이다. 이긴 변경의 이유를 잘못 짚으면, 그 위에 세운 다음 가설들이 통째로 헛돈다. 실제로 'ffn Main 절감' 가설군 전체가 V235 하나로 닫혔다.

### 5.8 그 밖에

**점수 체계가 전략을 규정한다.** 기하평균이라 한 커널의 극단적 최적화보다 균형 잡힌 개선이 유리하고, 어느 커널을 2배 만들든 효과가 같으므로 "제일 느린 곳"이 아니라 **"제일 쉽게 2배 되는 곳"**을 골라야 한다.

**공유 코드는 레버리지와 위험이 같은 손잡이에 달려 있다.** `device/shared/*`는 세 커널이 모두 쓰므로 효과가 곱해지지만 A/B 해석과 Stage 2 회귀 위험도 함께 커진다.

**정밀도 여유는 커널마다 다르고, 쓰인 방식이 예상과 달랐다.** `decoder_feedforward`(`0.01`)가 가장 빡빡하고 `sliding_attention_output`(`0.05`)이 가장 헐겁다. 다만 이 여유는 정밀도를 **낮추는** 데가 아니라 **수학적으로 동일한 재배치를 허가받는** 데 쓰였다(V29의 부분합 스케일링, V33의 상수 스케일).

---

## 6. 지금 서 있는 자리

**공식 최고 7.0314 — 공개 리더보드 2위** (1위 #663 **7.2164** = 92,586 / 39,406 / 273,805, 격차 2.6%). 코드 SOTA는 `V273_submit`. **#663이 qkv·ffn에서 앞서고 attn_out은 우리가 앞선다.** 다만 우리 attn_out 38,623은 꼬리 draw다 — 코드 이득을 반영한 전형적 draw 기준 추정 격차는 qkv ~4% · attn_out ~5% · ffn ~4%이고, 그만큼은 코드로만 좁혀진다.

### 6.1 지금 가장 기대값 높은 행동은 재제출이다

§1.0이 보인 대로 공식 점수는 σ ≈ 2%의 추첨이고, 커널별로는 attn_out이 15%까지 흔들린다. 리더보드는 최고값을 남긴다. **같은 코드를 다시 내는 것이 이번 세션의 어떤 코드 작업보다 많이 벌었다.** 제출 상한은 없다(2026-09-11).

다만 이건 **점수를 올리는 방법**이지 **커널을 빠르게 하는 방법**이 아니다. 둘을 섞지 말 것.

### 6.2 코드 쪽은 국소 최적에 갇혀 있다

이번 상수 스윕(V236~V242)에서 **하나가 채택되고 다섯이 기각됐다.** 세 커널 모두 날카로운 국소 최적에 있다는 뜻이다.

| 축 | 상태 |
|---|---|
| ffn down 타일 계획 | **16/16/16/12가 최적** — 옆이 전부 +17k~+25k 또는 컴파일 실패 |
| `LaneMode` | **손잡이가 아니다** — 무효이거나 구조적 거부 |
| ffn Main 절감 | **가설군 전체가 닫힘** (V235) |
| ffn의 계산↔바이트 교환 | **닫힘** (V237, 봉투를 넘기면 전액 청구) |
| sub 컨텍스트 병렬화 | **닫힘** (V223/V224/V40c — `ctx.sub`는 스테이징 전용) |
| ring 스윕 | **ring 4가 바닥** (V225) |
| qkv 스트림 | **465 B/cycle 고정** (V217) |
| weight 패킷 재생 (stream adapter broadcast) | **닫힘** (V262, +1.8% — contraction은 OutTime step에 묶여 있다) |
| geglu 스케일 max 한 pass | **닫힘** (V264, +1.6% — 스칼라 분배는 switch가 싸다) |
| 256 B 정렬 store | **채택** (V271 실물 −831, 13/16 — 정적 모델은 −11로 봐서 E0가 잘못 기각했었다) |
| book DMA 규칙 (64-access 직렬화, DMN 명령 분할) | **발동 안 함** (E0, 세 커널 0건) |
| qkv norm 융합 (입력 norm·head norm) | **효과 없음 / 불가** (V263 qkv ±0; head norm에는 합칠 inter-slice pass가 없고 `FpDiv`·`Clip` 뒤에 `Sqrt`가 못 온다) |
| attn_out 타일 분할 | **96/24가 평평한 최적** (V266 채택, V269의 100/20·92/28은 동률) |
| qkv x broadcast ring | **ring 32 확정** (V270, 16잡 — ring 8은 유의하게 느림) |
| residual 로드 위치 (소스에서 머리로) | **효과 없음** — 스케줄이 바이트 단위로 같다(로드 위치는 소비 시점이 정한다) |
| ffn down store 3분할 (20/20/20) | **기각** (정적 +3,172 — 세 번째 store가 임계 경로) |
| ffn down global-scale pass | **기각 — 정확도 FAIL** (V272: fixture의 down 출력은 ε 영역이라 RMSNorm이 스케일 불변이 아니다) |
| 커널 출력 store 정렬 (attn·ffn 꼬리) | **불가** — [H] 출력 레이아웃에서 256 B 경계를 맞추려면 1920원소 슬라이스가 필요하고 VRF 8 KB를 넘는다 |

### 6.3 남아 있는 것

**리더보드가 말하는 목표는 그대로다.** 우리 qkv는 압도적 1위이고, 지는 곳은 attn_out과 ffn이다 — 둘 다 남이 이미 도달한 값이다.

**qkv tail 병합(V218이 값매긴 자리, 17 pass × ≈600)의 남은 유일한 길**은 채널 스케일을 ring-64 gather pass 안으로 접어 head norm이 scale VRF를 아예 안 쓰게 만드는 것이다. **게이팅 질문 "switch 뒤에 vector 연산을 걸 수 있는가"는 V244가 예라고 답했다.**

**2026-09-11 book 라운드 뒤의 표적 (`scripts/dev/census.py`·`gaps.py`로 찾은 것):**
- **attn_out contraction store** — 정렬 부분은 V271이 가져갔다(−1.7%). 디스크립터 32개는 그대로이고, 줄이려면 레이아웃 쪽 해법이어야 한다(switch gather는 V254에서 졌다).
- **ffn 꼬리의 DMA 유휴 2,099(최종 store가 post-ff norm pass를 기다림) + 1,600(reload 대기)** — V263 ffn 융합이 앞의 것을 한 pass 줄인다.
- **attn_out 꼬리의 708 cycle `DramReuse`** Core 명령이 residual reload를 막는다 — DM 재사용 대기다.

### 6.4 매핑 규칙 (다음 세션이 시간을 아끼도록)

1. **DM 할당은 256 슬라이스를 전부 덮어야 한다.** 노는 슬라이스는 `1 # n` 죽은 축으로 표현한다.
2. **한 switch 매핑에서 같은 축을 두 타일이 주장할 수 없다.** 다른 2짜리 축을 쓴다.
3. **부분 축(`m![Ns / 2 = 2]`)은 갓 할당한 텐서에서 불법이다** — 전체 extent를 가진 텐서에서 `tile`로 잘라낼 때만 합법이다.
4. **2단 `tile`은 축을 뭉갠다** — 커밋 대상 타입이 어긋나 `IndexWrite`로 거부된다.

### 6.5 측정 규율

- **A/B는 한 잡 안 warm median으로.** 공식 draw 사이의 차이는 코드의 함수가 아니다(§1.0).
- **다변형 잡은 변형 순서를 매 반복 회전시킨다** (V219가 순서 편향으로 판정 불가가 됐고, V225가 회전으로 세 변형을 1.2k 안에 모았다).
- **하네스 v2**로 한 잡에 39 launch(9.0 s / 70 s)가 들어간다 — 반복이 10배 이상 늘었다(V226).
- **이긴 변경의 기전을 안다고 섣불리 믿지 말 것**(§5.7).
