# RESULTS.md — 실험 기록 (전체)

> 규칙은 [RULES.md](RULES.md). 성공·실패 **모두** 기록한다. 중복 실험 방지와
> A/B 비교가 이 문서의 존재 이유다. 실측(RNGD cycles)이 없는 결과로는 "채택" 판정을
> 내리지 않는다 (RULES §5). **새 가설은 코드보다 먼저 여기에 슬롯으로 등록한다** (RULES §2.1).

## 요약 보드

기준: `V0_baseline`. 점수 = 3커널 speedup의 기하평균. makespan은 정적 스케줄(스크리닝용),
RNGD cycles만 점수다.

| 브랜치 | 분기점 | 가설 한 줄 | qkv makespan | attn_out makespan | ffn makespan | 기하평균 (makespan 기준) | RNGD 실측 | 정확도 | 상태 |
|---|---|---|---:|---:|---:|---:|:---:|:---:|:---:|
| `V243_submit` 검증 (V250 step 0) | `V209` | **t4가 정말 V209보다 빠른가를 분포로 확정한다.** 공식 draw는 V243 10회 평균 **6.2363** vs V209 계열 9회 평균 **6.3151**로 *반대*를 말하지만, 공식 draw는 시간이 다른 배치라 머신 드리프트에 오염돼 있다. 한 잡 안에서 교대 실행하는 짝비교만이 드리프트에 면역이다 | — | — | — | — | **잡 11개 짝비교: −556 / −922 / −251 / −1,015 / −2,152 / −270 / −614 / −1,010 / −184 / −185 / −805 → 11/11 음수, 평균 −724 (−0.25%), 부호검정 p = 0.00049** | 전부 **PASS** | **채택 확정.** 같은 7잡의 base median은 292,856~297,032(평균 295,321, sd 1,400 = 0.5%)로 **오늘 잡간 드리프트는 작다** — 공식 draw의 0.079 차이는 t4가 아니라 **다른 시간대의 머신 속도**다. t4의 실제 값어치는 ffn −0.25% = 점수 +0.08%로 draw 노이즈(σ 1.8~2.8%)에 완전히 묻힌다. **공식 draw로 코드를 비교하지 말 것 — 짝비교만이 증거다.** |
| `V250_ffn_down_l_split` | `V236` | **down의 클러스터 축을 H → L로.** up/gate는 L을 클러스터로 쪼개는데 down은 H를 쪼개서, geglu 출력이 클러스터를 건너야 하고 그래서 HBM을 왕복한다(정적 7,522: store 1,653+1,653+755, load 2,934+527). down도 L로 쪼개면(64 행그룹 × 4청크 = **256 슬라이스, 60행, 1,920열** — V237/V246이 죽인 것은 하나도 안 건드린다) 생산자와 소비자가 같은 클러스터에 있어 **switch로 칩 안에서** 전달되고 `inv_s`는 스크래치가 아예 필요 없어진다. 대가는 클러스터마다 **부분** [H]이라 마지막에 한 번 더하는 것 | — | — | — | — | 잡 1개, n=9: base **298,663** vs ls **338,933 (+40,270, +13.5%)** | **FAIL** (max\|Δ\| 0.82, 14.8% within tol) | **기각(결정적) — 그리고 일반 법칙을 준다.** 정확도도 틀렸지만(순열 또는 클러스터 합산) **고칠 가치가 없다**: 슬라이스당 3,840 B × 256 = 클러스터당 983 KB를 **switch로 뿌리는 데 ~37k**가 든다(≈27 B/cycle). 같은 바이트를 **HBM에서 읽으면 2,934**(≈335 B/cycle)다. ⇒ **대량 재분배는 HBM이 switch보다 한 자릿수 빠르다. switch는 링이 작거나(V206 ring 4) 스칼라일 때만 이긴다.** V245의 switch-scatter도 컴파일됐더라도 같은 이유로 졌을 것이다 |
| `V251_attnout_direct_x` → `V252_submit` | `V243` | **attn_out의 x 스테이징 왕복 제거.** 지금은 x를 16슬라이스로 싣고(535) f8 두 조각으로 쪼갠 뒤 HBM에 store 399 + 399, 다시 512슬라이스로 load 933 한다. **쪼개기를 소비자 레이아웃(512슬라이스)에서 직접** 하면 슬라이스당 읽는 바이트는 그대로(256 bf16 = 512 B = 2×256 f8)인데 **명령 3개가 사라지고**, 쪼개기를 기다리느라 DMA 엔진이 쉬던 527 cycle도 사라져 O-weight 스트림이 2,464가 아니라 ~1,000에 시작한다 | — | — | — | — | **잡 7개 짝비교: −4,159 / −3,637 / −3,851 / −3,695 / −6,341 / −4,305 / −3,401 → 7/7 음수, 평균 −4,198 (−7.9%), 부호검정 p = 0.0078** | 전부 **PASS** (변형별 대조 확인: `attn_pass_lines=2`) | **채택 — 이번 캠페인 최대 코드 이득.** 예상 median 점수 6.294 → **6.469**. 프로덕션 `V252_submit`(= V243 + 이것, `src/device/`만, 3/3 PASS 확인). **이 수법은 attn_out 전용이다:** qkv·ffn은 슬라이스마다 x **전체**가 필요해서 직접 로드가 512 디스크립터 × 전체 벡터(V154의 45k 병리)가 되지만, attn_out은 슬라이스마다 x의 1/16만 쓴다 |
| 하네스 회귀 (V251 중 발견) | — | 반복된 arm 복제가 match 블록을 통째로 중복시켜 **attn_out과 qkv가 첫 실행만 대조**하고 있었다(`if i == 0`). 변형이 빠르면서 틀려도 통과할 수 있는 상태였다 | — | — | — | — | — | — | **수정.** 세 shim 모두 `plan.order[..i].iter().all(...)`로 **변형별 첫 실행을 대조**한다. 이후 모든 잡에서 attn_out PASS 줄이 2개임을 확인했다. **교훈: 하네스 자체를 정기적으로 검증할 것 — 측정 도구의 조용한 회귀는 모든 결론을 무효화한다** |
| `V253_attnout_split_store` | `V251` | **RAW 사슬을 짧게.** epilogue의 임계 경로는 contraction store → HBM read-after-write 1,600 → load이고 **마지막 store만** 그 위에 있다. 120행을 60+60 두 store로 쪼개면 마지막 store가 2,040 → ~1,020이 되고 앞 60행은 tile1의 weight 로드와 겹친다 | — | — | — | — | 잡 4개 짝비교(V251 대비): **+3,533 / +595 / +1,343 / +1,693 → 4/4 양수, 평균 +1,791 (+3.6%)** | 전부 **PASS** | **기각.** DMA 명령 하나(~548)를 더 만드는 비용과 스케줄러의 재배치가 짧아진 RAW 사슬보다 크다. **V199가 잰 "attn_out에서 DMA 명령 하나의 실물 가치는 음수"가 다시 확인됐다** — attn_out은 명령 수에 민감하고, 그래서 V251(명령 3개 제거)이 그렇게 크게 이긴 것이다 |
| `V254_attnout_ring_store` | `V251` | **store 디스크립터를 줄인다.** contraction store는 **2,040 cycle, util 0.003**으로 7,680 B를 쓴다(live 슬라이스 16개 × 120 B = 64 디스크립터) — V251 이후 커널에 남은 최악 항목이다. 행그룹 4개를 ring-64로 묶으면 8 디스크립터 × 960 B가 되어 store가 ~560이 된다. ring이 packet-major로 배달하므로 repack pass를 하나 붙인다 | — | — | — | — | 잡 4개 짝비교(V251 대비): **+2,021 / +2,801 / +2,001 / +3,225 → 4/4 양수, 평균 +2,512 (+5.1%)** | 전부 **PASS** | **기각.** switch(3,840 B/클러스터) + repack pass가 아낀 디스크립터보다 비싸다. **V250의 법칙이 작은 데이터에서도 성립한다** — 원본 코드의 주석("ring이 아끼는 것보다 비싸다")이 새 레이아웃에서도 옳았다. V253과 합쳐 **attn_out의 contraction store는 두 방향(쪼개기·묶기) 모두 닫혔다** |
| `V255_attnout_norm_in_place` (= V45 슬롯) | `V251` | **post-attention RMSNorm을 contraction 레이아웃에서.** [H] 벡터가 HBM을 왕복하지 않고 **f32 스칼라 하나만** 클러스터를 건넌다: store 2,040 + reload 546이 4 B store/load이 되고, 그 사이 RAW 지연은 어차피 필요한 피연산자 로드 3개가 덮는다. **두 번 lowering에서 죽었던 자리**인데, 막던 것은 reduce 순서였다 — chunk reduce 뒤 live 슬라이스는 행그룹(**바깥** 축)이고 VRU는 안쪽만 줄인다(V186). 슬라이스별 부분합 16개를 먼저 한 슬라이스로 ring하면 cross-slice 합이 intra-slice 합이 되고, 옮기는 건 16 × 32 B뿐이다 | — | — | — | — | 잡 5개 짝비교(V251 대비): **+1,727 / +2,489 / +831 / +3,629 / +2,771 → 5/5 양수, 평균 +2,289 (+4.8%)** | **FAIL** (5/5) | **기각 — 이번엔 컴파일이 아니라 실측으로.** **처음으로 컴파일에 성공했다**(ring gather가 reduce 순서 벽을 우회한다). 그런데 ① Main pass가 3개 → 5개로 늘고 ② 피연산자 3개를 32디스크립터 레이아웃으로 싣고 ③ 출력 store가 live 슬라이스 8개 → 16개가 되어, 없앤 store 2,040 + reload 546보다 비싸다. 정확도도 틀렸지만(ring의 packet-major 순서 또는 클러스터 합산) **고칠 값어치가 없다.** ⇒ **V45 가족은 이제 "막혔다"가 아니라 "측정해서 졌다"로 닫힌다** |
| `V256_qkv_chunked_kv` | `V252` | **qkv를 chunked reduction으로 바꾸는 것의 사전 측정.** 지금은 슬라이스마다 통짜 행을 갖고 **H 전체**가 필요해서 x를 512슬라이스로 복제한다 — `ops.rs:78`의 ring-32 broadcast, **정적 Main 7,943**으로 qkv 최대 Main 항목(실물 ≈16k, 커널의 16%). 슬라이스에 H **청크**를 주면 x를 스테이징에서 직접 읽을 수 있다(V251을 성립시킨 바로 그 렌즈). **K/V만** 바꿔서 broadcast 이득 **없이** 구조 비용만 잰다: weight 런 15,360 B → 1,920 B, `vector_inter_slice_reduce` 추가, head gather가 ring-64-stride-1 → ring-32-stride-2 | — | — | — | — | 잡 5개 짝비교: **+20,974 / +19,506 / +20,340 / +21,942 / +20,806 → 5/5 양수, 평균 +20,714 (+21%)** | 전부 **PASS** (변형별 6줄 확인) | **기각(결정적) — 그리고 qkv 전면 전환도 함께 닫는다.** 구조 비용이 broadcast가 아끼는 것(≈16k)보다 **한참** 크다. q까지 바꾸면 스트림 벌점만 더 커진다. **그리고 V215를 뒤집는다:** V215는 *탐침*에서 3,840 B 런이 −3,240으로 빨랐지만, **실제 레이아웃에서 1,920 B 런은 +21%다.** V237/V246과 같은 교훈이 세 번째로 확인됐다 — **탐침 수치는 레이아웃 결정에 쓸 수 없다.** 5잡으로 몇 시간을 아꼈다 |
| `V252_submit` draw 3회 | — | V252(= V243 + V251)의 실제 분포를 본다 | — | — | — | — | **6.4795 / 5.6868 / 6.3222** (2026-09-10 17:06~17:12) | — | **최고 6.4795로 기존 SOTA 6.6075를 넘지 못했다.** 예상 median 6.466과 일관된다. 5.6868은 한 커널이 크게 나쁘게 뽑힌 outlier |
| `V257_attnout_one_piece` | `V252` | **x를 f8 한 조각으로.** attn_out의 tolerance는 셋 중 가장 느슨하고(atol **0.05** vs qkv 0.04, ffn 0.01) single-piece 질문은 더 빡빡한 두 커널에만 물어봤다(V137/V138 qkv, V142 ffn). 조각을 하나로 줄이면 contraction의 `Dummy2` 재생 축이 사라져 **weight 패킷을 두 번이 아니라 한 번 fetch**하고, split pass 하나와 x 바이트 절반이 없어진다 | — | — | — | — | **잡 7개 짝비교: −5,406 / −5,706 / −4,652 / −6,106 / −4,680 / −5,778 / −4,802 → 7/7 음수, 평균 −5,304 (−9.9%), 부호검정 p = 0.0078** | **PASS** — **그런데 이 PASS는 공허하다** | **채택 → `V258_submit`** (3/3 PASS 확인). **다만 근거를 반드시 남긴다.** 채점 fixture의 attn_out x는 `s.signs(ctx, "x", 1.0)`으로 **정확히 ±1**이다. 따라서 x·16 = ±16이 f8e4m3에 **정확히** 표현되고 **낮은 조각은 항상 0**이다 — 두 변형의 max\|Δ\|가 0.01562로 **바이트 단위 동일**했던 이유가 이것이다(그 값은 계산 오차가 아니라 출력 bf16 반올림 1 ulp다). **실제 attention 출력(value 행들의 볼록결합)에서는 한 조각이 ~3.6% 상대오차를 낸다.** ⇒ **Stage 1 전용이다. Stage 2로 그대로 가져가면 안 된다** |
| `V259_attnout_tiles_post_1p` | `V257` | V257이 `Dummy2` 재생을 없애 contraction Main이 절반이 됐으니, V203이 88/32를 고를 때와 조건이 다르다 — 타일 분할을 다시 스윕한다(104/16, 72/48) | — | — | — | — | 두 변형 모두 **비정상**(1a stdev 32%, 1b +88%) | **FAIL** (non-finite) | **취소(구현 결함, 결과 무효).** 변형 생성 스크립트가 contraction pass의 행 수만 바꾸고 **weight 타일 로드의 행 수는 안 바꿔** 둘이 어긋났다(`m![H / 120, H % 120 = 32 …]`는 `m![H % 120 = 32`로 시작하지 않아 치환에서 빠졌다). **재시도하지 않는다:** V203이 이미 같은 축을 쟀고 변형 간 차이가 2% 안이었으며, V257이 바꾼 것은 Main이지 구속 자원이 아니다. 부수 교훈: **변형을 문자열 치환으로 생성할 때는 치환이 모든 자리에 닿았는지 확인할 것** |
| `V258_submit` draw 4회 | — | V258(= V252 + V257)을 공식 제출 | — | — | — | — | **6.8026 / 6.5029 / 6.4821 / 6.8279** (2026-09-10 17:23~17:28) | 3/3 **PASS** | **새 공식 SOTA 6.8279** (직전 6.6075, **+3.3%**). draw 평균 **6.654**로 V243의 10회 평균 6.2363 대비 **+6.7%** — attn_out 두 변경(V251 −7.9%, V257 −9.9%, 합쳐 ≈−17%)이 예측한 점수 +6.1%가 **분포 이동으로 확인**됐다. 최고 draw(6.8279, `c5dc8d05`)의 커널값: qkv **96,160** / attn **42,553** / ffn 288,220 (리더보드 API. 2026-09-11 정정 — 이 칸에 있던 96,759 / 42,467 / 290,230은 기하평균 6.8026, 즉 첫 draw의 값이었다) |
| `V260_attnout_split_store` | `V257` | **V253를 제대로 다시 한다.** V253은 store 두 개를 **둘 다 함수 끝**에 뒀기 때문에 겹치는 게 없었고 — 추가 명령 비용만 쟀다. 이번엔 rows 0..60을 **tile0의 contraction 직후**에 내보내 tile1의 weight 로드와 겹친다. 분할이 60/60인 이유: HBM 쪽 offset tile은 블록을 **정확히 나눠야** 한다(버퍼보다 덜 뻗는 view는 `unpad`인데 HBM에는 `unpad` API가 없다) — 그래서 88/32 타일 경계에 못 맞춘다 | — | — | — | — | 잡 5개 짝비교(V257 대비): **+6,200 / +1,908 / +5,318 / +6,568 / +4,924 → 5/5 양수, 평균 +4,984 (+10.3%)** | **PASS** | **기각 — 그리고 V253의 판정을 정정한다.** V253의 +1,791은 "명령 하나"의 값이었고, 제대로 겹치게 해도 **더 나쁘다**: 스트림 중간에 store를 끼우면 남은 weight 로드가 그만큼 밀린다. attn_out은 DMA 명령에 극도로 민감하다(V199·V251·V254와 같은 방향). **attn_out의 출력 store는 이제 세 방향 모두 닫혔다** |
| `V260_ffn_split_store` → `V261_submit` | `V258` | **down 출력 store를 반으로 쪼개 앞 절반을 구멍에 넣는다.** V258 스케줄에 **3,245 cycle의 유휴**가 있다(129,389–132,634) — 마지막 타일의 pass A(3,863 Main)와 pass B를 DMA 엔진이 기다리는 구간이고, 2,510짜리 출력 store는 그 *뒤에* 실행된다. rows 0..30은 두 번째 pass B에서 이미 완성되므로 그 절반을 구멍에 밀어넣으면 임계 경로에는 절반만 남는다. 분할이 30/30인 이유: HBM offset tile은 블록을 정확히 나눠야 한다(`unpad`는 HBM 쪽 API가 없다) | — | — | — | — | **잡 8개 짝비교: −2,449 / −2,417 / −5,881 / −2,467 / −1,688 / −4,515 / −139 / −3,421 → 8/8 음수, 평균 −2,872 (−0.97%), 부호검정 p = 0.0039** | 3/3 **PASS** | **채택 → `V261_submit`.** 같은 수법이 attn_out에서는 **+10.3%로 기각**됐다(V260 attn) — 차이는 명령 하나의 값이다: attn_out은 DMA 명령에 음수 가치(V199·V251·V254), ffn은 양수(V199 −11k). **ffn에는 넣고 attn_out에는 넣지 않는다** |
| V260 구현 결함 (기록) | — | 첫 측정에서 ffn이 −1,479로 이겨 보였다 | — | — | — | — | — | **FAIL (non-finite)** | **결과 무효.** 블록 치환이 **네 번째 down 타일의 contract+reduce를 통째로 삼켜** rows 48..60이 계산되지 않았다. 쓰레기 값은 빠를 수도 있다. **변형별 정확도 대조(V251에서 복구)가 아니었으면 −0.5% 승리로 보고했을 것이다.** 오늘 두 번째 생성기 결함이다(V259가 첫 번째) — **문자열 치환으로 변형을 만들 때는 치환 범위를 확인하고, 정확도 열을 항상 볼 것** |
| `V261_submit` draw 4회 | — | V261(= V258 + V260 ffn split store) 제출 | — | — | — | — | **6.6046 / 6.6123 / 6.1597 / 6.2407** (2026-09-10 17:53~17:59), 평균 6.404, 최고 6.6123 | 3/3 **PASS** | **기존 SOTA 6.8279를 넘지 못했다 — 그러나 이것은 V261의 판정이 아니다.** V261의 코드 이득은 ffn −0.97% = 점수 **+0.34%**로 draw σ(≈2%)의 6분의 1이라 4회로는 원리적으로 안 보인다. V258의 4회 평균(6.654)과의 차이도 **다른 시간대**라 비교 불가다(§10.0l). V261의 근거는 짝비교 8/8, p = 0.0039다 |
| E0 스케줄 census (브랜치 없음) | `V261` | **book의 두 DMA 규칙이 우리 스케줄에서 발동하는가.** ① "같은 bank 64 access" 규칙에 걸린 tensor-unit pass는 컴파일러가 DMA처럼 스케줄한다 — Main/Sub 노드가 `DmaEngine` 컨텍스트를 갖는지 센다. ② 레이아웃이 DMN에 고르게 안 나뉘면 명령이 쪼개진다 — 소스 줄당 DMA 명령 수를 센다 (`scripts/dev/census.py`) | — | — | — | — | V236 qkv · V258 attn/ffn 덤프: **①은 세 커널 모두 0건, ②도 0건** (2건 이상인 줄은 전부 같은 함수를 두 번 부른 것 — K/V 로드, norm weight). 대신 **attn contraction store = 2,040 static, 디스크립터 32개(개당 ~64)** vs 같은 7,680 B의 꼬리 store **648, 디스크립터 8개(개당 ~81)** | — | **두 규칙 모두 닫힌다.** **[정정 2026-09-11]** 여기서 "store 비용은 정렬이 아니라 디스크립터 수"라고 결론내고 E4(256 B 정렬 store)를 실행하지 않았는데 **틀렸다** — 그것은 **정적 모델만** 본 판단이었고 정적 모델은 쓰기 정렬을 값매기지 않는다. 같은 변경이 정적 −11, **실물 −831 (13/16, `V271`)** → `V273_submit` |
| `V262_qkv_stream_broadcast` (E1) | `V261` | **qkv weight 패킷을 두 f8 조각에 재생한다.** `fetch::<m![Qs % 8, H / 64, Dummy2], …>`는 weight에 없는 `Dummy2` 때문에 64 B 패킷을 DM에서 **두 번** 읽는다. `contract_outer`의 stream adapter는 TRF Element에 있고 입력 Time에 없는 축을 OutTime **최내측 broadcast**로 받는다 (furiosa-opt-lower-0.6.0 `stream_adapter.rs`: `OutTime = [outer Time, broadcast]`) — fetch/collect에서 `Dummy2`만 빼면 성립한다. V257과 달리 x에 무관하게 정확하다 | — | — | — | — | 잡 7개 짝비교(base → bc): **+877 / +1,996 / +3,238 / +2,594 / +2,316 / +1,924 / −341 → 6/7 양수, 평균 +1,801 (+1.8%)** | **PASS** | **기각.** 합법·정확하지만 **느리다.** qkv contraction pass는 fetch가 아니라 **OutTime 960 step**에 묶여 있고, adapter 재생은 DM 재읽기보다 싸지 않다. ⇒ V257의 −9.9%는 재생 제거가 아니라 **조각 하나를 통째로 없앤** 효과였다. **측정 함정:** 처음 두 잡은 `--no-run`이 **debug** 빌드여서 `arena.sh`가 **예전 release 바이너리**를 올렸다 — 항상 `--release --test test_kernels --no-run` |
| `V263_rmsnorm_fused` (E2) | `V261` | **RMSNorm 3 pass → 2 pass.** 평균제곱 pass의 `widen_pad` 뒤에 cross-slice 합을 붙인다 (`CanTransitionTo<InterSliceReduce> for Widen`, furiosa-opt-std-0.6.0 `stage/markers.rs:349`). inter-slice reduce 뒤에는 Tag/Filter/Output만 오고 `Clip`은 종단 단계라 `+EPS`는 sqrt pass로 옮겨 `vector_fp_binary(AddF, EPS)`(f32 즉치값 허용). 비트 단위 동일. 세 커널의 norm(qkv 입력, attn_out 뒤, ffn 앞·뒤)에 적용 | — | — | — | — | 1차(변형당 4회, 잡 6개): 세 커널 모두 3/6 → 판정 불가. **2차(변형당 8회, 잡 8개): attn_out −898 / −476 / −1,804 / −576 / −66 / −688 / −2,284 / −214 → 8/8 음수, 평균 −876 (−1.9%), p = 0.0078** · qkv 3/8 음수, 평균 +608 · ffn 5/8 음수, 평균 −761 | **PASS** (잡마다 10/10) | **attn_out 채택 → `V265_submit`, ffn 채택 → `V267_submit`.** 변형당 8회 배치 셋(E2b 8잡 · E2c 8잡 · E2d 16잡)을 합치면 **attn_out 23/32, 평균 −487 (−1.05%), p = 0.020** · **ffn 24/32, 평균 −1,082 (−0.37%), p = 0.007** · qkv 11/32, 평균 +316 (+0.32%), p = 0.11. attn_out은 첫 8잡 8/8 뒤 다음 8잡 4/8로 흔들렸다 — **효과가 σ의 절반 이하면 16잡 이상을 모은다.** qkv 입력 norm은 융합하지 않는다(ffn pre-ff norm은 복제본 `normalize_reduced_f32_fused`로 분리했다). 교훈: **효과가 2% 미만이면 변형당 4회로는 안 보인다 — 변형당 8회가 기준** |
| `V264_ffn_scale_max_one_pass` (E3) | `V261` | **geglu 스케일 max를 한 Sub pass로.** 지금은 슬라이스별 max pass → Main ring-256 `CustomBroadcast`(정적 1,031) → Sub max pass → pow2 scale. intra max → `widen_pad` → inter max 한 체인이면 switch와 pass 하나가 사라지고, gather를 기다리지 않고 나란히 돈다 | — | — | — | — | 1차 형상(gathered x 위) **lowering 거부**: `VRU reduce axes must be innermost in Partitioning` — `m![L / 480 % 16, 1 # 16]`은 패딩이 최내측이다(원래 코드가 switch를 쓴 이유). **재시도: f32 geglu 출력 `g`의 `UpGateRowsFull = m![L / 30 % 256]`**(행 그룹 축이 슬라이스 분할 전체)에서 `Dummy256` refill → 기존 `m_all`과 같은 타입, 빌드 통과. **잡 8개 짝비교(ds → g1): +4,341 / +5,395 / +4,329 / +7,157 / +4,185 / +3,525 / +3,365 / +5,169 → 8/8 양수, 평균 +4,683 (+1.6%), p = 0.0078** | **PASS** (잡마다 6/6) | **기각.** switch 하나와 pass 하나를 없앴는데 **4.7k 느려졌다.** ring-256 switch가 나르는 것은 **스칼라 16개**뿐이고, 대신 넣은 pass는 **256 슬라이스 × 30행 f32**를 읽으며 같은 `g`를 읽는 `gather_pack_full`의 Main pass와 겹친다. ⇒ **스칼라는 switch가 대량 pass보다 싸다** (V250의 짝: 벌크 재분배는 HBM, 스칼라 분배는 switch). geglu 스케일 경로는 닫는다 |
| `V265_submit` | `V261` | V261 + V263의 attn_out norm 융합 (`rmsnorm.rs`만, +6/−12줄) | — | — | — | — | Arena 검증 `V265_verify` **PASS** (2026-09-10 23:52 UTC, clean-target 빌드 — 복사한 target은 rustc ICE). draw 5회 **6.4389 / 6.7245 / 6.2679 / 6.3316 / 6.3702** (2026-09-10 23:52 ~ 09-11 00:01 UTC), 평균 6.427, 최고 6.7245 | **3/3 PASS** | **공식 최고 6.8279를 넘지 못했다 — 그러나 V265의 판정은 아니다.** 코드 이득(attn_out −1.1% ≈ 점수 +0.37%)은 draw σ(≈2%)보다 한참 작고, 시간대가 다른 draw끼리는 비교할 수 없다(§10.0l). V265의 근거는 짝비교다 |
| `V266_attnout_tile_resweep` (E5) | `V261` | **attn_out O-weight 타일 분할 재스윕.** 88/32는 V203이 contraction이 지금의 두 배(두 조각)였을 때 고른 값이다. 104/16 · 96/24 · 72/48을 한 잡 안에서 회전 비교(변형당 8회). V259의 생성기 결함(로드 사이트 누락)은 **사이트 개수 검사(12/12/2/2/2)**로 막았다 | — | — | — | — | 잡 8개(88/32 대비): **104/16** 5/8, 평균 −116 · **96/24** 6/8, 평균 −594 (−1.25%), p = 0.29 · **72/48** 3/8, 평균 +280. E5b(88/32 vs 96/24 단독, 변형당 8회 × 16잡, `scripts/dev/pairjobs.sh`): **13/16, 평균 −788 (−1.7%), p = 0.021** — 두 배치 합계 19/24, 평균 −723 (−1.5%), p = 0.007 | **PASS** (잡마다 전 변형) | **96/24 채택 → `V268_submit`.** V203의 88/32는 contraction이 x 두 조각을 재생하던 때의 최적이었고, V257이 그 Main을 절반으로 줄이자 최적이 큰 첫 타일 쪽으로 옮겨 갔다. **V259의 "재시도하지 않는다"는 틀렸다** — 결함 있는 생성기의 무효 결과와 V203의 옛 조건에 기댄 판정이었다 |
| `V267_submit` | `V265` | V265 + V263의 ffn norm 융합 — post-ff norm은 제자리, pre-ff norm은 복제본(`normalize_reduced_f32_fused`)으로 qkv 입력 norm과 분리 (`rmsnorm.rs` +83/−13, `ops.rs` 1줄) | — | — | — | — | Arena 검증 `V267_verify` **PASS** (2026-09-11 00:18 UTC, lab3 증분 빌드 37초). draw 3회 **6.2254 / 7.0314 / 6.6032** (00:18~00:22 UTC) | **3/3 PASS** | **새 공식 최고 7.0314** (`e1fd59ad`: 98,645 / **38,623** / 283,438 — attn_out은 리더보드 전체 최고). **그러나 이 점수는 V267 코드의 증거가 아니다.** 같은 코드가 4분 안에 6.2254와 6.6032도 냈고(평균 6.620), 7.0314는 attn_out이 평소(41~42k)보다 ~7% 튄 꼬리 draw다(§10.0l). V267의 근거는 V263 짝비교(ffn 24/32)다 |
| `V268_submit` | `V267` | V267 + V266의 attn_out 타일 96/24 (`projection.rs`의 `project_output`만, 타일 사이트 30곳) | — | — | — | — | Arena 검증 `V268_verify` **PASS** (2026-09-11 00:25 UTC, lab3 증분 빌드 23초). draw 3회 **6.9642 / 6.3307 / 6.5570** | **3/3 PASS** | **현재 코드 SOTA.** 이번 라운드의 채택분 셋(V263 attn_out · V263 ffn · V266)이 모두 들어 있다. draw 평균 6.617로 V267의 3회 평균 6.620과 같다 — 코드 이득(점수 +0.5%)은 draw σ보다 작아 원리적으로 안 보인다(§10.0l). 공식 최고는 V267의 7.0314 그대로다 |
| `V269_attnout_tile_refine` (E5c) | `V266` | **96/24 주변 미세 스윕.** 100/20 · 92/28을 96/24(`tb`)와 한 잡 안에서 회전 비교 (`scripts/dev/multijobs.sh`, 변형당 8회 × 16잡) | — | — | — | — | **100/20: 8/16, 평균 +3, p = 1.0 · 92/28: 8/16, 평균 +135, p = 1.0** | **PASS** (잡마다 7/7) | **기각 — 96/24가 평평한 최적의 한가운데다.** 타일 분할 축은 이것으로 닫는다 |
| `V270_qkv_ring_retest` (R16) | `V263` | **qkv x broadcast ring 재측정.** V206(3잡, n=4)은 qkv에서 모든 ring이 1/3이라 판정이 약했다. 같은 V206 헬퍼(`broadcast_x_r16` · `broadcast_x_r8`)를 변형당 8회 × 16잡으로 ring 32와 비교 | — | — | — | — | **ring 16: 7/16, 평균 +698, p = 0.80 · ring 8: 3/16, 평균 +1,308, p = 0.021 (유의하게 느림)** | **PASS** (잡마다 11/11) | **기각 — V206의 판정을 강한 통계로 확인했다.** qkv는 ring 32가 최적이다(ffn의 ring 4와 반대: qkv는 사본마다 x2 영역을 다시 읽는 부담이 크다) |
| 정적 탐침 X1 · X5 · X3 · X6 (브랜치 없음, `lab3`의 V268 트리에서 `--dump-schedule`) | `V268_submit` | **X1** attn contraction store를 256 B 정렬 HBM 오프셋(`m![H / 120, H % 120 # 128]`)에 · **X5** residual 로드를 커널 머리로 · **X3** ffn down 출력 store를 20/20/20으로 · **X6** ffn down global-scale pass 제거 | — | 23,386 → **23,375 (X1)** · **23,386 (X5)** | 139,568 → **142,740 (X3)** · **139,088 (X6)** | — | — | — | **X1은 정적 모델이 정렬을 값매기지 않는다(−11) → 실물로(V271, 이겼다). X5는 스케줄이 바이트 단위로 같다 — 로드 위치는 소스가 아니라 소비 시점이 정한다. X3는 세 번째 store(2,764)가 임계 경로에 남아 +3,172 → 기각. X6는 꼬리 Main pass·로드·Sub 하나씩이 빠져 −480 → 실물로(V272, 정확도에서 졌다)** |
| `V271_attnout_aligned_store` (X1) | `V269` | **attn contraction 출력을 256 B 정렬 오프셋에.** 슬라이스 32개가 각자 240 B를 granule 중간에서 쓰기 시작한다(store 2,040 정적, util 0.003 — 꼬리의 최대 항목). 출력을 행그룹마다 16 B 패딩을 둔 `m![H / 120, H % 120 # 128]`에 쓰고 그 레이아웃에서 reload | — | — | — | — | 잡 16개 짝비교(tb → tx): −512 / −606 / −788 / −504 / +360 / −570 / +442 / −1,137 / −2,790 / +130 / −450 / −1,889 / −2,434 / −754 / −1,478 / −310 → **13/16, 평균 −831 (−1.7%), p = 0.021** | **PASS** (잡마다 6/6) | **채택 → `V273_submit`. 그리고 E0의 결론을 뒤집는다** — 정적 모델은 이 변경을 −11로 봤다. book의 RMW 벌점은 실물에 있고 **정적 탐침으로는 안 보인다** |
| `V272_ffn_no_global_scale` (X6) | `V270` | **ffn down global-scale pass 제거.** 가설: post-FF RMSNorm은 양수 스칼라에 불변이다 — RMSNorm(x·g) = x / √(mean x² + ε/g²)이라 mean(x²) ≫ ε이면 ε의 무게만 바뀐다. down 출력의 유일한 소비자가 그 norm이므로 곱셈 pass·로드·VRF 스테이징이 할 일이 없다 | — | — | 139,568 → 139,088 | — | 잡 16개: **매 잡 FAIL 1줄 — `g6`의 ffn 출력이 tolerance 밖** (기준 `dn`은 V263 이래 전 잡 PASS). 시간은 9/16, 평균 −498, p = 0.80이지만 FAIL 변형의 시간은 의미가 없다 | **FAIL** (16/16 잡, `g6`) | **기각 — 계산이 같지 않았다.** 스케일 불변성은 mean(x²) ≫ ε일 때만 성립한다. 채점 fixture의 weight 행 스케일이 ≤1e-3이라 down 출력의 평균제곱이 ε(1e-6) 근처 이하이고, 원래 커널의 RMSNorm은 **ε가 분모를 지배한다**. global scale(1/12,928)을 빼면 x가 12,928배가 되어 정규화가 제대로 돌고 출력이 달라진다. 정적 −480은 진짜였다. ⇒ **norm의 스케일 불변성을 쓰기 전에 ε 영역인지 확인할 것** (fixture 수치의 함정 — V257의 반대 방향) |
| `V273_submit` | `V268` | V268 + V271 (`projection.rs` · `rmsnorm.rs` · `ops.rs` 1줄) | — | — | — | — | Arena 검증 `V273_verify` **PASS** (2026-09-11 01:36 UTC, lab3 증분 빌드 34초). draw 3회 **6.6436 / 4.7458 / 6.1271** — 4.7458은 qkv 하나만 279,933 cycle이다(V265 이래 바뀌지 않은 qkv 코드의 채점기 이상치; attn 43,485 · ffn 288,516은 정상이고 전부 PASS) | **3/3 PASS** | **현재 코드 SOTA** |
| 정적 탐침 Z1 (브랜치 없음, `lab3`의 V273 트리) | `V273_submit` | **꼬리 pass가 VRF를 직접 쓴다** (`.vector_final().to_vrf()`; 0.6.0에 `CanApplyToVrf for PositionVectorFinal`이 있다) — DM commit과 Sub staging pass를 없앤다. 사이트별로 잰다: attn norm · ffn post/pre norm · qkv q norm · k/v norm · RoPE sin | 43,006 → **+0 (q norm) · +81 (k/v norm) · +2,584 (RoPE)** · 전부 +465 | 23,375 → **23,090 (−285)** | 139,568 → **−285 (post) · −206 (pre) · −491 (둘 다)** | — | — | — | 정적으로는 attn·ffn 후보, qkv RoPE는 나쁘다 → attn을 실물로 (`V274`) |
| `V274_attnout_norm_vrf_direct` (Z1) | `V271` | attn_out post-attention RMSNorm의 sqrt pass가 rms VRF를 직접 쓴다 (`tn` = V273의 V263 norm 복사, `tz` = Z1) | — | 23,375 → 23,090 (`tn`은 V273과 스케줄이 바이트 단위로 같다) | — | — | 잡 2개 모두 **`tz`의 첫 launch에서 `HAL error PXI-601 (Execution: Timeout)`** — 같은 잡의 `tn`은 45,461로 정상 완료. 첫 16잡 배치의 빈 중앙값(`pass=4`)도 이 멈춤이었다 | **HANG** | **기각 — Main vector pass의 VRF 직접 쓰기는 하드웨어를 멈춘다.** ffn(−491)·qkv norm의 같은 형태도 함께 닫는다. Sub staging은 VRF의 필수 경로다 |
| 정적 탐침 Q2a · Q2b (`lab3`의 V273 트리) | `V273_submit` | **qkv의 x TRF staging 두 번(1,223 × 2, 512슬라이스 × 7.7 KB)을 줄인다.** TrfTensor엔 `reshape`가 없으니 weight 뷰를 relabel한다. **Q2a** query 레이아웃 Sub staging 하나를 Q·K·V가 공유 · **Q2b** ring-32 broadcast pass가 `collect().to_trf()`로 TRF를 직접 씀 (`CanApplyToTrf for PositionCollect`) — x의 DM commit과 staging 둘을 없앤다 | 43,006 → **43,572 (Q2a, +566)** · **41,921 (Q2b, −1,085, DMA 명령 21 → 20, 로드 순서 Q → K → V라 RoPE 없는 V의 꼬리가 마지막)** | — | — | — | — | — | Q2b를 실물로 (`V276`) |
| `V276_qkv_switch_to_trf` (Q2b) | `V271` | 위 Q2b (arm `qb`, 헬퍼는 V271 하네스에 복사) | 41,921 | — | — | — | 잡 16개 짝비교 (`""` → `qb`): **16/16 음수, 평균 −4,258 (−4.3%), p < 1e-4 — 그러나 가짜** | 하네스에서는 PASS → **`qb`를 첫 launch로 둔 잡: q 138.65% · k 161.38% · v 192.75% FAIL** (기준 arm은 PASS) | **기각 — 계산이 틀렸다.** Main switch pass의 TRF 쓰기는 contraction이 읽는 TRF에 x를 넣지 않는다. 하네스는 variant별 **첫 launch에서만** 정확도를 보는데, 기준 arm `""`이 먼저 돌며 Sub staging으로 TRF에 올바른 x를 남겨 두었고 `qb`가 그걸 읽어 PASS했다 — 일을 덜 하니 빨랐다. `V278` 제출 검증(차가운 단독 실행)이 같은 수치로 FAIL해서 드러났다. ⇒ **상태를 쓰는 방식을 바꾸는 변형은 변형을 첫 launch로 둔 잡으로 먼저 검증한다** (RULES §10.0p) |
| `V277_ffn_switch_to_trf` (F2b) | `V273_submit` | ffn up/gate의 ring-4 broadcast pass가 TRF를 직접 쓴다 (Q2b의 ffn판, arm `f2`). **이 브랜치의 첫 커밋 `1d38e0e`는 V273_submit + 하네스 base** (`mk_base273.py`: variant arm 제거, sweep ×3) | — | — | 139,568 → **138,997 (−571, DMA 명령 42 → 41)** | — | 잡 9개 (중단): **9/9 양수, 평균 +6,211 (+2.1%)** — +6,855 / +6,043 / +3,031 / +7,337 / +5,963 / +4,991 / +6,567 / +8,111 / +7,005 | 하네스 PASS (Q2b와 같은 이유로 의미 없음) | **기각 — 느리고, Q2b와 같은 쓰기라 정확도도 믿을 수 없다** |
| `V278_q2b_submit_verify_failed` | `V273_submit` | V273 + Q2b in place (`project_query` / `project_key_value`가 TRF를 받는다) | 41,921 | — | — | — | Arena 검증 `V278_verify`: **qkv FAIL ×3 라운드** (cycles 95~97k), attn·ffn PASS | **FAIL** | **draw 안 함.** Q2b 무효의 첫 증거. 브랜치 이름에 실패를 적어 둔다 (draw 스크립트가 `*_submit`을 집지 않게) |
| 정적 탐침 load order s1 · s2 · s3 · s5 · s6 (`lab3`의 V273 트리) | `V273_submit` | **Q2b 스케줄의 이득(V weight 마지막)을 올바른 staging만으로 얻는가.** projection마다 읽을 Sub staging을 바꾼다: s1 staging 셋 · s2 V가 query staging · s3 K가 query staging · s6 K/V staging 하나 · s5는 소스에서 K/V projection을 먼저 | **s5 43,006 (바이트 단위 동일)** · **s1/s2/s3/s6 +566~+569** — Q가 먼저 로드되지만 10,781에야 시작한다 | — | — | — | — | — | **닫힘.** 소스 순서는 무관(X5 · pa/pb/pc와 같은 결론)하고, staging 공유는 Q를 늦게 먼저 로드할 뿐이다 |
| `V279_qkv_single_x_staging` (Q2a) | `V277` 하네스 base `1d38e0e` | Q2a: query 레이아웃 Sub staging 하나를 Q·K·V가 공유 (arm `qa`). Sub staging만 TRF를 쓰므로 정확도는 구성상 보장 | 43,572 | — | — | — | 변형 먼저 잡: **PASS**, `qa` 105,827 vs 99,280. 짝비교 **11/11 양수, 평균 +7,448 (+7.5%)** (중단) | **PASS** (변형 먼저) | **기각 — 크게 느리다.** 정적 +566(+1.3%)의 5배 이상. K/V contraction이 query 라벨의 TRF를 읽는 비용과 로드 순서 변화(Q 먼저)를 정적 모델이 크게 과소평가한다 |
| 정적 탐침 D1 (`lab2`의 V279 트리) | `V273_submit` | **attn_out의 `DramReuse`** (19,672–20,380, Core 708, 그 앞 DMA 유휴 508): `addr.py` 주소 지도에서 reload 텐서가 슬라이스 오프셋 0 — contraction store staging 버퍼 자리 — 에 놓인다. reload 버퍼를 커널 시작에 `DmTensor::new()`로 만들어 재사용을 피한다 (페이지 여유: 31,744 + 960 ≤ 32,768 B/슬라이스) | — | **23,375 (동일, DramReuse 그대로)** | — | — | — | — | **닫힘.** DramReuse는 그 할당에 묶여 있지 않다. 참고: 모든 DM 텐서가 물리 페이지 0(슬라이스당 32 KB)에 있고 O-weight 타일이 30,720 B를 쓴다 |
| `V273_submit` draw 4회 추가 | — | 코드 SOTA의 분포를 더 뽑는다 (제출 상한 없음) | — | — | — | — | **6.4183 / 6.6584 / 6.2922 / 6.7103** (2026-09-11 04:04~04:14 UTC). 누적 7회: 6.6436 / 4.7458 / 6.1271 / 6.4183 / 6.6584 / 6.2922 / 6.7103 | 전부 PASS | 기록 7.0314를 넘지 못했다. 이상치 4.7458을 뺀 6회 평균 6.475 |
| `V280_hw_span_dump` | `V277` 하네스 base `1d38e0e` | **실물 타임라인 도구.** `furiosa_profiled_run`은 span마다 `name` + `begin_cycle` + `end_cycle`을 `span::npu`로 넘기는데 하네스는 begin/end만 읽었다. span 이름을 기록하고, 테스트 바이너리 `main` 첫 줄에서 `TUC_PROFILE_LEVEL=trace`를 켠다(엔트리포인트 레벨에서는 launch당 `Task` 1개뿐) | — | — | — | — | launch당 span **ffn 144 · qkv 82 · attn 24** (`Task` · `DMA` · `Renegade::TuExec` · `StoVrf`/`StoTrf`/`StoTab` · `Cluster`). cycle·정확도 그대로 | PASS | **도구 채택.** 판독: ① `Cluster` = 클러스터 간 HBM store 뒤 ExplicitSync 대기, **실물 0.4~15k**(정적 600): ffn 9개 합 32.4k(11%), qkv 16.6k(16%), attn post-store 9~14k ② 동기화 뒤에 나열된 Sub staging은 대기 끝까지 멈춘다 ③ **대기 길이는 같은 코드에서 launch마다 무작위**(attn 4.1~14.2k) — attn launch 분산의 주성분 ④ PE는 명령 목록을 정적 begin 순서로 걸으며 입력이 준비될 때까지 멈춘다(attn tile1 로드는 tile0 로드가 끝나야 발행). RULES §10.0q |
| span 비교: `qb` · `qa` (V276 · V279 트리 + span 패치, 커밋 없음) | — | Q2b의 가짜 −4.3%와 Q2a의 +7.5%가 실물에서 어디서 오는가 | — | — | — | — | 같은 잡 qkv warm: base **102,973** · `qb` **89,648**(틀린 계산) · base 100,682 · `qa` **114,226** | `qb` FAIL(차가운) · `qa` PASS | `qa`는 x2 store 뒤 동기화(14.9k)가 Q 로드보다 **앞에** 나열돼 DMA가 12.1k 놀고 뒤에 15.5k 동기화가 또 온다. `qb`는 weight 로드 셋이 동시에 겹쳐 돈다. ⇒ **목록 순서와 동기화 위치가 실물 비용을 정한다.** 올바른 변형으로 같은 순서를 얻는 방법은 아직 없다(load-order s1–s6, Q2a, V282) |
| `V281_attnout_tail_both_clusters` (BC) | `V280` | **post-attention norm 꼬리를 두 클러스터에서** (`m![Qs / 2048]` 복제 로드, rmsnorm 헬퍼는 클러스터 제네릭). 가설: 한 PE 그룹이 놀아서 post-store 동기화가 길다 | — | 23,375 → 23,477 | — | — | 잡 1개(trace), 변형당 8 launch: base 45.8~48.9k · **bc 41.7~49.8k** (중앙값 48.5k vs 47.2k). span: 대기 base 9.2k · 11.6k, **bc 4.1k · 13.4k** — 42k대 launch는 정확히 대기가 짧은 launch | PASS | **기각 — 가설이 틀렸다.** 동기화 대기 길이는 구조가 아니라 launch마다 무작위다 |
| `V282_attnout_shared_tile_buffer` (ST) | `V280` | **O-weight 두 타일을 한 DM 버퍼로** (`to_dm_view` 타일 로드). 정적 순서에서 tile1·scale 로드를 contraction0 앞으로 끌어와 실물에서 동시에 발행되게 | — | 23,375 → **27,431** — 순서는 의도대로(tile1 15,319 · scale 18,436 < contraction0 18,636), DramReuse도 사라짐 | — | — | 잡 1개(trace), 변형당 8 launch: **st 50.6~58.3k vs base 47.7~52.5k (+17%)**. span: tile1 로드는 여전히 tile0 뒤(24,866), **tile0 로드 18.1k → 23.0k**, 겹친 x_trf staging 0.4k → 3.0k | PASS | **기각.** 같은 버퍼로의 타일 로드는 동시에 돌지 않고, 겹치는 메모리 트래픽이 서로를 늦춘다 — "목록 순서만 당기면 로드가 동시에 돈다"는 모델은 틀렸다 |
| `V284_attnout_one_cluster_no_store` (OC) | `V280` | **attn_out을 한 클러스터에서, 중간 store·ExplicitSync 없이.** 0.6.0은 모든 `DmaStore` 뒤에 `ExplicitSync`를 넣고(세 커널 정적 스케줄에서 예외 없음), contraction store 뒤의 것이 실물 4~16k다. contraction을 한 클러스터(16 행그룹 × 240행 × 16 열청크 = 256 슬라이스, weight 바이트 동일)에서 돌리고 출력을 같은 클러스터 DM→DM `to_dm`으로 reducing 레이아웃에 중계 (`scratchpad/oc.py`) | — | 23,375 → **34,166** (tile0 로드 10,816 → 21,082) | — | — | 잡 3개(trace), 변형당 8 launch: **+8,356 / +9,104 / +7,199 → 3/3, 평균 +8,220 (+17.5%)**. span: 중간 동기화는 사라졌지만 **tile0 로드 18.5k → 34.1k (1.84×)**, tile1 6.6k → 10.1k, contraction 192행 4.5k(96행 3.0k), DM→DM 중계 1.9k | PASS | **기각.** 두 클러스터에 쓰는 로드는 DMA 대역폭을 거의 두 배로 쓴다(book: DM 256 B/cycle *per cluster*) — 로드 손해(+19k)가 동기화(−12k)보다 크다. 동기화 제거는 클러스터 수를 줄이지 않는 방법으로만 |
| `V285_attnout_order_probe` (ordA) | `V280` | **동기화 대기가 프로그램 구조에 달렸는지 순서 강제로 본다.** 새 도구 셋: `BEAM_SEARCH_TRACE_DUMP_PATH`(스케줄러 노드 이름 `T29 := Dma.StoD(...)`), `SCHEDULER_MANUAL_ORDERING_PATH`(한 줄에 `T<a> -> T<b>`), `DUMP_PE_PROGRAM`(PE C 프로그램). C 프로그램에서 ExplicitSync는 두 클러스터 master PE 간 IPC다: `sync_intra_chip_cluster(n)`(push)과 `wait_sync_intra_chip_cluster(n)`(상대 push 대기)이 서로 다른 블록에 있고, base는 push(1)과 wait(1) 사이에 residual 로드·staging 블록이 끼어 있으며 타임스탬프 쌍이 3개다. **ordA** = residual·scale·rms weight 로드와 staging을 전부 contraction store 앞으로(간선 6개) → push(1) 바로 뒤에 wait(1), 쌍 2개. 순서 파일은 빌드의 모든 커널에 적용되고 캐시로 격리되지 않으므로 **attn만 참조하는 하네스**를 순서 없이(X)·있게(Y) 두 번 빌드해 잡마다 번갈아 돌린다 (`/root/tk/one.sh`) | — | 23,375 → 24,421 | — | — | attn만 도는 하네스, 잡 12개(쌍 6개, 순서 교대), 잡당 16 launch: X/Y median 47,818/50,133 · 49,516/51,496 · 48,537/48,376 · 46,806/47,932 · 46,866/48,048 · 47,994/52,550 → **Y가 5/6 느림, 평균 +1,833 (+3.9%)**. span: 중간 동기화는 **Y 10.4k · 13.5k · 13.1k · 15.5k vs X 15.0k · 12.7k · 26.8k · 25.3k** — 사이에 끼인 작업을 없애도 짧아지지 않고 동기화는 여전히 ~43~47k에 끝난다. store 앞 DMA가 +2.5~4k(작은 로드가 전부 store 앞 FIFO로) | PASS | **기각 — 그리고 가설을 닫는다.** ExplicitSync 대기는 push와 wait 사이의 명령 구성(타임스탬프 쌍 3 → 2 포함)에 달려 있지 않다. ffn 9개 동기화에서 보인 '사이가 빈 동기화가 짧다'는 상관은 원인이 아니었다 |
| `V286_qkv_load_rate_probe` | `V280` | **qkv weight 로드의 실물 속도가 슬라이스 레이아웃에 달렸는가.** 두 점 회귀로 qkv Q·V 로드는 **494 B/cycle**(방문당 고정비 ≈ 0)인데 ffn up/gate 645, attn tile0 680이다. 같은 15.73 MB q_weight를 세 레이아웃으로 추가 로드(V208 관용구: 한 행을 TRF로 staging해 살림)해 **span으로 명령별 시간을 직접** 잰다: 슬라이스당 8행(프로덕션, 256 live/클러스터) · 16행(128 live) · 32행(64 live). qkv만 도는 하네스, 잡 2개 | — | — | — | — | launch 4개 모두 같은 결과: **16행 26.4k / 26.0k (FIFO 맨 앞) = 612 B/cycle · 32행 25.3k (그 바로 뒤) = 630 B/cycle · 8행 31.2k / 31.5k (커널 끝, 경합 없음) = 505 B/cycle**, 프로덕션 Q 로드 30.6k = 520 B/cycle | PASS | **확인 — 슬라이스당 행을 두 배로(live 슬라이스 절반) 하면 qkv 로드가 ~20% 빠르다.** ffn up/gate(57.6 KB 런, 645)와 같은 급. qkv DMA 체인(Q 31k + K/V 15.4k × 2)에서 ~11k가 걸려 있고, x broadcast도 목적지 슬라이스가 절반이 된다. → `V287` 설계 |
| `V287_qkv_half_live_slices` (h2) | `V280` | **V286을 커널에 적용: Q/K/V weight를 live 슬라이스 절반에**(Q 16행 `m![Qs / 16 % 128, 1 # 2]`, K/V 8행; 패딩을 안쪽에 둬 모든 DMN 사용). x는 모든 슬라이스에 복제된 채 reshape, contraction은 슬라이스당 행 2배, head gather는 `Broadcast1 { slice1: 32, slice0: 2 }`(같은 64-슬라이스 ring, head는 같은 슬라이스에 도착). qkv만 도는 하네스, 변형 먼저 회전 | 43,006 → 44,782 | — | — | — | 변형 먼저 잡: **PASS**(q max\|Δ\| 0.03125 = base). 짝비교 12잡(변형당 8 launch): **12/12 양수, 평균 +2,994 (+2.9%), p = 0.0005**. span(launch #1): 로드는 정말 빨라짐(Q 26.9k = 593 B/cy · K/V 14.3k/14.1k ≈ 560 vs base 31.5k · 15.7k), contraction Q 5.0k → 8.9k · K/V 3.5k → 6.2k/5.0k. **스케줄러가 순서를 바꿔 rope store(280)의 동기화가 Q 로드 직후·V2 로드 앞에 놓였고 그 동기화가 두 launch 모두 16.5k/17.1k**(base의 rope 동기화는 2.4~4k) | PASS | **기각 — 로드 속도 이득(~11k)이 커널 끝까지 가지 않는다.** 동기화를 뺀 작업량은 h2가 ~12k 적지만(75k vs 87k), 레이아웃 교체가 명령 순서를 바꿔 긴 동기화를 V2 로드 앞에 놓았다. rope store 둘을 하나로 합치는 `V288`(base에 `rs`, h2에 `hr`)로 이어서 본다 |
| `V288_qkv_rope_single_store` (rs · hr) | `V280` | **RoPE의 cos·sin 행 store 둘을 하나로.** 두 gather 행을 Main pass 두 개로 한 DM 버퍼([Dummy2, Ds])의 두 타일에 복사하고 한 번 store(`shared/mlp.rs`의 x_hi/x_lo 관용구). store 3 → 2… 가 아니라 **동기화 4 → 3**(정적). `rs` = V273 qkv + 이것, `hr` = V287 h2 + 이것. qkv만 도는 하네스, 변형 먼저 3-way 회전(변형당 6 launch) | 43,211 (rs) · 44,858 (hr) | — | — | — | 잡 8개: base 98,513 / 99,055 / 107,059 / 103,095 / 103,125 / 99,617 / 101,719 / 101,239 · **rs 106,620 / 106,460 / 115,026 / 118,026 / 107,236 / 106,530 / 109,600 / 108,536 (8/8 느림, 평균 ≈ +8.1k, +8%)** · hr 전부 base보다 +4~12k. span: 동기화는 3개가 됐지만 **합친 rope store의 동기화가 31.7k / 32.9k(hr 47.5k / 48.9k)이고, rs 두 launch 모두 정확히 Q weight 로드가 끝나는 cycle(±10)에 끝났다** — base의 rope 동기화는 2.3~2.9k | PASS | **기각 — 그리고 동기화 메커니즘의 첫 단서.** store를 합치자 스케줄러가 합친 store와 그 동기화를 x2 store 직후·Q 로드 발행 앞으로 옮겼고, 동기화는 FIFO의 Q 로드가 끝날 때까지 풀리지 않았다. ⇒ 적어도 이 경우 **ExplicitSync는 DMA FIFO에서 먼저 발행된 큰 로드의 완료에 묶였다.** (attn 중간 동기화와 h2의 rope 동기화는 DMA 유휴 중에 끝나서 일반 규칙은 아니다.) store를 합치거나 옮기는 후보는 동기화가 큰 로드 뒤에 줄 서는지 먼저 확인할 것 |
| `V289_qkv_order_q1` (Q1) | `V280` | **동기화 동안 FIFO를 채우는 순서를 순서 강제로 잰다.** qkv에 `T52 -> T17`, `T17 -> T54`(Q weight 로드를 x2 store 직후·x8 로드 앞에). 정적으로는 rope gather·store가 앞으로 오고 x8 로드가 Q 뒤로 밀린다(43,006 → 46,512, hwsim3는 +7.5k 예측). `one.sh sliding_project_qkv ordQ1.txt q1 6` | 43,006 → 46,512 | — | — | — | 짝 6개(순서 교대) X/Y median: 100,805/114,834 · 103,189/112,271 · 100,361/114,872 · 100,673/115,896 · 102,445/109,954 · 103,903/117,202 → **Y가 6/6 느림, 평균 ≈ +12.6k (+12%)**. span: x2 동기화 12.5k/11.3k는 **Q 로드(24.8k~56.3k)보다 먼저 풀렸다**(V288식 묶임 없음); 대신 x8 로드가 FIFO에서 Q 뒤에 서서 55.8~57.5k에야 끝나고 rope 동기화도 10.8k/9.8k | PASS | **기각.** FIFO 모델의 예측(x8이 Q 뒤로)대로다. 부수 확인 — **span 로그 전체에서 '동기화가 DMA 완료에 풀리는' 경우를 셌다**: rs 16/32(합친 rope store 동기화 전부, 우연 기대 1.4%), qa 2/6, hr 1/32, 그 밖의 base·변형은 0~2/68~114(우연 수준). ⇒ **큰 로드 완료에 묶이는 동기화는 특정 구조(rs·qa)에서만 생기고, base 커널의 긴 동기화(9~16k)는 DMA 완료와 무관하다** |
| `V290_attnout_dummy_early_store` (DS) | `V280` | **launch의 첫 동기화에 일회성 비용이 있는가.** 긴 동기화는 대개 launch의 첫 중간 동기화다(attn 10~15k, qkv·ffn x2). 다시 읽지 않는 dummy store(`post_attn_rms_weight`를 scratch HBM에 복사; `residual_hbm` 복사는 드라이버 panic)를 넣고 순서 강제(`T5 -> T13`, `T7 -> T13`)로 tile0 로드 앞에 둔다. X = dummy가 스케줄러 기본 위치(커널 끝), Y = 앞쪽. `one_ds.sh` 6쌍 | 23,377 (X) · 24,490 (Y) | — | — | — | X/Y median 42,899/47,735 · 48,004/49,725 · 46,654/48,493 · 48,019/46,776 · 46,056/50,864 · 51,270/51,847 → **Y가 5/6 느림, 평균 ≈ +2.1k (+4.4%)**. span: **dummy store에는 `Cluster` span이 생기지 않았다**(정적에서도 store 3개에 동기화 2개), dummy 로드·store(~2k)가 tile0 로드를 ~2.0k → ~5.5k로 밀었고 중간 동기화는 X 4.1~14.1k · Y 3.2~10.9k로 같은 분포 | PASS | **기각 — 가설은 이 방법으로 시험할 수 없다.** ExplicitSync는 store가 아니라 **store한 HBM을 다시 읽는 로드**(와 커널 끝)에 붙으므로 다시 읽지 않는 store는 동기화를 만들지 않는다(RULES §10.0r 1항 정정) |
| `V291_qkv_h2_rope_store_early` (h2o) | `V280` | **V287 h2의 로드 이득을 순서로 되찾을 수 있는가.** h2 span에서 x2 store보다 먼저 나간 rope store(277)의 동기화는 ~2.7k, Q 로드 뒤로 밀린 rope store(280)의 동기화는 16.5k/17.1k이고 V2 로드 앞에 섰다(동기화를 뺀 h2 작업량은 base보다 ~12k 적다). 순서 강제 `T120 -> T52`(rope store 280을 x2 store 앞으로) — 정적으로 gather 둘·store 277·K 로드·store 280·x2 store 순이 되고 makespan 44,782 → 43,723. X = h2 기본 순서, Y = h2 + 순서. `scripts/dev/span/one_h2.sh` 6쌍 | 43,723 (Y) | — | — | — | X/Y median 101,244/95,872 · 99,897/99,728 · 108,124/98,608 · 103,906/97,466 · 104,678/100,144 · 103,822/98,826 → **Y가 6/6 빠름, 평균 ≈ −5.2k (−5.0%)**. span(Y): **K/V 로드가 FIFO 맨 앞(13~26.5k)**, Q 34.7~63.3k, V 66.4~80.8k; 동기화 합은 62~74k로 오히려 크고 **세 번째 동기화(~47k)가 6 launch 모두 마지막 V 로드 완료 ±9 cycle에 풀렸다** — 그러나 그 뒤의 일(rope 로드·후처리)은 어차피 V 뒤라 임계 경로에 비용이 없다 | PASS | **h2 대비 확정 이득 — 순서 강제 전용이라 채점에는 아직 못 쓴다.** V287의 h2 − base가 +3.0k였으므로 base 대비 추정 ≈ −2.2k. base와 직접 비교 `V291b`(두 바이너리 교대) 진행 |
| `V291b_qkv_base_vs_h2o` | `V280` | **V291(h2 + 순서 강제)을 프로덕션 qkv와 직접 비교.** 순서 파일은 빌드의 모든 커널에 적용되므로 두 바이너리를 잡마다 교대: X = V280 하네스 qkv만(프로덕션 = V273 코드, 순서 없음), Y = 같은 하네스 + `v287.py` + 기본 arm을 `sliding_project_qkv_h2`로 + `ordH2.txt`. `scripts/dev/span/one_base_h2o.sh` 8쌍 | — | — | — | — | X(프로덕션)/Y(h2+순서) median: 95,014/97,044 · 98,171/97,680 · 103,227/97,110 · 101,989/96,504 · 103,637/101,942 · 104,555/95,034 · 97,953/103,342 · 99,155/100,116 → **Y−X = +2,030 / −491 / −6,117 / −5,485 / −1,695 / −9,521 / +5,389 / +961 — 5/8, 평균 −1,866 (−1.9%), p = 0.73** (평균 X 100,463 · Y 98,596; 두 바이너리를 인접 잡으로 짝지어 잡 간 노이즈 ±5k) | PASS | **판정 불가(유의하지 않음) — 그리고 소스로 재현할 길이 없어 닫는다.** 같은 순서를 소스에서 요청하는 정적 탐침 둘이 실패했다: (1) K weight 로드에 이른 소비자(한 행 TRF staging)를 달아도 정적 스케줄이 plain h2와 동일, (2) **Q만** 절반 슬라이스로 바꾸고 K/V는 V273 그대로여도 스케줄러가 h2와 같은 재배치(K 로드 2,295 → 24,562)를 한다. 추정 이득이 실현돼도 점수 +0.6% 수준 |
| `V292_qkv_switch_x` | `V280` | **qkv x 스테이징을 두 클러스터에서 하고 칩 안에서 복제 — x2 HBM 왕복(store + ExplicitSync + x8 reload) 제거.** 입력 RMSNorm·hi/lo 분해를 `XBlocks = m![Ns, 1 # 4, H / 480]`(32-슬라이스 부분링마다 8사본 × 480 청크 8개, 클러스터 복사축 `Qs / 2048`)에서 돌리고 `CustomBroadcast { ring_size: 32 }` all-gather 한 번(패딩 24칸은 trim)으로 256 슬라이스 전부에 x. 정적 switch 1,223(V273 x8 broadcast 7,943). 사전 정적 탐침 ps.py: 클러스터마다 자기 HBM 영역만 store/reload해도 동기화는 그대로 붙는다 | **41,852** | — | — | — | 잡 8개 짝비교(arm `sw`, 변형 먼저): **7/8 음수, 평균 −1,284 (−1.3%)**. span: Q 로드 발행 32.5k → 9.8k, x2 동기화 소멸; 남은 비용은 V 로드 뒤 rope 왕복·꼬리 17.7k | **6/6 PASS** | **채택 → `V292_submit`** |
| `V292_submit` | `V273_submit` | V273 + V292를 `sliding_project_qkv` 자체에 | — | — | — | — | Arena 검증 잡 2개 **25/25 PASS**. 리더보드 draw 6.6559 외 | **PASS** | **제출(draw)** |
| `V293_ffn_switch_x` | `V292` | **같은 기법을 ffn에:** pre-FF norm·hi/lo·geglu 스칼라를 두 클러스터 `XBlocks`에서, x는 `replicate_blocks`, 스칼라는 슬롯 0에서 ring-32 broadcast(`broadcast_scalar_blocks`), post-FF 꼬리는 클러스터 0 블록 0(= ReducingSlices)의 residual을 그대로 쓴다 | — | — | — | — | 잡 16개(submit 8 + **rerun 8**): **13/16 음수, 평균 −1,799 (−0.6%), p≈0.02**. span: x2 동기화가 사라진 대신 첫 중간 동기화가 geglu 왕복으로 옮겨 5.4k → 9.8k로 길어졌다 | **PASS** | **채택 → `V293_submit`** |
| `V293_submit` | `V292_submit` | V292_submit + V293을 `decoder_feedforward` 자체에 | — | — | — | — | Arena 검증 잡 2개 **25/25 PASS** (qkv 95.7~102.7k · attn 43.6~44.0k · ffn 284.6~287.0k) | **PASS** | **현 코드 SOTA, draw 중** |
| `V294_cluster_dma_probe` | `V280` | **동기화 대기의 정체.** 같은 7.86 MB(q_weight 절반)를 클러스터 0에만(c0) · 1에만(c1) · 둘 다 동시에(c01, 프로덕션 분할) 로드 | — | — | — | — | 잡 4개(submit 1 + rerun 3): c0 **29.9~30.2k** · c1 **29.1~29.4k** · c01 **34.0~44.9k**. span(클러스터 0만): 한쪽이 놀면 끝 동기화 **정확히 1,298**, 동시 로드면 클러스터 0 로드는 단독과 같고 끝 동기화 **4.3k/7.9k/10.3k/16.3k** ⇒ **동시 DMA에서 클러스터 1만 느려진다** | — | **진단 — 동기화 = 클러스터 1 지연** |
| `V295_asymmetric_split_probe` | `V294` | V294 모델(클러스터 1 속도 r≈0.64): 대칭 T≈40.7k, 클러스터 0에 61%면 T≈36.5k. q_weight 5/8을 클러스터 0(16행/슬라이스), 3/8을 클러스터 1에(arm `c53`) vs c01 | — | — | — | — | 잡 4개: **c53 70.5~71.1k** vs c01 35.8~41.4k (c0 29.8~30.2k, c1 29.1~29.6k). span: 클러스터 0이 w0(45.8k)와 w1(빈 몫)을 발행했고 w1은 **69.8k에야 끝났다** — 클러스터별 명령 둘은 **직렬화된다**(클러스터 1의 w1은 w0가 끝난 47k에 시작해 단독 속도로 22.6k). 두 클러스터가 동시에 도는 것은 **한 명령 안의 두 몫**뿐이고, 그 안에서 HBM 읽기 대역폭을 나눠 클러스터 0이 우선한다(book: DMA 엔진은 칩에 8개 = 클러스터당 4, DM 쓰기 256 B/cycle per cluster, HBM 읽기 대역폭은 공유) | — | **기각 — 비대칭 분할은 명령을 나눠야 해서 직렬화된다** |
| `V296_qkv_chunk256_load_probe` | `V295` | q_weight를 attn식 256 B 열 청크 레이아웃(`m![Qs / 128 % 16, H / 256 # 16]`)으로 로드 | — | — | — | — | **컴파일러 ICE**(visa `Option::unwrap()` on None, 패딩 슬라이스 인수 `H / 256 # 16`) | — | **막힘 — H = 15 × 256이라 2의 거듭제곱 청크는 패딩이 필요하다** |
| `V297_qkv_row_interleave_probe` | `V295` | **런 구조만 바꾸는 탐침:** 슬라이스당 8행은 그대로 두고 행을 슬라이스에 교차 배치 — qi1 = 슬라이스 `Qs % 256`(읽기 한 번 = 256 B 정렬 행 하나 3,840 B), qi2 = `Qs / 2 % 256`(두 행 7,680 B) | — | — | — | — | 잡 3개: **qi1 34.9k / 29.6k / 31.0k vs c01 43.0k / 33.5k / 35.2k (평균 −5.4k, −15%)**, qi2는 c01과 비슷하거나 느림 | — | **진단 — 행 하나씩 읽으면 빠르다** |
| `V298_qkv_head_interleave_probe` | `V297` | qi1은 head 모임(gather)을 깨므로 **head의 64 슬라이스 안에서만** 교차: Q `m![Qs / 512 % 4, Qs % 64]` × `[Qs / 64 % 8, H]`, K `m![Ps / 256 % 4, Ps % 64]` × `[Ps / 64 % 4, H]` | — | — | — | — | 잡 4개: **Q 36.9/32.7/31.7/35.4k vs 41.9/41.0/39.0/40.9k (4/4, −15.6%)** · **K 21.8/19.2/21.4/24.6k vs 28.8/26.6/25.5/26.4k (4/4, −18%)** | — | **진단 — head 안 교차도 이득 유지** |
| `V299_qkv_head_interleave` | `V292` | **V298을 커널에:** Q/K/V weight를 head 안에서 행 교차(1행 HBM 읽기). contraction 출력은 [슬라이스 `Ds % 64`] × [원소 `Gs, Ds / 64`]이고, 원소를 Time에 둔 ring-64 `Broadcast1` 모음은 packet-major로 도착해 `[Gs, Ds / 64, Ds % 64]` = 자연 순서. 1값 패킷은 commit 불가(8~32 B)라 **transpose 엔진이 4 스텝을 한 패킷으로** 묶는다 | — | — | — | — | 잡 8개(submit 1 + rerun 7), arm `hi` vs V292 `sw`: **8/8 음수, 평균 −9,079 (−8.8%)** (hi 91.2~95.7k vs sw 100.0~103.5k) | **PASS** (변형 먼저) | **채택 → `V299_submit`** |
| `V299_submit` | `V293_submit` | V293_submit + V299를 `sliding_project_qkv` 자체에 | — | — | — | — | Arena 검증 잡 2개(submit + rerun) **25/25 PASS**, 리더보드 draw 시작 | — | **현 코드 SOTA** |
| `V300_ffn_upgate_interleave_probe` | `V280` | V297을 ffn up weight(29.5 MB)에: ug0 프로덕션(30행 57.6 KB 런) · ug1 행 교차(1,920 B 비정렬 읽기) · ug2 두 행 교차(3,840 B 정렬 읽기) | — | — | — | — | 잡 4개: ug0 **57.4~61.7k** · ug1 75.1~79.5k (**+27%**) · ug2 60.0~65.9k (+4%) | — | **기각 — ffn up/gate는 프로덕션 런이 최선**(1행 이득은 행이 256 B 정렬일 때만) |
| `V301_ffn_pieces_in_lane_v293` | `V299_submit` | **V235(x의 f8 두 조각을 TRF Lane으로 → pass A의 LUT 조회를 weight 원소당 1회로)를 V293 ffn 경로에.** V235는 gate weight 로드가 pass A를 묶어 중립이었지만, V293에서는 gate pass A가 로드가 끝나는 사이클에 시작해 40.8k를 그대로 임계 경로에 올린다. `contract_up_gate_full_lane` + `fold_x_pieces`(V235 그대로) | — | — | — | — | 잡 8개(submit 1 + rerun 7), arm `ln` vs V299_submit ffn: **8/8 음수, 평균 −6,548 (−2.3%)** (ln 281.6~283.9k vs 286.8~290.9k) | **PASS** (변형 먼저) | **채택 → `V301_submit`** |
| `V301_submit` | `V299_submit` | V299_submit + V301을 `decoder_feedforward` 자체에 | — | — | — | — | Arena 검증 잡 2개(submit + rerun) **25/25 PASS** (ffn 281.0~287.2k), 리더보드 draw 시작 | — | **현 코드 SOTA** |
| `V302_gather_cluster1_probe` | `V299` | **RoPE 테이블 gather를 클러스터 1에 쓸 수 있는가** (정적 컴파일 탐침). 출력 클러스터 타입 `m![1 = 1 # 2]`, `m![Qs / 2048 = 1 # 2]` | — | — | — | — | 둘 다 컴파일되지만 PE 프로그램에서 **클러스터 0만 실제 디스크립터**를 쓰고 클러스터 1의 gather·store 자리는 `0xffffffff`(빈 몫) — 클러스터 위치의 tile 표기는 받아들여지지만 무시된다 | — | **막힘 — RoPE HBM 왕복은 남는다** |
| `V303_ffn_down_pieces_in_lane` | `V301_submit` | V301(x 조각을 Lane으로)을 **down contraction**에도: `down_tile_lane_fns!`(Lane x_trf + 조각 fold 패스) × 4 타일 | — | — | — | — | 잡 8개(submit 1 + rerun 7), arm `dl` vs V301_submit ffn: **6/8 음수, 평균 −682 (−0.24%), p≈0.29** | **PASS** | **미채택 — down contraction은 각 down 타일 로드를 기다리므로 TU 절감이 임계 경로에 거의 닿지 않는다**(V235가 중립이던 것과 같은 구조) |
| `V304_attnout_one_tile` | `V301_submit` | attn_out O-weight를 96 + 24행 두 타일 대신 **120행 한 타일**(DMA 명령 1개, contraction pass 1개) | — | — | — | — | 잡 8개(submit 1 + rerun 7), arm `t1`: **3/8 음수, 평균 +41** (잡마다 중앙값 41~51k로 흔들림) | **PASS** | **중립 — 미채택** |
| `V305_ffn_down_one_load` | `V301_submit` | V301_submit ffn 조사에서 geglu 왕복 동기화가 DMA 대기열을 13.5k 놀리는 원인(down 타일 1~3 로드가 geglu store 뒤)을 없애려고 **down weight 전체를 DMA 명령 하나로** 로드, contraction 4개는 그 버퍼의 16/16/16/12행 view | — | — | — | — | 정적 순서부터 빗나감: 한 명령 로드(`mlp.rs:1536`, 108.1k~117.8k)가 geglu store(98.6k~101.9k)·down x 로드 뒤, 정적 makespan 134.2k → 139.8k. 실물 잡 6개(submit 1 + rerun 5) arm `d1`: **6/6 악화, +13.1k~+15.1k (평균 +14.1k, +5.0%)** — 잔여 rerun 중단 | **PASS** | **기각 — 로드 위치는 소비자가 정한다: 합친 로드도 첫 down contraction 앞(= geglu store 뒤)에 가고, 타일 로드↔contraction 파이프라인만 잃는다** |
| `V306_ffn_out_scale_tail` | `V301_submit` | V301_submit ffn span 판독: **down_scale 로드가 DMA가 논 지 6.4k 뒤에 발행된다** — TUC 큐에서 그 앞에 선 Main/Sub 체인(gate fold · pass B 4개 · ring-32 스칼라 switch 2개 · 스칼라 StoVrf 2개 · gelu)이 정적 추정의 ~3배로 돈다. geglu 출력 스케일 `out_scale`(클러스터당 스칼라, 선형)을 **끝의 `down_global_scale` 곱 옆으로** 옮김(cluster 0 block 0 = ReducingSlices라 reshape만) — 체인에서 switch 1 · switch config DMA 1 · StoVrf 1이 빠지고 꼬리에 StoVrf 1 | 132,546 (V301 134,178) | — | — | — | 잡 12개(submit 1 + rerun 11) arm `o1`: **12/12 음수, −2.7k ~ −8.7k, 평균 −4,933 (−1.76%), p = 0.0002** | **PASS** (잡마다 2/2) | **채택 → `V306_submit`** (Arena 25/25 PASS ×2, a589eed). 정적 −1.6k의 3배 — 로드 앞 큐 창에서 작은 TU pass를 빼면 실물은 정적 차이의 몇 배를 번다 |
| `V307_qkv_rope_gather_per_cluster` | `V301_submit` | qkv RoPE 테이블 스테이징(클러스터 0 gather 2 · HBM store 2 · ExplicitSync · reload)을 **클러스터마다 on-chip 인덱스로 직접 gather**로 교체: rope_offset(바이트 오프셋)을 head 슬라이스 8개(클러스터당 live 4)에 로드 → Sub pass `LogicRightShift 9`로 행 번호 → `dma_gather_unscaled`가 각 클러스터의 head 슬라이스로 제 행을 읽음 | 41,671 (V301 41,595; mid-kernel sync·store 사라지고 `DmaOther` 2개 추가) | — | — | — | 잡 12개(submit 1 + rerun 11) arm `g1`: **6/12 음수, 평균 +90** (−3.5k ~ +2.7k) | **PASS** (잡마다 6/6 — 클러스터별 gather는 정확) | **중립 — 미채택.** 동기화를 없애도 클러스터 1의 DMA 지연은 끝 동기화로 옮겨 갈 뿐이다 |
| `V308_ffn_head_order_probe` | `V306_submit` | **순서 강제 탐침(채점 불가, 상금 측정용):** V306 ffn 머리의 DMA 유휴 5.7k(x 로드 뒤 rms weight · up LUT · x 복제 cfg가 TU pass 사이에 끼어 up weight 발행이 11.6k)를 없애려고 `SCHEDULER_MANUAL_ORDERING_PATH`로 up weight 로드(`T63`)를 rms weight(`T18`) · LUT(`T87`) · cfg(`T54`)보다 먼저 결정하게 함 → up weight가 x 로드 바로 뒤(정적 1,768) | X 132,546 / Y 134,213 (+1,667) | — | — | — | 짝 6개(X/Y 번갈아, 첫 잡 뒤는 `rngd rerun`): **Y가 6/6 느림, +184 ~ +3,858, 평균 +1,942 (+0.7%)** | **PASS** | **기각 — 머리 유휴는 순서로 못 번다.** 작은 로드가 44k 로드 뒤로 밀려 x 스테이징이 늦어지는 값이 유휴 5.7k보다 크다. 정적 makespan이 부호를 맞췄다(V305·V306·V307에 이어 넷째) |
| `V309_qkv_tail_order_probe` | `V306_submit` | **순서 강제 탐침(채점 불가):** qkv 꼬리(마지막 K weight 뒤 sin gather · store · 두 번째 ExplicitSync · reload · head norm 로드)를 앞으로: `T106/T116/T84/T90 -> T55`, `T55 -> T119`. 강제하자 빔 탐색이 **RoPE 테이블 스테이징 전체를 커널 머리로** 옮겼다(gather 1,003 · store 4,401/7,135 → K/V weight 7,472 → Q 15,554) | X 41,595 / Y 41,533 (−62) | — | — | — | 짝 16개(첫 잡 뒤 `rngd rerun`): **9/16 음수, 평균 −280 (−0.3%)**, 짝 차이 ±4.6k | **PASS** | **중립.** 같은 날 정적 스크린: V 마지막(`T55 -> T56`) +1,465(게이트 기각) · K/V를 Q 앞 +1,101 · rope를 V 앞 +920 · **rope를 Q 앞 −623**(유일한 정적 개선). ffn 순서 5종(down_scale을 tile0 앞 +519, gate_global·erf cfg를 gate 앞 +837, m_every cfg 0, 작은 로드 전부 gate 앞 +1,687, up 앞 +1,027)은 전부 같거나 나쁨. 소스에서 RoPE 스테이징을 첫 함수로 떼어 호출해도 스케줄은 바이트 단위로 같다(생성 순서는 빔을 움직이지 않는다) |
| `V310_ffn_down_scale_layout_probe` | `V306_submit` | **로드 탐침(V211 방식):** V306 ffn 머리에 `down_weight_scale`(3.69 MB)를 한 번 더 로드(한 행 VRF staging으로 유지) — arm `s8` 현행 8청크×60행(행마다 120 B 런), `s4` 4청크×30행(240 B), `s2` 2청크×15행(480 B) | — | — | — | — | 잡 8개(submit 1 + rerun 7), 기준 대비 증분: **s8 +27,658 (26.5~28.6k) · s4 +22,076 (19.9~24.4k) · s2 +18,808 (17.0~20.6k)**, 8/8 잡에서 s2 < s4 < s8 | **PASS** | **진단 — down scale은 레이아웃만으로 로드당 ~8.9k(2청크) · ~5.6k(4청크)를 더 낸다.** V211(weight 2청크 −16k)과 합치면 2청크 down은 로드에서 ~25k. V246(4청크 +8.2%)은 타일마다 ring-2 gather + pack pass를 더해 큐 막힘 창에 pass 8개를 넣었다 — 출력 정리를 커널 끝 한 번으로 모으는 설계는 아직 안 쟀다 |
| `V311_ffn_down_two_chunks` | `V306_submit` | V211(weight −16k) + V310(scale −8.9k)을 커널에 적용: down을 **2청크 × 슬라이스당 15행**(weight 3,840 B 정렬 런, scale 480 B 런), 타일 4/4/4/3행, pass B는 행마다 f32 스칼라 패킷, 끝에 ring-8 gather + transpose pack 한 번 → store 한 번(V260 반쪽 store 폐기), x 청크 7,680열 재로드, inv_s 방송은 청크 = up/gate 클러스터 | 133,319 (V306 132,546; tile 8,250 → 6,966, down_scale 9,774 → 4,961, **x2 재로드 2,934 → 10,184**) | — | — | — | 첫 잡: **d2 347,422 vs 279,803 (+67.6k)**, span: geglu hop 뒤 DMA 명령 하나가 **73k**(x2 재로드 — 슬라이스 128개/클러스터가 같은 15,360 B 영역을 다시 읽는다) | **FAIL** (max|Δ| 1.16, tol 안 23.7%) | **기각 — x 스테이징이 막는다.** 같은 HBM 영역을 여러 슬라이스가 읽는 복제 로드는 영역 크기 × 사본 수에 초선형으로 나빠진다(8청크 3,840 B × 32 사본 5.3k → 2청크 15,360 B × 128 사본 73k). 정확도 결함은 성능이 막혀서 추적하지 않았다. down 청크 수 레버는 x 복제 비용 때문에 닫힌다(4청크도 같은 벽의 절반) |
| `V312_ffn_scales_into_inv_s` | `V306_submit` | **정적 스크린:** V306 꼬리 회귀(out_scale 계산과 down_global·up_global 로드가 두 반쪽 store 사이에 끼어 꼬리 19.8k → 21.0k)를 없애려고 `out_scale × down_global_scale`을 geglu 단계의 클러스터별 1/s_c에 곱해 store — pass B가 두 스케일을 이미 적용하므로 꼬리의 곱 pass · StoVrf 2개 · 스칼라 로드 2개가 빠진다 | **133,318 (+772)** — up_global·down_global 로드가 geglu store 사이(87.2k~88.7k)로 옮겨 가 store·동기화가 밀린다 | — | — | — | Arena 미수행(정적 기각) | — | **기각(정적)** — 스칼라 로드는 소비자를 따라 움직일 뿐 사라지지 않는다. 꼬리의 1.2k를 geglu 창으로 옮기면 더 비싸다 |
| `V313_ffn_down_single_store` | `V306_submit` | V306 꼬리 판독: V260 반쪽 store(행 0..30을 마지막 타일 로드의 빈칸에 저장)가 V306 스케줄에서는 **마지막 타일 뒤로 밀려** 두 store 사이에 긴 동기화(8.1k)가 끼었다. **반쪽 분할을 없애고 60행을 마지막 타일 뒤 한 번에 store**(tile plan 16/16/16/12 그대로). 같은 날 정적 스크린: 반쪽 유지 12/16/16/16 스케줄 실패 · 16/12/16/16 +1,140 · 16/16/12/16 +940 · 16/16/16/8/4 +1,753; 단일 store 16/16/16/12 **−558** · **16/16/16/8/4 −1,011** · 16/16/12/16 +762 · 16/12/16/16 +962; qkv 머리 fused norm +1,201 | 131,988 (V306 132,546) | — | — | — | 잡 12개(submit 1 + rerun 11) arm `s1`: **12/12 음수, −458 ~ −4,250, 평균 −2,898 (−1.04%), p = 0.0002** | **PASS** | **채택 → `V313_submit`** (Arena 25/25 PASS ×2, 8a38b1b). 16/16/16/8/4 단일 store는 V314에서 3-arm으로 비교 |
| `V314_ffn_down_plan_single_store` | `V306_submit` | 3-arm 짝비교: 기준 V306 · `s1` 16/16/16/12 단일 store(= V313) · `s2` **16/16/16/8/4 단일 store**(정적 스크린 최고 −1,011). 같은 날 추가 정적 스크린(단일 store): 16/16/12/8/8 −628 · 16/16/16/4/8 −600 · 16/16/8/12/8 −628 · 16/16/8/8/12 +832 · 16/8/16/16/4 +1,065 · 12×5 +1,936 | s2 131,535 | — | — | — | 잡 12개: **s2가 12/12 기준보다 느림(+3.9k ~ +8.1k)**; s1은 V313 재현(10개 중 8개 음수, 약 −1.4k) | **PASS** | **s2 기각 — 정적 스크린은 타일 수를 못 본다.** 타일 하나마다 LUT 로드 · StoTab · pass A/B · 로드 고정비가 실물에서 정적보다 훨씬 비싸다(V238과 같은 결론). 5타일 계획은 정적으로 좋아 보여도 Arena에 올리지 않는다 |
| `V315_ffn_down_lane_retest` | `V313_submit` | V313 꼬리가 마지막 down 타일 pass A(12행, 9.1k)에 묶여서 **V303(down x 조각을 TRF Lane으로 → LUT 조회 절반 + fold pass)을 다시 잼**: arm `l4` 네 타일 모두, arm `l1` 마지막 타일만(Time·Lane TRF 두 벌 staging) | — | — | — | — | 잡 12개(3-arm): **l1 7/12 음수, 평균 −524 (−0.19%)** (−2.4k ~ +1.3k) · **l4 5/12, 평균 +374** | **PASS** | **중립 — 미채택.** 마지막 타일의 pass A 절반은 fold pass와 두 번째 TRF staging이 되갚는다(V303 −0.24%와 같은 결론) |
| `V316_qkv_rope_rotate_fusion` | `V313_submit` | RoPE의 rotate_half 복사 pass 2개 + sin 곱 pass를 **x 절반 × sin 절반 VRF의 Main pass 2개**로 융합(텐서당 Main 4 → 3, sin VRF 1개 → 절반 VRF 2개). 오프셋 tile에서 VRF를 만들 때 타입은 `m![Ds = 128]`(`# 256` 없이), fetch는 `m![Gs], m![Ds = 128]` 한 패킷이어야 lowering이 통과한다 | — | 정적 42,415 (base 41,595, **+820**) | — | — | 잡 12개: **rf 12/12 양수, 평균 +2,433 (+2.6%)** (+574 ~ +3,758) | **PASS** | **기각.** Main pass를 줄여도 sin 절반 VRF staging 두 번이 더 비싸다 — 정적 makespan 부호가 다시 맞았다(pass 변경 5/5) |
| `V317_cluster_interleave_probe` | `V297` | **클러스터 1 지연이 HBM 주소 순서 탓인가?** 모든 프로덕션 레이아웃에서 클러스터 0이 텐서 앞 절반을 가진다. 같은 런 구조(읽기 한 번 = 3,840 B 행 하나)로 클러스터만 교차: qi1(V297, 앞/뒤 절반) · **qx1(클러스터 = 행 parity `Qs % 2`)** · **qxh(512행 = KV head 묶음마다 교대 `Qs / 512 % 2`)** | — | — | — | — | 잡 6개(submit 1 + rerun 5): qi1 평균 **33,912** · **qx1 38,735 (+4,823, +14%, 6/6 느림)** · qxh 34,953 (+1,041, 4/6 느림) | — | **진단 — 주소 순서가 아니다.** 행마다 클러스터를 바꾸면 오히려 느려진다(HBM 읽기가 두 클러스터 사이를 오가며 연속성을 잃는다). 클러스터 0 우선은 클러스터 번호(발행 순서) 쪽 |
| `V318_attnout_cluster_alternate` | `V313_submit` | V317을 attn_out에 직접: `project_output`의 클러스터를 **120행 묶음마다 교대**(`TwoClusters = H / 120 % 2`, 행 묶음 `H / 240 % 16`) — 런 구조·store·디스크립터 수는 그대로, 491 KB 묶음이 어느 클러스터로 가는지만 바뀜 | — | 정적 23,375 = base 23,375 | — | — | 잡 12개: **cx 7/12 음수, 평균 +83 (+0.2%)** (−1,235 ~ +1,374; 잡 중앙값 39.8~50.8k) | **PASS** | **중립 — 미채택.** contraction 병합 동기화(= 클러스터 1 지연)는 주소 순서로 줄지 않는다(V317과 같은 결론) |
| `V319_qkv_rms_weight_first` · `V319_ffn_rms_weight_first` | `V313_submit` | S313 정적 스케줄: x 로드가 끝나는 순간 **mean-square pass와 입력 RMS weight 로드가 같은 정적 시각에 시작하고 pass의 instruction index가 작다**(qkv #46/#47, ffn #86/#87) → PE 큐에서 로드가 pass 뒤에 서고 실물 발행이 2,361 / 1,142 늦다. 소스에서 `rms_weight.to_dm`을 pass 위로 옮김(arm `wf`) | — | 정적 41,595 / 131,988 **그대로** | — | — | **스케줄이 한 칸도 안 바뀜**(W319 덤프: 인덱스·시각이 base와 동일, 소스 줄 번호만 다름). 첫 잡 qkv −908 · ffn −44(노이즈) → 재실행 중단 | **PASS** | **무효 — instruction index는 소스 선언 순서가 아니다.** V319b 정적 탐침: 로드와 그 소비자(StoVrf)를 **둘 다** pass 위로 옮겨도 #46 pass / #47 로드, 시각까지 동일 — 같은 시각 동률의 PE 큐 순서는 소스로 못 바꾼다 |
| `V320_pcopy_probe` (정적 탐침) | `V313_submit` | std 0.6.0에 있는 **Sub 컨텍스트 DM→DM 병렬 복사 `to_dm_view_pcopy`**(DMA 엔진을 안 씀)로 commit 전용 Main pass(qkv x_hi/x_lo → x2 gather 2개, RoPE rotate_half 복사 4개 등)를 대체할 수 있는가. qkv `stage_x_hi_lo_blocks`의 commit pass 2개를 pcopy로 | — | — | — | — | **컴파일 실패: `mir: Unknown primitive: DmTensorView::to_dm_view_pcopy`** — std에는 있지만(`context.rs`의 axis slicing도 이것을 씀) 0.6.0 lowering이 모른다. `hbm_cluster_shuffle`은 `todo!()` | — | **막힘 — pcopy 없음** |
| `V321_asymmetric_one_command_probe` | `V317` | **한 로드 명령 안의 비대칭 클러스터 분할**(V295는 명령 둘 → 직렬화). 축을 먼저 패딩하고 stride: `Qs # 5120 / 2560`(클러스터 0에 62.5%, qa5) · `Qs # 4608 / 2304`(56.25%, qa9) vs 대칭 qs1. 세 arm 모두 행 하나씩 읽고 H 32열 tile로 keep-alive(패킹된 행 축 tile은 `visa: tile index with non-empty packing is not supported`) | — | 정적 로드 qs1 13.4k · qa5 16.6k · qa9 15.0k(정적은 비대칭을 느리게 본다) | — | — | 잡 6개: qs1 평균 **32,932**(잡 중앙값 29.9~36.6k) · qa5 32,169(**−764**, 3/6) · **qa9 30,661(−2,271, −6.9%, 5/6)** — qa9 잡 중앙값은 30.1~30.9k로 거의 흔들리지 않는다. PE 프로그램의 빈 몫(`0xffffffff`) 수가 세 arm 같아 클러스터 1 디스크립터는 실재 | — | **진단 — 한 명령 안의 비대칭 분할은 이긴다.** 클러스터 1 몫이 줄면 그 지연이 끝을 정하지 않고 잡마다의 무작위 지연도 사라진다. 최적은 56% 근처(62.5%는 과함) → 커널 적용 V322(attn_out) |
| `V322_attnout_asymmetric_split` | `V313_submit` | V321을 attn_out에: O-weight 행을 **한 명령 안에서 2048 / 1792**로(`H # 4096 / 2048`, 행 묶음 128행 `H # 4096 % 2048 / 128`), 타일 1개(패킹 축 tile 불가), contraction이 패킹된 행 축 위에서 돌고 store는 묶음당 256 B(`m![H / 128, H % 128]`) + 맞춘 reducing 로드 | — | 정적 24,227 (base 23,375, **+852**) | — | — | 잡 7개(재실행 중단): **as 6/7 양수, 평균 +1,348 (+3.0%)** (−804 ~ +3,566) | **PASS** | **기각.** 패킹 축 lowering·타일 1개·128행 묶음·53% 몫이 한꺼번에 바뀌어 원인 분리가 안 된다 → attn 런 구조(256 B 열 청크)에서 로드만 재는 탐침 V323 |
| `V323_attn_load_asymmetric_probe` | `V313_submit` | V322의 원인 분리: attn_out 하네스에서 **o_weight 로드만**(256 B 열 청크 런) — sym(프로덕션 `H / 1920`, 120행 묶음) · a53(`H # 4096 / 2048`, 128행) · a67(`H # 5120 / 2560`, 160행). 56.25%(`H # 4320`, 135행)·60%(`H # 4608`, 144행)은 **DMA lowering 실패**(`prev ([[H_1=3840]+480]=4320) != &sub_shape ([[H_1=3840]+75]=3915)`: 패딩량이 묶음 크기의 배수여야 한다) | — | — | — | — | 잡 6개: **a53 − sym 평균 −461 (4/6 음수)** (−2,089 ~ +2,538) · **a67 − sym +1,892 (2/6)**. sym 잡 중앙값 26.7~39.9k로 잡마다 크게 흔들림 | — | **진단 — attn 청크 런에서는 비대칭 이득이 노이즈 수준.** 행 단위 읽기(q_weight, V321 −6.9%)와 달리 256 B 열 청크 로드는 클러스터 1 지연이 몫으로 줄지 않는다. V322의 +3.0%는 로드가 아니라 나머지 변경(타일 1개·패킹 축 contraction·store) 몫. **재측정(2026-09-12, 같은 패키지 job 20380을 `rngd rerun` 16잡, 로그 pod `/root/tk/aslpB_*`): a53 − sym 11/16 양수, 평균 +691 (+2.0%, p≈0.21) · a67 − sym 16/16 양수, 평균 +2,614 (+7.4%, p≈3e-5)** (잡 9는 sym stdev 61%의 머신 이상치라 평균에서 제외; sym 잡 중앙값 31.1~37.6k). ⇒ **기각 — attn 256 B 열 청크 로드는 클러스터 0 몫을 늘리면 오히려 느려진다. attn 클러스터 몫 계열을 닫는다** |
| `V324_ffn_load_asymmetric_probe` | `V300` | ffn up weight(29.5 MB, 슬라이스당 30행 연속 57.6 KB 런 — V294에서 대칭 지연이 가장 컸던 런 종류)에 한 명령 비대칭: s30(프로덕션 `L / 7680`) · u53(`L # 16384 / 8192`, 32행) · u67(`L # 20480 / 10240`, 40행). 셋 다 H 64열 tile + f4 LUT로 keep-alive | — | — | — | — | 잡 3개(재실행 중단): s30 **60,175 / 58,407 / 62,023** · u53 67,242 / 66,117 / 67,667(**+5.6~7.7k, +11%, 3/3**) · u67 69,115 / 70,437 / 70,741(**+8.7~12.0k, 3/3**) | — | **기각 — 연속 런 로드에는 비대칭이 크게 해롭다.** 몫이 커진 클러스터 0의 슬라이스 런이 길어지고(61~77 KB) 그 클러스터가 끝을 정한다. 비대칭 이득은 행 단위 읽기(V321)에서만 |
| `V325_qkv_x_weight_one_buffer` | `V313_submit` | S313/V319의 머리 동률(mean-square pass #46이 입력 RMS weight 로드 #47보다 먼저 큐에 섬, 실물 slack 2,361)을 **그래프로** 깬다: x와 RMS weight를 한 DM 버퍼의 Dummy2 tile 둘로 로드 → x를 읽는 pass가 두 로드 모두에 의존(arm `jb`) | — | 정적 42,347 (base 41,595, **+752**) — 동률은 사라짐(weight 로드가 x 로드 바로 뒤), Q 로드 정적 시작 +230 | — | — | 잡 12개: **jb 6/12 음수, 평균 −353 (−0.38%)** (−3,075 ~ +1,651) | **PASS** | **중립 — 미채택.** 머리 slack은 동률 하나가 아니라 x 경로 pass 전체가 Q 로드 앞에 서는 구조라, 로드 하나를 앞당겨도 Q 발행 시각은 거의 그대로다 |
| `V326_qkv_queue_balance_probe` | `V321_asymmetric_one_command_probe` | **qkv 로드 큐 전체의 클러스터 몫 균형 탐침(로드 전용).** V313 실물 임계 경로(`crit.py`): qkv 95.3k 중 RoPE cs 재로드 동기화 8.9k + 끝 동기화 4.0k ≈ 13%가 클러스터 1 지연이다. V321은 로드 하나(q_weight)에서 56.25%가 −6.9%였지만 head 정렬 때문에 명령마다 가능한 몫이 다르다(Q 9/7·10/6 head, K/V 4/4·5/3) → **명령별 몫이 달라도 큐 전체 몫이 지연을 없애는가**를 잰다. arm `b` Q·K·V 전부 50% · `q5` Q 62.5% + K/V 50%(전체 56.25%) · `kv5` Q 50% + K/V 62.5%(56.25%) · `q9` Q 56.25% + K/V 50%(53.1%). 세 로드 모두 행 하나씩 읽기(V321 qi1형, K/V는 `Ps # 2560 / 1280`) + H 32열 TRF keep-alive | — | — | — | — | 잡 12개(submit 20568 + rerun 11, 로그 pod `/root/tk/bal_*`), 로드 창 중앙값 b 대비: **q5 +690 (+1.2%, 4/12 음수) · kv5 +403 (+0.7%, 5/12) · q9 −1,081 (−1.8%, 9/12, p≈0.15)**. b 잡 중앙값 55.4~63.8k(잡 안 stdev 최대 4.7%), q9 잡 안 stdev ≤2% | PASS (생산 arm) | **기각 — 게이트 불통과.** 큐 전체 몫 56.25%를 명령별 62.5% + 50%로 만들면 대칭보다 **느리다**(q5·kv5) ⇒ 몫은 **명령마다** 맞춰야 하고 62.5%는 과하다. Q만 56.25%(q9)가 −1.8%로 유일하게 이기지만, V299 head 교차(클러스터당 KV 그룹 4 × 64 슬라이스)에 9 head가 안 들어가 커널 이식 비용(head 레이아웃·RoPE·출력 재구성)이 이득을 넘는다 → **qkv 클러스터 몫 계열을 닫는다** |
| `V327_qkv_asym_heads` | `V313_submit` | **(V326 통과 시에만)** 이긴 몫을 실제 커널에: V299 head 교차를 KV 그룹당 32 슬라이스로 바꾸고 그룹 축을 패딩(`Ns # 10 / 5` 류), 비대칭 텐서의 RoPE cos/sin은 전용 per-cluster gather(V307형, HBM 왕복 없음), Q 출력 reshape(`projection.rs:341`)·head norm reshape(`rmsnorm.rs:207`) 수정. 컴파일 스파이크 1일 한도 | — | — | — | — | — | — | **취소 — V326 게이트 불통과** |
| `V328_ffn_custombroadcast_trim` | `V313_submit` | book Switch Engine: **CustomBroadcast의 SFR 쓰기는 DMA 엔진과 sub 컨텍스트를 점유한다** — 정적 스케줄은 switch pass를 MainContext로만 둔다(V306이 정적의 3배를 번 기전). V313 실물: erf 스칼라 ring-32 switch(`xsw.rs:368`, 145.4k) → StoVrf(`f8split.rs:70`, 2.76k) → StoVrf(`mlp.rs:722`)가 끝난 150,722 직후에야 down_scale 로드가 발행된다(slack 3,144). 같은 부류: `broadcast_inv_s_down` ring-256(`mlp.rs:285`, inv_s 재로드 slack 2,441). 스칼라 분배를 대량 pass로 바꾸지 않고(V264 +1.6%) switch 자체를 없애는 형태 | — | — | — | — | — | — | **설계 단계 기각.** (a) `erf_scale`(= s_gate·inv_s/√2)은 두 클러스터가 같은 x 사본에서 계산하는 **전역 스칼라 하나**라, gelu pass의 VRF로 512 슬라이스에 놓는 방법이 switch 말고는 대량 pass(V264 +1.6%)나 512 디스크립터 복제 로드(V154 병리)뿐이다. pass B 스케일 VRF에 섞으려 해도 같은 분배가 필요하다(`mlp.rs:713-745`, `1481-1527`). (b) geglu의 s_c는 up/gate 클러스터마다 다른 데이터의 max라 청크별 inv_s 분배가 필요하고, 전역 s로 바꿔 꼬리 곱으로 옮기려면 hi/lo 분할 **전에** 클러스터 간 스칼라 교환(동기화 1개 추가)이 든다 |
| `V329_ffn_h_reload_copies` | `V313_submit` | down x 재로드(`mlp.rs:1758`)가 1.97 MB를 216 B/cycle(9.1k)로 읽고 그 뒤에 down1~3(≈53k)이 줄 선다 — `x2_hbm`의 3,840 B 청크 영역 하나를 클러스터당 32 슬라이스가 읽는 복제 로드(V154·V311 병리). **로드 탐침:** 같은 바이트를 HBM 사본 1개(64 reader) · 2개(32) · 4개(16)에서 재로드. 사본은 store로 명시적으로 쓴다(V15 함정) | — | — | — | — | — | — | **설계 단계 보류 — 기대 순이득 ≈0.** 사본 2개면 영역당 reader가 32 → 16이라 재로드(실물 9.1k)가 ~3k 줄 것으로 보이지만, 사본마다 store가 2개씩 늘고(V313 실물 geglu store 0.9~1.6k) 그 store가 같은 DMA 창(동기화 앞)에 선다. HBM 사본 축(`H / 960 % 2`)을 store 목적지에 쓰려면 piece 축(`Dummy2`)과 2단 tile이 필요해 축이 뭉개질 위험도 있다 |
| `V330_ffn_down_aligned_chunks` (설계 검토) | `V313_submit` | V321의 pad-then-stride로 **down 청크를 256 B 정렬 폭으로** 바꿀 수 있는가: down weight가 실물 417 B/cycle(960 B 미정렬 청크 읽기)인데 정렬 청크 읽기(attn ~680, qkv 행 ~600) 수준이면 −17~27k. 후보 폭 2048열(`L # 16384 / 2048`, 8청크, 1,024 B 읽기) · 1024열(16청크) · 512열(32청크, 256 B 읽기) | — | — | — | — | — | — | **설계 단계 기각.** ① geglu hi/lo 스케일 s_c가 up/gate 클러스터(열 7680 경계)마다 달라 down 청크마다 inv_s 하나가 필요한데, 2048·1024열 청크는 7680 경계를 가로지른다. ② 512·256열은 경계와 맞지만 클러스터당 청크가 15·30개(2의 거듭제곱 아님)라 청크→클러스터 inv_s 분배를 broadcast로 표현할 수 없다(V190). ③ up/gate 경계를 8192로 옮기면 연속 런 비대칭 벌점(V324 +11%)이 up·gate 두 로드에 붙는다. ④ 512열이면 down_scale이 슬라이스당 240 × 32 B 세그먼트가 되어 V310 추세로 +14~19k. **전역 s(hi/lo 분할 전 클러스터 간 스칼라 교환 = 동기화 추가) 없이는 열리지 않는다** |
| `V331_ffn_inv_s_ring` (arm `iv`·`i0`) | `V315_ffn_down_lane_retest` 하네스 (= V313_submit ffn) | **geglu 뒤 inv_s 분배의 ring-256 CustomBroadcast 제거.** V313 실물 FIFO에서 down1 앞에 inv_s 재로드(`mlp.rs:284`, slack 2,441) → switch config 로드(`510`, 1,958) → ring-256 switch pass(`mlp.rs:285`, 1.6k) → StoVrf가 선다. book: CustomBroadcast의 SFR 쓰기는 DMA를 점유한다. arm `iv`: 클러스터당 64 슬라이스(행 그룹 × up/gate 절반의 첫 청크)에 복제 로드 + ring-4 CustomBroadcast · arm `i0`: 256 슬라이스 전부에 복제 로드(switch 없음, 8 B × 512 디스크립터). 값은 비트 단위로 같다. V206(사본을 늘리고 ring을 줄여 −5.3%)이 선례 | — | — | 정적 iv +1,038 · i0 +4,854 (base 131,988) | — | 잡 12개(submit 20627 + rerun 11, 하네스 스윕을 분 단위로 회전), warm 중앙값 기준선 대비: **iv +3,832 (+1.39%, 10/12 느림, p=0.039) · i0 +10,708 (+3.87%, 12/12 느림)**. cold 중앙값 차 iv +1,902 · i0 +11,205 | **PASS** (잡마다 4/4) | **기각.** 2개 디스크립터 + ring-256 CustomBroadcast가 대안보다 싸다: 8 B 스칼라를 클러스터당 64 슬라이스에 복제 로드 + ring 4는 +3.8k, 256 슬라이스 전부에 복제 로드는 +10.7k — 늘어난 복제 디스크립터 하나가 ≈21 cycle로, switch의 DMA 점유보다 비싸다. 정적 스크린 부호가 다시 맞았다 |
| `V332_ffn_x2_direct_commit` (arm `cp`) | 같은 하네스 | **x 경로의 복사 전용 Main pass 2개 제거.** `stage_x_hi_lo_full_blocks`(`xsw.rs:297-309`)는 hi/lo Sub pass 출력을 x2 버퍼 tile 둘로 다시 복사한다(m1: store 명령 하나를 아끼려던 것인데 V293이 store를 칩 안 switch로 바꿔 이유가 사라졌다). hi/lo pass가 x2 tile에 직접 commit하고 x_hi VRF는 tile 0에서 staging. ffn 머리 slack(2.3k · 1.2k) 창의 TU pass를 줄인다. 상태 쓰기 변경이므로 변형 먼저 잡 필수 | — | — | — | — | 빌드 실패 | — | **막힘 — 컴파일러.** Sub hi/lo pass가 x2 tile에 `commit_view`하는 형태가 `lir: internal compiler error: no entry found for key` (`decoder_feedforward_cp`, 2026-09-12 lab5). m2(geglu hi/lo를 한 store로)와 같은 ICE다 — 복사 pass를 없애는 길은 0.6.0 LIR이 막는다 |
| `V333_ffn_norm_cast_merge` (arm `cm`) | 같은 하네스 | **cast 전용 pass를 RMSNorm 마지막 pass에 합친다.** `normalize_blocks_f32_fused`의 마지막 pass(`xsw.rs:240-253`) 끝에 `.cast::<bf16, m![H % 8 # 16]>()`를 붙이고 `stage_x_hi_lo_full_blocks`의 첫 cast pass(`xsw.rs:266-276`)를 없앤다 — 머리 창 Main pass 1개, 수치 동일 | — | — | 정적 −200 | — | 같은 잡 12개: **cm warm +1,368 (+0.49%, 6/12 음수, p=1.0)**, cold 중앙값 차 −146 (7/12) | **PASS** | **중립 — 미채택.** 머리 창의 cast pass 하나를 없애도 up weight 발행이 당겨지지 않는다(V319·V325와 같은 결론: 머리 slack은 x 경로 pass 사슬 전체의 구조) |
| `V334_qkv_qhead_layout_probe` (컴파일 탐침) | `V326_qkv_queue_balance_probe` | V326의 유일한 승자 q9(Q만 56.25%, 로드 창 −1.8%)를 커널로 옮기는 첫 관문: **query head 단위 9/7 레이아웃이 lowering되는가.** 클러스터 `Qs # 4608 / 2304`, head 슬롯 `Qs # 4608 % 2304 # 4096 / 256`(9 → 16; modulo 뒤 패딩 뒤 stride — 미시험 형태), 슬라이스 = 슬롯 16 × head 안 행 `% 256 % 16`, 원소 16행 × H(행 하나씩 읽기), H 32열 TRF keep-alive | — | — | — | — | — | — | **막힘 — 첫 관문에서.** `visa: internal compiler error: split (axis_index: 0, inner_size: 4) is not valid on shape([Qs_256=9, Qs_1=16])` — modulo 뒤에 슬롯 축을 9 → 16으로 패딩한 형태는 슬라이스 분할로 내려가지 않는다(V2의 `split … not valid on shape([H_16=240])`와 같은 부류). 9/7 Q를 컴파일되는 형태로 두는 길은 V321 qa9(슬라이스 = head 안 행 위치, 원소 = head)뿐이고 그것은 ring-256 head gather가 필요하다(V298) ⇒ **q9를 커널로 옮기는 계열을 닫는다** |
| `V335_attn_stack_split_load_probe` | `V323_attn_load_asymmetric_probe` | **클러스터 1 지연은 두 클러스터가 같은 HBM 채널 32개(stack bit = 주소 bit 8 × XOR 채널 16, 채널당 64칸 큐)를 두고 경합해서 생기는가 — 게이트 탐침.** 계기: vinxst가 한 제출 사이에 attn을 44,761 → 33,437(−25%)로 줄여 1위(7.4769, 2026-09-12 01:07 UTC). o_weight 행은 4096 B = 256 B granule 16개라 256열 청크 c의 stack bit = c & 1. o_weight 로드만 도는 arm 셋(TRF keep-alive는 V323과 같다): sym(프로덕션 `H / 1920`, `[H / 120 % 16, Qs / 256]`) · **ctl**(열 절반 분할이지만 두 스택을 다 읽음: `Qs / 2048`, `[H / 120, Qs / 256 % 8]`) · **stk**(열 parity 분할 = 클러스터마다 한 스택: `Qs / 256 % 2`, `[H / 120, Qs / 512]`). `V198_stack_alternation`(설계만)과 달리 스택을 클러스터별로 나눈다 | — | — | — | — | 잡 16개(submit 20940 + `rngd rerun` 15, arm 4회씩, 로그 pod `/root/tk/v335_r*.log`): **stk − sym 15/16 양수, 평균 +15,328 (+42.0%), 중앙값 +16,764** · ctl − sym 11/16 음수, 평균 −1,750(잡 r10의 sym 62.5k 이상치 포함) / 중앙값 −392 ≈ 중립. span(클러스터 0 기준, arm당 32 launch 중앙값): 로드 창 sym 34.3k · ctl 35.1k · stk 52.8k, **클러스터 0 자신의 로드 span sym 23.8k · ctl 24.5k · stk 40.1k(1.68×)**, 끝 동기화(= 클러스터 1 지연) sym 10.4k · ctl 10.5k · **stk 12.7k(없어지지 않음)** | PASS(생산 arm) | **기각 — 가설이 틀렸다.** ① 클러스터 하나를 한 스택에 가두면 그 클러스터의 로드가 1.68배 느려진다(book "stack bit를 안 바꾸면 16채널만" 실측). ② 스택을 나눠도 클러스터 1 지연은 그대로(오히려 +2.3k)라 **지연은 HBM 채널 공유가 아니다.** 부수 판독: V321(qa9, 슬라이스마다 c0 9행 · c1 7행) −6.9%와 V323(a53 · a67, 행 묶음 통째 패딩 → 두 클러스터 모두 슬라이스당 행 증가) · V324(u53, 같은 방식) +2~11%는 **"클러스터 로드 시간은 슬라이스당 바이트가 정한다"**로 한꺼번에 설명된다 → 비대칭은 슬라이스 단위로만 효과가 있다 |
| `V336_attnout_stack_split` | `V313_submit` | **(V335 통과 시)** attn_out을 열 parity 클러스터로: 두 클러스터가 3840행 전체의 짝/홀 청크 부분합을 만들고 스택 분리 256 B 정렬 store(`[H / 120, Qs / 256 % 2, H % 120 # 128]`) → 클러스터 0이 두 parity를 `ReducingSlices`로 reload → 더하기를 평균제곱·최종 pass에 VRF로 접은 꼬리(f2, VRF 7,712 B) 또는 따로 더하기 pass(f1). 새 함수만 추가하고 공유 `rmsnorm.rs`의 기존 함수는 그대로 둔다. 추가 비용 추정 +2.2~4.6k vs 회수 가능한 지연 8~11k | — | — | — | — | — | — | **취소 — V335 게이트 불통과**(스택 분리 로드 +42%, 지연 그대로). 준비만 해 둔 코드(`src/device/sliding/stack_split.rs`, arm `s1` · `s2`)는 측정 없이 브랜치에 기록으로 남긴다 |
| `V337_attn_row_read_probe` | `V313_submit` | **(V335 기각 시 백업)** attn o_weight를 행 하나씩 읽기(V299식 교차, 한 번 읽기 = 4096 B 행, 슬라이스당 8행) + `H # 4096 / 2048` 53.3% 몫(V321 근거) — 로드 탐침 `row53` vs sym부터 | — | — | — | — | — | — | **설계됨 (조건부)** |
| `V339_attn_uneven_tiles_load_probe` | `V335_attn_stack_split_load_probe` | **클러스터 1 지연을 슬라이스당 바이트로 없앤다 — 행 교차 없이.** V321 span 재판독: qs1(대칭) 지연 9.7k → **qa9(슬라이스마다 c0 9행 · c1 7행) 4.2k · qa5(10 · 6행) 0.7k**, 로드 창 −3.4~3.9k; 반면 V323 a53/a67(행 묶음 통째 패딩 → 두 클러스터 모두 슬라이스당 행 증가)은 지연 10.9~11.3k 그대로. attn은 행 교차가 출력 순서를 흩어 놓아 못 쓰므로, **시간으로 나눈다**: tile0 = 두 클러스터 대칭(행 묶음 96행 × 16 청크), **tile1 = 클러스터 0 전용**(나머지 행, 48행 묶음). 클러스터 0은 우선권으로 tile1까지 하고 클러스터 1은 tile0만 한다(슬라이스당 바이트 c1 < c0), 출력은 연속 행 블록 그대로. o_weight 로드만 도는 arm: sym(프로덕션 한 명령) · pt2(프로덕션 두 타일 96/24 대칭) · **u60**(tile0 3072행 대칭 + tile1 768행 c0, c0 60%) · **u53**(구현은 tile0 3584행 대칭 + tile1 256행 c0 — 슬라이스당 c0 128 · c1 112행, c0 53.3%) | — | — | — | — | 잡 16개(submit 21005 + `rngd rerun` 15, 로그 pod `/root/tk/v339_r*.log`), pt2 기준 짝비교: **u60 13/16 음수, 평균 −2,863 (−7.5%), 중앙값 −3,422** · u53 10/16, 평균 +1,891(이상치 67.6k·46.0k 포함) / 중앙값 −800 · sym(한 명령) 14/16, 평균 −1,293 (−3.4%). span 중앙값(arm당 32 launch): 로드 창 pt2 39.2k · sym 35.9k · u53 36.9k · **u60 35.1k**, 끝 동기화(= 클러스터 1 지연) pt2 14.3k · sym 12.7k · u53 10.7k · **u60 5.8k** | PASS(생산 arm) | **진단 확인 — 시간으로 나눈 슬라이스당 비대칭이 클러스터 1 지연을 절반 이하로 줄인다(행 교차 없이, 출력 순서 그대로).** 다만 u60의 창(35.1k)은 한 명령 대칭 sym(35.9k)과 거의 같다: **로드 전체는 두 클러스터가 나눠 쓰는 대역폭(~470 B/cycle)에 묶이고, 분할은 누가 기다리는지만 바꾼다.** 두 타일 구조(pt2)가 한 명령보다 3.4% 느리다는 것도 이번에 처음 쟀다 → 커널 V340 |
| `V340_attnout_uneven_tiles` | `V318_attnout_cluster_alternate` (= V313_submit + 커널 변형 하네스) | **(V339 로드 창 이득 확인 시)** 커널 적용: tile0 = 모든 120행 묶음의 0..96행을 두 클러스터가 그대로, **tile1 = 32개 묶음 전부의 96..120행을 클러스터 0 혼자**(원소에 `H / 1920`을 넣어 두 클러스터 묶음을 슬라이스마다 나란히; 슬라이스당 c0 144행 · c1 96행). x TRF는 클러스터 0용으로 한 번 더 staging. contraction store는 프로덕션 레이아웃 그대로(꼬리 행은 쓰지 않음), 클러스터 0이 reload한 버퍼의 꼬리 행을 **자기 DM에서 DM→DM으로 덮어쓴다**(HBM 왕복·동기화 없음). 새 모듈 `src/device/sliding/uneven.rs`, 공유 함수 불변. 판정: 커널 변형 먼저 잡 + 짝비교 16잡 | — | — | — | — | 잡 16개(submit 21029 + `rngd rerun` 15, arm `ut` 먼저·7 launch씩, 로그 pod `/root/tk/v340_r*.log`): **ut − prod 12/16 음수, 평균 −491 (−1.1%), 중앙값 −1,018**. span(첫 잡): 창 47.8k → 45.5k, 클러스터 1의 store(= 중간 동기화 끝) 42.5k → **38.5k**, 그러나 첫 동기화 길이는 13.2k → 13.5k 그대로(클러스터 0의 store도 앞당겨짐). **새 임계 경로는 클러스터 0**: 정적 스케줄러가 작은 c0 로드 둘과 store를 tile1 앞에 세워 tile1이 contraction0 끝(23.8k)에야 발행 → tile1 로드 11.2k → contraction1 4.7k → DM→DM 꼬리 덮어쓰기 1.9k → 꼬리가 41.5k에 시작 | **PASS**(변형 먼저, max\|Δ\| 0.01562 — 프로덕션과 동일) | **채택 — 코드 SOTA `V340_submit`(fd9eeda, origin push).** V342 잡 16개에서 재현(ut − prod 12/16, −602, −1.3%) → **두 배치 합산 32잡 24/32 음수, 평균 ≈ −1.2%, 부호검정 p ≈ 0.007.** **제출 검증(2026-09-12, lab2 빌드): Arena job 21171 25/25 PASS · `rngd rerun` 21172 25/25 PASS.** 기대 기하평균 +0.4%라 draw 하나로는 안 보인다. 첫 배치 판정은 "약한 개선(p≈0.08) 보류"였다. 늦게 발행되는 tile1과 그 뒤 c0 작업이 이득을 먹는다. tile1 크기 스윕은 로드 대역폭 한계(V339) 때문에 기대값이 작아 보류. draw 체인 전환(V313_submit → V340_submit)은 사용자 확인 대기(RULES §10.0u) |
| `V341_attn_access_pattern_probe` | `V339_attn_uneven_tiles_load_probe` | **attn o_weight의 읽기 패턴 스윕(로드만).** V335·V339로 로드 창이 분할 방식이 아니라 두 클러스터가 나눠 쓰는 대역폭(~470 B/cycle)에 묶인다는 것이 드러났다 — 남은 레버는 바이트당 속도다. 슬라이스당 읽기 단위를 바꾼 arm: c256(프로덕션, 120행 × 256열) · c512(60행 × 512열) · c1024(30행 × 1024열) · **r53i**(V299식 행 교차: 한 번 읽기 = 4096 B 행 하나, `H # 4096 / 2048`로 c0 8행 · c1 7행) · **r15i**(행 교차, 클러스터당 128 live 슬라이스 × 15행). 동기: 우리 attn은 공식 draw 137회 최저 38,347(p5 40.0k, 중앙값 45.3k)이고 vinxst는 33,437 — 13% 아래라 운이 아니라 구조 차이다 | — | — | — | — | 잡 16개(submit 21073 + `rngd rerun` 15, 로그 pod `/root/tk/v341_r*.log`), sym(c256) 기준 짝비교: **c512 +2,146 (+5.9%, 5/16 음수) · c1024 +2,528 (+7.0%, 3/16) · r53i +505 (+1.4%, 6/16) · r15i +402 (+1.1%, 9/16, 중앙값 −933)**. span 중앙값: 로드 창 sym 35.2k · r15i 35.7k · r53i 37.5k · c512 37.8k · c1024 41.0k, 클러스터 0 로드 span sym 23.7k · c512 26.3k · c1024 26.7k · r53i 26.6k · r15i 23.4k | PASS(생산 arm) | **기각 — 프로덕션 256 B 열 청크가 이미 가장 빠른 읽기 패턴이다.** 판독(V335·V339·V341 합산 모델): **클러스터 0은 레이아웃이 같으면 늘 ~332 B/cycle, 클러스터 1은 클러스터 0이 로드하는 동안 ~171, 혼자일 때 ~332 B/cycle** — sym(c0 23.7k · 끝 35.2k)과 u60(끝 35.0k 예측 = 실측 35.1k)을 정확히 맞춘다. 이 모델의 최적 분할은 **c0 ≈ 66%**(창 ≈ 31.3k, sym 대비 −11%)이고, 행 묶음 통째 패딩(V323)은 슬라이스당 행을 늘려 이 모델 밖에서 진다. V340이 약했던 이유는 분할이 아니라 tile1 발행 순서다 → V342 |
| `V342_attnout_uneven_tiles_store_dep` | `V340_attnout_uneven_tiles` | **(정확한 길, 사용자 우선순위)** V340의 두 결함을 고친다: ① 클러스터 1의 로드 뒤 contraction이 96행(3.3k)으로 길어졌다 → **세 타일**: T_A(두 클러스터, 행 0..72) · T_B(두 클러스터, 72..96, contraction A와 겹침) · T_C(클러스터 0 전용, 32개 묶음 전부의 96..120행). ② T_C가 store 뒤로 밀려 클러스터 0 사슬이 임계 경로가 됐다 → **store가 T_C에 의존하게**: T_C의 contraction을 원소 `H / 1920` 타일로 둘로 나눠 클러스터 0 자기 묶음의 꼬리 행은 `contraction`(store 대상)에 바로 커밋, 클러스터 1 묶음의 꼬리 행만 `tails` → reload 뒤 DM→DM. 합산 로드 모델(V341) 예측 −3~5k | 정적: ua 27,069 · ub 27,144 (ut 26,721, prod 23,375). **ua는 T_C가 여전히 store 뒤(16.0k)**, **ub는 T_C가 T_B 직후·store 앞(13.5k)이고 T_A가 x 로드보다 먼저 발행**(꼬리 행 DM→DM이 reload 앞에 오자 스케줄러가 순서를 바꿨다) | — | — | — | 잡 16개(submit 21145 + `rngd rerun` 15, arm 4개 × 4 launch, 변형 먼저): **ut − prod 12/16 음수, 평균 −602 (−1.3%), 중앙값 −990** · **ua − prod 6/16, 평균 +477 (+1.0%)** · **ub − prod 4/16, 평균 +1,155 (+2.4%)**. span 중앙값: 창 prod 46.6k · ut 46.4k · ua 47.2k · ub 47.5k | ut · ua **PASS**, **ub FAIL 16/16**(max\|Δ\| 3.79, 허용오차 안 67.8%) | **기각.** 세 번째 타일(DMA 명령 +1)은 클러스터 1의 짧아진 로드 뒤 contraction보다 비싸다(ua). ub의 정확도 오류는 패딩된 store 레이아웃을 `H % 120 = 96 # 128` 읽기 타일로 부분 reload한 곳으로 추정(미확인). **부수 수확: V340 ut가 재현됐다 — 두 배치 합산 32잡 24/32 음수, 평균 ≈ −1.2%(부호검정 p ≈ 0.007)** |
| `V343_attnout_local_rms_stage1` | `V313_submit` | **(Stage 1 전용 fallback — 사용자 결정 2026-09-12: "정확한 길 먼저, 이건 준비만")** 클러스터마다 자기 절반 행의 RMS로 정규화해 contraction store → 동기화 → reload 병합을 없앤다(두 클러스터가 각자 꼬리 · 각자 `residual_hbm` 절반 store). **수학적으로 부정확**하고 fixture 허용오차 안에서만 통과한다. 오프라인 시뮬레이션(`generate_references.py`의 Synth로 입력 재생성, fixture와 0 차이 확인): 전역 RMS max\|Δ\| 0.01562 · **2등분 로컬 RMS max\|Δ\| 0.04688, 전 원소 허용오차 안(최악 여유 +0.032), 절반 RMS 편차 0.95%** · 4등분부터 실패(12원소). ~~기대 attn −10%~~ **[정정, 같은 날] 로드 모델로 다시 따지면 기대는 −1~3k(−3%) 수준이다**: 병합(store · 동기화 · reload)은 사라지지만 커널 끝은 여전히 클러스터 1 사슬이 정한다 — 클러스터 1은 마지막 contraction이 ~41.5k에 끝나고 그 뒤에 자기 절반의 relayout(DM→DM, reducing 레이아웃이 inner reduce 규칙 때문에 필요)과 꼬리 pass·store를 해야 해 ~46k(프로덕션 47.8k). 허용오차에 기대는 대가에 비해 이득이 작다 | — | — | — | — | — | — | **보류 — fallback, 기대값 낮음** (Stage 2 전 반드시 되돌림, V257과 같은 부류) |
| `V344_attn_half_live_load_probe` | `V341_attn_access_pattern_probe` | **attn o_weight 256 B 열 청크를 live 슬라이스 절반 · 1/4에.** V286(qkv 행 읽기)에서 슬라이스당 행을 2배로(live 절반) 하자 클러스터 0 로드가 ~20% 빨랐고(505 → 612 B/cycle), V341 r15i(128 live 행 읽기)도 r53i(256 live)보다 c0 span이 −12%였다. attn의 프로덕션 청크 레이아웃(256 live)에서는 아직 안 쟀다. vinxst의 attn −25%(한 제출 사이)가 이런 레이아웃 축일 수 있다. o_weight 로드만 도는 arm(한 명령, `H / 1920`, 256 B 열 청크, TRF keep-alive): sym(`[H / 120 % 16, Qs / 256]`, 120행) · **h2i**(`[H / 240 % 8, Qs / 256, 1 # 2]`, 240행, 짝수 슬라이스 live) · **h2o**(`[1 # 2, H / 240 % 8, Qs / 256]`, 슬라이스 0..127 live) · **h2c**(`[H / 240 % 8, 1 # 2, Qs / 256]`, 16칸 블록 교대) · **h4i**(`[H / 480 % 4, Qs / 256, 1 # 4]`, 480행). 판정: 로드 창(= 끝 동기화까지, max(c0, c1)) 짝비교 16잡. 창이 −8% 이상 줄면 커널 설계로 간다(contraction 슬라이스당 행 2배 비용 포함) | — | — | — | — | 잡 16개(submit 21255 + `rngd rerun` 15, arm당 3 launch, 로그 pod `/root/tk/v344_r*.log`), sym 기준 짝비교: **h2i +209 (+0.6%, 6/16 음수) · h2c −747 (−2.0%, 9/16, 중앙값 −220) · h4i −1,077 (−2.9%, 7/16, 중앙값 +89) · h2o +15,085 (+41.3%, 0/16)**. span 중앙값(arm당 32 launch): 창 sym 36.1k · h2i 35.7k · h2c 36.1k · h4i 35.9k · h2o 50.5k. 클러스터 0 로드 span sym 23.2k · h2i 23.7k · h2c 23.4k · h4i 23.0k · **h2o 38.6k(1.66×)**. 끝 동기화(클러스터 1 지연)는 모든 arm이 11.8~12.7k | PASS(생산 arm) | **기각 — live 슬라이스 수는 attn 청크 로드 속도를 바꾸지 않는다.** 처리율은 슬라이스 수가 아니라 DMN(= DMA 엔진) 수에 묶인다. 슬라이스 0..127(DMN 절반)에 몰면 클러스터 0 로드가 1.66× 느려진다(V335 스택 한정 1.68×와 같은 급). book의 "8 DMA Engines, one per pair of DMNs … a single-DMN access pattern halves DM bandwidth"와 합치면 엔진 k가 두 클러스터의 DMN k를 번갈아 맡는 구조로 읽힌다. 그러면 두 클러스터가 모든 DMN에서 동시에 로드할 때의 합산 ≈503 B/cycle이 천장이고, 남은 레버는 두 클러스터 로드가 끝까지 겹치게 하는 바이트 분할뿐이다(V345). V286의 qkv 행 읽기 이득(16행 +20%)은 attn 256 B 청크에는 옮겨지지 않는다 |
| `V345_attn_optimal_split_load_probe` | `V344_attn_half_live_load_probe` | **로드 모델의 최적 분할(c0 ≈ 63~70%)을 V340 커널 레이아웃 그대로 잰다.** V339 u60(c0 60%)에서는 클러스터 0이 클러스터 1보다 5.8k 먼저 끝났고(창 35.1k), 합산 모델은 c0 ≈ 66%에서 두 클러스터가 ≈31k에 같이 끝난다고 예측한다. vinxst attn 33,437은 이 바닥(≈31k) + 짧은 꼬리 + 좋은 draw로 설명된다. V344 첫 잡: live 슬라이스 절반(h2i · h2c) · 1/4(h4i)은 sym과 같은 급이고, 슬라이스 0..127에 몰면(h2o) +43% → 처리율은 DMN/엔진 수에 묶인다. V340과 같은 120행 묶음 타일(tile0 = 두 클러스터 R행, tile1 = 클러스터 0 전용 120−R행 × 두 절반): u60(R 96, c0 60%) · u63(R 88, 63.3%) · u67(R 80, 66.7%) · u70(R 72, 70%) + sym. 로드 전용(TRF keep-alive), 짝비교 16잡. 창이 u60 대비 −2k 이상이면 커널 V346(분할 + tile1 조기 발행 + 꼬리 정리)으로 간다 | — | — | — | — | 잡 16개(submit 21295 + `rngd rerun` 15, arm당 3 launch, 로그 pod `/root/tk/v345_r*.log`), k60 기준 짝비교: **k63 −1,017 (−2.9%, 12/16 음수, 중앙값 −779)** · k67 −743 (−2.1%, 10/16, 중앙값 −241) · k70 −172 (−0.5%, 9/16) · sym +143 (+0.4%, 7/16). span 중앙값(arm당 32 launch): 창 k60 34.7k · **k63 33.9k** · k67 34.2k · k70 35.2k · sym 36.1k. 끝 동기화(클러스터 1 지연) k60 4.3k · k63 2.1k · k67 1.3k · k70 1.2k · sym 12.9k | PASS(생산 arm) | **진단 — 최적 분할은 c0 ≈ 63%(k63)이고 이득은 작다.** 합산 모델이 예측한 31k 바닥은 실물에서 ≈33.9k(464 B/cycle)라, 분할 최적화의 여지는 V340 분할(k60) 대비 ≈−0.8k뿐이다. 67% 이상은 클러스터 0이 늦게 끝난다. **부수 판독이 더 중요하다:** 로드 전용 k60의 클러스터 1 지연은 4.3k인데 V340 커널에서는 13.5k였다. 커널의 큰 지연은 분할 탓이 아니라 늦게 발행된 tile1이 클러스터 0 로드를 35k까지 끌어 클러스터 1을 계속 느리게 만든 탓이다 → V346(tile1 조기 발행)이 본체 |
| `V346_attnout_split63_store_dep` | `V342_attnout_uneven_tiles_store_dep` (V340 하네스) | **V340의 두 결함을 고치고 분할을 63%로.** V340 실물 span(v342_r0, ut#1): tile1이 contraction0 · 작은 로드 둘 · store 뒤(24.3k)에야 발행됐고, tile1 contraction은 store 동기화 뒤에 서서 4.7k가 걸렸다(프로덕션 24행 타일은 1.5k). 꼬리 행 DM→DM(1.9k)은 reload 뒤에 섰다. 그래서 클러스터 0 사슬(contraction1 끝 40,080)과 클러스터 1 store(동기화 끝 38,966)가 함께 끝을 정했다. 설계: (1) R = 88(c0 63.3%; V345 첫 잡에서 로드 창 −2k). (2) 두 클러스터의 tile0 contraction과 클러스터 0의 tile1 contraction이 **패딩 버퍼 하나**(`H % 120 # 240`)에 커밋한다 — 클러스터 0 자기 꼬리 행은 실제 행에, 클러스터 1 꼬리 행은 클러스터 0의 패딩 행에. store가 이 버퍼를 읽으므로 tile1이 tile0 바로 뒤에 발행된다(프로덕션과 같은 순서). (3) store 뒤 **클러스터 0 전용 store**가 클러스터 1 꼬리 행을 HBM에 덮어쓴다(rope의 부분 HBM 쓰기가 선례). 그러면 reload 한 번이 바로 norm에 들어간다(DM→DM 없음). 모델 예측: 두 사슬 ≈38k, 창 ≈44k(V340 46.0k 대비 −2k). 공유 함수는 그대로 두고 새 모듈 `uneven88.rs`에 둔다. 판정: 변형 먼저 정확도 + 정적 스케줄(tile1 발행 위치) + v6 − ut 짝비교 16잡 | 정적 v6 28,910 (ut 26,721, +2,189). **tile1 로드가 tile0 직후(11,979)에 발행**되고 channel_scale 로드와 store(20,285)가 그 뒤에 선다 → 순서 게이트 통과. 꼬리 행 DM→DM(22,871)은 store 동기화 뒤 | — | — | — | 잡 1개(21355, 변형 먼저): v6 44.9~46.2k · ut 43.2~46.6k — 출력이 틀려 비교에 쓰지 않는다 | **v6 FAIL — `non-finite device output at 0`**(ut PASS 0.01562). 재실행 중단 | **기각 — 정확도 FAIL.** 원인 추정: V342 ub와 같은 형태 — 꼬리 행을 reload 버퍼에 먼저 DM→DM한 뒤 HBM→DM을 **DM tile**(`H % 120 = 88 #{!} 120`)에 부으면, 앞에 써 둔 꼬리 행이 HBM 패딩(한 번도 안 쓴 영역)으로 덮이는 것으로 보인다(ub는 유한한 오답 3.79, v6은 NaN). 프로덕션·V340처럼 HBM 텐서 전체를 DM 텐서 전체로 reload한 뒤 꼬리를 DM→DM하는 경로만 검증돼 있다 → V347. 구현 경로(V346b): 패딩 HBM 레이아웃의 부분 쓰기(행 88부터 `H % 120 = 32 #{!} 120 # 128`)는 **`lir: incorrect buffer size at T40 (8176 != 8000)`**(ffn m1/m12와 같은 컴파일러 결함)로 막혔다. 그래서 head store를 행 0..88만 담는 HBM 텐서(`m![H / 120, H % 120 = 88 # 128]`)로 두고, 꼬리 행을 reload 버퍼에 먼저 DM→DM한 뒤 그 텐서 전체를 행 0..88 tile로 reload한다(부분 HBM 접근 없음). 컴파일러 함정 둘: reshape한 mutable view를 변수에 담으면 `mir: expected a DmTensorViewMut`(owned `DmTensor::reshape`로 우회), 패딩 버퍼의 tile 타입은 `H % 120 = 88 #{!} 120 # 240`처럼 원래 축 크기까지 적어야 visa를 통과한다 |
| `V347_attnout_split63_store_dep_reload_first` | `V346_attnout_split63_store_dep` | **V346에서 틀린 조각만 되돌린다.** 유지하는 것: R = 88(c0 63.3%), 패딩 contraction 버퍼(`H % 120 # 240`)로 store가 tile1 contraction에 의존 → tile1이 tile0 직후에 발행된다(V346 정적 11,979로 확인). 되돌리는 것: 꼬리 먼저 DM→DM + HBM→DM tile reload(V342 ub · V346 FAIL의 공통 형태). 대신 V340의 검증된 병합을 쓴다 — store가 버퍼의 실제 행 120개 전부를 `m![H / 120, H % 120 # 128]`에 쓰고(클러스터 1의 행 88..120은 쓰레기), reload가 텐서 전체를 버퍼 전체로 읽은 뒤 꼬리 행을 DM→DM으로 덮어쓴다. 기대: 클러스터 1 store 동기화 ≈38.0k(V340 38,966), 클러스터 0 contraction1이 더는 동기화 뒤에 서지 않는다 → ≈ −1k(−2%) | 정적 v7 28,018 (tile1 11,979 · store 20,285 — V346과 같은 순서) | — | — | — | 잡 1개(21366, 변형 먼저): v7 46.2~52.3k · ut 43.2~48.1k — 출력이 틀려 비교에 쓰지 않는다 | **v7 FAIL — max\|Δ\| 3.90, 허용오차 안 28.3%**(ut PASS) | **기각 — 정확도 FAIL. V346/V347 계열을 닫는다.** V340의 검증된 병합(전체 store · 전체 reload · 꼬리 DM→DM)으로 되돌려도 틀리므로, 원인은 두 변형의 공통분모인 **패딩 DM 버퍼(`H % 120 # 240`)에 대한 tile commit, 또는 owned `DmTensor::reshape`로 클러스터 타입을 오가는 것**이다(컴파일은 되지만 수치가 틀린다). 중간 버퍼를 읽는 하네스 없이는 어느 쪽인지 가를 수 없다. 틀린 출력의 사이클이지만 스케줄은 실물이라 참고로 적는다: V346(꼬리 먼저) v6 − ut ≈ −0.6k, V347(reload 먼저) v7 − ut ≈ +1.8k — 고쳐도 기대값이 ~1% 안쪽이다 |
| `V338_contention_census` (분석) | `V313_submit` | **ffn·qkv에서 두 클러스터가 동시에 HBM을 읽거나 쓰는 명령 census**와 256 B granule 경계로 스택을 나눌 수 있는지 분류. 1순위 ffn down weight(행 7680 B, 512열 = 256 B granule → `L / 512 % 2`; 지금 960 B 미정렬 417 B/cycle), 2순위 geglu x 재로드(9.1k) | — | — | — | — | — | — | **설계됨** |
| 정적 탐침 R1a · R1b (`lab3` V273 트리) | `V273_submit` | **RoPE 행을 `dma_gather_unscaled` + SPM 인덱스로 head 레이아웃에 직접** — cos/sin DMA store 2개, 동기화 2개, cs 로드 제거가 목표(실물 ~7.9k). V169는 i32 복제 로드 정렬로 막혔었다 | R1a: **컴파일 실패** (i32 인덱스 4 B = SRAM 폭 8의 배수 아님) · R1b (`m![1 # 2]` 패딩): **컴파일 성공, 43,006 → 44,090** — store·cs 로드는 사라지지만 gather마다 `DmaOther`(SPM 인덱스 staging) + **ExplicitSync가 여전히 붙는다** | — | — | — | — | — | **보류.** 동기화 수는 그대로이고, 인덱스 단위가 틀렸다(unscaled = 행 번호, `rope_offset` = 바이트 오프셋) — on-chip 변환(i32 VE op)이 더 필요하다. 실물 미측정 |
| 정적 탐침 ffn store 병합 m1 · m2 · m12 (`lab3` V273 트리) | `V273_submit` | **store 하나 = ExplicitSync 하나**(ffn 9개)이므로 연달아 있는 store를 합친다. **m1** erf/out 스칼라를 한 `[Dummy2, 1 # 8]` 버퍼로(store·동기화·로드·switch 하나씩 제거) · **m2** geglu hi/lo를 매크로에서 한 버퍼의 두 타일로 직접 commit(복사 pass 없음, store·동기화 하나 제거) | — | — | m1 · m12: **codegen 크래시** "incorrect buffer size at T67: DramShape [Dummy2=2:8]: 64 != 36" (HBM `[Dummy2, 1 # 8]`의 뒤 패딩을 잘라 계산) · m2: 스케줄은 **−594, 동기화 9 → 8**까지 나왔으나 **LIR ICE "no entry found for key"** | — | — | — | **막힘 (컴파일러).** 기대 이득도 store당 1~2k다 |
| `V236_sweep_attnout_tiles` | `V226` | **스윕 하네스 브랜치.** V209 채점 코드 + V226 하네스, 변형마다 첫 실행 출력을 대조하도록 세 shim 모두 수정(V235까지는 base만 검증했다) | — | — | — | — | — | — | **채택(인프라)** |
| `V237_ffn_down_four_chunks` | `V236` | **down의 L 분할을 8청크 → 4청크.** V211이 잰 정렬 벌점(960 B 런, 6/8이 granule 중간에서 시작)을 1,920 B 런으로 없앤다(정렬돼도 안 돼도 정확히 8 granule: 128+1,920=2,048). fetch/useful 1.267 → 1.067. 대가는 live 슬라이스 128개 = 슬라이스당 열 2배. V200은 이 형상을 *길이* 곡선만으로 취소했는데 정렬 항은 그때 미측정이었다 | — | — | — | — | 잡 1개, 변형당 n=12 회전: base **293,566** vs d4 **344,709 (+51,143, +17.4%)** | 3/3 **PASS** | **기각(결정적) — 그리고 V235를 반쪽으로 만든다.** down pass A가 슬라이스당 열 2배를 물자 정렬 이득(−11.6k 추정)을 압도하는 +51k가 나왔다. ⇒ **"ffn Main은 스트림 뒤에 숨는다"는 단방향이다: 봉투 아래로 줄이면 0이지만 넘기면 전액 청구된다.** ⇒ **"계산을 내주고 정렬을 산다" 가족 전체가 닫힌다.** 부수 확정: **DM 할당은 256 슬라이스를 전부 덮어야 한다**(`slice extent 128 does not match the device config`) — 노는 슬라이스는 `1 # n` 죽은 축으로 표현한다; **한 switch 매핑에서 `Dummy2`를 두 타일이 주장할 수 없다** |
| `V238_ffn_down_four_tiles` | `V236` | **down 타일 5개(16/16/16/8/4) → 4개(16/16/16/12).** pass마다 4 KB LUT 재로드 838 + 타일 로드 고정비 ~550이므로 타일 하나가 정적 DMA ~1.4k, Main·Sub pass 하나씩 | — | — | — | — | 잡 4개 in-job A/B: **−556 / −922 / −251 / −1,015, 4/4 승, 평균 −686 (−0.23%)** | 3/3 **PASS** | **채택 → `V243_submit`** (작지만 부호가 4/4에서 일정하고 공짜다) |
| `V240_lane_and_tiles` | `V236` | **한 번도 안 스윕한 상수: `LaneMode`와 attn_out 타일 개수.** qkv·attn_out contraction을 Interleaved → Sequential, ffn을 Sequential → Interleaved, attn_out O-weight를 2타일 → 3타일(44/44/32) | — | — | — | — | 잡 1개: **attn_out** base 51,614 · ls 51,722(+108) · t3 52,501(+887); **qkv** base 96,525 · ls 96,312(−213) | 전부 **PASS** | **전부 기각.** LaneMode는 **바꿀 수 있는 곳에서는 무효, 유효할 만한 곳에서는 못 바꾼다** — ffn Interleaved는 `contract_lane (Interleaved): OutPacket mismatch. Expected: 1 # 8, got: H / 16 % 4 # 8`로 **구조적 거부**(V210의 fold 규칙이 ffn에서도 확인). attn_out 3타일은 V203의 2타일 최적을 재확인 |
| `V241_ffn_down_two_pass_a` | `V236` | **down pass A를 30행 2타일로**(pass B는 partials를 독립 타일링). pass A 5개 → 2개면 LUT 재로드 3개(2.5k) + 로드 고정비 3개(1.65k) = 정적 DMA ~4.2k 절약 | — | — | — | — | 잡 1개, n=12: base **298,007** vs d2 **315,497 (+17,490, +5.9%)** | 3/3 **PASS** | **기각.** 아낀 고정비보다 **잃은 파이프라이닝**이 4배 크다. 첫 contraction이 16행이 아니라 30행을 기다린다 |
| `V242_ffn_down_tile_plan` | `V236` | 타일 **개수**가 아니라 **순서**를 스윕한다: `s1` = 8/16/16/16/4(첫 타일을 작게 해서 일찍 시작), `s2` = 4/8/16/16/16 | — | — | — | — | 잡 1개, n=9: base **296,496** vs s1 **322,112 (+25,616, +8.6%)**; s2는 컴파일 실패 | **PASS** | **기각 — 그리고 진짜 구속 자원을 밝힌다.** 첫 타일을 **키워도(+17.5k) 줄여도(+25.6k)** 나빠지고, `s2`는 아예 `Cannot find evict target for Vrf (size 3932160)`로 죽는다. 16행 scale VRF는 8 KB 파일의 **7,680 B**이고, **꼬리의 작은 타일들이 그 압력을 빼주는 장치**다. 현행 16/16/16/8/4는 날카로운 국소 최적이며 V238의 16/16/16/12만이 그 옆의 개선이다 |
| `V247_sub_context_offload` | `V236` | **"3개 ctx를 다 쓴다"를 실측한다.** 정적으로 qkv는 Main 21,549(31 pass) vs Sub 10,163(25 pass), ffn은 76,836 vs 28,210 — Main이 Sub의 두 배다. `stage_x_hi_lo_hbm`이 **`ctx.sub`도 vector 체인을 돌려 DM에 commit할 수 있음**을 증명하므로(V223의 "sub는 vector pass를 못 돌린다"는 `fetch_table_lookup`이 Main 전용이라는 뜻이었다), head RMSNorm의 sqrt pass 3개를 Sub로 옮긴다. **V218의 pass당 ≈600이 컨텍스트별 발행비인지(그러면 겹친다) PE core in-order 비용인지(그러면 안 겹친다)를 가른다** | — | — | — | — | 잡 1개, n=12 회전: base **96,789** vs sb **97,101 (+312)** | 3/3 **PASS** | **기각(중립) — 그리고 질문에 답한다.** 3 pass를 Main에서 뺐는데 실물이 안 움직였다. ⇒ **pass 비용은 PE core in-order 발행비이고 컨텍스트를 나눠도 겹치지 않는다.** RULES §8의 "Main·Sub는 Tensor Unit 파이프라인을 두고 경합한다"가 실물로 확인됐다. **"3개 ctx를 모두 쓴다" 축은 닫힌다** |
| `V247_attnout_hoist_loads` | `V236` | **커널 내 연산 순서 interleave.** attn_out의 epilogue [H] 로드 3개(546씩)가 스케줄에서 **O-weight 스트림 *안쪽*** 12,824 / 13,370 / 19,335 / 23,883에 끼어 스트림을 쪼개고 514 + 362 + 1,600의 엔진 유휴를 남긴다. 이 로드들을 커널 맨 앞으로 올려 스트림을 연속으로 만든다(선두 유휴 1,003 + 527이 일부 흡수할 것) | — | — | — | — | 잡 1개, n=12 회전: base **51,957** vs hl **52,315 (+358)** | **PASS** | **기각(중립).** 스케줄러의 배치가 이미 최선이다. 소스 순서를 바꿔도 되돌려 놓는다(V21·V42와 같은 결론). **"연산 순서를 손으로 interleave한다" 축도 닫힌다** |
| `V248_attnout_packet_32` | `V236` | **contract 3중 합 스윕(Packet ↔ Time).** attn_out의 fetch 패킷을 64원소 → 32원소로: 같은 축소를 Packet과 Time 사이에서 다르게 나눈다. book 모델은 무효를 예측한다(`fetch = Time × (Packet / read_size)`, main의 read_size ≤ 32 B이므로 64 f8 = 패킷 1개 × read 2회 = 32 f8 = 패킷 2개 × read 1회). V234는 128이 거부됨을 이미 보였다 | — | — | — | — | 잡 1개, n=12 회전: base **51,225** vs p32 **51,355 (+130)** | **PASS** | **기각(중립) — 모델이 맞다.** 이제 Packet/Time 분할은 추정이 아니라 **실측된** 무효다. Lane은 qkv·attn_out에서 이미 8차선 만석이고 ffn의 4/8은 V235가 닫았으므로, **contract 3중 합 스윕 전체가 닫힌다** |
| `V248_ffn_paired_scalars` | `V236` | V46: geglu의 두 스칼라(erf_scale·out_scale)를 HBM 스크래치 하나에 담아 **로드 1회 + ring-256 switch 1회**로(현재는 각각 521 로드 + 719 config switch) | — | — | — | — | — | — | **막힘.** `HbmTensor<f32, Chip, m![Dummy2, 1 # 8]>`가 `lir: incorrect buffer size at T62: buffer.size() (32) != num_chips * intra_chip_size (4)`로 거부된다 — `1 # 8` 패딩 패킷 위에 축을 하나 더 얹으면 HBM 할당기가 논리 크기를 못 맞춘다 |
| `V244_switch_then_vector` | `V236` | **게이팅 탐침:** switch 뒤에 vector 체인을 걸 수 있는가, 그 VRF 피연산자는 switch **후** 슬라이스 매핑을 쓰는가 | — | — | — | — | — | — | **열렸다.** q의 ring-64 gather pass에 `fetch_cast::<f32>` → switch → collect → `vector_fp_binary(&scale_vrf)` (scale VRF는 **head 레이아웃**)를 걸었더니 **컴파일 통과**. ⇒ 채널 스케일을 gather 안으로 접을 수 있고, 그러면 head norm이 scale VRF를 안 써서 V239의 벽(규칙 3)이 비켜간다. 다만 switch가 bf16 대신 f32를 나르게 되어 traffic 2배이고, **회계상 순이득은 5 pass가 아니라 3 pass**다(kv 사본 pass +1, gamma 준비 +2, k·v 추출 +2). 미측정 |
| `V245_down_scale_by_rows` | `V236` | **ffn 최대 낭비를 스케줄이 지목한다:** `up_scale`과 `down_scale`은 **같은 3.69 MB**인데 up은 통짜 행(7,200 B 런, **util 0.556, 4,880**), down은 슬라이스별 120블록 청크(**120 B 런, util 0.277, 9,774**)다. 스케일을 행 레이아웃(32 슬라이스 × 57,600 B 연속)으로 싣고 **switch로 청크를 흩뿌리면** 바이트는 그대로 두고 벌점만 없앤다 → 정적 −4,894 ≈ 실물 −10k | — | — | — | — | — | — | **막힘(구조적) — `mir: Collect output packet must be exactly 32 bytes (one flit)`.** switch를 거치는 collect 패킷은 정확히 32 B여야 하는데 **목적지 청크가 120 블록**이고 32 ∤ 120이다. 2청크(480)·4청크(240)로 중간 단계를 둬도 **마지막 목적지는 언제나 120**이라 같은 벽이다. broadcast로 우회하면 8슬라이스가 **같은** 데이터를 받아 청크 선택이 불가능하다. ⇒ **8청크 아래에서 down scale의 정렬은 닫힌다** |
| `V246_down_thirty_rows` | `V236` | **V237의 치명상을 없앤 4청크:** 64 행그룹 × 4청크 = **256 슬라이스 전부 live**, 슬라이스당 30행 × 3,840열 = 오늘과 **같은 연산량**. weight 런 1,920 B(util 0.701 → ~0.88), scale 런 240 B(0.277 → ~0.55). 30행은 4행 transpose에 안 맞으므로 pass B는 V181식 스칼라 패킷 + ring-2 gather로 60행 dense를 복원한다 | — | — | — | — | 잡 1개, n=9: base **296,895** vs c4 **321,223 (+24,328, +8.2%)** | 3/3 **PASS** | **기각 — 그리고 정렬 가설을 완전히 닫는다.** 연산량을 똑같이 맞췄는데도 +24k다. V237(+17.4%)과 합쳐 **4청크는 live 슬라이스 수와 무관하게 진다.** V211이 잰 27%는 **추가 로드 탐침에서만** 나타나고 실제 레이아웃 교체에서는 회수되지 않는다 — 런 길이 곡선은 커널 안 위치에 따라 다르다(V216과 같은 결론). **down의 8청크 레이아웃은 두 번 독립적으로 확인된 최적이다** |
| `V239_qkv_tail_one_buffer` | `V236` | **qkv tail 17 pass → 12.** q의 두 group 행·k·v가 슬라이스당 한 버퍼를 쓰면 sqrt 3 pass가 1개로, RoPE의 rotate-half 4개가 2개, sin 2개가 1개가 된다(V218: pass당 실물 ≈600) | — | — | — | — | — | — | **막힘(구조적) — 매핑 규칙 두 개에 걸린다.** ① **부분 축(`m![Ns / 2 = 2]`)은 갓 할당한 텐서에서 불법이다**: `StoVrf … lower_fetch_unit: There should be not-exactly-matched from_in_slice slots`. 부분 축은 **전체 extent를 가진 텐서에서 tile로 잘라낼 때만** 합법이다(mlp의 `H % 60 = $rows`가 그래서 되는 것이다). ② **2단 tile은 축을 뭉갠다**: `Dummy2` → `Gs` 순으로 자르면 커밋 대상이 `m![Gs #{!} 4, 1 # 8]`이 되어 `IndexWrite: Output tensor must be identical to table tensor`. ⇒ **2행 생산자와 1행 생산자가 한 버퍼를 쓰려면 평평한 4-extent 행 축이 필요하고, 그러면 모든 피연산자 VRF도 진짜 4행 텐서에서 와야 하는데 채널 스케일이 파라미터 3개로 쪼개져 있어 불가능하다.** 남은 길: **채널 스케일을 ring-64 gather pass 안으로 접어** head norm이 scale VRF를 아예 안 쓰게 만드는 것 |
| `V235_ffn_pieces_in_lane` | `V226` | **x의 두 f8 조각을 Lane으로 → LUT를 한 번만.** pass A는 **정확히 LUT 처리율 한계**(≈12.5 elem/cycle): 슬라이스당 115,200 원소 = 9,216 cycle(V227 dequant 실측 9,263)인데 `Dummy2`가 fetch 시간축에 있어 230,400 = 18,432(실측 18,263)를 문다. **V210이 여기서 죽었다**(`StoVrf: packet part should not be broadcasted`). 해법: scale replay를 패킷이 아니라 **Time**으로, 조각 합은 **별도 fold pass**로 빼서 pass B를 손대지 않는다 | — | — | **138,178** (V209 138,830) | — | 잡 1개, 변형당 n=12, 짝 교대: 프로덕션 median **295,667** vs V235 **296,131 (+464, +0.16%)** | 3/3 **PASS** | **기각(중립) — 그러나 ffn 최대 발견.** 설계는 정확히 성공했다: **pass A 36,526 → 18,526 static Main(−18,000)**, fold +7,762, **MainContext 76,836 → 66,598**. 그런데 **실물이 안 움직였다.** ⇒ **ffn의 MainContext는 정말로 DMA 뒤에 숨는다**(정적 DmaEngine 94.2%가 옳다). **V206의 −16,351은 Main 절감이 아니라 DMA 쪽 효과였다.** ⇒ **'ffn Main 절감' 가설군 전체가 닫힌다.** PeCore 4,970 → 12,617 |
| `V234_ffn_wide_lut_packet` | `V226` | pass A fetch 패킷 64 → 128 원소 | — | — | — | — | — | — | **기각(분석).** ① `mir: OutPacket must be 1 or 2 flits (32 bytes each), got 128 bytes`. ② 통과해도 이득 0: fetch 비용 `Time × (Packet / read_size)`, read_size ≤ 32 B이므로 128 f4(64 B)는 2 read → 1,800×2 = 3,600으로 64 f4의 3,600×1과 같다. 이 계산이 pass A의 벽을 **LUT 12.5 elem/cycle**로 확정했다 → V235 |
| `V230_ffn_begin_interleaved` | `V226` | **book의 마지막 미사용 API.** up/gate pass A를 한 sequencer 연산으로 | — | — | — | — | — | — | **구조적으로 닫힘.** 컴파일러가 만드는 축 이름이 `interleave`인데 `declared [Gs_1=2,…]` vs `pipeline [interleave_1=2,…]`로 contraction이 거부한다. **어떤 사용자 축도 못 맞춘다** — `Ident::new`가 대문자 시작을 assert하고 `src/axes.rs`는 채점에서 되돌려진다 |
| *(코드 변경 없음)* | `V209` | **공식 점수는 추첨이다.** 같은 커밋(`21d3e20`)을 다시 제출했다 | — | — | — | — | `b78a63a0` 6.2833 (103,697 / 49,657 / 293,889) → `b772875a` **6.5010** (97,847 / 48,019 / 290,803) → `826c0058` **6.6075**. 세 커널이 같은 방향으로 움직인다(−5.6% / −3.3% / −1.1%) | **PASS** | **채택 — 이번 세션 최대 이득.** 잡 전체의 머신 속도가 σ≈2%로 흔들리고 리더보드는 최고값만 남긴다. 코드 작업(V227 기각, V232 기각)보다 재제출이 더 벌었다 |
| `V226_harness_v2` | `V209` | **측정 인프라.** 70초 Arena 상한의 병목은 디바이스가 아니라 호스트다 — `run_test`가 TESTS 항목마다 가중치를 재합성·재업로드한다(ffn 99.5 MB, qkv 73.4 MB). 각 shim이 텐서를 한 번만 만들고 launch 루프를 돌게 바꾸면(재업로드는 `residual` 7.7 KB뿐, qkv는 멱등이라 0) 반복 횟수가 3~4배가 아니라 10배 이상 늘어난다. flat 500 ms settle은 span 개수가 멈출 때까지 폴링하는 방식으로 교체 | — | — | — | — | **39 launch = 9.0 s / 70 s**. V209 코드 13반복: qkv cold 100,155 median 99,122 stdev 3,163 (3.19%) · attn_out cold 55,083 median 54,163 stdev 1,524 (2.81%) · ffn cold 291,103 median 297,156 stdev 2,089 (0.70%) | **PASS** | **채택 — 이후 모든 잡의 기준 하네스.** 예산이 7배 남아 REPS를 25로 올렸다. 부수 사실: **ffn은 cold가 warm보다 6k 빠르다**(291.1k vs 297.2k) — "cold 페널티 +8k"는 ffn에 대해 틀렸다. qkv·attn_out의 warm stdev가 3%·2.8%라 "warm ±0.7%"도 qkv에는 틀렸다(그건 인접 쌍의 이야기다) |
| `V227_ffn_weight_in_trf` | `V226` | **가중치를 고정 피연산자로, x를 스트림으로 (최대 판돈).** book: Outer는 `Lane ≤ 8`이고 "when Lane < 8, the inactive lanes' reduction trees are idle, so per-cycle throughput drops proportionally to Lane::SIZE / 8". **이 크레이트의 세 커널은 전부 x를 TRF에 두어 `Lane = m![1]`** — 8차선 중 1개다. book의 정식 GEMV/GEMM은 반대(가중치가 TRF, 출력채널이 Lane). up/gate 8행을 TRF 8차선에 올리고(8 × 3,840 f8 = 30,720 B < TRF 64 KB) x를 스트림한다. 대상은 `contract_up_gate_full` **36,526 static** — 프로젝트 최대 단일 항목 | — | — | 146,750 (sub-fill) | — | 잡 1개, 변형당 n=9, 순서 회전: 프로덕션 median **295,980** · sub-fill **333,724 (+37,744, +12.8%)** · main-fill **311,505 (+15,525, +5.2%)** | 3/3 **PASS** | **기각 — 그러나 가설의 물리는 확인됐다.** `contract_up_gate_full` **36,526 → 5,984 static Main (6.1×)**, book이 예측한 Lane 1 → 8 비율 그대로다. 진 이유는 contraction이 아니라 **f4→f8 LUT**: `to_trf`가 table lookup 출력을 거부해(`mir: unsupported type conversion`) dequant를 별도 pass로 빼야 하고, 그러면 슬라이스당 115,200 B × 2행렬의 f8 버퍼를 DM에 쓰고 다시 읽는 왕복이 생긴다. 그 왕복이 contraction 이득을 삼킨다. **ffn pass A는 contraction-bound가 아니라 LUT-bound다**(정적 18,263/행렬은 LUT+contract 융합인데, LUT만 떼도 9,263/행렬이다). 부수 확인: **sub context의 read_size는 8 B로 고정**이라 32 B 패킷이 4 read다 — 같은 fill이 sub 3,840 vs main 960 cycle이고 실측도 main-fill이 sub-fill보다 22k 빠르다. **벌크 스테이징을 sub에 두지 말 것.** 그리고 **정적 makespan은 ffn에서 다시 틀렸다**: +7,920을 예측했는데 실물은 +37,744(4.8배 증폭) |
| `V228_ffn_down_weight_in_trf` | `V227` | ~~같은 뒤집기를 down pass A에~~ | — | — | — | — | — | — | **취소 — V227이 기각됐다.** down도 f4라 같은 dequant 왕복을 물고, 같은 이유로 진다 |
| `V229_qkv_weight_in_trf` | `V226` | 같은 뒤집기를 qkv/attn_out에 — 여기는 **LUT가 없어** dequant 왕복이 생기지 않는다(가중치가 이미 f8). 다만 판돈이 작다: qkv contraction은 4,635 static, attn_out은 2,712뿐이고, TRF fill(≈960 cycle/타일)이 그 대부분을 되가져간다. 기대 −1k~−4k static | — | — | — | — | — | — | **보류 — 기대값이 노이즈에 가깝다** |
| `V230_begin_interleaved` | `V209`+`V226` | **저장소가 한 번도 쓴 적 없는 API, 0.6.0에 존재 확인**(`context.rs:139`, `begin_interleaved<I: AxisName, D, Chip, Cluster, Slice, Element>(lhs, rhs)` → Time이 `Symbol<I>`). book: "combines two tensors with **identical mappings** into a single sequencer operation… at most two". ffn의 `up_w`/`gate_w`는 매핑이 동일하고 같은 `x_trf`를 쓴다 → pass A 2개를 1개로. 설계: `ctx.main.begin_interleaved::<Gs, _, _, _, _, _>(up_w.view(), gate_w.view()).fetch::<m![Gs, L % 30, H / 64, Dummy2], m![H % 64]>()` 이후는 기존 체인 그대로, 출력은 `m![Gs, L % 30, H / 16]`. **인터리브 축 이름은 `Gs`를 써야 한다** — `src/axes.rs`는 채점에서 무시되므로 새 축을 만들 수 없고, 크기 2인 축은 `Gs`와 `Dummy2`뿐인데 `Dummy2`는 x의 hi/lo가 이미 쓴다. 기대: pass 1개 + LUT 테이블 재로드 838 절약 ≈ ffn −1%. **주의: 남은 위험은 pass B다** — partials가 Gs를 달고 나오므로 Gs → L%30 연쇄 tile이 필요하고, 연쇄 tile은 안쪽 매핑을 **전체 extent 기준**으로 써야 통과한다(V232에서 확인). up/gate scale도 함께 interleave하면 pass B 8회 → 4회까지 가능하지만 `geglu_full`이 분리된 up/gate를 받으므로 그쪽도 손봐야 한다 | — | — | — | — | — | — | **설계됨 — 다음 세션 1순위 코드 후보** |
| `V231_v45_post_norm_rows` | `V228` | V45 재개: post-norm을 생산자 레이아웃에서. 자진 철회 사유가 "기대 −1k인데 노이즈 ±2~4%"였고 **V226이 그 사유를 없앴다** | — | — | — | — | — | — | **설계됨** |
| `V232_qkv_head_norm_fusion` | `V228` | 구 V178/V220. q/k/v head RMSNorm을 `[4, Ds]` 결합 버퍼에서 9 → 5 pass. V218이 이 자리를 실물 −6,833으로 값 매김 | — | — | — | — | — | — | **설계됨** |
| `V232_qkv_rope_no_rotate_half` | `V226` | **RoPE의 `rotate_half` 스테이징 제거.** 프로덕션은 q와 k의 반쪽을 서로 바꾼 위치로 복사하는 데만 main pass 4개를 쓴다(그 다음 sin 곱셈이 제자리에서 읽을 수 있도록). `out[i] = q[i]·cos[i] + q[i^128]·sin[i]`이므로 반쪽을 직접 스트림해서 곱을 뒤바꾼 위치에 commit하면 같다 — j<128은 sin[j+128]과 곱해 j+128에, j≥128은 sin[j−128]과 곱해 j−128에. sin은 이미 출력 위치 기준으로 회전 부호를 갖는다 | 43,785 (V209 43,006) | — | — | — | 잡 1개, 변형당 n=12, 짝 교대: 프로덕션 median **94,717** vs V232 **96,189 (+1,472, +1.6%)** | 3/3 **PASS** | **기각.** 의도한 것은 했다 — **Main 명령 31 → 29, 21,549 → 21,007 cycle**. 그런데 **Core(PeCore) 명령이 54 → 60**이다: commit 1개가 tile commit 2개로 갈라지면서 디스크립터 생성 일이 늘었고, 그게 아낀 pass 2개보다 비싸다. **pass 수를 줄여도 commit 수가 늘면 손해다** — V218의 "pass당 ≈600"은 commit 수를 고정한 채로만 성립한다 |
| `V233_qkv_rope_pass_fusion` | `V232` | ~~구 V179/V221. RoPE ternary 3 → 2 pass~~ | — | — | — | — | — | — | **보류 — V232가 같은 자리에서 졌다.** RoPE pass를 더 줄이려면 commit 수를 늘리지 않는 형태여야 한다 |
| `V196_dmn_interleave` | `V194` | **캠페인 A의 본체 — V8을 실물로 재판정.** book: "DMN interleaving: alternate across 2 DMNs per cluster, else **50% loss**". 지금 레이아웃은 슬라이스당 57.6 KB(ffn up) **연속 블록**이라 소스 주소가 올라가는 동안 슬라이스가 1씩 증가 → 32블록 연속으로 같은 DMN에 쓴다. 슬라이스 배정을 `m![L / 30 % 8, L / 240 % 32]`(연속 행그룹이 슬라이스 32칸 간격 = DMN 8개 순회)로 바꾼다. **판정은 PASS-safe 추가 로드의 증분 A/B로** 한다(다운스트림 재작업 없이 DMA 속도만 분리): p0 = 현행 레이아웃 추가 로드, p1 = stride-32 인터리브, p2 = stride-128 | — | 40,204 (p0 = p1, 정적 완전 동일) | — | — | job 4개(변형당 n=4): base 52,430 / 53,687 / 56,253 / 55,499 · **p0 증분 +29,398 / +25,211 / +24,985 / +26,124 (평균 +26,430)** · **p1 증분 +31,250 / +26,887 / +26,955 / +27,083 (평균 +28,044)** | 전부 **PASS** | **기각 — DMN 인터리빙 가설은 틀렸다.** 슬라이스 축을 뒤집으면 4/4 잡에서 오히려 **+1,614 (+6%)** 느리다. book의 "alternate across 2 DMNs, else 50% loss"는 우리 로드 형상에 적용되지 않는다. V8은 이제 실물로 닫혔다. **부수적으로 훨씬 중요한 발견:** 같은 15.73 MB를 추가로 읽는 데 26,430 cycle = **595 B/cycle**이다. 커널 전체를 바이트로 나눈 306 B/cycle의 2배다 → **세 커널은 순수 DMA 바운드가 아니다.** attn_out 54k 중 weight DMA는 ~31k뿐이고 나머지 ~23k는 DMA가 아니다 |
| `V197_segment_curve` | `V194` | 고정 3.93 MB를 7,680 / 3,840 / 1,920 / 960 / 480 / 240 / 120 B 런으로, 정렬·비정렬 두 기준점에서 추가 로드해 **비용 = f(런 길이, 정렬)** 곡선을 실측한다. book의 "256 B 정렬 위반 시 읽기 2×"와 V174의 2.13×를 하나의 모델로 고정 | — | 52,910 (c128) / 40,075 (c256) / 40,204 (c1024=c2048) | — | — | 잡 3개(변형당 n=3), 같은 15.73 MB의 증분: **c128 +49,341 (319 B/cy)** · **c256 +25,430 (618 B/cy)** · base 512 B +26,430 (595 B/cy, V196 p0) · **c1024 +28,222 (557 B/cy)** · **c2048 +28,762 (547 B/cy)** | 전부 **PASS** | **곡선 확정.** (1) **256 B 미만은 정확히 2× 벌점**(128 B가 256 B의 1.94배) — book의 "256 B 정렬 위반 시 읽기 2×"가 실물로 확인됐다. (2) **최적은 256 B 런이고 더 길면 완만히 나빠진다**(256 → 2048에서 +13%). 길수록 좋다는 통념은 틀렸다. (3) 최고 실효 속도 **618 B/cycle = 스펙 1,500 B/cycle의 41%**. **따름정리:** ffn up/gate의 57,600 B 런(V181)은 런 길이 면에서는 최악 구간이고, V181의 −31k는 런 길이가 아니라 inter-slice reduce 제거에서 왔다. ffn down은 weight 960 B·scale 120 B인데 8청크 유지가 이미 최적(4청크는 weight를 1920 B로 늘려 손해, 16청크는 scale을 60 B로 만들어 손해) → **V200/V202 전제 붕괴, 취소** |
| `V198_stack_alternation` | `V194` | HBM 스택 선택자가 주소 **bit 8**이므로 슬라이스별 기준 주소를 256 B 밀면 두 스택을 번갈아 쓴다. 현행 대비 추가 로드 증분 A/B | — | — | — | — | 설계됨 | — | **설계됨** |
| `V198_attnout_256b_runs` | `V194` | **V197 곡선의 유일한 실행 가능한 이득.** attn_out의 Qs 분할을 8청크(512 B 런) → **16청크(256 B 런)**로. 곡선의 최적점이고, 부수적으로 슬라이스당 필요한 x가 절반(256열 × 2조각)이라 staged-x 재로드도 512 KB → 256 KB. 슬라이스당 60행 → 120행으로 256 슬라이스 전부 live 유지, 행 수는 4의 배수(transpose), reduce 축은 여전히 최내측(V186 제약 준수) | — | **27,375** (base 28,301) | — | — | 잡 4개(변형당 n=5): warm base 53,849 / 52,376 / 53,613 / 52,204 vs **a256 49,937 / 50,083 / 49,120 / 49,101 → 4/4 잡 승, 평균 −3,450 (−6.5%)**; cold base 54,165 / 53,277 / 56,549 / 56,973 vs **a256 50,571 / 48,591 / 49,549 / 50,373 → 평균 −5,470 (−9.9%)** | 전부 **PASS** | **채택** — 4/4 잡, warm −6.5% / cold −9.9%. stdev도 절반 이하(2,461~3,165 → 561~1,605)로 안정적이다. **적용 조건:** 이 수법은 분할 축이 2의 거듭제곱일 때만 256 B 정렬이 성립한다. attn_out은 Qs=4096이라 되고, qkv·ffn up/gate는 H=3840, ffn down은 L/청크=960이라 어떤 2의 거듭제곱 분할로도 256 B 정렬이 안 된다 → **다른 두 커널엔 이식 불가** |
| `V199_merged_x_store` | `V194` | DMA 명령 고정비 549를 깨끗이 값매김: ffn down 타일 5개를 1개·2개로 합치고, 큰 로드 하나를 사이에 계산 없이 2개로 쪼갠다(V185는 pass가 끼어 교란됐다) | 43,006 (qkv) | 27,147 (attn) | **138,686** (ffn) | — | 잡 4개, 커널별 in-job A/B: **qkv** base 100,491 / 101,197 / 102,289 / 104,072 vs m1 99,088 / 100,872 / 101,924 / 102,156 → **4/4 승, −1,002**; **attn_out** base 54,172 / 54,461 / 55,027 vs m1 56,969 / 57,525 / 58,163 → **3/3 패, +3,000**(r3은 stdev 6.3k로 제외); **ffn** base 319,138 / 318,422 / 322,316 / 321,260 vs m1 310,337 / 309,219 / 312,247 / 312,401 → **4/4 승, −9,233 (−2.9%)** | 전부 **PASS** | **부분 채택** — ffn −9.2k(정적 −2.7k의 3.4배), qkv −1.0k는 채택. attn_out은 +3.0k로 **기각**(x 스테이징이 8슬라이스뿐이라 복사 pass 2개가 아낀 명령보다 비싸다). DMA 명령 하나의 실물 가치는 커널마다 다르다: ffn ~9k, qkv ~1k, attn_out은 음수 |
| `V200_ffn_down_4_chunks` | `V194` | **캠페인 B의 본체.** down의 L 분할을 8청크 → **4청크**로: weight 런 960 B → 1,920 B, scale 런 120 B → 240 B, 슬라이스당 60행 → 30행, 256 슬라이스 전부 live(64 행그룹 × 4청크). 모델 기준 실효 바이트 51.6 MB → 42.2 MB. V183과 달리 **클러스터 분할·출력 레이아웃·크로스클러스터 hop 불변**이고 x는 슬라이스별 직접 로드(switch 없음). pass B는 V181의 스칼라 패킷 형태(30은 4의 배수가 아니고 scale VRF가 8행 상한) | — | — | — | — | — | — | **취소 (V197 곡선)** — 측정된 곡선으로 재계산하면 weight 960 B → 1,920 B는 오히려 느려지고(560 → 545 B/cy) scale 240 B의 이득(+5.7k)을 x 두 배(−3.3k)와 상쇄해 순증 +0.6k. 8청크가 이미 최적 |
| `V200_production` | `V194` | **캠페인 프로덕션** = V194 + attn_out 256 B 런(V198) + qkv·ffn 스테이징 store 병합(V199). attn_out은 store 병합을 **안** 넣는다(V199에서 +3.0k). 대체된 형상은 같은 잡의 대조군으로 유지(`c512`, `s2`) | **43,006** | **27,375** | **138,686** | — | 잡 6개 in-job A/B: **ffn** new 307,885 / 307,977 / 308,855 / 308,821 / 310,677 / 312,307 vs s2 319,938 / 319,314 / 319,306 / 321,014 / 320,860 / 322,560 → **6/6 승, −11,078 (−3.5%)**; **attn_out** new 50,250 / 48,845 / 50,479 / 52,035 / 51,295 vs c512 53,547 / 53,802 / 54,814 / 55,558 / 53,717 → **5/5 승, −3,707** (r3은 stdev 7.9k로 제외); **qkv** new vs s2 = +5,825 / −4,299 / +3,294 / −2,234 / −421 / +509 → **평균 +447, 노이즈** | 전부 **PASS** | **채택 — warm median qkv 101.5k / attn_out 50.5k / ffn 308.8k → 기하평균 6.19** (같은 방식의 V194는 5.85, **+5.8%**). qkv의 store 병합은 V199의 −1.0k가 재현되지 않았다(잡별 ±5k 노이즈) — 해는 없으니 유지하되 이득으로 세지 않는다 |
| `V201_apply_interleave` | `V196` | V196~V198이 고른 인터리브·정렬 형상을 qkv q/k/v와 attn_out O 로드에 적용 | — | — | — | — | 설계됨 | — | **설계됨 (조건부)** |
| `V202_ffn_down_2_chunks` | `V194` | V183의 레이아웃을 **교란 요인 둘 없이** 재구성: ring-32 x broadcast 없음, 새 f32 hop 없음. 레이아웃과 구현을 분리한다(보고서 §10 열린 질문 4) | — | — | — | — | — | — | **취소 (V197 곡선)** — 2청크는 weight 3,840 B 런(535 B/cy)에 x가 4배라 +8k 예상 |
| `V201_geglu_merged_store` | `V200` | ffn의 **두 번째** 스테이징(geglu 출력)의 store 2개를 1개로. 입력 스테이징 병합(V199/V200)이 −11k였고 geglu store는 정적 1,653 × 2로 더 비싸다 | — | — | 137,945 (g1, 정적 −741) | — | 잡 4개(n=4): base 312,513 / 308,001 / 310,281 / 309,141 vs g1 309,445 / 309,615 / 308,965 / 309,399 → **2/4, 평균 −628** | 전부 **PASS** | **기각 (노이즈)** — 같은 수법인데 재현되지 않는다. **차이는 위치다:** 입력 스테이징 store는 커널 선두에서 weight 스트림을 막고 있어 −11k, geglu store는 커널 중반이라 스트림 뒤에 숨는다. 명령 병합의 가치는 명령 수가 아니라 **그 명령이 선두에 있는지**로 정해진다 |
| `V202_down_bulk_load` | `V200` | ffn down weight를 5개 명령이 아니라 1/2/3개로 로드(load 높이와 pass 높이를 분리, pass는 그대로 16/16/16/8/4를 view로) | — | — | **143,319 (d1, 정적 +4,633)**; d2·d3는 컴파일러 SIGABRT | — | 큐에 넣지 않음 | — | **기각(정적 + 컴파일러)** — 1개 로드는 파이프라이닝을 잃어 정적으로만 이미 +4.6k, 2·3개 분할은 컴파일러가 죽는다(signal 6). V181이 up/gate에서 통한 이유는 거기선 pass가 로드 뒤에 몰려 있어도 스트림이 계속 이어지기 때문 |
| `V204_submit` | `V194` | **제출본** = V200의 채점 커널과 동일하되 `src/ops.rs`는 **무수정**(RULES §4.1은 ops.rs에서 함수 본문만 허용하는데 V200은 A/B 대조군 함수를 거기에 추가했다). 변경은 전부 `src/device/`에 있다 | **43,006** | **27,375** | **138,686** | — | Arena 검증 2잡 **50/50 PASS**; **공식 채점 `46bc9eed` (2026-09-10 05:12 UTC): qkv 101,132 / attn_out 56,845 / ffn 317,445 = 5.9032** | **PASS** | **공식 SOTA 채택** (직전 V165 5.7576 대비 **+2.5%**). 공식 draw의 attn_out 56,845는 같은 코드의 Arena 범위(47.9k~52.0k) 위쪽 바깥 — cold 1회 draw 분산 |
| `V203_attnout_tiles_120` | `V200` | V198이 슬라이스당 60행 → 120행으로 바꿨으니 타일 분할을 그 형상에서 다시 스윕한다. 현행 88/32는 V188의 44/16을 그대로 두 배 한 것이고 새 형상에서 측정된 적이 없다. 120(1타일) / 104-16 / 72-48 / 60-60 | — | 27,525 / 27,630 / 27,822 / 28,639 (정적, base 27,375) | — | — | 잡 4개(변형당 n=3) 평균: **base(88/32) 49,888** · t104 50,600 · t72 50,376 · t60 50,824 · **t120 52,820** | 전부 **PASS** | **기각 — 88/32가 이미 최적.** 1타일(t120)은 +2.9k로 가장 나쁘다(V188에서 60행일 때 1타일이 나빴던 것과 같은 방향). 캠페인 종료 |
| `V205_byte_scaling` | `V200` | **미해결 질문 #1을 실측으로 가른다.** 커널은 바이트당 ~310 B/cycle인데 같은 커널에 추가한 로드는 595~618 B/cycle이다(V197). 둘 중 하나다 — (a) 커널 자신의 스트림이 반속(접근 패턴 문제) 또는 (b) 스트림은 이미 최고 속도이고 커널의 절반이 직렬 비-DMA 시간(스케줄 문제). 구조를 전부 고정한 채 attn_out O-weight 타일의 슬라이스당 행 수만 120/88/60/32/16으로 바꿔 `cycles = a + bytes/R` 직선을 맞춘다 | — | 28,639 / 24,705 / 21,262 / 17,820 / 15,852 (base 27,375) | — | — | 잡 4개(변형당 n=3), 잡별 회귀: **R = 581 / 656 / 683 / 686 B/cycle (평균 648)**, 절편 **27,662 / 31,753 / 24,381 / 29,329 (평균 28,281)**. warm min 기준도 동일(R 643, 절편 26,837) | 전부 PASS(사본 재사용으로 우연히 통과; 타이밍 전용) | **(b)로 확정.** attn_out의 가중치 스트림은 **645 B/cycle**로 이미 V197 최적점(618)급이다 — **접근 패턴은 병목이 아니다.** 커널 50k 중 **27k(54%)가 바이트와 무관한 고정비**다. 세 커널에 같은 R을 적용하면 고정비는 qkv 52k / attn_out 27k / ffn 155k = 전체의 절반. **캠페인의 표적이 대역폭에서 고정비로 바뀐다** |
| `V206_x_broadcast_ring_sweep` | `V200` | V205의 따름정리: 고정비의 정체가 **MainContext 작업이 실물에서 DMA 뒤에 숨지 않는 것**인지 본다. 가장 큰 순수-이동 Main 항목은 x ring-32 broadcast로 qkv·ffn **각각 7,943 정적**. V158이 32를 고른 것은 첫 성공값일 뿐 스윕된 적이 없다. ring 32/16/8/4(클러스터당 사본 8/16/32/64). 사본 축과 ring 축은 **서로 다른 축**이어야 한다(`m![Dummy256/k, Dummy256%k]`는 `Switch snoop bitmap disagrees`) → `Gf`(16)·`C`(64)를 사본 축으로 | 43,006 / 43,212 / 43,702 / 44,999 (r32/16/8/4) | — | 138,686 / 137,970 / 138,263 / 138,830 | — | **qkv 잡 3개(n=4)**: r16 −3,405/+2,213/+383 · r8 −1,110/+2,933/+2,308 · r4 −1,304/+2,243/+2,580 → 전부 1/3, 평균 −270/+1,377/+1,173. **ffn 잡 3개(n=3)**: r16 −13,922/−15,594/−11,400 · r8 −15,517/−14,387/−14,702 · **r4 −16,856/−17,094/−15,103 → 3/3, 평균 −16,351 (−5.3%)** | 전부 **PASS** | **ffn 채택(ring 4), qkv 기각.** **정적 makespan은 ffn r4를 +144로 예측했다 — 5.3% 실측 승리를 통째로 놓친다.** 새 규칙: **ffn은 MainContext가 실제 구속 자원이고 스케줄이 '숨는다'고 보는 Main 작업도 실물에서는 비용이다**(ffn Main 83.5k 정적 vs DMA 128k). qkv는 반대다 — Main이 스트림에 비해 작고, x2 영역을 사본마다 다시 읽는 부담(V158 병리)이 이득을 상쇄한다. 프로덕션 반영은 `V209` |
| `V207_qkv_byte_scaling` | `V200` | V205의 사다리를 qkv에. 다운스트림 형상과 런 길이를 전부 고정한 채 **로드하는 가중치 행렬 수만** 바꾼다: Q+K+V(31.46 MB) / Q+K(23.59 MB) / Q(15.73 MB) | 43,006 / 39,009 / 34,309 (DMA busy 38,994 / 31,964 / 24,934) | — | — | — | 잡 2개(n=4) warm median: base 99,796 · w2 96,559 · w1 **93,266** / base 106,761 · w2 96,137 · w1 **94,576**. **K+V 15.73 MB를 빼도 6,530 / 12,185밖에 안 줄어든다** | w1·w2는 **FAIL(by design)** | **놀라운 결과 — qkv는 가중치 스트림 바운드가 아니다.** 바이트를 절반 지워도 −6~12k뿐이라 한계 비용이 1,300~2,400 B/cycle로 보인다(하드웨어 피크보다 빠르다 = 불가능). 해석은 **제거하면 숨어 있던 계산이 드러난다**는 것이고, V208이 이를 확증한다 |
| `V208_qkv_run_length_probe` | `V200` | V207의 역방향. q_weight를 **한 번 더** 로드해(V196/V197 탐침 관용구) 증분을 잰다. p0 = qkv 자신의 레이아웃(슬라이스당 8행 = **30,720 B 연속 런**), p1 = 같은 바이트를 **1,920 B 런**으로(128 행그룹 × 2 열청크, 슬라이스당 16행). V197 곡선은 2,048 B에서 끊겨 있었고 거기서도 여전히 하강 중이었다 | 43,006 / 56,813 (p0) / 59,251 (p1) | — | — | — | 잡 3개(n=4, stdev 376~2,538): base 99,740/101,258/100,309 · **p0 +35,227/+33,951/+32,388 (평균 +33,855 = 465 B/cycle)** · **p1 +42,949/+40,549/+39,084 (평균 +40,861 = 385 B/cycle)** | 전부 **PASS** | **런 길이 곡선은 2,048 B 너머에서도 계속 떨어진다: 256 B 618 · 512 B 595 · 1024 B 557 · 2048 B 547 · **30,720 B 465**.** qkv의 31.46 MB는 465 B/cycle이면 **67.7k** — 실측 101k의 3분의 2다(친구 팀의 "데이터 전송만 하면 7만대"와 일치). **그러나 H를 쪼개 런을 줄이는 길은 닫혔다**: 1,920 B는 256 B 정렬이 아니라(3840 = 15×256이라 2의 거듭제곱 분할이 전부 어긋난다) 385 B/cycle로 더 나쁘다 — V26이 기각된 이유가 실측으로 확인됐다. **V207과 합치면:** 더하면 제값(465 B/cy)을 받고 빼면 조금밖에 안 돌려받는다 → **qkv는 DMA와 계산이 상당히 겹쳐 있고 구속 자원은 계산 쪽이다** |
| `V209_ffn_ring4_production` | `V204_submit` | V206의 ffn ring 4를 제출 형태(`src/device/`만)로. qkv는 ring 32 유지 | — | — | — | — | 잡 5개, 커널당 4~5회: **ffn median 294,574**(범위 293,916~301,725) · qkv 102,800 · attn_out 52,670 | **PASS**(15/15, max\|Δ\| 0.00781 = V204와 동일) | **채택 — ffn 317,445 → ~295,000 (−7.1% vs 공식 draw, −5% vs Arena median).** qkv·attn_out 불변. 공식 draw가 반복된다면 기하평균 **≈ 6.05**(현 공식 5.9032 대비 **+2.5%**). **리더보드 제출은 사용자 지시 대기** |
| `V210_ffn_x_pieces_in_lanes` | `V209` | **가장 큰 판돈.** ffn pass A(`contract_up_gate_full`)는 **36,526 정적 Main**으로 프로젝트 최대 항목인데, x_trf의 Lane이 `m![1]`이라 **Contraction Engine 8차선 중 1개만** 쓰고, `Dummy2`(x hi/lo)가 fetch **시간축**에 있어 **모든 weight 패킷을 두 번 읽는다**. Dummy2를 **Lane으로** 옮기면 한 번의 weight fetch가 두 차선을 먹인다 → pass A 절반 기대(정적 −18.3k, V206 규칙대로면 실물 −35k급) | — | — | — | — | 컴파일 단계에서 막힘 | — | **막힘 — 그러나 정확한 벽 세 개를 확정했다.** (1) `TrfTensor<..., m![Dummy2], m![H]>` + `fetch::<m![L % 30, H / 64], m![H % 64]>` + `contract_outer` 자체는 **통과한다**(replay 없는 fetch가 성립). (2) **Lane fold 규칙:** `Interleaved`는 Lane을 OutPacket으로 옮기며 **OutPacket이 정확히 `Lane # 8`이어야** 하고(`OutPacket mismatch. Expected: Dummy2 # 8`), 차선이 2개뿐이면 8-wide 출력 버스가 **1/4 속도**로 돈다(book §Lane Folder). `Sequential`은 Lane을 OutTime으로 옮기며 **Lane 축이 OutTime의 맨 끝**이어야 한다(`Expected Dummy2, got (L % 30, Dummy2, H / 64) % 2`). 따라서 pass A 출력은 `m![L % 30, H / 64, Dummy2, H / 16 % 4]` 형태로만 나온다 — 조각별 슬라이스가 **stride 8의 비연속**이 된다. (3) 그 형상을 pass B가 한 번에 먹으려면 블록 스케일을 Dummy2로 재생해야 하는데, **fetch 안의 더미 축을 VRF로 저장하는 것은 `visa: while lowering StoVrf`로 거부된다**(패킷 안 재생·480원소 패킷 둘 다). 조각별로 pass B를 두 번 돌리는 우회는 비연속 뷰 때문에 32 B flit(`Collect output packet must be exactly 32 bytes`)을 못 채운다. **남은 길:** 스케일을 HBM에서 Dummy2 포함해 로드(바이트 2배 = +7.4 MB, 이득 잠식) 또는 `vector_intra_slice_reduce`가 Way4를 요구하는 제약을 피하는 조각 합산 패스. 브랜치에 전부 남아 있다 |
| `V211_ffn_down_alignment_probe` | `V209` | **정렬 벌점을 실물로 값매김.** down weight는 `m![H, L]` f4라 한 행이 7,680 B인데 `DownRowsByColumns`가 L을 8청크로 나눠 슬라이스당 1,920 nibble = **960 B 런**을 만든다. 청크 c의 시작은 c×960이고 **960 mod 256 = 192**라 8개 중 6개가 granule 중간에서 시작한다 → 런마다 256 B 경계를 하나 더 넘는다. V200a·V202a는 모델로만 계산하고 스스로 취소했다. V208 탐침 방식으로 잰다: q0 = 현행 레이아웃으로 29.5 MB 추가 로드, q1 = 같은 바이트를 **2청크**(128 행그룹 × 2, 슬라이스당 15행, 런 7,680 nibble = **3,840 B, 전부 256 B 정렬**)로 | — | — | 138,830 / 167,537 (q0) / 162,722 (q1) | — | 잡 3개(변형당 n=3) median: base 294,372 / 293,886 / 297,708 · **q0 증분 +73,574 / +75,415 / +72,722 (평균 +73,904 = 399 B/cycle)** · **q1 증분 +56,888 / +58,531 / +58,441 (평균 +57,953 = 509 B/cycle)** | 전부 **PASS** | **확정 — 정렬 벌점은 실재하고 크다.** 같은 29.5 MB에 **q1이 15,951 cycle 싸다(3/3 잡, 바이트당 27% 빠름)**. 비율 509/399 = **1.28**은 "960 B 런이 정렬되면 granule 4개, 어긋나면 5개"의 5/4 = 1.25와 정확히 맞는다 — book의 "reads crossing a 256-byte boundary" 모델이 실물로 확인됐다. **현행 프로덕션이 q0 레이아웃이므로 ffn은 지금 이 벌점을 물고 있다.** 7,680 B를 2의 거듭제곱 개수로 나눠 256 B 정렬이 되는 것은 **2청크(3,840 B)뿐**이다(4청크 1,920 B는 128 mod 256으로 어긋남). 적용의 대가는 슬라이스당 x가 4배(3,840 → 15,360 B)인데, V206이 통한 ring-4 broadcast를 down x에 쓰면 HBM 바이트는 그대로 두고 switch만 낼 수 있다 → `V213` |
| `V212_ffn_bf16_stationary_x` | `V209` | pass A의 `Dummy2` 재생을 없애는 다른 길: 가중치 스트림은 f8로 두고 **고정 피연산자(x_trf)만 bf16**으로. 정밀도는 오히려 올라간다(bf16 가수 8비트 = hi+lo가 재구성하는 값 그대로) | — | — | — | — | 컴파일 거부 | — | **기각(구조적) — 두 trait bound가 동시에 막는다:** `bf16: ContractionWeight<f8e4m3>` 미충족(Contraction Engine은 두 피연산자의 타입이 같아야 한다)과 `f8e4m3: FetchCast<bf16>` 부재. **x의 hi/lo 분해는 선택이 아니라 하드웨어 요구사항이다** — V29가 f8×f8을 고른 이유가 이제 완전히 확정됐다 |
| `V213_ffn_down_two_chunks` | `V209` | **V211을 적용한다.** down을 `m![H / 15 % 128, L / 7680]`(2청크)로 옮기면 weight 런이 3,840 B·전부 정렬(−16k), scale도 120 B → 480 B 세그먼트(−7k 추정). 슬라이스당 15행은 V214의 겹치는 4행 타일(0·4·8·11)로 덮는다 | 178,859 (V209 138,830) | — | — | — | 실측 미수행(정적에서 기각) | — | **기각(구조적) — 컴파일은 되지만 출력 store가 이득을 삼킨다.** 15행 출력 버퍼(30 B)는 `SRAM access width 8`에 걸리고 16/20 B 패딩은 **SIGABRT**다. 타일마다 4행(8 B) 버퍼 4개로 쪼개면 컴파일은 통과하지만 **store 4개가 각각 9,963 = 총 +40k**다(기존 단일 store 2,510). 원인이 구조적이다: 8청크에서는 출력이 **live 슬라이스 32개 × 60행(120 B)**인데 2청크에서는 **128개 × 15행(30 B)**이라 store가 4배 파편화된다. 정렬 이득(−16k)·scale 이득(−7k)에서 겹침(+7.6k)·store 파편화(+7.5k 이상)·x 4배(+11.6k, ring-4로 상쇄 가능)를 빼면 남는 게 없다. **down의 256 B 정렬은 이 출력 형상 아래에서 닫힌다** |
| `V214_overlapping_tile_probe` | `V209` | **V213의 잠금장치를 푸는 탐침.** pass B 출력 패킹이 4행 transpose를 요구해 타일 행 수가 4의 배수여야 하는데, V213의 슬라이스당 행은 15다. **겹치는 타일**로 덮을 수 있는가? 기존 60행 레이아웃에서 마지막 타일을 16@48+4@56 대신 **16@44**로 바꾸면 행 44~47이 두 번 로드·계산·기록된다(값이 같으므로 무해할 것) | — | — | 143,881 (V209 138,830, +5,051 = 추가 4행분) | — | ffn 302,143 (V209 ~294.5k, +7.6k = down 작업 6.7% 증가분) | **PASS** — max\|Δ\| 0.00781로 **V209와 완전히 동일** | **성립 확인.** 컴파일러가 겹치는 `commit_view` 대상을 받아들이고 결과도 비트 단위로 같다. **4의 배수가 아닌 행 수를 4의 배수 타일로 덮는 길이 열렸다** → V213의 15행(4@0·4@4·4@8·**4@11**)이 가능하다. 대가는 1/15 = **6.7%의 down 계산**이고 이 탐침이 그 값을 실측했다(+7.6k). V213의 예상 이득(weight 정렬 −16k + scale 세그먼트 −7k)이 이를 넘는다 |
| `V215_qkv_run_length_tiles` | `V209` | **V211을 qkv에 적용.** qkv의 런은 코드베이스에서 가장 길고(슬라이스당 30,720 B) 가장 느리다(V208: 465 B/cycle). H = 3840 = 15×256이라 **행 경계는 언제나 256 B 정렬**이므로, 열 분할(V208 p1이 385 B/cy로 기각) 없이 **행 매핑만 순환식으로** 바꾸면 런이 짧아진다 — DMA 명령은 그대로 1개. p1 = 프로덕션 블록 매핑(슬라이스 p가 행 8p..8p+8, 30,720 B 런 1개), p8 = 순환 매핑(슬라이스 p가 행 p, p+256, …, **3,840 B 런 8개**). 다중 명령판(2·4·8개 `to_dm`)은 **컴파일러 SIGABRT**라 폐기 | 56,813 (p1) / 56,684 (p8) — **정적 모델은 런 길이를 못 본다** | — | — | — | 잡 3개(변형당 n=3), 같은 잡 안 짝비교 6쌍: **p8 − p1 = −4,046 / −7,938 / −3,964 / −1,610 / −1,928 / +147 → 5/6 승, 평균 −3,240** | 전부 **PASS** | **확인 — 런 길이는 실물에서만 보이고 10%짜리다.** 같은 15.73 MB·같은 1개 명령인데 3,840 B 런이 30,720 B 런보다 평균 3,240 cycle 싸다(465 → **≈514 B/cycle**, V211의 3,840 B 정렬 509와 일치). 프로덕션 전체(31.46 MB)에 적용하면 67.7k → 61.2k = **qkv −6.5k**. **그러나 그대로는 못 쓴다:** 순환 매핑에서는 head h의 256행이 256슬라이스에 1행씩 흩어져 현재의 ring-64 head gather가 **ring-256(≈61k)**이 된다. 4행 연속을 유지하는 절충(2×4행 = 15,360 B 런)은 ring-64와 호환되지만 이득이 ≈1k뿐이다. **전체 이득을 가지려면 head 레이아웃까지 같이 재설계해야 한다** |
| `V216_ffn_upgate_run_length` | `V209` | V215를 ffn에. up/gate는 코드베이스 최장 런(슬라이스당 **57,600 B**)이다. f4 행은 1,920 B(128 B 정렬)지만 **행 쌍은 3,840 B = 15×256**이므로, 순환 행쌍 매핑(슬라이스 p가 행 2p, 2p+1과 512마다)이면 같은 바이트가 **3,840 B 정렬 런 15개**로 온다 — 명령 1개 그대로. q0 = 프로덕션, q1 = 순환 | 162,722 (q0) / **162,722 (q1)** — 완전히 동일하므로 차이는 순수 런 길이다 | — | — | — | 잡 3개, 같은 잡 안 짝비교 6쌍: **q1 − q0 = −723 / +3,471 / +9,402 / +6,775 / +5,303 / +10,975 → 5/6 패, 평균 +5,867** | 전부 **PASS** | **기각 — 그리고 qkv와 정반대다.** 같은 변환이 qkv에서는 −3,240(5/6 승), ffn up/gate에서는 **+5,867(5/6 패)**이다. 차이는 슬라이스당 세그먼트 수로 보인다(qkv 8개 vs ffn 15개)와 스트림이 이미 포화인지 여부다. **V181의 57,600 B 통짜 런이 옳았다는 것이 실측으로 확인됐고, ffn up/gate의 런 길이 축은 닫힌다.** 교훈: 런 길이 곡선은 커널마다 다르며 한 커널의 결과를 다른 커널로 이식할 수 없다 |
| `V217_qkv_channel_heads` | `V209` | **V215의 −6.5k를 가져오는 설계.** 슬라이스 d가 (클러스터가 가진 모든 head의) **채널 d**를 갖게 한다: weight 원소축이 `m![Gs, Ns % 4]`가 되고 H 런은 한 행 = **3,840 B 정렬**(V215가 514 B/cycle로 실측), head RMSNorm은 switch 대신 **256슬라이스 inter-slice reduce**(`V217_probe_reduce_256`에서 lowering 확인), cos/sin·per-channel scale은 슬라이스당 256개 → **1개**, 그리고 **ring-64 head gather가 통째로 사라진다** | **591,293** (프로덕션 43,006) | — | — | — | 실측 불필요(정적에서 기각) | — | **기각 — 그리고 이유가 qkv 전체를 설명한다.** 투영 자체는 예상대로다(weight 로드 **13,383** vs 프로덕션 13,512, 조금 더 싸다). 그런데 **store 하나가 556,076**이다(명령 2개). `q_out`은 `m![Ns, Gs, Ds]`라 채널 분산 레이아웃에서는 슬라이스마다 2 B짜리 쓰기가 8군데로 흩어지고, HBM 비정렬 쓰기는 RMW ~50× 벌점이다. **일반 법칙:** store가 싸려면 슬라이스가 출력의 **연속 구간**을 가져야 하고, 그러면 슬라이스는 한 head의 연속 채널 C개를 갖는다. 256슬라이스 / (클러스터당 head쌍 8개) = head당 32슬라이스 → **C = 8 → weight 런 = 8×3840 = 30,720 B**, 즉 **프로덕션 레이아웃 그 자체**다. 런을 3,840 B로 줄이려면 C=1, 즉 4,096슬라이스가 필요한데 칩에는 512개뿐이다. **런 길이와 head gather의 ring 크기는 같은 손잡이(head가 몇 슬라이스에 걸치는가)이고, ring 비용은 ring 크기에 비례하므로 줄인 런은 언제나 더 비싼 ring으로 되갚는다.** ⇒ **qkv의 스트림은 465 B/cycle에 고정이다.** 남은 레버는 스트림이 아니라 나머지 ~33k(V178·V179) |
| `V218_qkv_tail_ablation` | `V209` | V217이 스트림을 닫았으니 **남은 ~33k가 어디 있는지** 현재 코드에서 다시 잰다(V145는 V82 시절 값이다). a1 = head RMSNorm 3개 제거, a2 = RoPE 제거, a3 = 둘 다(투영+store만). 전부 FAIL by design, cycle만 읽는다 | 43,006 / 41,743 (a1) / 39,005 (a2) / **35,571 (a3)** | — | — | — | 잡 2개(변형당 n=3) median 기준 base 대비: **a1 −5,747 / −7,919(평균 −6,833)** · a2 −8,522 / −14,687(평균 −11,605) · **a3 −15,812 / −16,211(평균 −16,012)** | FAIL(by design) | **핵심: tail은 실물 16k = 커널의 16%이고, 정적 예측의 2.15배다. 그런데 head RMSNorm만 떼면 실물 −6,833에 정적은 −1,263 — 5.4배다.** 클럭비 2.0을 훨씬 넘으므로 그 초과분(≈4.3k)은 *일*이 아니라 **pass 발행 직렬화**다(PE core in-order, q/k/v 합쳐 9 pass). RoPE도 2.9배. **⇒ qkv의 남은 레버는 pass의 크기가 아니라 개수다.** 미측정 슬롯 V178(head-norm 9 → 5 pass)·V179(RoPE ternary)가 정확히 이것을 겨냥하며, pass당 실물 ≈600 cycle이므로 6 pass를 줄이면 **≈−3.6k** |
| `V225_ffn_ring_sweep_tail` | `V209` | V206의 ring 스윕이 ring 4에서 끝났는데 그때까지 계속 좋아지고 있었다(r16 −13.6k · r8 −14.9k · r4 −16.4k). 양옆을 마저 본다: **r8**(32사본, 축 `C / 2`)과 **r2**(128사본, 축 `L / 120`). 복사축과 ring축은 서로 다른 심볼이어야 하고(V156) **복사축이 원소 매핑에 등장하면 안 된다**(`H`를 쓰면 `Switch OutSlice slot must source from InSlice…; offending axis: H / 480`) | 138,830 (r4) / 138,263 (r8) / 142,620 (r2) | — | — | — | 잡 3개, **변형 순서를 매 반복 회전**시켜 cold 전이가 한 변형에 몰리지 않게 함(V219의 교훈): median r8 295,554 · **r4 295,482** · r2 296,674 → **r8 − r4 = +72, r2 − r4 = +1,192** | 전부 **PASS** | **ring 4가 최적임을 확정 — 스윕 종료.** r8은 노이즈 안이고 r2는 사본 바이트가 2배(983 KB → 1.97 MB)라 DMA로 되갚는다. V206의 하강 추세는 r4에서 바닥을 친 것이었다. **V209는 그대로 최선이다.** 부수적으로: 순서 회전 하네스는 변형 간 편차를 크게 줄였다(세 변형 median이 1.2k 안) — **앞으로 모든 다변형 잡은 순서를 회전시킬 것** |
| `V223_ffn_gate_on_sub` | `V209` | **"각 컨텍스트를 다르게 점유해 병렬화" 아이디어의 ffn판.** up과 gate의 pass A는 서로 독립인데 둘 다 MainContext에 있다(합쳐 36,526 정적, 프로젝트 최대). gate를 `ctx.sub`로 옮기면 겹칠 수 있다 — book은 Main·Sub가 Tensor Unit 파이프라인을 공유하지만 상한이 "둘의 합"이 아니라 "큰 쪽"이라고 한다 | — | — | — | — | 컴파일 거부 | — | **기각(구조적) — `fetch_table_lookup`이 Main 전용이다**(`no method named fetch_table_lookup for TuTensor<Tu::Sub, …>`). ffn의 f4 경로는 LUT를 반드시 거치므로 pass A는 Main에 못 뗀다 |
| `V224_attnout_tile1_on_sub` | `V209` | 같은 아이디어의 attn_out판. attn_out은 **LUT가 없으므로**(V32가 f8×f8로 바꿨다) 그 제약을 받지 않는다. 두 weight 타일의 contraction은 서로 독립인데 둘 다 Main에 있다 → tile1을 `ctx.sub`로 | — | — | — | — | 컴파일 거부 | — | **기각(구조적) — `visa: while lowering TensorUnit / CommandTuExec::verified failed`**(Multi 형상 fetch sequencer). **V40c(ffn pass B)·V223과 합치면 결론은 하나다: `ctx.sub`는 contraction도 vector pass도 못 돌린다 — TRF/VRF 스테이징(`to_trf`·`to_vrf`) 전용이다.** 따라서 "연산 유닛을 다르게 점유해서 병렬화"는 이 DSL에서 **DMA ∥ Main** 하나뿐이고, 그것이 V206이 이미 쓴 축이다 |
| `V220_qkv_head_norm_fusion` | `V209` | (구 V178) q·k·v head RMSNorm을 합친 `[4, Ds]` 버퍼에서 mean-square·sqrt·normalize를 각각 한 pass로(현재 텐서마다 3 pass + scale VRF 로드). 스케일·gamma는 행별 VRF 피연산자로 조립한다(q는 `q_weight_scale`+`q_rms_weight`, k는 `k_*`, v는 `v_weight_scale`만 — v는 gamma가 없다: `v_norm = RMSNorm(..., with_scale=False)`) | — | — | — | — | 설계됨 | — | **설계됨 — V218이 이 자리를 −6,833으로 값매김했다.** V219의 기울기 × 줄인 pass 수가 기대값 |
| `V221_qkv_rope_pass_fusion` | `V209` | (구 V179) RoPE의 남은 pass를 줄인다. V192가 이미 cos 곱을 sin 덧셈 사슬에 융합했으므로 **먼저 현재 pass 수를 세고**(V218 a2가 명령 64개 = 커널의 41%를 차지한다) rotate_half의 `commit_view` 2회와 blend를 재구성한다 | — | — | — | — | 설계됨 | — | **설계됨 — a2가 −11,605로 tail 최대 항목이다** |
| `V222_pass_budget_audit` | `V209` | V219의 기울기가 확정되면 세 커널 전체에서 **(줄일 수 있는 pass 수) × 기울기**로 융합 후보를 줄 세운다. 후보 원천: ffn pass B 8회 호출(scale VRF 8 KB 때문에 타일 ≤8행이라 회수는 최소지만 호출당 명령 3개), ffn geglu 사슬(8명령/5,848 정적), attn_out tail. **V219 없이는 착수하지 않는다** | — | — | — | — | 설계됨 (조건부) | — | **설계됨 (조건부)** |
| `V217_probe_reduce_256` | `V209` | V217의 **게이팅 탐침**: inter-slice reduce가 **256슬라이스**를 건널 수 있는가? 저장소 최대는 지금 32슬라이스(`attention.rs:176`)다. 안 되면 V217은 죽은 설계이므로 아무도 시작하면 안 된다 | 56,189 (base 43,006 + Q weight 재로드분) | — | — | — | 컴파일 확인 | — | **열렸다 — `vector_inter_slice_reduce::<m![1 # 256], …>`가 lowering을 통과한다.** 정적 비용도 무시할 수준이다. Way8 → Way4 `vector_narrow_split`을 먼저 걸어야 한다는 제약만 있다(`vector_intra_slice_reduce`도 동일). **V217의 핵심 전제가 확인됐으므로 다음 세션은 여기서 시작한다** |
| `V0_baseline` | `main` | 원본 skeleton (기준) | 116,583 | 194,020 | 1,693,200 | 1.000 | 244,885 / 405,253 / 3,706,465 (**1.000**) | **PASS** | **기준 (실측)** |
| `V164_qkv_q_scatter_zero_index` | `V160` 브랜치(v157a 위) | q의 최종 store(head 레이아웃 `1 # 64`에서 `to_hbm_view`, V146이 이 경로를 scatter보다 +4k로 측정)를 **on-device zero index(kv_offset − kv_offset, `SubFxp` + Stash)로 `dma_scatter`**로 대체 | — | — | — | — | — | — | **막힘** — `dma_scatter`는 소스에 없는 테이블 축(k_cache의 Ts처럼)을 인덱스 키로 relabel해야 하는데 `q_out [Ns, Gs, Ds]`는 전 축이 소스에 있고, device fn 안에서 `&mut HbmTensor`를 다른 타입으로 볼 수 없다 |
| `V170_ffn_ablation_ladder` (f3/f6/f7) | `V168` 브랜치 | **ffn 실물 ablation.** f3: gate block-scale 로드 생략(up의 것을 재사용) = scale 로드 1개(512 디스크립터 × 60 세그먼트)의 실물 비용; f6: post-FF RMSNorm/residual/gate tail 생략; f7: geglu 체인(gelu·곱·스칼라 broadcast 2개) 생략. 정확도 FAIL by design | — | — | 152,342 (f3) / 157,261 (f6) / 154,998 (f7) | — | job 15590 (n=4 warm median): base **346,246** · f3 353,570 (+7k) · f6 **346,008** (−0.2k) · f7 347,866 (+1.6k) | FAIL(by design) | **측정 완료** — post-norm tail·geglu 체인·scale 로드 1개를 빼도 ffn은 안 줄어든다: **ffn은 실물에서 weight/scale DMA 스트림에만 묶여 있다**(99 MB @ ~300 B/cycle). 남은 레버 없음(바이트·정렬 모두 구조적) |
| `V169_qkv_rope_gather_idx8` | `V160` 브랜치(v157a 위) | cos/sin을 HBM hop 없이 head 레이아웃으로 직접 gather: `rope_offset`을 복제 로드로 `[Ns]` 인덱스(8부)로 만들어 `dma_gather_scaled` | — | — | — | — | — | — | **막힘** — i32 스칼라의 복제 로드가 `tail_size % min_align (4)` 정렬 검사에 걸림(4 B 원소는 broadcast 불가). 기대 이득 ≤2k(V147)라 추가 반복 안 함 |
| `V168_ffn_gather_before_store` | `V167` | V167과 같은 기전을 ffn down 출력 hop store(DownRows `1 # 8`, 64 디스크립터)에: switch gather(ring 256) 후 클러스터당 1 디스크립터 store. 정적 중립(157,766) | — | — | 157,766 (v168) | — | job 15585 (n=4): base 346,393 · **v168 355,930** (+9.5k) | v168 **FAIL** (14% tol 안) | **기각** (V167과 같은 결론: 패딩 레이아웃 64-디스크립터 store는 싸고 ring-256 gather가 비싸다) |
| `V167_attnout_gather_before_store` | `V162` | attn_out 투영 출력 store를 64 디스크립터(패딩 `1 # 8` 레이아웃)에서 **클러스터당 switch gather(ring 256, `Broadcast1 { slice1: 32, slice0: 8 }`, 12원소 패킷) 후 2 디스크립터 store**로. V24(a)의 실물 A/B: 정적 중립(28,180 vs 28,223), 실물은 패딩 레이아웃 store가 V146에서 +4k/store였으므로 이득 가능. 컴파일 교훈: switch OutTime에 fetch의 패킷 시간축을 함께 적는다 | — | 28,180 (v167) | — | — | job 15584 (n=5): base 55,816 · v162 53,301 · **v167 57,168** (stdev 0.5k) | v167 **FAIL** (23% tol 안: collect 순서가 어긋남) | **기각** — 맞아도 +1.4k 느리다. 패딩 레이아웃 64-디스크립터 store는 실물에서 싸다(V146의 +4k는 store가 아니라 다른 경로 차이) |
| `V166_ffn_down_three_tiles` | `V165` | ffn down DMA 타일 5(16/16/16/8/4) → 3(28/28/4; V40(a) `7c4abaa`를 cherry-pick, pass A 큰 타일 + pass B 16/12행 뷰). 정적 +3k이지만 실물 타일당 ~2k(attn_out 근거) → 디스크립터 −1,024 | 44,792 | 28,173 | 160,809 | — | job 15582/83: ffn cold **350,878 / 346,780**, warm 346,786 / 347,054 (V165: cold 348.7~352.3k, warm 344.5~347.8k) | PASS ×2 | **중립 → 채택 안 함** (실물 타일당 비용이 정적 +3k와 상쇄; ffn 노이즈 ±5k 안) |
| `V181_ffn_upgate_full_rows` | `V165` | **1위의 ffn 315k를 재현할 후보(V174 근거).** up/gate를 슬라이스당 **30행 × H 전체**(`L / 30 % 256`, 클러스터 L/7680)로: weight 57.6 KB·scale 7.2 KB가 각각 연속 1세그먼트(지금은 60 × 960 B, 60 × 120 B), H 반쪽 inter-slice reduce 소멸, x는 qkv처럼 ring-32 broadcast로 전 슬라이스 복제. 30행의 4행-transpose 제약은 pass B가 transpose 없이 행별 스칼라 패킷(`[30, 1 # 8]` f32)을 commit하고, ring-16 gather 뒤 480행에서 transpose로 조밀화해 우회. geglu는 한 슬라이스의 up/gate 패킷에서(pairs 불필요). down·이후 경로 불변 | — | — | **141,374** (v181) | — | job 15616 (n=4): base 348,242 · v181 320,240 (FAIL, 값 순서 뒤섞임) → **fix1 job 15621 (n=4): base cold 345,672 / median 348,967 · v181 cold 317,053 / median 317,947 (stdev 949; −31.0k, −8.9%)** | fix1 **PASS** (max\|Δ\| 1.08% = base와 동일 오차) | **확인 — ffn −8.9% (1위의 315k 수준)**. 다중 패킷 gather는 packet-major로 도착(fix1). 프로덕션 판 V182로 승격 |
| `V182_ffn_full_rows_production` | `V165` | **프로덕션 후보 (V165 + V181 ffn).** 채점 ffn이 `stage_x_hi_lo_hbm_full` + `feedforward_v181`(슬라이스당 30행 × H 전체 up/gate, x ring-32 broadcast, 행 패킷 ring-16 gather)을 호출; qkv·attn_out은 V165 그대로. tests/는 채점 커널 3개 × 5회(cold + warm 4회) | 44,792 (= V165) | 28,173 (= V165) | **141,374** | — | job 15622/15623/15624 (각 커널 5회): **cold qkv 112,761 / 106,397 / 110,234 · attn_out 51,513 / 54,066 / 52,928 · ffn 318,198 / 322,386 / 319,094**; warm median qkv 106,4k · attn_out 53,3k · ffn 319,3k (stdev ffn 1.3k) | **PASS 45/45** | **채택 후보 — Arena cold median 기하평균 5.86 (V165 동일 기준 5.72, +2.6%)**. 공식 제출은 사용자 지시 대기. ffn 349k → 319k(−8.6%), qkv·attn_out은 V165 범위 그대로 |
| `V183_ffn_down_full_rows` | `V181` | **ffn down도 세그먼트 통합.** down을 클러스터 = L 반쪽(`L / 7680`), 슬라이스당 **15행 × 7680열**(`H / 15 % 256`)로: weight 15 × 3.84 KB·scale 15 × 480 B 세그먼트(지금 60 × 960 B·60 × 120 B). x(geglu 조각)는 V181의 ring-32 broadcast(480패킷, TRF 15 KB), pass A 4/4/4/3행, pass B 4행(VRF 7.7 KB) 행별 스칼라 패킷, ring-4 gather 뒤 두 클러스터 반쪽 합은 f32 패킷 HBM 경유(`Dummy8` 축; `1 # 8` 패딩 HBM은 size check 실패) → reducing 레이아웃에서 add·global scale·bf16·transpose | — | — | 162,078 (v183; 정적은 main +16k) | — | job 15627 (n=3): base 346,424 · v181 **317,528** (stdev 285) · **v183 344,925** (stdev 854; v181 대비 **+27.4k**) | v183 **PASS** (max\|Δ\| 1.08%, mean 0.000617 ≈ base) | **기각(현 형태)** — 정확도는 맞지만 v181보다 +27k(정적 +21k와 같은 급 → 추가 main 작업이 스트림 뒤에 숨지 않는다). 세그먼트 이득(≤19k)보다 x broadcast 480패킷(정적 15.6k)·f32 패킷 hop(store 128 디스크립터 9.6k)·tail이 크다. hop만 줄인 v184로 원인 분리 |
| `V184_ffn_down_full_rows_cheap_hop` | `V183` | V183의 hop만 교체: ring-16 gather(240패킷/root) → f32 transpose 조밀화(`[H % 240 / 2], [H % 240 % 2 # 8]`) → 32 디스크립터 dense store `[L / 7680, H]` → reducing 슬라이스가 두 반쪽을 f32로 재적재, 한쪽은 VRF 피연산자(`AddF`)로 더함 | — | — | 152,891 (v184) | — | job 15630 (n=3): base 347,282 · v181 318,795 · v183 345,931 · **v184 336,545** (stdev 462) | v184 **PASS** | hop 개선 −9.4k(디스크립터 128 → 32 + dense) 확인, 그래도 v181 대비 **+17.7k** → 남은 초과 = x ring-32 broadcast 480패킷(정적 15.6k)이 down 체인의 임계 경로에 그대로. **기각**: down의 세그먼트 통합은 이득이 없다(ffn 후반은 스트림이 아니라 main 체인이 병목) |
| `V185_ffn_upgate_row_tiles` | `V181` | V181의 통행 weight 로드(슬라이스당 57.6 KB 1개)를 4개 행 타일(8/8/8/6, 각각 여전히 연속 1세그먼트)로 쪼개고 pass A를 타일이 도착하는 대로 실행. scale은 행렬보다 먼저 로드 | — | — | 145,098 (v185) | — | job 15634 (n=3): base 345,572 · v181 **318,254** · **v185 327,124** (stdev 2036) | v185 **PASS** | **기각 — v181 대비 +8.9k**. DMA 명령을 늘리면 명령당 고정비(~2k 실물)가 붙고, 스트림은 이미 포화라 pass A를 앞당겨도 숨을 곳이 없다. 로드는 적고 크게가 최적(V136·V42와 같은 방향) |
| `V186_attnout_contiguous_rows` | `V182` | attn_out contraction의 32 행 그룹이 슬라이스 8칸 간격(`m![H / 60 % 32, 1 # 8]`)이라 [H] store가 120 B × 64 디스크립터(정적 2,510). 두 슬라이스 축을 뒤집어(`m![Qs / 512, H / 60 % 32]`) live 슬라이스와 그 행을 연속으로 | — | — | — | — | — | — | **차단(설계)** — 컴파일러: `VRU reduce axes must be innermost in Partitioning`. inter-slice reduce가 도는 축(Qs/512)은 반드시 최내측 슬라이스 축이어야 하므로 축 순서는 고정이다. 대안은 reduce 뒤 switch gather(V191) |
| `V187_overhead_probes` | `V182` | **탐침 4종(중복 실행, PASS-safe).** a1 attn_out contraction store 1회 추가(정적 +2,510) · a2 staged x2 복제 로드 1회 추가(정적 +0) · q1 qkv RoPE cos/sin HBM 왕복 1회 추가(정적 +4,551) · f1 ffn down store 1회 추가(정적 +2,510). 증분 = 그 연산의 실물 비용 | 49,343 (q1) | 30,683 (a1) / 28,173 (a2) | 143,884 (f1) | — | job 15720/15722 (각 n=3): base attn 51,800 / 55,024 · **a1 +1.5k급(잡별 61,931 / 53,737, 분산 큼)** · **a2 55,456 / 55,710 (+2~3k)** · qkv base 105,992 / 107,655 · **q1 113,109 / 114,041 (+7.1k / +6.4k)** · ffn base cold 315,574 / 317,202 · **f1 317,135 / 318,477 (+1.6k / +1.3k)** | 전부 **PASS** | **결론: RoPE 테이블 왕복이 6.5k로 가장 비싸다**(qkv 6%). 패딩 레이아웃 [H] store는 실물 ~1.5k뿐(정적 2,510과 비슷) → V191/V192 가치 낮음. attn_out의 x2 복제 로드는 2~3k → V190 후보 |
| `V188_attnout_tile_sweep` | `V182` | attn_out O-weight 행 분할 스윕: 60(1타일) / 56-4 / 52-8 / 44-16 / 40-20 vs 48/12. 48/12는 단발 측정으로 골랐고, V125는 1타일의 암묵 스트림 속도가 더 높다고 시사했다 | — | 29,627 / 29,278 / 28,415 / 28,301 / 28,428 (정적) | — | — | job 15729/15730 (각 n=3): base **53,845 / 52,914** · t60 57,295 / 54,534 · t56 56,390 / 55,737 · t52 54,270 / 53,866 · **t44 53,398 / 52,715** · t40 54,673 / 54,576. cold: base 55,783 / 54,483 · **t44 52,453 / 52,143** | 전부 **PASS** | **44/16 채택 후보** — 두 잡 모두에서 warm −0.3k, **cold −2.8k**로 가장 낮다. 1타일(t60)·56/4는 오히려 +2.5k(타일이 클수록 contraction이 스트림 뒤로 밀린다). 정적 순위(48/12 최선)는 또 틀렸다 |
| `V189_rope_table_trip` | `V182` | V187 q1이 RoPE 테이블 왕복을 **실물 6.5k**(qkv의 6%)로 값매김했다. q2 = 간접 gather 2개만 중복(정적 45,830) · q3 = HBM 왕복 1개만 중복(45,174) · **q4 = cos/sin을 한 스테이징 버퍼 `m![Dummy2, Ds]`로 합쳐 head 레이아웃을 로드 1번으로 채움(44,472, 정적 −320)** | 44,472 (q4) | — | — | — | job 15737/15738 (각 n=4): base **107,036 / 105,982** · q2 108,105 / 108,445 (**+1.8k** = gather 2개) · q3 108,764 / 108,712 (**+2.2k** = 왕복 1개) · **q4 102,563 / 103,593 (−3.4k)**; cold base 115,615 / 106,506 vs **q4 101,549 / 103,619 (−8.5k)** | 전부 **PASS** | **q4 채택 후보** — 스테이징 버퍼를 합쳐 로드를 하나 줄이자 warm −3.4k·cold −8.5k. 탐침 분해도 맞는다(gather 1.8k + 왕복 2×2.2k ≈ q1의 6.5k) |
| `V190_attnout_x_on_chip` | `V182` | attn_out의 x 두 조각을 HBM에 굽지 않고 **칩 안에서** 배포: 만든 8슬라이스(`XSlices`)에서 `[Dummy2, Qs % 512]` 버퍼로 복사한 뒤 행 그룹 축으로 ring broadcast 1번 → store 2개 + 512디스크립터 2 MB 로드 소멸. x1 = `CustomBroadcast { ring_size: 256 }`, x2 = `Broadcast1 { slice1: 32, slice0: 8 }` | — | **24,781 (x1) / 24,062 (x2)** vs base 28,173 (**정적 −4.1k, −15%**) | — | — | job 15734/15735: base PASS · **x1 FAIL** (max\|Δ\| 229%) · **x2는 HAL 타임아웃(커널 행)** | — | **차단** — 목적지 패턴이 주기 8(슬라이스 s는 청크 s mod 8)이라 링으로 표현되지 않는다. `CustomBroadcast{256}`은 모든 슬라이스에 같은 청크를 뿌리고, `Broadcast1{slice1:32, slice0:8}`은 하드웨어가 멈춘다. reduce 축이 최내측이어야 해서(V186) 축 순서도 못 바꾼다 |
| `V191_production_rope_tiles` | `V182` | **프로덕션 후보 = V182 + 측정된 두 승리**: qkv의 cos/sin 공유 스테이징 버퍼(V189 q4) + attn_out 44/16(V188). 변형 없음 | **44,472** | **28,301** | 141,374 | — | job 15736/15737/15738 (각 5회): cold **qkv 110,547 / 106,761 / 107,377 · attn 52,681 / 56,209 / 49,451 · ffn 322,694 / 320,106 / 314,936**; cold median 107,377 / 52,681 / 320,106 → **기하평균 5.918** (V182 동일 기준 5.864) | **PASS 45/45** | **채택 후보** — cold 기준 +0.9%. qkv는 −2.9k로 확실, attn/ffn은 잡간 분산 안 |
| `V192_rope_blend_fused` | `V191` | RoPE가 `x*cos`를 스크래치에 쓰고 두 번째 pass에서 `rotate_half(x)*sin`을 더한다. 곱과 덧셈을 한 벡터 체인으로 합쳐 q·k의 스크래치 pass 2개(409+345 정적)를 없앤다 | **43,920** (q6) | — | — | — | job 15739/15740 (각 n=5): base 98,683 / 102,064 · **q6 98,266 / 101,448** (warm −0.5k); cold base 108,803 / 104,627 vs **q6 103,388 / 98,938 (−5.5k)** | 전부 **PASS** | **q6 채택** — warm는 −0.5k로 작지만 cold가 −5.5k. 채점이 cold라 반영 가치 있음 |
| `V193_final_candidate` | `V182` | **최종 후보**: 채점 커널에 q4+q6(qkv)와 44/16(attn_out)을 모두 적용하고, **V182의 옛 모양을 같은 잡의 변형(`old`, `t48`)으로 유지**해 잡간 cold 분산(±5k) 없이 한 잡 안에서 A/B | **43,920** | **28,301** | 141,374 | — | **잡 5개(15741~15743, 15757, 15758), 변형당 n=3.** qkv 새 105,101 / 104,163 / 103,251 / 102,902 / 103,236 vs old 108,736 / 107,944 / 108,114 / 106,532 / 106,744 → **5/5 잡에서 −3.5k~−4.9k, 평균 −3,883 (−3.6%)**. attn 새 55,013 / 51,685 / 54,950 / 55,603 / 54,012 vs t48 55,918 / 52,674 / 55,492 / 62,859(stdev 8,400 이상치) / 52,894 → 4/5 잡 승, 이상치 제외 평균 −330 | 전부 **PASS** | **qkv 변경 확정**(5/5, 잡간 분산을 뺀 순효과). **44/16은 약한 승**(4/5, V188 스윕에서는 cold −2.8k로 두 잡 모두 최저) — 퇴보는 아니어서 유지 |
| `V194_production` | `V182` | **프로덕션(변형 없음)** = V182 + q4 + q6 + 44/16. 제출 형태 | **43,920** | **28,301** | 141,374 | — | cold 6회(15744~15749): qkv 112,762 / 104,501 / 106,364 / 108,035 / 107,336 / 109,821 (median **107,686**) · attn 55,197 / 64,547 / 56,476 / 58,151 / 53,057 / 54,556 (median **55,516**) · ffn 319,976 / 321,430 / 320,170 / 321,072 / 320,072 / 321,610 (median **320,621**) | **PASS 90/90** | **채택 후보.** 잡간 절대값은 이 배치가 전반적으로 느렸다(같은 코드가 잡별로 attn 51.7k~64.5k) — 판정은 V193의 잡 내 A/B로만. V165 공식 draw에 델타를 적용한 기대 공식 ≈ **6.05** (5.7576 대비 +5.0%) |
| `V195_attnout_store_gathered` | `V194` | attn_out의 [H] store(패딩 레이아웃 64디스크립터, V187 f1이 실물 ~1.5k로 값매김)를 없애기: 32 행 그룹을 ring(`Broadcast1{slice1:32, slice0:8}`)으로 한 슬라이스에 모으고(도착은 packet-major, V181 교훈) H 순서로 되읽어 재포장한 뒤 클러스터당 1디스크립터로 저장 | — | 28,728 (a4, 정적 +427) | — | — | job 15754~15756, 15759 (변형당 n=4): base 52,182 / 53,805 / 53,187 / 54,123 · **a4 56,060 / 58,188 / 58,898 / 57,708** | 전부 **PASS**(순서 교훈 적용으로 정확도는 맞음) | **기각 — 4/4 잡에서 +3.6k~+5.7k**. switch gather + 재포장 pass가 패딩 store보다 훨씬 비싸다. 패딩 레이아웃 store는 그냥 두는 게 낫다 |
| `V172_qkv_fetch_interference_probe` | `V165` | **탐침(A).** 세 커널이 같은 ~290 B/cycle에 묶인 원인 후보 중 소스에서 만질 수 있는 것은 "DMA는 DM 뱅크 우선순위가 가장 낮다"(책) — 스트리밍 중 이전 타일의 contraction fetch가 DMA를 늦춘다. Q contraction을 **한 번 더**(결과는 스크래치 store) 돌려 초과 비용을 잰다: 간섭이 없으면 +pass 자체(정적 2.2k ≈ 실물 4.5k), 그 이상이면 간섭 | 44,789 (v172) | — | — | — | job 15613 (n=3): v157a 104,984 · v172 112,645 (stdev **10.6k**: 102,008~123,282) | PASS | 노이즈로 판정 불가(r2 대기); attn_out 결과로 보아 간섭은 작을 것 |
| `V173_attnout_fetch_interference_probe` | `V165` | 탐침(A) attn_out판: tile0 contraction 중복(+스크래치 store) | — | 30,933 (v173) | — | — | job 15613: v162 54,270 (stdev 74) · v173 56,943 (stdev 249) → +2.7k = 정적 Δ 그대로 | PASS | **간섭 없음** — 스트리밍 중 fetch는 DMA를 늦추지 않는다(가설 1 기각) |
| `V174_ffn_scale_fullrow_probe` | `V165` | **탐침(B).** 1위의 ffn 315k(우리 349k)를 설명할 후보 = block-scale을 120 B 세그먼트 60개/슬라이스가 아니라 full row(240 B × 60 = 1 세그먼트, 바이트 2배)로 읽는 것(V44). 슬라이스별 열 오프셋이 불가하므로 **모든 슬라이스가 앞 절반을 쓰는 FAIL-by-design** 판으로 up/gate scale 2개의 실물 세그먼트 비용만 잰다. −10k 이상이면 cluster 축 H-split 재설계(V177)로 | — | — | 167,083 (f174a) / 168,574 (f174b) | — | job 15613: base 348,737 · **f174a +22.9k (7.4 MB 연속 = 3.1 cycle/KB)** · **f174b +24.3k (3.7 MB, 120 B 세그먼트 = 6.6 cycle/KB)** | PASS | **세그먼트 로드는 바이트당 2배** → 프로덕션 scale 로드 3개 ≈ 73k (ffn의 21%). 슬라이스가 행 전체를 맡는 레이아웃(V181)이면 scale 3.7 MB가 연속 1세그먼트 → ≈ −26k(up/gate) + weight 세그먼트 이득 |
| `V175_ffn_fetch_interference_probe` | `V165` | 탐침(A) ffn판: up pass A(LUT+contract, 정적 19k) 중복 | — | — | — | — | — | PASS | 설계됨 |
| `V176_loads_first_ordering` | `V165` | **조건부 후보.** V172~V175에서 간섭이 커널의 8% 이상이면: x 스테이징이 마지막 weight 타일에 의존하게 만들어(1행 VRF 피연산자 ×1.0) 모든 weight가 fetch 없는 상태로 스트리밍된 뒤 계산. 이득 = 간섭 − 노출되는 계산 | — | — | — | — | — | PASS | 설계됨 (조건부) |
| `V177_ffn_h_split_on_cluster_axis` | `V165` | **조건부 후보(큰 재설계).** V174가 크면: up/gate의 H 반쪽을 슬라이스 축이 아니라 **클러스터 축**에 두어(클러스터당 L 전체 × H 절반) scale이 슬라이스당 1세그먼트가 되게. 대가: 클러스터 간 partial 합산이 HBM 경유(f32 [L] 2×60 KB 왕복) | — | — | — | — | — | ? | 설계됨 (조건부, Stage 2 후보) |
| `V178_qkv_head_norm_fusion` | `V165` | **작은 확정 이득.** q/k/v head norm의 mean-square·sqrt를 commit_view로 모은 [4, Ds] 버퍼에서 한 pass씩(9 → 5 pass), normalize는 행별 VRF 피연산자(scale·gamma). 실물 ≈ −2k, 프로그램 축소로 cold 분산도 완화 기대 | — | — | — | — | — | PASS(수치 동일) | 설계됨 |
| `V179_qkv_rope_ternary_fusion` | `V165` | **작은 확정 이득.** q_cos·q_sin·add 3 pass → ternary MulAdd로 2 pass(q, k 각각), VRF 스테이징 1개 감소. 실물 ≈ −1k | — | — | — | — | — | PASS(수치 동일) | 설계됨 |
| `V180_attnout_tiles_52_8_44_16` | `V165` | 48/12는 단발 스윕에서 골랐다. 52/8·44/16·36/24를 짝 하네스(4회씩)로 1k 해상도 비교 | — | — | — | — | — | PASS | 설계됨 |
| `V171_qkv_broadcast_v82_attnout_ffn` | `V158` | **사용자 요청: V165의 qkv만 취하고 attn_out·ffn은 V82 그대로.** V158과 src/는 동일(ops.rs의 qkv 17줄만 V82와 다름, `src/device/` 무변경), tests/도 V82로 되돌려 diff가 그 hunk 하나뿐 | 44,792 | 27,424 (= V82) | 157,763 (= V82) | — | job 15600/15601 cold: qkv **109,246 / 106,598**, attn_out 56,332 / 57,874, ffn 356,910 / 351,194 | **PASS ×2 (5/5)** | **확인** — V82의 attn_out·ffn 그대로(cold 값도 V82 범위), qkv만 −28%. 제출용 대안 브랜치 |
| `V165_qkv_broadcast_attnout_two_tiles` | `V158` | **프로덕션 후보.** V158 + attn_out O-weight 타일 20×3 → 48/12 (V136의 diff). tests/는 cold/warm 7회 | 44,792 | 28,173 | 157,763 | — | Arena cold 12회 median qkv 107,405 / attn_out 53,639 / ffn 349,168; **공식 채점 `d0239b5b`: 105,544 / 53,374 / 349,160 = 5.7576** | **PASS** (Arena 12/12 + 공식) | **채택 — 현재 SOTA (공식 5.7576, 2026-09-09 17:15 UTC)** |
| `V160b_qkv_weights_half_descriptors_contig` | `V160` | V160의 정확도 수정 시도: live 128 슬라이스를 연속(`m![1 # 2, Qs / 16 % 128]`)으로 두고 head gather를 ring 32(`slice0: 1`)로, head 레이아웃 `m![1 # 2, Ns % 4, 1 # 32]` | **71,140** (정적 +26k) | — | — | — | job 15581: v160 median **132,378** vs v157a 108,634 (+24k) | PASS | **기각** — 정확도는 잡혔지만 정적 예측대로 느림(연속 128 슬라이스 + ring-32 gather가 직렬화). weight 디스크립터 절반 방향 종료 |
| `V162_attnout_two_tiles_broadcast` (V162 48/12, V163 40/20) | `V159` | attn_out O-weight DMA 타일 3 → 2(Phase C 단발 −2.5k)를 v161(x2 broadcast) 위에 얹어 base·v161·v162·v163을 한 잡에서 4회씩 | — | 28,223 (v162) / 27,900 (v163) | — | — | job 15574 (n=4): base **55,306** · v161 54,019 · v162 **53,600** (stdev 0.4k) · v163 53,017 (stdev 4.3k, 62k 이상치 1) | 전부 PASS | **2타일 −3~4% 확인** → 프로덕션 V165 (48/12) |
| `V160_qkv_weights_half_descriptors` | `V157` | **weight 스트림이 디스크립터 발행에 묶였는지 검증.** Q를 클러스터당 128 live 슬라이스 × 16행(`m![Qs / 16 % 128, 1 # 2]`), K/V를 128 × 8행으로 → weight당 디스크립터 512 → 256, 슬라이스당 바이트 2배. head gather는 stride-2 ring-64(`Broadcast1 { slice1: 64, slice0: 2 }`). v157a와 같은 잡에서 비교 | 45,791 | 27,424 | 157,763 | — | job 15571: v160 **median 103,023** (stdev 0.7k) vs v157a 107,207 → −4k | v160 **FAIL** (55% tol 안 — stride-2 `1 # 2` 슬롯의 ring gather가 패딩 슬롯을 섞는 듯) | 타이밍 −4% 확인, 정확도 → V160b(연속 live 슬라이스 + ring-32 gather) |
| `V158_qkv_x_ring32_broadcast` | `V82` | **프로덕션.** V157(a)를 채점 대상 `sliding_project_qkv`에 적용(변형 fn 없음). tests/는 cold/warm 반복(7회) | 44,792 | 27,424 | 157,763 | 5.55 | job 15566: qkv **cold 107,552** / warm 108,556 · 110,686; attn_out 58,474 / 57,830; ffn 353,628 / 351,700 | **3/3 PASS** | **채택 후보 (실측 SOTA)** — 리더보드 제출은 사용자 지시 대기 |
| `V159_ffn_attnout_x_ring_broadcast` (V159 ffn / V161 attn_out) | `V157` | 같은 기전을 축약 분할 커널에: 청크 축을 innermost 슬라이스 축에 그대로 두고 패딩 슬롯만 live로 바꾸는 `CustomBroadcast`(`m![Dummy8, 1 # 16, H / 1920]` → `m![Dummy8, Dummy256 / 16, H / 1920]`, ring 32). ffn up/gate x2 512 → 32 디스크립터, down x2 512 → 128, attn_out x2 512 → 128 | 44,792 (qkv v157a) | 27,663 (v161) | 155,561 (v159) | — | job 15570: attn_out v161 median 56,491 vs base 55,548; ffn v159 344,358 vs base 342,369 (n=4, stdev ≤1.1k) | 전부 PASS | **기각** — 이득 없음. 병리는 "같은 영역을 512 디스크립터가 읽는" qkv에만 있었다 |
| `V157_qkv_x_ring_broadcast_both_clusters` | `V154` | V155의 진단: `BothClusters = m![Dummy2]`(더미 클러스터 축) 위의 switch pass는 cluster 0만 채운다. 복사본 로드와 switch를 **실제 클러스터 축 `Qs / 2048`** 위에서 하고 끝에 Replicated로 reshape. (a) 실축 직접 로드 + 32 B 패킷 ring 32, (b) 더미축 로드 후 reshape → switch, (e) 실축 16부 ring 16 | 44,792 (a·b) / 44,998 (e) | 27,424 | 157,763 | — | job 15563: qkv v157a **108,374** (cold 117,689, stdev 1.3k) · v157b 111,920 · v157e 108,641 vs base 151,163 (cold 163,293) | **3/3 PASS** (전 실행) | **채택 후보 — qkv −28%** → 프로덕션 V158 |
| `V155_qkv_x_ring_broadcast_forms` | `V154` | V154의 기전을 유지하고 **매핑 표기만 바꾼 4형**: (a) 32 B 패킷 시간축을 switch에 통과, (f) `Dummy256 / 32, 1 # 32` → `Dummy256 / 32, Dummy256 % 32` 단일 축 인수분해 ring 32, (e) 16부 + ring 16(정적 3.8k), (d) 4부 + ring 64. 하네스에 실패 원소의 64-그룹 히스토그램과 변형별 cold/median/stdev 요약 추가 | 44,792 (a·g) / 44,998 (e) / 45,099 (d) | 27,424 | 157,763 | — | job 15559: v155a **106,734** (stdev 1.7k) · g 107,168 · e 108,295 (stdev 207) · d 108,393 vs base 150,823 (stdev 6.2k) | 네 형 모두 FAIL — **cluster 1만 틀림**(q 행 2048~4095, k/v head 4~7), cluster 0은 완전히 맞음 | **기전 확인, 원인 = 더미 클러스터 축** → V157 |
| `V154_qkv_x_ring32_broadcast` | `V82` (하네스는 V151 브랜치 것) | **x 복제 로드(512 디스크립터, 정적 18.4k, util 0.157) → HBM에서 클러스터당 8부(16 디스크립터) + ring-32 `CustomBroadcast`(32 × 240 flit).** 책의 문서화된 형태(`1 # 32` 패딩 슬롯 → live 축). V138이 "바이트가 아니다"를 보였으므로 디스크립터 수를 512 → 16으로 | **44.8k** (v154; switch pass 7,943) | 27,424 | 157,763 | — | job 15555/15556/15557: qkv v154 **104,968 / 111,154 / 107,652** (base 152,427 / 144,741 / 151,148) | qkv **FAIL** (q/k/v 원소 54~57%만 tol 안, max\|Δ\| 6~7) | **−40k(−28%) 확인, 정확도만 남음** → V155 |
| `V151_hop_via_outputs_probe` (V151/V152/V153) | `V82` | **스크래치 → 접촉된 출력 버퍼 hop.** V146/V148이 보인 "스크래치 store가 scatter보다 3~4k 비싸다"를 세 커널에서 건설적으로 확인: qkv cos/sin hop을 `q_out` 행 0·1로(V151), attn_out 투영 출력 hop을 `residual_hbm`으로(V152), ffn down 출력 hop을 `residual_hbm`으로(V153). 변형 device fn 3개 + 각 변형 cold/warm 2회 하네스(tests/)를 한 잡에 | 61,472 (v151) | 27,872 (v152) | 157,763 (v153) | — | job 15551: 이득 없음, v152 FAIL | v151/v153 PASS, v152 **FAIL** | **기각** |
| `V150_test_order_warm_repeat` | `V82` (tests/만 변경) | **측정 방법 탐침.** 채점 순서(qkv 첫 실행 = cold)가 qkv의 초과 비율·노이즈의 원인인지: 세 커널을 두 바퀴 + qkv 한 번 더 돌려 first-launch 비용과 프로세스 내 재현성을 한 잡에서 읽는다 | 61,381 | 27,424 | 157,763 | 5.244 | cold 154,097 / 171,776 → **warm 145.6k~147.7k** | PASS | **측정 완료** — qkv 노이즈 = first-launch 비용 |
| `V149_qkv_ablate_no_rope` | `V82` | **실물 ablation.** RoPE 단계(gather 2 + HBM hop + rotate pass)를 통째로 뺀다(head norm 유지). q/k FAIL이 정상; cycle만 읽는다 | 56,258 | 27,424 | 157,763 | — | qkv **141,431** (−7.7k) | FAIL(by design) | 측정 완료 |
| `V148_qkv_ablate_weights_only` | `V82` | **실물 ablation.** V145+V146: 투영과 store만 남긴 qkv — 옮겨야 하는 바이트의 하드웨어 하한 | 53,583 | 27,424 | 157,763 | — | qkv **138,471** (−10.6k) | FAIL(by design) | 측정 완료 |
| `V147_qkv_ablate_no_rope_gather` | `V82` | **실물 ablation.** cos/sin의 인덱스 gather 2개 + HBM hop을 같은 바이트의 평범한 [Ds] 로드(q/k norm weight를 대역)로 대체 | 58,583 | 27,424 | 157,763 | — | qkv **147,044** (−2.0k) | FAIL(by design) | 측정 완료 |
| `V146_qkv_ablate_no_scatter` | `V82` | **실물 ablation.** k/v ring-cache `dma_scatter` 2개(인덱스 의존 write)를 같은 바이트의 HBM 스크래치 store로 대체 | 60,859 | 27,424 | 157,763 | — | qkv **157,677** (**+8.6k**) | FAIL(by design) | 측정 완료 — 스크래치 store가 scatter보다 비싸다 |
| `V145_qkv_ablate_no_tail` | `V82` | **실물 ablation.** head RMSNorm 3개 + RoPE를 빼고 ring-64 gather 직후의 raw 투영을 그대로 저장 — 후처리 tail 전체의 하드웨어 비용 | 54,067 | 27,424 | 157,763 | — | qkv **132,646** (−16.4k) | FAIL(by design) | 측정 완료 |
| `V1_ffn_down_chunked_dequant` | `V0_baseline` | down proj: 슬라이스별 L/8 청크만 dequant, 재배치 DMA 제거 | 116,583 | 194,020 | **609,223** | **1.406** | — | — | makespan 측정 |
| `V2_attnout_rows_over_256_slices` | `V1` | O proj: 32→256 슬라이스 (H/60 × Qs/1024), 4-way inter-slice reduce | 116,583 | **106,461** | 609,223 | **1.723** | — | — | makespan 측정 |
| `V7_qkv_x_replicate_via_hbm` | `V2` | x 복제를 switch(62k)/DM→DM DMA 대신 HBM 경유 로드로 (qkv: `HbmTensor::new()` 스크래치, attn_out: 입력 HBM에서 청크 직접 로드) | **95,433** | **58,015** | 609,223 | **2.250** | 190,342 / 111,005 / 1,213,877 (**2.430**) | **PASS** | **채택** (실측) |
| `V8_weight_rows_interleaved_dma` | `V7` | weight 행을 4행 블록으로 슬라이스에 교차 배치해 HBM→DM DMA 인터리빙 | 95,433 | 59,225 | 609,223 | 2.235 | — | — | **기각** (makespan; DMA 노드 불변) |
| `V10_attn_weight_tiles_fused_lut` | `V6` | attn_out weight 5×12행 타일 선로드 + f8→bf16 LUT를 contraction 체인에 융합(V5 흡수); qkv는 융합만(타일화는 역효과) | **93,127** | **53,848** | 412,304 | **2.646** | — | — | makespan 측정 |
| `V13_ffn_dma_trims` | `V12` | (a) geglu 출력을 HBM 경유로 ByColumns 로드 **채택**; (b) scale 행렬당 1회 로드는 head 증가로 **기각**(354,617) | 93,127 | 50,110 | **348,874** | **2.866** | — | — | makespan 측정 |
| `V49_hbm_copies_real_stores` | `V41` | 사본 축(`Dummy8`/`Dummy256/16`)은 store를 복제하지 않는다(위 §첫 RNGD 실측). 사본마다 `to_hbm_view`를 명시적으로 호출해 네 곳(qkv x 16부, ffn up/gate·down, attn_out)을 실제로 채운다. 사본 수는 store 추가 비용과 load 분산 이득의 균형으로 다시 튜닝 | — | 144,128 | — | — | — | — | **기각** (사본 수 1/2/4/8/16 전수 측정; c=1이 최적) |
| `V50_drop_false_replication` | `V38` | 소스에 없는 축은 DMA write를 복제하지 않는다. 그 전제에 기댄 두 곳(qkv x의 16부 HBM 사본, V30의 RoPE 테이블 head-layout 직접 gather)을 되돌린다 | **58,962** | 27,424 | 158,182 | 5.310 | **149,244 / 56,522 / 354,825 (4.972)** | **3/3 PASS** | **채택 — 현재 SOTA (첫 유효 5×대)** |
| `V48_qkv_shared_spm_index` | `V39` | cos/sin gather 934×2·k/v scatter 929×2가 각각 HBM 인덱스를 따로 읽는다(`*_scaled`). byte-offset 인덱스를 SPM에 한 번 만들어 `dma_gather_unscaled`/`dma_scatter_unscaled`로 4개 DMA의 인덱스 로드를 공유 | — | — | — | — | — | — | 설계됨 (기대 qkv −0.5k~−1k; 인덱스 비용 분해 미확인) |
| `V47_ffn_global_scale_into_eps` | `V39` | down global scale pass(401, tail)는 post-FF RMSNorm이 스케일 불변이라 불필요. eps만 s²로 나눠(`vector_clip(Add, &eps_vrf)`) 접고 pass 제거 | — | — | — | — | — | — | 설계됨 (기대 ffn −0.4k) |
| `V46_ffn_scalar_broadcast_merge` | `V39` | geglu 스칼라 2개(erf_scale·out_scale)의 broadcast_scalar_pairs가 각각 로드 521 + switch config 719를 낸다. 두 스칼라를 DM `[2, 1 # 8]` 하나에 tile-view 로드해 ring-256 switch 1회로 | — | — | — | — | — | — | 설계됨 (기대 ffn −1.2k) |
| `V45_post_norm_in_producer_layout` | `V39` | attn_out·ffn tail의 출력 store 2,510(64 desc) + hop 1.6k + 8슬라이스 norm을 없애고 post-norm을 투영 reduce 레이아웃(클러스터당 32 row group × 60원소)에서 수행. mean-square 부분합은 클러스터 안 switch gather + 클러스터 간 HBM 스칼라 교환. 최종 store는 64 desc(2,510) | — | — | — | — | — | — | 설계됨 (순이득 ≈ −1k씩; 구현 비용 큼) |
| `V44_ffn_scale_segments` | `V39` | block-scale 로드 9,774×3 = DMA 큐의 18.5%. 원인은 바이트(3.7 MB)가 아니라 **120 B 세그먼트 30,720개**(모델 ≈ 548 + bytes/1.21 KB + 0.2/세그먼트). 슬라이스가 full row(240 B × 60 = 1 세그먼트)를 읽고 자기 절반만 쓰려면 슬라이스별 fetch offset이 필요한데 fetch view는 전 슬라이스 공통이라 불가. 남은 길: (a) pair가 행(30/30)을 나누고 partials를 ring-2 switch로 교환 → scale 1 세그먼트(순 ≈ −1.5k/행렬) (b) 슬라이스 패리티 마스크(`TagMode::AxisToggle`은 Ident라 device fn 불가) | — | — | — | — | — | — | 설계됨 (보류: 분석 완료, 구현 경로 (a)만 유효) |
| `V43_qkv_v_weight_tail_split` | `V39` | qkv tail 4.3k의 V contract 1,225: V weight를 슬라이스당 3행 + 1행 두 tile로 로드해 마지막 contract를 ~1/4로 | — | — | — | — | — | — | **기각 (설계)** — 슬라이스당 4행을 나누면 출력 commit_trim이 6 B/2 B가 되어 8 B 최소(bf16 4개)에 걸림; f32 출력이면 head norm 재작성 필요 |
| `V42_attnout_tile_shapes` | `V39` | attn_out weight 타일 20/20/20 → 28/28/4: 마지막 타일 contract 956 → ~250이 tail에서 빠지길 기대 | 45,744 | **28,466** | 158,182 | 5.706 | — | — | **기각** (+1,042: 스케줄러가 4행 타일을 두 번째로 로드, 마지막 로드가 28행) |
| `V41_x2_hbm_copies` | `V39` | ffn up/gate x2 로드 3,411·down x2 로드 2,934·attn_out x2 로드 1,318은 같은 HBM 구간을 64~256 슬라이스가 반복해 읽는 패턴. V15처럼 HBM 스크래치에 8부 사본을 쓰고(`[Dummy8, …]`, store 디스크립터 불변) 슬라이스 매핑으로 사본을 분산 | 45,744 | **27,272** | **157,282** | **5.801** | 103,373 / 55,598 / 345,309 (5.702) | **3/3 FAIL** | **기각** (사본 버그를 attn_out·ffn까지 확대) |
| `V40_ffn_down_pass_a_big_tiles` | `V39` | ffn down 타일 5개(16/16/16/8/4)마다 DMA 고정 548 + LUT 테이블 838. pass A만 큰 타일로(타일별 60행 partials 버퍼에 tile commit), pass B는 ≤16행 tile view로 → 타일 수 감소 | 45,744 | 27,424 | 159,050 (a) / 159,781 (b) | 5.756 | — | — | **기각** — (a) 28/28/4 +868 (b) 16/24/16/4 +1,599 (c) pass B를 sub로: 컴파일 실패. pass A(316/행)가 다음 타일 로드(481/행+1,386) 안에 끝나려면 타일이 0.66배씩 줄어야 해 4행 마지막 타일 기준 5개가 최소 |
| `V39_x2_single_store` | `V38` | x의 f8 hi/lo 두 조각을 HBM 스크래치에 tile store 2회(qkv 553×2, attn_out 377×2, ffn head 553×2·down 1,653×2)로 쓴다. 두 pass가 한 DM 버퍼(`m![Axis % 960]`, 실제 축의 480-tile 2개에 `commit_view`)에 쓰고 `[Dummy2, Axis % 480]`로 reshape해 store 1회로. 절단 split(V35)을 qkv/ffn에도 적용해 hi 재로드 없이 두 pass가 x만 읽게 | — | — | — | — | — | — | **실패** (StreamUnmatchedSegment) |
| `V38_post_norm_store_from_reducing` | `V37` | attn_out·ffn의 post-norm tail은 정규화 pass 뒤에 switch pass(Broadcast1 8→1, 744 + config 로드 719)로 [H]를 한 슬라이스에 모아 store한다. 정규화 pass에서 bf16으로 cast해 ReducingSlices(8슬라이스 × 480) 그대로 HBM에 store(8 디스크립터)하면 switch pass가 tail에서 사라진다 | 45,744 | **27,424** | **158,182** | **5.779** | 109,309 / 56,152 / 357,983 (5.511) | qkv **FAIL** | **보류** (V15 사본 버그 상속) |
| `V37_ffn_upgate_pass_a_big_tiles` | `V36` | ffn DMA 큐의 타일당 고정비 = DMA 548 + LUT 테이블 로드 838(`?` 4 KB, pass마다) ≈ 1.4k × 13타일. up/gate의 16행 한도는 pass B의 scale VRF(8 KB)에서 오므로 pass A(LUT+contract)만 큰 타일(20/30/60행)로 하고 60행 partials 버퍼에 tile commit, pass B는 16/16/16/12 tile view로 유지 | 45,744 | 27,954 | **158,712** | **5.736** | — | — | makespan 측정 |
| `V36_qkv_scales_in_head_norms` | `V35` | qkv DMA 큐의 K/V weight-scale 로드가 838×2(512 디스크립터 × 8 B), Q scale 로드 792가 V weight 로드 바로 앞에 선다(합 2.5k). 세 scale을 투영 epilogue 대신 head RMSNorm(한 head/슬라이스, 64 디스크립터 ~555)의 mean-square·normalize pass에 `MulF(Mul1)`로 접는다; 소비자가 tail이라 로드도 V weight 뒤로 갈 가능성 | **45,744** | 27,954 | 165,733 | **5.654** | — | — | makespan 측정 |
| `V35_attnout_trunc_split` | `V33` | attn_out tile0 로드는 x_lo pass 발행에 묶인다(V34 교훈). x_lo가 x_hi의 DM 왕복(cast f8 → VRF 재로드 327 + 지연)을 기다리지 않도록 hi를 VE 안에서 절단(`BitAnd 0xFFF00000`, 유효 4비트)으로 만들고 lo = 16·(x − trunc x)를 x VRF에서 직접 계산: 두 pass가 x만 읽어 연속 발행. (c) V34 fold 재적용 | 46,541 | **27,954** | 165,733 | **5.621** | — | — | makespan 측정 |
| `V34_attnout_scale_in_rmsnorm` | `V33` | (V27 계획을 V33 위에서) attn_out 채널 scale 로드(594)가 DMA 큐 선두에서 tile0을 막고 타일 epilogue마다 narrow/MulF/widen이 붙는다. 투영은 scale 없이 bf16으로 내고 post-attn RMSNorm의 두 pass(mean-square, normalize)에 `MulF(Mul1, scale)`로 접는다; scale은 tail에서 ReducingSlices로 로드 | 46,541 | 28,002 | 165,733 | 5.618 | — | — | **기각** (동일) |
| `V33_attnout_immediate_scale` | `V32` | attn_out x 스테이징 체인(max x² 413 → 상수 패킷 410 → VRF 327 → hi 410 → lo 410)이 DMA 큐 선두를 1.2k 비운다(tile0 로드가 3,922에 시작). s=16이 상수이므로 hi/lo pass의 MulF에 즉치 16을 써서 앞 세 pass를 없앤다 | 46,541 | **28,002** | 165,733 | **5.618** | — | — | makespan 측정 |
| `V32_attnout_f8_contraction_no_lut` | `V31` | attn_out도 O weight가 f8: x([Qs])를 8슬라이스에서 f8 hi/lo(×2^k)로 만들어 HBM `[Qs/512, Dummy2, Qs%512]`에 두고 슬라이스별 청크를 로드, f8×f8 contraction으로 융합 LUT 3개(`?` 838×3, 타일 pass 1,863→~1k)를 없앰; post-attn RMSNorm이 스케일 흡수. 스케일은 동적 2^k 대신 **상수 16**(attention 출력은 RMS-정규화된 value 행의 볼록결합이라 \|x\| ≤ √256 = 16, \|x·16\| ≤ 256 < 448) | 46,541 | **29,318** | 165,733 | **5.533** | — | — | makespan 측정 |
| `V31_qkv_f8_contraction_no_lut` | `V30` | qkv 가중치는 이미 f8: x를 f8 hi/lo(×2^k, 16부 사본)로 TRF에 주고 f8×f8 contraction으로 LUT pass 3개(`?` 838×3 DMA, Q LUT 5k Main)를 없앰; q/k/v RMSNorm이 스케일 불변이라 1/s 불필요 | **46,541** | 30,037 | 165,733 | **5.488** | 113,089 / 72,831 / 393,884 (4.840) | qkv **FAIL** | **보류** (V15 사본 버그 상속) |
| `V30_qkv_rope_tables_direct_gather` | `V29` | rope의 cos/sin 행을 `dma_gather_scaled`로 헤드 레이아웃(두 클러스터, 슬라이스당 1행 복제)에 직접 가져와 HBM hop(store 337 + load 555)×2를 제거 | **48,638** | 30,037 | 165,733 | **5.408** | — | — | makespan 측정 |
| `V29_ffn_block_scale_after_contract` | `V28` | FFN dequant pass(VE 91k) 제거: f4→f8 LUT를 **f8×f8 contraction**에 직결, 16열 블록 partial을 f32로 내보낸 뒤(pass A) 블록 partial에만 scale(pass B). x는 f8 두 조각(hi/lo, 합이 bf16 x·2^k와 정확히 같음)으로 TRF에, weight 패킷을 Dummy2 시간축으로 2회 스트림해 Time Reducer가 합산. 2^k는 벡터별 max로 동적 선택 | 50,981 | 30,037 | **165,733** | **5.324** | 126,473 / 66,775 / 383,693 (4.842) | qkv **FAIL** | **보류** (V15 사본 버그 상속) |
| `V28_ffn_tile_shapes` | `V25` | FFN 타일 재편: up/gate 16/16/16/12, down 16/16/16/8/4(마지막 타일 작게) + geglu의 global scale을 스칼라 준비 pass(s_gate/√2, s_up·s_gate/2)로 gelu·mul pass에 fold | 50,981 | 30,037 | **168,757** | **5.292** | — | — | makespan 측정 |
| `V27_attnout_scale_in_rmsnorm` | `V26` | attn_out 채널 scale(64 디스크립터 로드 1,318이 DMA 큐 선두에서 첫 타일을 막음)을 epilogue 대신 post-attn rmsnorm 두 pass에 접어 넣어 로드를 tail로 | — | — | — | — | — | — | V34로 대체 |
| `V26_qkv_hsplit_x_halved` | `V39` (2026-09-09 밤 구현; 원안 분기점 V25) | qkv 투영을 H/1920 열 반으로 나눠(Q 16행×1920, K/V 8행×1920, 2-way inter-slice reduce) 복제 x 바이트를 절반으로 (x 로드 5.2k, x_trf 1.2k×2). x2 HBM 스크래치를 `[Dummy256/16, H/1920, Dummy2, H%1920]`로 두어 슬라이스당 1세그먼트 | — | — | — | — | — | — | 구현됨 (컴파일 결과는 아래 상세·체인 보드 참조) |
| `V25_ffn_geglu_two_clusters` | `V24` | up/gate의 HBM hop(store 4,535×2 + load 1,328×2 = 11.7k)을 없앤다: geglu를 두 클러스터 reduce 출력 레이아웃(슬라이스당 60행, V18식 패딩 패킷)에서 직접 수행; 출력 store 앞 ring-16 gather로 4,535 → 1,870 | 50,981 | 30,037 | **170,158** | **5.277** | — | — | makespan 측정 |
| `V24_gather_before_hbm_store` | `V23` | (a) 투영 출력을 HBM에 쓰기 전 클러스터 안 switch gather → **기각**(live 슬라이스가 8칸 간격이라 ring 256, 2,055 = store 절감분); (b) post-norm 피연산자(x·residual)를 HBM에서 ReducingSlices로 직접 로드, ffn global scale을 8슬라이스 레이아웃에서 → **채택** | **50,981** | **30,037** | **179,922** | **5.180** | — | — | makespan 측정 |
| `V23_ffn_whole_scale_loads` | `V22` | FFN block scale을 pass 타일(15개, 36k) 대신 행렬당 1회 로드(슬라이스당 7.2 KB)로; V13b 재시도 | 51,631 | 30,487 | **181,625** | **5.131** | — | — | makespan 측정 |
| `V22_attnout_weight_tiles` | `V21` | attn_out weight를 20행 타일 3개로 선로드해 융합 LUT+contract(5k)를 DMA(13.5k)와 겹침 | 51,631 | **30,487** | 186,976 | **5.082** | — | — | makespan 측정 |
| `V21_qkv_rope_tables_early` | `V20` | cos/sin gather·HBM hop·VRF 스테이징을 커널 맨 앞에서 수행해 q/k rope가 V 로드와 겹치게 | 51,631 | — | — | — | — | — | **기각** (불변; 스케줄러가 gather 하나만 앞당김) |
| `V20_qkv_tail_per_cluster` | `V19` | q/k/v의 HBM hop 제거: 클러스터별 ring-64 switch gather로 헤드/슬라이스 레이아웃을 만들고 후처리·저장·scatter를 두 클러스터에서 병렬 수행 | **51,631** | 31,113 | 186,976 | **5.048** | — | — | makespan 측정 |
| `V19_qkv_tail_heads_layout` | `V18` | qkv 후처리(q/k/v rmsnorm·rope·저장)를 헤드별 슬라이스 분산 레이아웃에서 수행 — HBM hop에서 직접 그 레이아웃으로 로드, rope의 InterTranspose·Broadcast1 제거 | **55,811** | 31,113 | 186,976 | **4.919** | — | — | makespan 측정 |
| `V18_attnout_scale_in_epilogue` | `V17` | attn_out 채널 scale을 contraction 체인 epilogue(inter-slice reduce 뒤 vector 곱)로 접어 넣어 tail의 scale pass 2개 제거 | 59,216 | **31,113** | 186,976 | **4.822** | — | — | makespan 측정 |
| `V17_qkv_hoist_weight_loads` | `V16` | qkv: Q weight의 LUT pass를 rmsnorm 앞에 발행해 Q 로드가 DMA 큐 선두로 (K/V까지 같은 방식은 무효) | **59,216** | 34,776 | 186,976 | **4.649** | — | — | makespan 측정 |
| `V16_rmsnorm_fused_residual` | `V15` | post-attn/post-FF rmsnorm의 마지막 vector pass에 residual add(+layer gate)를 접어 넣어 tail pass 제거 (새 함수 `normalize_add[_gate]`) | 60,412 | **34,776** | **186,976** | **4.618** | — | — | makespan 측정 |
| `V15_x_replicate_hbm_copies` | `V14` | qkv의 x 복제 로드가 같은 7.5 KB HBM 구간을 512번 읽는 패턴(420 B/cycle) → x를 HBM에 8부 쓰고 슬라이스별로 다른 사본 읽기 | **60,412** | 38,240 | 191,122 | **4.434** | — | — | makespan 측정 |
| `V14_two_clusters` | `V13` | 모든 DM 텐서가 `Cluster = m![1 # 2]`(클러스터 1개만 live)였다. 세 커널의 투영을 두 클러스터에 실제로 나눠 512 슬라이스 사용; DMA 처리량·연산 2배 | **73,445** | **38,240** | **191,122** | **4.147** | 183,005 / 78,708 / 418,837 (**3.936**) | **PASS** | **채택 — 현재 실측 SOTA** |
| `V12_ffn_rows_per_pass_12` | `V11` | FFN `ROWS_PER_PASS` 4→12: up/gate는 1920열 절반씩 dequant(scale VRF 5.8 KB), down은 그대로; pass 수 90→30 | 93,127 | 50,110 | **352,164** | **2.857** | — | — | makespan 측정 |
| `V11_residual_1920_tiles` | `V10` | `residual::add`를 480×8 타일에서 1920×2 타일로 (공유 코드) | 93,127 | **50,110** | **408,566** | **2.716** | — | — | makespan 측정 |
| `V9_ffn_x_via_hbm` | `V7` | ffn의 x→Replicated DM→DM DMA(54k)를 V7 기법(HBM 스크래치 경유, 18.4k)으로 | 95,433 | 58,015 | **574,845** | **2.295** | — | — | makespan 측정 |
| `V3_qkv_hsplit_no_broadcast` | `V0_baseline` | QKV: H를 8슬라이스로 분할해 x 전체 브로드캐스트(62k) 제거, inter-slice reduce | — | — | — | — | — | — | 보류 (V7 우선; 아래 참조) |
| `V4_attnout_qsplit_no_broadcast` | `V2` | O proj: Qs를 8슬라이스로 분할해 x 브로드캐스트(66k) 제거 | — | — | — | — | — | — | 설계됨 |
| `V5_lut_in_contract_chain` | SOTA | f8→bf16 table lookup을 별도 pass 없이 contraction 체인 안에서 수행 | — | — | — | — | — | — | V10에 흡수 |
| `V6_ffn_upgate_overlap` | `V9` | FFN weight를 4행 타일로 스트리밍(LUT+cast를 fetch 단계에 융합), up/gate 인터리브, down 더블버퍼, geglu 직접 relayout, scale 타일화 | 95,433 | 58,015 | **412,304** | **2.559** | — | — | makespan 측정 |

> 단위: cycle. `—` 미측정. `FAIL(accuracy)` tolerance 위반. 상태 전이는 RULES §2.1.

## 2026-09-09 첫 RNGD 실측 (Arena) — 계보의 절반이 무효

Arena 로그인이 끝나 **처음으로 실물 cycle과 정확도가 나왔다**(job 15346~15352). 정적 makespan만
보고 쌓아 온 계보의 절반이 정확도로 무효라는 것이 여기서 드러났다.

| 브랜치 | qkv | attn_out | ffn | 실측 기하평균 | 정확도 |
|---|---:|---:|---:|---:|:---|
| `V0_baseline` | 244,885 | 405,253 | 3,706,465 | 1.000 | PASS |
| `V7_qkv_x_replicate_via_hbm` | 190,342 | 111,005 | 1,213,877 | 2.430 | PASS |
| `V14_two_clusters` | 183,005 | 78,708 | 418,837 | **3.936** | **PASS** |
| `V29_ffn_block_scale_after_contract` | 126,473 | 66,775 | 383,693 | (4.842) | qkv FAIL |
| `V31_qkv_f8_contraction_no_lut` | 113,089 | 72,831 | 393,884 | (4.840) | qkv FAIL |
| `V38_post_norm_store_from_reducing` | 109,309 | 56,152 | 357,983 | (5.511) | qkv FAIL |
| `V41_x2_hbm_copies` | 103,373 | 55,598 | 345,309 | (5.702) | **3/3 FAIL** |

**makespan은 상대 스크리닝 도구로는 유효하다.** 실측/makespan 비율이 세 커널 모두 2.0~2.4로 균일하고
(V0 2.10/2.09/2.19, V38 2.39/2.05/2.26), 실측 기하평균의 순서가 makespan 기하평균의 순서와 같다
(V38 5.511 vs 5.779, V41 5.702 vs 5.801). 같은 잡을 두 번 돌린 V41의 편차는 1.5%다
(104,942/57,370/350,081 → 103,373/55,598/345,309). **다만 makespan은 정확도를 전혀 보지 못한다.**

### 근본 원인: `Dummy` 사본 축은 store를 복제하지 않는다

V15가 도입하고 V31·V41이 확대한 "HBM 사본" 관용구는 이렇게 생겼다.

```rust
let mut x_hbm: HbmTensor<bf16, Chip, m![Dummy8, H]> = HbmTensor::new();  // 목적지에만 사본 축
x.view().to_hbm_view(&mut ctx.tdma, x_hbm.view_mut());                   // 소스엔 없음
let x: DmTensor<_, _, _, m![Dummy256 / 8, Dummy8], m![H]> = x_hbm.to_dm(&mut ctx.tdma);
```

가정은 *"소스에 없는 축은 store를 복제한다(디스크립터 추가 비용 0)"* 였다. **틀렸다.** 스케줄이 그대로 말한다.

| | store `to_hbm_view` | load `to_dm` |
|---|---:|---:|
| V14 (사본 1개) | `ops.rs:65` **434** | `ops.rs:66` 18,400 |
| V15 (사본 8개) | `ops.rs:67` **434** | `ops.rs:68` 5,367 |
| V38 attn_out (사본 1개) | `projection.rs:170/172` **377 / 377** | `:175` 1,318 |
| V41 attn_out (사본 8개) | `projection.rs:173/175` **377 / 377** | `:179` 1,166 |

목적지가 8배 커졌는데 store 시간은 1 cycle도 늘지 않고, load만 흩어져 빨라졌다. 즉 **8곳에서 읽는데
1곳에만 쓴다.** 나머지 7/8 슬라이스는 초기화되지 않은 HBM 스크래치를 읽는다. bf16이면 쓰레기 비트가
NaN/Inf가 되어 `non-finite device output at 0`으로, f8e4m3이면 거의 모든 비트 패턴이 유한값이라
"유한하지만 220%대로 틀린 값"으로 나타난다 — 관측된 두 실패 양상이 정확히 이것이다.

`to_hbm_view`([furiosa-opt-std-0.6.0 `src/tensor/memory.rs:506`])는 `to_dm_view`와 달리
`assert_dma_layout`/`assert_dm_to_dm_dimension_preserved`를 부르지 않는다. 그래서 축이 안 맞아도
조용히 컴파일된다.

**감염된 위치 (head 기준):**

| 위치 | 도입 | 사본 | 증상 |
|---|---|---:|---|
| qkv x — `shared::mlp::stage_x_hi_lo_copies_hbm` (`mlp.rs:186`) | V15 (bf16 8부) → V31 (f8 16부) | 16 | qkv NaN |
| ffn up/gate x2 — `stage_x_hi_lo_hbm` (`mlp.rs:83`) | V41 | 8 | ffn 233% |
| ffn down geglu — `stage_geglu_hi_lo_hbm` (`mlp.rs:231`) | V41 | 8 | ffn 233% |
| attn_out x2 — `projection::project_output` (`projection.rs:172`) | V41 | 8 | attn_out 222% |

V38이 qkv만 깨지고 attn_out·ffn은 PASS인 것이 이 표의 직접적인 확인이다. **`HbmTensor::new()`
자체(V7/V9의 HBM hop)는 무죄다** — V7은 PASS했고, 사본 축이 없는 store는 정상이다.

### 이것이 뜻하는 것

- **V15 이상 전 계보(V15~V41)가 정확도로 무효다.** 지금 점수가 되는 최선은 `V14_two_clusters`의 **3.936×**.
- 사본을 진짜로 쓰면(사본마다 명시적 store) 계보 전체가 되살아난다 — 그 실험이 **V49**다.
- V15의 makespan 이득(load 18,400 → 5,367 = −13,033)은 store 7개 추가(+3,038)로 대부분 남는다.
  16부 사본(V31)은 store 30개 추가라 사본 수 자체를 다시 튜닝해야 한다.

## 현재 SOTA

**공식 리더보드 기준: `V165_qkv_broadcast_attnout_two_tiles` (`0704eda`) — 5.7576** (qkv 105,544 / attn_out 53,374 / ffn 349,160, 2026-09-09 17:15 UTC, moa-submitter `d0239b5b`). 그 전 공식은 V82 5.0860. 아래 문단은 2026-09-09 낮 시점의 기록이다.

실측(RNGD) 기준(2026-09-09 낮): **`V50_drop_false_replication` — 기하평균 4.972×** (149,244 / 56,522 / 354,825,
3/3 PASS, job 15360). V38에서 "소스에 없는 축이 DMA write를 복제한다"는 잘못된 전제에 기댄 두 곳
(qkv의 16부 HBM 사본, V30의 RoPE 테이블 gather)을 되돌린 것이다. 이전 실측 SOTA는 V14(3.936×).
V15~V41은 전부 정확도 FAIL이라 점수가 없다. 문서 최신본은 이 브랜치에 있다.
야간 자동 체인의 실측/빌드 결과는 pod `/root/auto/wt/auto/BOARD.md`(RULES §11). 자세한 서사는 [SOTA.md](SOTA.md).

## 죽은 길 (다시 시도하지 말 것)

- **소스에 없는 `Dummy` 축을 목적지에 붙여 HBM store를 복제하기** (V15/V31/V41) — 복제되지 않는다.
  store 디스크립터도 바이트도 그대로이고(그래서 "공짜"로 보인다), 사본 1개만 채워진 채 나머지를
  읽는다. `to_hbm_view`는 축 검사를 하지 않아 조용히 컴파일된다. 같은 전제가 `dma_gather_scaled`의
  목적지 cluster/slice 축에도 적용됐다(V30의 RoPE 테이블) — 역시 복제되지 않는다. 실측 없이
  makespan만 보고 채택한 변경이 어떻게 되는지의 표본이다.
- **사본마다 store를 불러 위를 고치기** (V49) — store 1개마다 Core 디스크립터 명령이 3개 붙고
  Core는 in-order라, 사본 수 1/2/4/8/16 어디서도 c=1(사본 없음)을 못 이긴다. 사본이라는 아이디어
  자체를 버려야 한다(V50). 단 **실측에서는** 정적 모델보다 사본의 값어치가 크므로 c=2의 실물 A/B는 열려 있다.

- **switch 기반 브로드캐스트 변형으로 x 복제 비용 줄이기** — book(Switch Engine)이
  명시: 모든 SwitchConfig의 비용 = `ring_size × Time × flits_per_packet`. 1개 슬라이스의
  7.5KB(bf16 [H])를 256 슬라이스로 뿌리면 어떤 변형이든 256 × 240 = 61,440 cycle.
  V0의 62,215/66,311이 정확히 이 값이다. ring 크기 조작, Broadcast01, CustomBroadcast
  모두 동일 비용. **바이트 수 자체를 줄이는 것(축 분할)만이 답.**
- **DMA로 Replicated 매핑에 복제** — FFN이 이미 이렇게 하고 있고(ops.rs:227) 54,432 cycle.
  512 슬라이스 × ~106 cycle의 디스크립터 비용에 묶인다. switch 대비 8k 이득뿐.
- **inter-slice reduce 출력(`m![X, 1 # k]`)을 switch로 클러스터 안에서 모아 HBM store 디스크립터를 줄이기** (V24a/b) —
  VRU reduce 축은 innermost 강제(`VRU reduce axes must be innermost in Partitioning`)라 live 슬라이스가 k칸 간격으로
  남고, `Broadcast1 { slice1: 32, slice0: 8 }`은 ring 256으로 취급되어 비용 = 256 × Time × flit(+~800 고정):
  4원소 패킷(Time 15) 4,615, 12원소 패킷(Time 5) 2,055. store 절감(2,510 → 458)과 정확히 상쇄. Main이 노는
  구간(ffn up/gate)에서만 쓸모가 있으나 V25가 hop 자체를 없애므로 불필요.
- **contract_lane으로 reduction 축 일부를 미축약 상태로 남기기** (per-block scale을
  contraction 뒤에 적용하려는 시도) — book(Lane Folder): "reduction axes cannot be
  partially preserved". FFN f4의 16-블록 scale은 contraction 전에 곱해야 한다.

- **슬라이스 축에 패딩(예: `m![H / 16 # 256]` = 240 실제 + 16 패딩)** — DMA `to_dm`가
  `internal compiler error: split (inner_size: 64) is not valid on shape([H_16=240])`로 죽는다.
  이후 192(=3×64)도 실패해 결론은 **live 슬라이스 수가 2의 거듭제곱**(아래 제약 표 참조).
  `1 # 8`, `1 # 32` 같은 패딩은 곱해서 256이 되니 괜찮다. (V2 시도에서 확인, 2026-09-09)

- **"rmsnorm 출력(`Slice = m![1 # 256]`)이 이미 256 슬라이스에 복제되어 있으니
  `broadcast_hidden`을 `unsafe reshape`로 대체"** (0909_baseline.md 축 A 관찰 1) — 근거가
  반대다. FFN은 같은 `Slice → Replicated` 변환을 `x.to_dm(tdma)`로 하는데 512 슬라이스 전부에
  쓰느라 54,432 cycle을 쓴다(ops.rs:227). 패딩 슬라이스에 유효 데이터가 있었다면 컴파일러가
  복사를 생략했을 것이다. rmsnorm의 마지막 `Broadcast1{slice1: 8}`는 ring 8 안에서만 모은다
  (attn_out에서 744 cycle). 즉 x는 8개 슬라이스에만 있다. Arena 없이 정확도 검증이 불가능한
  변경이므로 실측 전에는 시도하지 않는다.

## 컴파일러가 강제하는 매핑 제약 (V2 구현 중 확인, 2026-09-09)

한 번씩 다 부딪힌 것들이다. 새 레이아웃을 설계할 때 먼저 대조할 것.

| 제약 | 에러 메시지 | 의미 |
|---|---|---|
| 슬라이스 축 live 개수는 **2의 거듭제곱** | `split (inner_size: 64) is not valid on shape([H_16=240])`; 192는 `cannot find the across_index` (`[H_1280=3, H_20=64]`) | `m![H / 16 # 256]`(240), `m![H / 20 # 256]`·`m![H / 60, H / 20 % 3 # 4]`(192) 모두 불가. 패딩을 안쪽 인자로 써도 정규화되면 같다. 베이스라인 live 수는 전부 256/32/8 |
| DmTensor 슬라이스당 element 크기는 8 B 배수 | `in-slice element extent is 30 B, not a multiple of the SRAM access width 8` | 15 × bf16 = 30 B 불가 |
| VRF는 슬라이스당 8 KB | `VRF data (15360 bytes) exceeds register file capacity (8192 bytes per slice)` | f32 [H] 벡터는 통째로 못 올림 → 1920 단위 타일 |
| transpose 패킷의 실제 행은 최대 4 | `output packet beyond max_in_rows (4) must be 1 # n pure padding` | 행 수는 4의 배수로 시간축 분해 (`H / 4 % k`, `H % 4 # 16`) |
| transpose 입력 시간축은 패딩 없는 축이어야 일관 | `cannot place in_rows in Time (H % 15) consistently with OutTime (H % 15 # 16 / 4)` | 15처럼 4로 안 나뉘는 행 수는 transpose 불가 |
| fetch는 패딩된 행 축을 stride 못 함 | `lower_fetch_unit: failed to stride_exact` | 2-D 타일의 행 축을 `# 16`으로 패딩해 fetch 불가 |
| DMA relayout은 패딩된 패킷을 stride 못 함 | `Condition failed: points ... b % a == 0` | `m![H / 3 % 5, H % 3 # 4]` → Slice `m![H]` DMA 불가 |
| collect 출력 패킷은 정확히 32 B | `Collect output packet must be exactly 32 bytes (one flit)` | bf16 16개 / f32 8개 / f8 32개 |
| collect 시간축은 switch가 만든 축까지 합친 형태 | `Collect time mismatch. Expected: H / 15, got: H / 3` | `fetch::<m![1], big>` → switch → `collect::<합쳐진 축, 32 B>` (rmsnorm gather 패턴) |
| commit_trim 패킷은 8/16/24/32 B | `commit_trim output packet must be one of [8, 16, 24, 32] bytes, got 6` | bf16 4/8/12/16개 |
| `m![H % 15 # 16 / 4]` 같은 패딩 축 분해는 **파싱은 됨** | (위 transpose 에러가 그 표현을 정상 출력) | 엔진별 지원 여부는 따로 확인 |

결론: 행 수를 슬라이스에 나눌 때 **행/슬라이스가 4의 배수**이고 **live 슬라이스 수가 2의 거듭제곱**인
조합을 고른다. H=3840이면 60행 × 64, 120행 × 32 (둘 다 8의 배수라 vector pass도 됨) — 256 슬라이스를
다 쓰려면 다른 축(Qs, L)과 함께 나눠 inter-slice reduce로 합친다(V2가 그 예). Qs=4096이면 16 × 256.
L=15360이면 60 × 256.

## V0에서 측정된 하드웨어 상수 (슬라이스당, 스케줄 기준)

| 항목 | 값 | 근거 |
|---|---|---|
| `fetch_table_lookup` pass | ≈ 6.2 elem/cycle | qkv Q: 16×3840 elem / 9,863 cyc |
| vector scale pass (f32) | ≈ 3.9 elem/cycle (4 lane) | ffn down: 2×15360 / 7,961 cyc |
| contraction | ≈ 15 MAC/cycle | qkv Q: 16×3840 / 4,105 cyc |
| HBM→DM DmaLoad 실효 | ≈ 580 B/cycle (피크 2,048의 28%) | Q weight 15.7MB / 26,475 cyc; util 평균 0.06–0.11 |
| DM→DM 복제 디스크립터 | ≈ 106 cycle/슬라이스 | 54,432 / 512 |
| switch 브로드캐스트 | ring × flits (bf16 [H]: 61,440) | book 공식 = 실측 |

---

# 실험 상세

각 실험은 아래 템플릿으로 추가한다. **최신 실험을 위에 쌓는다.**

<!-- ===================== 템플릿 (복사해서 쓸 것) =====================
## V{n}_{description}

- **상태:** 설계됨 / 구현됨 / makespan 측정 / 채택 / 기각 / 보류
- **분기점:** `V{k}_{...}` (커밋 `abc1234`)
- **가설:** (무엇이 병목이라고 보았고, 무엇을 바꾸면 왜 줄어든다고 예상했는가)
- **변경 파일:** `src/device/...` (RULES §4 허용 범위 확인 완료)
- **공유 코드 영향:** 없음 / `shared/mlp.rs` 수정 → vision/audio 경로 동시 영향

### 측정

| 커널 | makespan (before → after) | RNGD cycles (before → after) | speedup |
|---|---|---|---:|
| `sliding_project_qkv` | | | |
| `sliding_attention_output` | | | |
| `decoder_feedforward` | | | |
| **기하평균** | | | |

- **정확도:** PASS / FAIL(어느 커널, 어느 tolerance를 얼마나 넘겼는지)
- **측정 방식:** RNGD 실측 / makespan only
- **지배 context (변경 후):** MainContext xx% / DmaEngine xx% / SubContext xx%

### 판정

- **이유:**
- **배운 것:**
- **다음 후보:**
======================================================================= -->

## 2026-09-09 15:01 UTC — 첫 공식 리더보드 제출 (moa-submitter, V82)

| 항목 | 값 |
|---|---|
| 팀 | **Goat Chovy #1557** (계정 knowin-kyeong, `moa-submitter login` 토큰 30일) |
| submission | `23cc6523` (V82 `07a187f`의 `src/ops.rs` + `src/device/`) |
| 공식 cycle | qkv **155,419** / attn_out **52,492** / ffn **349,760** |
| 공식 score | **5.0860** (baseline 250,514 / 404,633 / 3,703,473 기준) |
| 리더보드 | 2위. 1위 Participant #905: 123,470 / 53,028 / 315,050 = **5.667** |

- 채점 서버의 수치는 Arena cold 실측과 같은 급이다(qkv 155k는 우리 cold 표본 140~172k 안, attn_out 52.5k, ffn 349.8k).
  즉 **Arena cold = 채점 환경**이고 warm 수치는 A/B 전용이다.
- 1위와의 차이는 qkv −32k(−21%), ffn −35k(−10%)이며 attn_out은 동급. qkv 123k는 우리 weights-only ablation(V148 138k)보다도
  낮다 — 상대는 **weight/x 경로 자체**를 우리보다 싸게 흘린다(예: DMA 인터리브·x 분배 방식). 이것이 남은 캠페인의 표적이다.
- 사용자 지시: Arena는 자유(50+/일), **moa-submitter는 사용자가 시킬 때까지 보류**.

## 2026-09-09 밤 Phase C — 타일 형상 crumbs의 실측 (각 1회, cold; job 15476~15500)

ffn up 타일(pass B 뷰, DMA 아님): V118 12,12,12,12,8,4 → 349,917 · V103 16,12,12,12,8 → 353,408 · V114 16,16,12,12,4 → 351,746
vs V82(12×5) 349.6~352.9k → **노이즈 안, 차이 없음**(pass B 타일 뷰 수는 실물에 무관).

attn_out O-weight DMA 타일(각 1회):

| 브랜치 | 타일 | DMA 타일 수 | attn_out cold | 정적 |
|---|---|---:|---:|---:|
| V82 | 20×3 | 3 | 55.9k (median of 5) | 27,424 |
| V136 | 48/12 | 2 | **53,161** | 28,173 |
| V131 | 40/20 | 2 | **53,442** | 28,428 |
| V132 | 24/20/16 | 3 | 53,725 | 27,243 |
| V128 | 16,16,16,12 | 4 | 53,800 | 27,484 |
| V133 | 28/16/16 | 3 | 54,094 | 28,196 |
| V125 | 60 | 1 | 55,683 | 29,627 |
| V129 | 16,16,12,8,8 | 5 | 56,426 | 27,742 |
| V134 | 32/16/12 | 3 | 56,872 | 28,068 |
| V126 | 12×5 | 5 | 59,519 | 27,654 |
| V130 | 8×7+4 | 8 | 62,506 | 29,966 |
| V127 | 4×15 | 15 | **83,057** | 34,176 |

- **DMA 타일 수가 많을수록 실물이 나쁘다**(15타일 +27k; 정적은 +6.8k). 정적 모델은 타일당 548만 매기지만 실물은 타일당 ~2k(디스크립터 512개/타일).
- 2타일(48/12, 40/20)이 단발로 −2.5k(−4.5%)이나 attn_out 노이즈(±2~4%)와 같은 크기 → V162/V163에서 반복 측정.

## 2026-09-10 — 실물 비용 모델 (런타임 소스 조사 + V145~V155)

`furiosa-opt-std 0.6.0`의 npu 백엔드와 vendored `libdevice_runtime.a`를 읽은 결과("드라이버 리버싱"):

1. **커널은 PE의 ARM 코어에서 도는 AArch64 프로그램**이며 DMA 디스크립터를 런타임에 ARM 코어가 직렬로 만든다(슬롯 alloc → memcpy → `dsb` →
   TUC 명령 큐 push; 아레나 64슬롯, 명령 큐 62, 완료 id 32). 따라서 **실물 DMA 비용은 디스크립터 수에 비례**하고 바이트는 부차적이다.
   정적 모델(548 + bytes/1.21 KB)이 qkv에서 틀린 이유다. 증거: V154(512 → 16 디스크립터 + switch) −40k, V138(바이트 절반) 0, V146(head 레이아웃
   store 2개) +8.6k.
2. cycle 스팬은 PE 쪽 `binary_executor`가 `start_arm_cycle/end_arm_cycle`로 찍고, 호스트의 `Kernel::run`은 스팬 밖이다. 프로그램은 프로세스당
   1회 로드(`OnceCell`), 스크래치는 컴파일러가 프로그램 stack 영역에 고정 배치 — 런타임 할당·제로화 없음 → V151~V153(hop) 무효과와 일치.
3. **cold = 명령 청크(64 KB 단위) DRAM→SRAM 스테이징**("stage_chunk … resident (skip DMA)"). 다중 청크 프로그램은 청크별 cycle 쌍을 내고 하네스는
   그 합집합을 재므로 cold 페널티가 점수에 들어간다. 프로그램 크기(명령 수)가 cold 점수에 영향을 줄 수 있다.
4. switch 제약(컴파일러 메시지): 시간축의 축을 슬라이스축으로 옮기는 분배는 불가(`OutSlice slot must source from InSlice or a fresh broadcast tile,
   not InTime`, V156 시도) → 축약 분할 커널(attn_out `Qs/512`, ffn `H/1920`·`L/1920`)의 x 청크 분배는 switch로 못 한다. 단일 축 인수분해
   (`Dummy256 / k, Dummy256 % k`)도 거부(`snoop bitmap disagrees`); 두 축 표기만 통과.
5. 컴파일러 env 노브(`NO_HINT`, `SCHEDULER_MANUAL_ORDERING_PATH`, `FORCE_WAIT_BEGIN` 등)는 존재하지만 채점 서버가 우리 환경을 쓰지 않으므로 무의미.

**설계 원칙(갱신):** DMA 디스크립터 수(그리고 같은 HBM 영역을 여러 디스크립터가 반복 읽는 패턴)를 먼저 센다. 큰 복제 로드는 "적은 디스크립터 +
on-chip switch"로, 출력 store는 디스크립터가 적은 경로(scatter/적은 슬라이스)로.

## V159/V161 — ffn·attn_out x2 ring broadcast: 기각 (2026-09-09 15:55 UTC, job 15570)

| 커널 | base median (n=3 warm) | 변형 median | Δ |
|---|---:|---:|---:|
| attn_out (v161: x2 512 → 128 디스크립터 + ring 32) | 55,548 (stdev 1.1k) | 56,491 (stdev 0.5k) | +0.9k |
| ffn (v159: up/gate x2 512 → 32, down x2 512 → 128 + ring 32 ×2) | 342,369 (stdev 0.9k) | 344,358 (stdev 0.8k) | +2.0k |

- 정확도 PASS(형태는 옳다). 이득이 없으므로 **"디스크립터 수"가 일반 법칙은 아니다**: qkv의 x2 로드가 특별했던 이유는 512개
  디스크립터가 **같은 7.7 KB 한 영역**을 읽는 것(HBM 채널 직렬화)이고, ffn(영역당 256개)·attn_out(영역당 64개)은 그 문턱 아래다.
- 부수 관찰: 이 잡의 ffn base warm median 342k는 평소 cold 350k보다 낮다(ffn도 cold 페널티 ~8k가 있다).

## V181/V182 — ffn up/gate를 슬라이스당 30행 × H 전체로: ffn −8.9%, PASS (2026-09-09 17:30~18:05 UTC, job 15616/15621)

**가설(V174에서).** ffn의 block-scale 로드 3개는 120 B 세그먼트 60개/슬라이스라 바이트당 연속 로드의 2배(6.6 vs 3.1 cycle/KB)를 쓰고,
weight 로드도 960 B 세그먼트 60개다. 슬라이스가 행 전체(H)를 맡으면 weight 57.6 KB·scale 7.2 KB가 각각 **한 세그먼트**가 된다.
1위의 ffn 315k(우리 349k)는 여기서 나올 가능성이 가장 컸다.

**설계.** up/gate 각각 `L / 30 % 256`(클러스터당 128 슬라이스 × 30행, `UpGateRowsFull`), x는 qkv(V158)처럼 `[Dummy2, H]`를 HBM에서 한 번
읽어 ring-32 broadcast로 전 슬라이스에 복제(`stage_x_hi_lo_hbm_full`). pass A는 `contract_up_gate_full`(fetch `[L % 30, H / 64, Dummy2]` →
LUT → contract → lane Sequential → 부분합 `[L % 30, H / 16]`), pass B는 8/8/8/6행 타일이 transpose 없이 행별 f32 스칼라 패킷
`[30, 1 # 8]`을 commit(4행-transpose 제약 우회). geglu는 같은 슬라이스의 up/gate 패킷에서(`geglu_full`, pairs 불필요).
그 뒤 ring-16 gather로 16 슬라이스의 30행 패킷을 한 슬라이스에 모아 480행을 transpose로 조밀화(`gather_pack_full`) → 기존 `stage_geglu_hi_lo_hbm`·down 경로.

**측정.**

| 잡 | 판 | base (n=4) | v181 (n=4) | Δ median | 정확도 |
|---|---|---:|---:|---:|---|
| 15616 | 최초(gather slice-major) | 348,242 | 320,240 (stdev 1.4k) | −28.0k | **FAIL** — 값이 tol 안(12.6%)이지만 순서가 뒤섞임 |
| 15621 | fix1(gather packet-major) | cold 345,672 / median 348,967 | **cold 317,053 / median 317,947 (stdev 949)** | **−31.0k (−8.9%)** | **PASS** (max\|Δ\| 1.08%, base와 동일) |

**배운 것.** (1) 다중 패킷 switch gather(`Broadcast1 { slice1: 16, slice0: 1 }`, 슬라이스당 30 패킷)의 도착 순서는 **packet-major**
(`[L % 30, L / 30 % 16]`)다 — OutTime을 `[L / 30 % 16, L % 30]`으로 쓰면 값은 맞지만 행 순서가 뒤섞인다. (2) 세그먼트 통합만으로 ffn 349k → 318k:
V174의 예측(−26k scale + weight 세그먼트 이득)과 맞고, 1위의 315k와 3k 차이다. (3) 부수 효과로 H 반쪽 inter-slice reduce와 pairs 복제가 사라졌지만
pass A의 fetch가 두 배(슬라이스당 57.6 KB)라 정적 makespan은 141k로 V165(158k)보다 −16k에 그친다 — 실물 이득이 정적의 2배인 것도 V174 모델과 일치.

**V182.** V165 + 위 ffn을 채점 함수에 넣은 프로덕션 브랜치(`V182_ffn_full_rows_production`, hop 인자 없음). lab2 컴파일: ffn 141,374 · qkv 44,792 · attn_out 28,173.
**V182 실측 (job 15622/15623/15624, 커널당 5회 × 3잡, 2026-09-09 18:15~18:35 UTC).**

| 잡 | qkv (cold → warm 4회) | attn_out | ffn |
|---|---|---|---|
| 15622 | 112,761 → 107,168 / 109,364 / 105,532 / 105,460 | 51,513 → 54,035 / 53,049 / 53,177 / 53,387 | 318,198 → 319,158 / 316,530 / 319,062 / 320,324 |
| 15623 | 106,397 → 106,448 / 105,562 / 107,120 / 107,568 | 54,066 → 53,711 / 52,953 / 53,007 / 54,188 | 322,386 → 318,296 / 319,930 / 318,202 / 321,932 |
| 15624 | 110,234 → 106,794 / 105,500 / 105,940 / 105,684 | 52,928 → 54,003 / 52,479 / 53,035 / 53,320 | 319,094 → 319,830 / 320,496 / 319,258 / 319,180 |

정확도 45/45 PASS(오차 통계는 V165와 동일: qkv max 1.45%, attn_out 0.71%, ffn 1.08%). cold median 110,234 / 52,928 / 319,094 → 기하평균 **5.86**
(V165의 Arena cold-12 median 107,405 / 53,639 / 349,168 → 5.72). 공식 채점 기대치: qkv cold가 105.5k면 5.99, 112.8k면 5.85.
**판정: 채택 후보(SOTA 후보). 리더보드 제출은 사용자 지시가 있을 때만.**


## 2026-09-10 메모리 대역폭 캠페인 (V196~V204, Arena 41슬롯) — 결과 요약

**출발점.** 세 커널의 실측 cycle ≈ 총 HBM 바이트 / ~310 B/cycle이었고, 이전 30슬롯 체인에서 오버헤드 깎기는 소진됐다.
목표는 "메모리 바운드 자체를 푼다"였다.

**먼저 닫은 두 전제.**
1. **바이트는 줄일 수 없다.** 채점 fixture가 가중치를 PRNG로 합성한다(`prng::f4_nibbles` / `f8_banded` / `bf16_uniform`,
   `scripts/generate_references.py`와 체크섬 일치). 균등난수에는 죽은 뉴런도 저랭크도 압축 여지도 없으므로
   sparsity·pruning·low-rank·codebook·엔트로피 코딩이 전부 무효다(대부분 동적 gather라 DSL에서도 불가).
2. **디바이스 팬아웃도 이미 최대다.** `#[device(chip = 1)]`이 `pe = 8`을 포함해 8 PE 전부를 잡고,
   `CLUSTER_SIZES=[1,2]` / `SLICE_SIZES=[64,128,256]`이라 2×256 = 512 슬라이스가 상한이다. 로드 경로 DMA 자원은 `DmaEngine` 하나뿐.

**남은 것은 런 길이와 명령 위치였고, 둘 다 통했다.**

| 실험 | 결과 | 슬롯 |
|---|---|---:|
| V196 DMN 인터리빙 | **기각** — 슬라이스 축을 뒤집으면 4/4 잡에서 +6%. book의 "50% loss" 규칙은 우리 형상에 적용되지 않는다. V8을 실물로 종결 | 4 |
| V197 런 길이 곡선 | **256 B가 최적(618 B/cy)**, 그 미만 정확히 2× 벌점, 그 이상 완만히 악화(2048 B는 +13%) | 3 |
| V198 attn_out 256 B 런 | **채택** — warm −6.5%, cold −9.9%, 4/4 잡 | 4 |
| V199 스테이징 store 병합 | **ffn 채택(−11,078, 6/6)**, qkv 중립, attn_out 기각(+3.0k) | 4 |
| V200 프로덕션 A/B + cold | 기하평균 **6.229** (잡 14개) | 14 |
| V201 geglu store 병합 | **기각(노이즈)** — 같은 수법이 커널 중반에서는 안 통한다 | 4 |
| V202 down 벌크 로드 | **기각** — 1명령은 정적 +4.6k, 2·3분할은 컴파일러 SIGABRT | 0 |
| V203 타일 재스윕 | **기각** — 88/32가 이미 최적 | 4 |
| V204 제출 | 공식 **5.9032** | 4 |

**두 가지 새 규칙(이후 세션이 쓸 것).**
1. **256 B 런으로 옮기려면 분할 축이 2의 거듭제곱이어야 한다.** 청크 오프셋이 256 B 정렬이어야 하기 때문이다.
   attn_out은 Qs=4096이라 되고, qkv·ffn up/gate는 H=3840, ffn down은 청크폭 960이라 안 된다.
2. **DMA 명령 병합의 가치는 명령 수가 아니라 위치다.** 커널 **선두에서 weight 스트림을 막는** store를 없애면 ffn −11k,
   같은 병합을 중반 geglu store에 하면 0이다.

## 2026-09-10 추가 실험 설계 (V172~V180) — "스트림 속도를 무엇이 정하는가"

**출발점.** V165에서 세 커널은 모두 weight 스트림 ≈ **290 B/cycle**(qkv 31.5 MB → 105k, attn_out 15.7 MB → 53k, ffn 99 MB → 349k)이고,
V160b(슬라이스 절반에 2배 바이트)가 정적 예측만큼만 느려진 것은 이 한계가 슬라이스별이 아니라 **칩 전체(DMA 엔진/HBM/NoC)** 수준임을 뜻한다.
V170 ladder에서 tail·geglu·scale 로드 하나를 빼도 ffn이 안 줄었으므로 계산 정밀도(fp8/fp4, 1위 팀 힌트)는 지금 병목이 아니다 —
우리 contraction은 이미 f8×f8 → f32 누산 → bf16 epilogue이고, 활성값을 한 조각으로 줄인 V137/V142는 tolerance에서 떨어졌다.

**가설 세 갈래와 탐침.**
1. *DM 뱅크 간섭*: 책은 "DMA는 DM 뱅크 우선순위가 가장 낮다"고 한다. 스트리밍 중 돌아가는 contraction fetch(qkv Q 30 KB/슬라이스, ffn pass A 57.6 KB/슬라이스)가
   DMA를 늦출 수 있다. 1타일 attn_out(V125, fetch 없는 스트림)의 암묵적 스트림 속도 316 vs 2타일 302 B/cycle이 약한 증거. → V172/V173/V175(중복 contraction의 초과 비용).
   크면 V176(loads-first).
2. *세그먼트/정렬*: ffn의 scale(120 B × 60/슬라이스)과 weight(960 B, 256 B 비정렬)는 attn_out(512 B 정렬)과 다르지만 속도가 같다 — 그러나 1위의 ffn 315k는
   여기서 나올 가능성이 남아 있다. → V174(full-row scale, FAIL-by-design 탐침). 크면 V177(클러스터 축 H-split).
3. *채점 draw 분산*: V165 cold 12회의 점수 범위 5.5~5.9. 프로그램 축소로 분산을 줄일 수 있는지 → V178/V179(작은 pass 융합)와 재제출 정책.

**판정 규칙(사전 등록).** 탐침 초과 비용 = 실측 Δ − (중복 pass의 정적 시간 × 2.05). 초과가 커널의 8% 이상이면 조건부 후보를 구현, 아니면 닫는다.
탐침 잡은 짝 하네스(base ×4, 변형 ×4)로 1잡씩.

## V170_ffn_ablation_ladder — ffn은 DMA 스트림 하한에 있다 (2026-09-09 16:19 UTC, job 15590)

| 변형 | 뺀 것 | 정적 | 실물 warm median (n=3) | Δ vs base 346,246 |
|---|---|---:|---:|---:|
| f6 | post-FF RMSNorm + residual + gate tail | 157,261 (−0.5k) | **346,008** | −0.2k |
| f7 | geglu 체인(gelu·곱·스칼라 broadcast 2개) | 154,998 (−2.8k) | 347,866 | +1.6k |
| f3 | gate block-scale 로드(9.8k 정적, 512 디스크립터 × 60 세그먼트) | 152,342 (−5.4k) | 353,570 | **+7.3k** |

- 계산·tail·소형 DMA를 빼도 ffn은 줄지 않는다(f3는 오히려 느려짐 — 로드가 빠지자 스케줄러가 순서를 바꿔 다른 것이 노출된 듯).
  **ffn의 실물 350k는 weight(88 MB) + scale(11 MB) 스트림 자체**이고, 그 스트림의 실효 대역폭(~300 B/cycle, HBM 피크의 ~40%)이 한계다.
- 바이트는 고정(f4 weight, f8 scale)이고 256 B 정렬은 H=3840·f4 행 1920 B 구조상 어떤 짝수 분할로도 못 맞춘다(V44 분석 + 이번 계산).
  qkv처럼 "같은 영역 반복 읽기" 병리도 없다(V159). **ffn은 이 구조에서 하한 근처**로 판단하고 이 세션에서는 닫는다.

## V165_qkv_broadcast_attnout_two_tiles — 프로덕션 후보 (2026-09-09 16:05~16:09 UTC, job 15578~15580)

- **분기점:** `V158` + attn_out O-weight 타일 20×3 → 48/12 (`V136`의 projection.rs diff). 커밋 `0704eda`.
- **실측 (3잡, 모두 3/3 PASS):**

| job | qkv cold | qkv warm | attn_out cold | attn_out warm | ffn |
|---|---:|---|---:|---:|---|
| 15578 | 109,572 | 104,670 · 105,568 | **54,009** | 51,824 | 348,688 · 347,166 |
| 15579 | 107,318 | 109,074 · 103,630 | **51,513** | 55,049 | 349,470 · 347,838 |
| 15580 | 109,408 | 104,582 · 104,798 | **54,919** | 53,994 | 352,346 · 344,516 |
| 15587 | 104,740 | 104,462 · 108,888 | **54,430** | 53,827 | 350,326 · 350,328 |
| 15588 | 110,796 | 105,654 · 109,144 | **53,904** | 56,653 | 351,370 · 349,326 |
| 15589 | 110,445 | 105,758 · 109,205 | **53,661** | 55,325 | 351,054 · 352,238 |

| 15593~98 (r7~r12, cold만) | 107,022 · 107,792 · 115,314 · 104,574 · 103,910 · 106,128 | | 53,845 · 53,065 · 53,633 · 53,959 · 51,395 · 51,713 | | 345,962 · 347,472 · 349,090 · 349,246 · 345,438 · 348,592 |

  **cold 12회 median: qkv 107,405 · attn_out 53,639 · ffn 349,168** → 공식 baseline 기준 (2.33 × 7.54 × 10.61)^(1/3) ≈ **5.72**
  (범위: qkv 103.9~115.3k, attn 51.4~54.9k, ffn 345.4~352.3k → 점수 5.5~5.9). 1위 5.667보다 중앙값이 위. 12/12 PASS.

- **V158 대비:** attn_out cold median 56,870 → **54,009** (−5%); qkv·ffn 불변. 공식 baseline 기준 추정 기하평균
  (2.29 × 7.49 × 10.60)^(1/3) ≈ **5.67**, 최고 draw 기준 ≈5.71 (1위 5.667).
- **판정:** 채택 후보(실측 SOTA). 남은 후보(V166 ffn 3타일, V167/V168 store 전 gather)가 이기면 그 위에 얹어 최종 프로덕션으로.

## V158_qkv_x_ring32_broadcast — 프로덕션 (2026-09-09 15:48 UTC, job 15566)

- **분기점:** `V82_ffn_up_tiles_12x5` (`07a187f`) + `src/ops.rs`의 qkv x 복제 17줄(V157 (a)형). 커밋 `ce9b659`. tests/만 cold/warm 7회 하네스.
- **실측 (3잡, 모두 3/3 PASS; 각 잡: qkv·attn·ffn → qkv·attn·ffn → qkv):**

| job | qkv cold | qkv warm | attn_out | ffn |
|---|---:|---|---|---|
| 15566 | **107,552** | 108,556 · 110,686 | 58,474 · 57,830 | 353,628 · 351,700 |
| 15568 | **112,640** | 117,846 · 106,408 | 55,252 · 56,290 | 350,098 · 351,152 |
| 15569 | **110,724** | 103,546 · 108,654 | 56,870 · 56,884 | 352,674 · 351,894 |

  qkv cold median **110,724** (V82 cold median 149k), attn_out·ffn 불변.
- **공식 baseline 기준 예상:** qkv 250,514/107,552 = 2.33×, attn_out(공식 52.5k) 7.71×, ffn(350k) 10.58× → 기하평균 **≈5.7** (1위 5.667).
- **판정:** 채택 후보(실측 SOTA). 리더보드 제출은 사용자 지시 후.

## V157_qkv_x_ring_broadcast_both_clusters — qkv −28%, 3/3 PASS (2026-09-09 15:38 UTC, job 15563)

- **분기점:** `V154` (기전) + V155 진단(cluster 1 미충족)
- **원인과 수정:** `BothClusters = m![Dummy2]`(더미 클러스터 축) 위의 switch pass는 cluster 0만 채운다. 복사본 로드와 switch를 실제 축
  `m![Qs / 2048]` 위에서 수행하고 끝에 `unsafe reshape`로 `BothClusters, Replicated`로 되돌리면 두 클러스터 모두 채워진다.

| 변형 | 형태 | cold | warm median (n=3) | stdev |
|---|---|---:|---:|---:|
| base (V82 qkv) | 512 디스크립터 복제 로드 | 163,293 | 151,163 | 2,479 |
| **v157a** | 실축 직접 로드, 8부/클러스터, 32 B 패킷, ring 32 | 117,689 | **108,374** | 1,317 |
| v157b | 더미축 로드 → reshape → switch | 105,642 | 111,920 | 2,222 |
| v157e | 실축 16부/클러스터, ring 16 | 107,985 | 108,641 | 1,435 |

- **두 번째 잡(15564):** base cold 149,240 / median 149,991 (stdev 2.3k); v157a cold 104,162 / **median 105,874** (stdev 668); v157b 107,432; v157e 108,435.
- **정확도:** 두 잡 모두 세 변형 매 실행 PASS(q/k/v, 잡 exit 0).
- **판정:** **채택 후보.** qkv 151k → 108k(−28%)는 attn_out 52.5k·ffn 350k와 합쳐 기하평균 5.09 → **≈5.6** (1위 5.667과 동급).
  프로덕션 브랜치 V158에 (a)형을 적용해 cold 3회를 잰 뒤 사용자 지시로 리더보드에 낸다.

## V154_qkv_x_ring32_broadcast — qkv −28% (정확도만 남음)

- **상태:** 측정 완료, 정확도 FAIL → V155에서 표기 변형으로 추적 (2026-09-09 15:16 UTC, job 15555~15557)
- **분기점:** `V82` (하네스는 V151 브랜치 것; 변형 device fn `sliding_project_qkv_v154`)
- **가설:** x 복제 로드(512 디스크립터, 정적 18,400 @ util 0.157)의 실물 비용은 바이트가 아니라 디스크립터/같은 주소 반복이다(V138).
  HBM에서 클러스터당 8부(16 디스크립터)만 읽고 ring-32 `CustomBroadcast`(`m![Dummy8, 1 # 32]` → `m![Dummy8, Dummy256 / 8]`)로 채우면
  정적 18.4k → 8.4k(switch 7,943 + 로드), 실물은 더 클 것.
- **정적:** qkv makespan 61,381 → **44,8xx** (DMA busy 39,958; switch pass 7,943 Main).

### 측정 (세 잡, 각 잡에서 base·v154 번갈아 2회 + 뒤에 1회씩)

| job | base (cold, warm, 뒤) | v154 (cold, warm, 뒤) |
|---|---|---|
| 15555 | 152,427 · 144,741 · 151,148 | **104,968 · 111,154 · 107,652** |
| 15556 | (r2) | (r2) |
| 15557 | (r3) | (r3) |

- **정확도:** v154 **FAIL** — q/k/v 모두 원소의 54~57%만 tol 안, max|Δ| 5.4~7.3(값 크기 ~4). 절반의 슬라이스가 잘못된 x를 받은 양상
  (garbage 또는 한 조각만). base는 PASS.
- **의미:** **qkv에서 −40k(−28%)가 실물로 확인됐다.** 정적 −16.5k의 2.4배 — x 복제 로드의 실물 비용이 정적 18.4k보다 훨씬 컸다는 뜻
  (≈ 45k+). 정확도만 잡으면 qkv ≈ 105~110k(cold)로 1위(123k)를 넘는다. 리더보드 제출 후보 1순위.
- **다음:** V155 — 같은 기전의 표기 4형 + 실패 원소 히스토그램으로 어느 슬라이스가 빠졌는지 확인.

## V151_hop_via_outputs_probe (V151·V152·V153, 한 브랜치에 변형 3개)

- **상태:** 구현됨 (`aad0683`, 2026-09-09 밤)
- **분기점:** `V82_ffn_up_tiles_12x5`
- **가설(하나):** 컴파일러가 배치한 HBM 스크래치(`HbmTensor::new()`)로의 store는 실물에서 같은 바이트의 scatter/기존 버퍼 store보다
  3~4k 비싸다(V146 +8.6k, V148 vs V145 +5.8k; 정적 모델은 −0.5k). 호스트가 이미 접촉한 출력 버퍼를 hop으로 쓰면 그 비용이 사라진다.
- **세 지점 (같은 기전, 커널별 변형 device fn):**
  - `sliding_project_qkv_v151`: cos/sin hop → `q_out`을 `[Gf, Ds]`로 reshape한 행 0·1 (마지막 q store가 덮어씀). 스크래치 −2.
  - `sliding_attention_output_v152`: 투영 출력 [H] hop → `residual_hbm`(먼저 reducing 레이아웃으로 로드해 둔 뒤 덮어쓰고, 최종 store가 다시 덮음). 스크래치 −1.
  - `decoder_feedforward_v153`: down 출력 [H] hop → `residual_hbm`(같은 방식). 스크래치 −1.
- **측정 방식:** tests/의 TESTS를 [base, base, v151, v151, attn base ×2, v152 ×2, ffn base ×2, v153 ×2, qkv base, v151]로 두어
  한 Arena 잡에서 변형마다 cold(그 프로그램의 첫 실행)와 warm을 읽는다. 채점은 cold이므로 판정은 cold 차이(3잡 반복)와 warm 차이를 함께 본다.
- **변경 파일:** `src/ops.rs`(변형 fn 3개 추가), `src/device/sliding/rope.rs`(`apply_rope_heads_hop`, `rope_heads_from_tables` 분리),
  `src/device/sliding/projection.rs`(`project_output_into`), `src/device/shared/mlp.rs`(`feedforward_hop`), `tests/test_kernels.rs`.
  기존 세 커널의 코드 경로는 불변(래퍼로 분리).
- **정확도:** 데이터 이동만 변경. WAR(residual 로드 → hop store)은 컴파일러가 텐서 단위로 강제.
- **기대:** qkv −6~8k, attn_out −3~4k, ffn −3~4k (기하평균 +3%). 통하면 남은 스크래치(qkv x2, attn_out x2, ffn 6개)의 pre-touch로 확장.

### 측정 (Arena job 15551, 2026-09-09 15:12 UTC; 한 프로세스에서 순서대로)

| 실행 | qkv base | qkv v151 | attn base | attn v152 | ffn base | ffn v153 |
|---|---:|---:|---:|---:|---:|---:|
| 1 (cold) | 158,004 | 148,927 | 55,376 | 56,657 **FAIL** | 354,507 | 355,883 |
| 2 (warm) | 142,487 | 151,418 | 56,398 | 56,799 **FAIL** | 352,485 | 354,792 |
| 3 (뒤) | 150,990 | 154,358 | | | | |

정적: v151 61,472 / v152 27,872 / v153 157,763 (base 61,381 / 27,424 / 157,763).

### 판정: **기각 (세 지점 모두)**

- qkv v151은 base와 구별되지 않고(148.9~154.4k vs 142.5~158.0k), ffn v153도 +1~2k로 노이즈 안. **"스크래치 첫 접촉이 3~4k"라는
  가설은 죽었다.** V146의 +8.6k는 스크래치가 아니라 **head 레이아웃(4 live 슬라이스/클러스터, `1 # 64` 패딩)에서 `to_hbm_view`로
  쓰는 경로 자체**가 scatter보다 비싸다는 뜻으로 읽어야 한다 — 그리고 q의 최종 store가 정확히 그 경로다(후보: q도 scatter로).
- attn_out v152는 residual을 먼저 reducing 레이아웃으로 로드한 뒤 같은 버퍼에 투영 결과를 쓰는 WAR 순서를 컴파일러가 지키지 않았거나
  로드가 hop store 뒤로 밀렸다 → 출력이 완전히 틀림(max|Δ| 2.8e5). 같은 텐서를 커널 안에서 읽고 덮어쓰는 hop은 안전하지 않다.
- 부수 관찰: "warm"이 항상 조용하지는 않다 — 세 번째 base 실행(다른 커널 10회 뒤)이 151k로 cold급. 프로그램 캐시 축출로 보인다.
  A/B는 **인접한 두 실행**끼리 비교한다.

## 2026-09-09 밤 3차 체인 — qkv 실물 ablation 사다리 (V145–V150)

- **상태:** 설계됨 → 구현됨 (브랜치 push 완료, 코드는 V82 위 한 곳씩 변경)
- **분기점:** `V82_ffn_up_tiles_12x5` (`07a187f`)
- **왜 ablation인가:** qkv는 정적 모델이 맞지 않는 유일한 커널이다 — 실측/makespan 비가 2.3~2.7(다른 둘은 2.05~2.2)이고
  같은 바이너리가 ±8% 흔들린다. V138(x 바이트 절반, makespan −16%)이 실측에서 무변화였고 V52(사본)는 +10k였으므로,
  "어느 구성요소가 실물에서 얼마인가"를 **정적 모델이 아니라 하드웨어에 직접 물어야** 한다. 각 브랜치는 qkv의 한 구성요소를
  같은 바이트의 더 단순한 DMA로 바꾸거나 통째로 뺀다. 정확도는 설계상 FAIL이고 cycle만 읽는다(V138이 FAIL에서도 cycle을
  보고함을 확인했다). 제출 제한이 없어졌으므로 **각 3회 반복**하고 V82 대조군을 사이사이에 넣어 세션 중 드리프트를 본다.
- **qkv만 가진 것(다른 두 커널에는 없음):** 인덱스 의존 DMA 4개(`dma_gather_scaled` ×2, `dma_scatter` ×2), cos/sin의 HBM
  hop(store 2 + load 2), 512 슬라이스가 같은 7.5 KB를 읽는 복제 로드, 그리고 **채점 순서상 첫 실행**(cold). 사다리는 이 넷을 하나씩 뗀다.
- **판정 규칙 (사전 등록):** W = median(V148), B = median(V82 대조군), C = V150의 두 번째 qkv(warm).
  (a) C ≈ 2.1 × makespan ≈ 125k이면 초과분의 상당 부분은 first-launch 비용이고 정적 모델은 복권된다 — V16~V37식 정적 최적화를
  V82 위에 다시 쌓는 쪽으로 간다(단 채점은 cold라 이득은 125k 기준으로 잰다).
  (b) B − W ≥ 25k이면 tail/gather/scatter가 표적이고, V145/146/147/149 중 어느 것이 그 몫을 설명하는지에 따라 다음 설계를 고른다.
  (c) B − W < 10k이면 qkv는 실물에서 weight 스트림에 묶인 것이고, 남은 일은 대역폭 패턴(V8식 인터리브의 실물 A/B) 뿐이다.
- **변경 파일:** `src/ops.rs` (V145/146/148/149), `src/device/sliding/rope.rs` + `src/ops.rs` (V147), `tests/test_kernels.rs`만 (V150).
- **공유 코드 영향:** 없음 (V147은 `apply_rope_heads` 시그니처에 stub 인자 2개 추가 — 호출처는 sliding qkv뿐).

| 브랜치 | 커밋 | 뺀 것 / 바꾼 것 | 남는 것 |
|---|---|---|---|
| `V145_qkv_ablate_no_tail` | `24a62a2` | head RMSNorm ×3, RoPE 전부 | 입력 norm, x 스테이징, weight 3개 + contract, ring-64 gather, q store, k/v scatter |
| `V146_qkv_ablate_no_scatter` | `a69f041` | `dma_scatter` ×2 → `to_hbm_view` 스크래치 store ×2 | 나머지 전부 |
| `V147_qkv_ablate_no_rope_gather` | `0877fec` | `dma_gather_scaled` ×2 + HBM hop → `[Ds]` 평범한 로드 ×2 (q/k norm weight를 대역) | RoPE 산술은 그대로 |
| `V148_qkv_ablate_weights_only` | `f3d9e9f` | V145 + V146 | 투영 + store만 (바이트 하한) |
| `V149_qkv_ablate_no_rope` | `b971cd8` | `apply_rope_heads` 호출 제거 | head norm 유지 |
| `V150_test_order_warm_repeat` | `42a9dec` | tests/의 TESTS를 qkv·attn·ffn ×2 + qkv로 | 커널 코드 = V82 |

### 측정

(pod `auto/BOARD.md`에서 옮겨 적는다. 각 3회, 중앙값으로 판정.)

(Arena job 15455~15500, 2026-09-09 13:00~14:00 UTC, 모두 cold = 프로세스의 첫 커널. 단위 cycle, 중앙값 굵게.)

| 브랜치 | qkv 3회 | qkv 중앙값 | Δ vs V82 | attn_out 3회 | ffn 3회 | makespan qkv |
|---|---|---:|---:|---|---|---:|
| V82 대조군 (4회) | 150,492 · 140,555 · 147,672 · 156,864 | **149,082** | — | 55,916 · 52,318 · 56,206 · 55,822 | 351,138 · 352,482 · 349,646 · 352,936 | 61,381 |
| V148 weights-only | 135,709 · 143,777 · 138,471 | **138,471** | −10.6k | 56,216 · 56,307 · 52,534 | 349,780 · 348,188 · 349,080 | 53,583 |
| V145 no-tail | 132,646 · 139,224 · 132,350 | **132,646** | **−16.4k** | 54,409 · 55,428 · 56,540 | 346,588 · 365,610 · 346,594 | 54,067 |
| V146 no-scatter | 159,780 · 157,574 · 157,677 | **157,677** | **+8.6k** | 57,772 · 57,862 · 53,878 | 349,480 · 351,422 · 347,102 | 60,859 |
| V147 no-rope-gather | 147,044 · 149,296 · 145,743 | **147,044** | −2.0k | 55,788 · 57,506 · 54,960 | 345,320 · 349,248 · 346,728 | 58,583 |
| V149 no-rope | 142,920 · 137,472 · 141,431 | **141,431** | −7.7k | 56,532 · 58,532 · 54,292 | 349,168 · 351,816 · 347,956 | 56,258 |

V150 (같은 V82 코드, 한 프로세스에서 qkv·attn·ffn → qkv·attn·ffn → qkv):

| job | qkv cold | qkv warm | qkv warm | attn cold → warm | ffn cold → warm |
|---|---:|---:|---:|---|---|
| 15474 | 154,097 | 145,593 | 147,681 | 60,994 → 57,260 | 352,174 → 351,350 |
| 15492 | 171,776 | 145,775 | 146,687 | 58,350 → 57,044 | 352,124 → 350,930 |

### 판정 (2026-09-09 밤)

1. **qkv의 ±8% 노이즈는 전부 first-launch 비용이다.** warm qkv는 4회 모두 145.6k~147.7k(±0.7%)이고 cold는 140k~172k.
   attn_out도 cold +1~4k, ffn은 차이 없음. 채점은 cold로 이뤄지므로 점수의 분산은 못 없애지만, **A/B 판정은 warm 반복으로 하면
   1% 해상도가 나온다** → 앞으로 측정 바이너리는 V150식(같은 프로세스에서 2회 이상)으로 만든다.
2. **qkv 실측의 89%는 weight 스트림 + x 스테이징이다** (V148 138k, V145 133k vs 149k). 후처리 tail 전체를 없애도 −16k(11%)이고,
   RoPE 단계가 그중 −7.7k, gather+hop은 −2k, head norm ≈ −8k. 규칙 (c)에 가깝다: 남은 레버리지는 tail이 아니라
   **weight/x 경로의 실물 비용**이다.
3. **예상 밖의 발견: 스크래치(`HbmTensor::new()`)로의 store가 같은 바이트의 scatter보다 3~4k씩 비싸다.** V146(scatter 2개 →
   스크래치 store 2개)이 +8.6k(3회 모두 157~160k, 분산 작음), V148도 V145 대비 +5.8k로 같은 방향. 정적 모델은 반대로 −0.5k를
   예측한다. 스크래치는 컴파일러가 배치하므로 런타임 할당은 아니고("compiled program places its own buffers"), 남는 설명은
   **컴파일러 배치 영역의 첫 접촉(page walk) 또는 배치 정렬**이다. 세 커널의 스크래치 store 수: qkv 4(x_hi·x_lo·cos·sin),
   attn_out 3(x_hi·x_lo·gathered), ffn 8(x_hi·x_lo·erf·out·geglu hi·lo·inv_s·down). 각 3~4k라면 qkv 12~16k, attn_out 9~12k,
   ffn 24~32k가 걸려 있다 — **세 커널 모두에 적용되는 가장 큰 단일 후보**다. 다음 실험(V151~)은 스크래치 대신 이미 접촉된
   출력 버퍼(qkv `q_out`, attn_out/ffn `residual_hbm`)를 hop으로 쓴다.
4. 사용자 지시(2026-09-09 밤): 공식 리더보드 제출은 **하루 5회로 간주**하고 확실한 개선만 올린다. Arena 잡도 남발하지 않는다 —
   한 잡에 여러 변형을 warm 반복으로 담는 측정 바이너리(tests/ 변경은 채점에 무시됨)로 잡당 정보량을 최대화한다.

## 2026-09-09 야간 자동 체인 슬롯 (V40–V48) — 공통 근거

V38 스케줄(cargo-furiosa-opt 0.6.0) 재분석. 세 커널 모두 **DMA 큐가 makespan**이다(DmaEngine busy: qkv 41,910/45,744,
attn_out 22,559/27,424, ffn 149,922/158,182). makespan ≈ 1,003(첫 DMA 지연) + 직렬 DMA 큐 + 발행 공백 + 마지막 DMA 뒤 tail.
book(Memory Performance): DMN 128 B/cycle × 8/클러스터, HBM 채널 64 B/cycle × 32 = 2 KB/cycle, 256 B 정렬 위반 시 읽기 2×,
TDMA I/O 256 B. 모델의 weight 스트림 1.21 KB/cycle는 DM 피크의 60%, HBM 피크(1.5 TB/s ≈ 1 KB/cycle @1.5 GHz)의 ~100% →
**weight 바이트는 이미 HBM 한계**. 남은 레버는 DMA 개수(548/개), 세그먼트(0.2/개), head, tail뿐이다.

| 커널 | 큐 구성 (V38) | 구조적 하한 근처 항목 | 줄일 수 있는 것 |
|---|---|---|---|
| qkv 41.9k | weight 28.3k · x2 로드 5.2k · x2 store 1.1k · gather 1.9k · 소형 로드 2.8k · store/scatter 2.3k(tail과 겹침) · tail 4.3k | weight, 소형 로드(모델 상수 5개, 각각 별도 텐서) | x2 바이트(V26), tail contract(V43), gather/scatter 인덱스(V48) |
| attn_out 22.6k | 타일 14.6k · x 스테이징 2.6k · 출력 store 2.5k · norm 로드 1.6k · tail 5k | 타일 바이트 | x2 사본(V41), 타일 형상(V42), post-norm 레이아웃(V45) |
| ffn 149.9k | weight 92k(세그먼트 ~15k 포함) · scale 29.3k · `?` 8.8k(LUT 7 + switch 4~5) · x2/hop/store ~12k · tail 8.8k | weight 세그먼트(행/슬라이스 60은 2의 거듭제곱·4행 transpose 제약으로 고정) | 타일 수(V40), x2 사본(V41), scale 세그먼트(V44), switch config(V46), tail pass(V47), post-norm(V45) |

죽은 길 추가: (1) **weight 로드를 큐 선두로 끌어올리는 hoist** — 스케줄러는 준비된 DMA를 하류 경로 길이로 줄 세우므로
x 체인의 로드(gamma·residual)가 항상 앞선다. 다른 레이아웃의 더미 소비자는 x 체인에 의존성을 만들 수 없다(VRF 피연산자는
같은 Cluster/Slice 타입이어야 함). head 1.3k/1.5k/1.9k는 구조적. (2) **LUT 테이블 상주** — `fetch_table_lookup`의 테이블은
타입으로 선택되고 API에 테이블 인자가 없어 pass마다 838 재로드를 피할 수 없다. (3) `ctx.pdma`(PCIe DMA)는 두 번째 DMA 엔진이
아니다(host I/O 전용).

문헌 메모(arXiv, 2026-09-09 검색): hi/lo 분해(V29–V35)는 Ozaki scheme의 2항 슬라이스(Ootomo+ 2306.11975, Uchino+ 2409.13313,
Mukunoki 2508.00441 FP8 Ozaki, 2603.10634 Ozaki-II FP8)와 같은 원리 — 필요 시 3항으로 확장 가능하나 현재 8비트 bf16 유효자리는
2항으로 exact. "Multi-Scale Dequant"(2605.13915)는 dequant 병목을 활성값 분해로 피하는 같은 아이디어(V29의 독립 검증).
TileFuse(2606.11357)는 NPU에서 block-scale을 contraction 뒤로 미루는 융합 커널. 스케줄링 문헌(TileLoom 2512.22168, 2602.20204)은
일반론이라 직접 적용 항목 없음.

**야간 체인:** `auto_results` 브랜치의 `auto/queue.txt` 순서대로 pod 드라이버가 makespan → 전체 크레이트 빌드 → (로그인 시)
Arena 제출을 수행하고 `auto/BOARD.md`에 기록한다(RULES §11). 아래 슬롯의 cycle 칸은 그 보드에서 옮겨 적는다.

## V40_ffn_down_pass_a_big_tiles

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V39_x2_single_store` (코드 = V38)
- **가설:** ffn down 타일 5개(16/16/16/8/4)는 타일마다 DMA 고정 548 + LUT 테이블 `?` 838 ≈ 1.4k를 큐에 낸다. V37처럼 pass A만
  큰 타일(28/28/4)로 돌리고 pass B(scale VRF 16행 한도)는 타일별 60행 partials 버퍼의 16/12행 tile view에서 돌리면 타일 2개가
  줄어 −2 × 1.4k ≈ −2.8k. pass A 316/행 < 로드 481/행 + 548이라 28행 타일도 Main이 따라간다. 마지막 4행 타일은 그대로 두어
  tail(pass A 1,463 + pass B 462)은 불변. partials 버퍼를 타일마다 따로 두는 이유: V37a에서 공유 버퍼가 pass B 전부를 마지막
  pass A 뒤로 밀었다(버퍼 단위 의존성).
- **변경 파일:** `src/device/shared/mlp.rs` (`down_tile_fns` → `down_contract_fns!`/`down_reduce_fns!` 분리 매크로)
- **정확도:** 연산 동일(같은 partial, 같은 pass B).
- **예상:** ffn −2.8k (a), −1.4k (b).

### 측정 (변형별, ffn; lab 컴파일, cargo-furiosa-opt 0.6.0)

| 변형 | 커밋 | ffn | 비고 |
|---|---|---:|---|
| V38 | | 158,182 | 16/16/16/8/4 |
| (a) 28/28/4, pass B main | `7c4abaa` | 159,050 | DMA busy 149.9k → 147.1k(−2.8k 예상대로)인데 **down scale 로드(9,774)가 tile1 뒤로 밀려** pass B 5개가 tail에 몰림(144.2k–151.0k) |
| (c) (a) + pass B를 `ctx.sub`로 | `07b2558` | 컴파일 실패 | `visa: while lowering TensorUnit` — pass B(MulF + inter-slice reduce + transpose + commit_view)는 main 전용 |
| (b) 16/24/16/4, pass B main | `d3fbddb` | 159,781 | DMA 끝 147.8k(−1.6k)인데 tile2(16행)가 143.0k에 도착 → pass A 5.1k → 148.1k, 4행 pass A가 그 뒤(149.6k) → store 151.8k |

- **측정 방식:** makespan only.

### 판정: **기각** (타일 수는 이미 최소)

- **이유/배운 것:** down 단계는 x2 로드(121.8k) 뒤에야 pass A를 시작할 수 있고, 그 뒤 DMA(타일 481/행 + 548 + `?` 838)와 Main
  (pass A 316/행 + 200)이 나란히 흐른다. 타일 k의 pass A가 타일 k+1의 도착 전에 끝나려면 `316·r_k ≤ 481·r_{k+1} + 1,386`, 즉
  **다음 타일이 앞 타일의 ≥0.66배**여야 한다(V37이 up/gate에서 본 조건과 같음). 마지막 타일 4행에서 거꾸로 올라가면 4·8·12·16·20 =
  60 → **5타일이 최소**이고 V38의 16/16/16/8/4가 이미 그 안에 있다. 타일을 줄이려면 x2 로드를 앞당겨(scale 로드 9.8k보다 먼저)
  pass A 체인을 일찍 시작시켜야 하는데, DMA 순서는 스케줄러가 소비자 우선순위로 정하므로 소스에서 제어 불가(V17).
- **다음:** 큰 타일은 down 입력이 준비된 뒤가 아니라 **앞**에 두는 구조(예: down 입력을 up/gate와 인터리브)가 필요 — Stage 2 과제.

## V41_x2_hbm_copies

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V39_x2_single_store`
- **가설:** 복제 로드는 같은 HBM 구간을 여러 디스크립터가 읽을 때 모델 예측보다 느리다(V15: 8부 사본으로 18.4k → 5.4k;
  V36: qkv x2 예측 3.8k → 실제 5.2k). ffn up/gate x2 로드 3,411(예측 2.2k; 3.84 KB를 256 슬라이스가 반복), down x2 로드
  2,934, attn_out x2 로드 1,318(예측 980)에 같은 기법: HBM 스크래치를 `[Dummy8, …]`로 8부(변형 16부) 쓰고(복제 store는
  디스크립터가 늘지 않음: V31 553) 로드 슬라이스 매핑에 `Dummy8`을 넣어 사본을 분산, `unsafe reshape`로 기존 타입 복귀.
- **변경 파일:** `src/device/shared/mlp.rs`, `src/device/sliding/projection.rs`
- **정확도:** 데이터 이동만 변경.
- **예상:** ffn −1k ~ −1.5k, attn_out −0.3k.

### 측정: ffn 158,182 → **157,282** (−900), attn_out 27,424 → **27,272** (−152), 기하평균 5.801 (커밋 `f96f159`)

- 첫 컴파일 통과. attn_out x2 로드 1,318 → 1,166; ffn DMA busy 149,922 → 149,352. 8부 사본은 store 디스크립터를 늘리지
  않았다(553 그대로). 이득이 예측(−1.5k)보다 작은 것은 복제 로드의 초과분 중 사본으로 없어지는 몫이 절반 정도라는 뜻 —
  16부 변형은 체인 뒤 후보.
- **측정 방식:** makespan only (야간 체인이 전체 빌드·Arena 대기 상태로 재기록).

### 판정: makespan 측정 (실측 대기)

## V42_attnout_tile_shapes

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V39_x2_single_store`
- **가설:** attn_out tail = 마지막 타일 contract 956 → store 2,510 → …. 타일을 28/28/4로 바꾸면 마지막 contract가 ~250이 되어
  −0.7k; 타일 수(DMA 고정비)는 동일. 변형 24/24/12(−0.4k), 32/24/4.
- **변경 파일:** `src/device/sliding/projection.rs` (`project_output` 타일을 행 수 매크로 `output_tile_fns!`로)
- **정확도:** 연산 동일.
- **예상:** attn_out −0.5k ~ −0.7k.

### 측정: attn_out 27,424 → 28,466 (+1,042) (커밋 `e587ed6`)

- 스케줄러가 타일 로드를 tile0(28) → **tile2(4)** → x2 → tile1(28) 순으로 발행했다. 마지막 로드가 28행이라 tail의 contract가
  956 → 1,212로 늘고, 4행 타일의 contract(444)까지 그 뒤에 붙었다(store 19,822 → 20,864).

### 판정: **기각** (스케줄러는 같은 우선순위의 DMA 중 짧은 것을 먼저 발행한다)

- **배운 것:** "마지막 타일을 작게"는 소스 순서로 만들 수 없다. 우선순위가 같은 타일 로드는 짧은 것부터 나가므로 마지막에는
  항상 큰 타일이 남는다 → 균등 타일(V38의 20/20/20)이 이 축의 최적. 마지막 타일을 작게 하려면 그 로드가 다른 로드보다 *늦게
  준비*되어야 하는데 HBM→DM 로드에는 데이터 의존성이 없다.

## V43_qkv_v_weight_tail_split

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V39_x2_single_store`
- **가설:** qkv tail 4.3k = V contract 1,225 → gather 328 → norm 345 → sqrt → norm 345 → scatter 929 → core 600. V weight를
  슬라이스당 3행 + 1행 두 tile(`m![Ps % 4 = 3 # 4]`, `= 1`)로 로드하면 마지막 contract가 ~1/4(−0.9k), DMA 고정비 +548 →
  순 −0.3k. V36의 2분할(−612 + 548 ≈ 0) 재검토: 분할을 비대칭으로.
- **변경 파일:** `src/device/sliding/projection.rs`
- **정확도:** 연산 동일(행 분할).
- **예상:** qkv −0.3k.

### 판정: **기각 (설계 단계)**

- 슬라이스당 4행을 3+1(또는 2+2)로 나누면 각 tile의 출력 `commit_trim::<m![Ps % 4 = 3]>`가 6 B(2 B)라 **8 B 최소**(bf16 4개)에
  걸린다. f32로 내보내면 8 B 이상이 되지만 이후 head gather·norm이 bf16 입력을 전제해 재작성이 필요하고, 추가 cast pass(~350)가
  tail에 들어가 이득(−0.9k + 548)을 상쇄한다. 브랜치는 코드 없이 예약만 남김.

## V44_ffn_scale_segments (분석 슬롯)

- **상태:** 설계됨 — 보류 (2026-09-09)
- **분기점:** `V39_x2_single_store`
- **분석:** up/gate/down block-scale 로드 9,774 × 3 = ffn DMA 큐의 18.5%. 바이트는 행렬당 3.7 MB(3.0k)뿐이고 나머지는
  **120 B 세그먼트 30,720개**(0.2/세그먼트 ≈ 6.1k). 슬라이스가 full row(240 B × 60행 = 14.4 KB 연속, 1 세그먼트)를 읽으면
  548 + 7.4 MB/1.21 KB ≈ 6.6k(−3.2k/행렬)인데, pass B가 자기 절반만 읽으려면 **슬라이스별 fetch offset**이 필요하고 fetch view는
  전 슬라이스 공통이라 불가. `Dummy2` 복제 pair도 마찬가지(VRF 피연산자 인덱스가 슬라이스 좌표를 못 본다).
- **남은 경로:** (a) pair가 열 대신 **행(30/30)** 을 나누고 partials(30행 × 120 f32 = 14.4 KB)를 ring-2 `InterTranspose`로
  교환(≈ 900 + config 719) → scale은 슬라이스당 30 full row = 1 세그먼트. 순이득 ≈ −1.5k/행렬. 단, 30행은 4의 배수가 아니라
  pass B transpose(`= rows / 4`)에 걸림 → 32/28 비대칭은 한 매핑으로 표현 불가. 사실상 막힘. (b) 슬라이스 패리티 마스크로
  full row 중 자기 절반만 남기기: `TagMode::AxisToggle`은 `Ident` 인자라 device fn에서 불가; 패리티를 데이터로 만들 원천 없음.
  (c) down은 full row가 57.6 KB/슬라이스(29 MB)라 애초에 불가.
- **판정:** 보류. 세그먼트 비용은 정적 모델의 가정이므로 **실측에서 V23(행렬당 1회 로드) vs V22(타일별 로드)** 를 A/B 하면
  실제 하드웨어의 세그먼트 비용을 알 수 있다 — Arena 로그인 후 큐에 추가.

## V45_post_norm_in_producer_layout

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V39_x2_single_store`
- **가설:** attn_out·ffn tail: 출력 store 2,510(64 desc) → core 600 → HBM 지연 1,000 → ReducingSlices 로드 546 → norm pass 4~5개 →
  store 648. post-norm을 투영 reduce 레이아웃(`m![H / 60 % 32, 1 # 8]`, 슬라이스당 60원소, V18 패딩 패킷)에서 직접 하면
  store 2,510 + hop이 빠지고, mean-square 부분합은 클러스터 안 switch gather(ring 256, ~1.1k + config 719) + 클러스터 간 HBM
  스칼라 교환(store 337 + 1,000 + load 555)으로 모은다. 최종 store는 64 desc 2,510. 순 ≈ −1k/커널. residual·gamma·scale은
  64 desc 로드(각 ~600, 큐 뒤쪽이라 무해).
- **변경 파일:** `src/device/shared/rmsnorm.rs`(새 함수), `src/device/sliding/projection.rs`, `src/device/shared/mlp.rs`, `src/ops.rs`
- **정확도:** 합산 순서만 변경(f32).
- **예상:** attn_out −1k, ffn −1k. 구현 비용이 커서 V40–V43 뒤로.

## V46_ffn_scalar_broadcast_merge

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V39_x2_single_store`
- **가설:** `broadcast_scalar_pairs`가 erf_scale·out_scale에 대해 각각 HBM 로드 521 + ring-256 switch(config `?` 719)를 낸다
  (타임라인 89,878–91,118, 101,314–102,554). 두 스칼라를 HBM `[2, 1 # 8]` 하나에 쓰고(스테이징 단계에서 tile store 2회 →
  1회로도 가능: `[Dummy2 …]`가 아닌 실제 축 tile) 한 번 로드·한 번 switch로 두 패킷을 뿌린다.
- **변경 파일:** `src/device/shared/mlp.rs`
- **정확도:** 데이터 이동만 변경.
- **예상:** ffn −1.2k.

## V47_ffn_global_scale_into_eps

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V39_x2_single_store`
- **가설:** ffn tail의 `down_global_scale` pass(401, mlp.rs:484)는 뒤따르는 post-FF RMSNorm이 스케일 불변이라 불필요하다:
  norm(s·x) = norm(x), eps 항만 s²로 나뉜다. eps' = EPS / s²를 Sub의 소형 pass 2개(큐 밖)로 만들어 `vector_clip(Add, &eps_vrf)`
  피연산자로 쓰면 pass가 빠진다(V16 `vector_clip`은 VRF 피연산자 허용: rope.rs:376).
- **변경 파일:** `src/device/shared/mlp.rs`, `src/device/shared/rmsnorm.rs`(`normalize_add_gate_reduced`에 eps VRF 인자), `src/ops.rs`
- **정확도:** f32 나눗셈 순서만 변경; eps 항 동일.
- **예상:** ffn −0.4k.

## 2026-09-09 3차: 단일 f8 조각(V137/V138) 기각, 그리고 **측정 노이즈**

### 측정 노이즈가 후보 대부분보다 크다 — 이 프로젝트의 방법론적 한계

같은 코드가 실행마다 이만큼 달라진다:

| 커널 | 같은 코드의 실측 (표본) | 편차 |
|---|---|---:|
| qkv (V50 코드, 5회) | 159,405 · 153,150 · 151,686 · 149,244 · 136,576 | **±8%** |
| attn_out (V50 코드, 5회) | 57,892 · 56,586 · 56,522 · 55,910 · 55,446 | ±2% |
| ffn (V50 코드, 2회) | 354,825 · 353,981 | 0.2% |
| ffn (V82 코드, 2회) | 351,454 · 346,534 | **1.4%** |

ffn을 0.2%로 본 것은 표본 2개의 우연이었다. 네 실행을 합치면 ffn도 **1.4% 수준**으로 흔들린다.

qkv가 가장 시끄럽고 ffn이 가장 조용하다. **1~3%짜리 변경은 단일 Arena 실행으로 판정할 수 없다.**
1% 차이를 신뢰구간 안에서 보려면 10회 이상 반복이 필요한데, 제출 예산이 그걸 허용하지 않는다.

**따라서 채택 기준을 이렇게 둔다:** (a) 정확도는 단일 실행으로 판정한다(이진값이라 노이즈가 없다),
(b) 속도는 makespan이 같은 방향을 가리키고 실측이 그것과 모순되지 않을 때만 채택한다,
(c) 5% 미만의 실측 차이는 단독으로는 증거가 아니다.

### V82 (ffn up 12×5) — **채택**, 그리고 반복 측정으로 확인

한 번만 재면 안 된다는 걸 바로 확인했다. V50과 V82를 각각 두 번씩 재면:

| 구성 | 실행 | qkv | attn_out | ffn | 기하평균 |
|---|---|---:|---:|---:|---:|
| V50 | job 15360 | 149,244 | 56,522 | 354,825 | 4.972 |
| V50 | job 15359 | 159,405 | 55,910 | 353,981 | 4.885 |
| V82 | 사용자 제출 | 136,576 | 56,586 | 351,454 | **5.135** |
| V82 | job 15436 | 153,150 | 57,892 | 346,534 | 4.929 |

**V50 평균 4.929, V82 평균 5.032.** 커널별로는 ffn −1.5%(354,403 → 348,994, 네 실행 모두 V82 쪽이
작다), qkv −6.1%·attn_out +1.8%는 노이즈다. **처음 본 5.135는 qkv가 가장 빨랐던 한 번의 draw였다** —
액면 그대로 보고했다면 2%를 과대평가할 뻔했다.

채택 근거는 여전히 유효하다: ffn이 makespan 예측(−0.26%)과 같은 방향으로 네 실행에서 일관되게 작다.
**다만 SOTA 숫자는 5.135가 아니라 ≈5.03으로 적는다.**

### V137 / V138 (qkv x를 f8 한 조각으로) — **기각, 두 가지 이유로**

| | qkv | attn_out | ffn | 정확도 |
|---|---:|---:|---:|:---|
| V138 (job 15429) | 140,458 | 52,244 | 345,658 | qkv **FAIL** |

1. **정확도 실패.** q 11.69% / k 27.53% / v 5.73%의 max 오차, 원소의 93~98%만 tolerance 안. atol
   0.04에 max|Δ| 0.113이라 아깝게 못 넘는다. f8e4m3의 가수 3비트가 3,840항 내적에서 충분히
   상쇄되지 않는다 — x_lo가 존재하는 이유가 실측으로 확인됐다.
2. **속도 이득도 없다.** makespan은 qkv 61,381 → 51,429(−16%)를 약속했지만 실측 140,458은 같은
   qkv 코드인 V82의 136,576보다 오히려 크다. **qkv의 실측은 정적 모델이 세는 DMA 바이트가 아니라
   다른 것이 지배한다** — V52(사본)에서도 같은 일이 있었다. qkv의 실측/makespan 비가 2.4~2.9로
   튀는 이유가 여기 있을 것이다.

**두 번째 이유가 더 중요하다:** qkv에서는 makespan을 줄여도 실측이 따라오지 않는다. V52(사본, 실측 +10k)와
V138(단일 f8, 실측 무변화)이 같은 방향을 가리킨다. **qkv는 정적 모델로 최적화할 수 없다.**

### V142 (ffn을 f8 한 조각으로) — **기각.** single-f8 방향 종료

| | qkv | attn_out | ffn | 정확도 |
|---|---:|---:|---:|:---|
| V142 (job 15435) | 151,686 | 55,446 | **346,223** | ffn **FAIL** (max\|Δ\| 0.03125, 93.26%만 tol 안) |

makespan은 ffn 158,182 → 153,790(−2.8%), 실측도 −1.5%로 **속도는 실제로 났다**. 그런데 ffn의
tolerance 여유가 가장 얇다(현재 max\|Δ\| 0.0078 / tol 0.0172, 45%). f8e4m3 한 조각은 그걸 넘는다.

**qkv(V138)와 ffn(V142) 둘 다 정확도로 떨어졌으므로 x_lo는 어디서도 뺄 수 없다.** 이 방향은 닫는다.
남은 attn_out(V139)은 x 로드가 1,318뿐이라 이득이 없어 제출하지 않는다.

## 2026-09-09 2차 체인 결과 — 사본은 죽은 길, 타일 높이 상한 확정

50개 항목(V52–V101)이 makespan·빌드·CPU 단계를 통과했고 Arena 1회를 썼다. 세 가지가 확정됐다.

### ① 사본은 실물에서도 진다 (V52, job 15418) — **방향 종료**

| | qkv | attn_out | ffn | 정확도 |
|---|---:|---:|---:|:---|
| V50 (사본 없음) | **149,244** | 56,522 | 354,825 | 3/3 PASS |
| V52 (qkv 사본 c=2, 진짜 store) | **159,668** (+10,424) | 56,442 | 354,921 | qkv **FAIL** (404~977%) |

**makespan의 순위가 옳았다.** V38(깨진 16부 사본)의 qkv가 109,309으로 빨랐던 것은 사본이 **기록되지
않아 store 비용이 0이었기 때문**이다 — 주소 분산이라는 이득만 공짜로 챙긴 값이었고, 그래서 그 숫자는
애초에 도달 가능한 목표가 아니었다. store를 진짜로 내면 사본당 Core 디스크립터 3개가 in-order로
직렬화되어 로드 이득을 넘는다(V49의 정적 예측과 일치). 덤으로 c=2 코드는 수치도 틀렸다(사본 축을
`Dummy256 / 128`로 쓴 store/load 매핑이 어긋난다) — 성능이 이미 졌으므로 추적하지 않는다.

**결론: qkv의 x 로드 18,400 @ util 0.157은 사본으로 회수할 수 없다.** attn_out·ffn 사본은 makespan이
2.6배·1.8배로 더 나쁘므로 함께 폐기한다. `V52`–`V80`(24개) 전부 기각.

### ② scale VRF 8 KB = **타일 높이 ≤ 16** (컴파일 에러로 정확히 확정)

`mir: VRF data (9,600 bytes)` — 20행 × 120 f32 = 9,600 B > 8 KB. 실패한 높이: 20·24·28·30·32·40·48·60.
**60행을 나누는 유효 분할은 높이가 {4, 8, 12, 16}인 것뿐이다.** (V37의 "ROWS_PER_PASS>12는 scale VRF
한계" 관찰이 여기서 정확한 숫자로 확정됐다.)

### ③ 사본 축은 `Dummy8`일 때만 컴파일된다

attn_out·ffn 사본 지점에서 `m![Dummy256 / X, …]` 형태(c=2, c=4)는 전부
`visa: internal compiler error`로 죽는다(12개). qkv 지점에서는 같은 형태가 컴파일된다 — 차이는 사본 축이
슬라이스 매핑에서 어떤 위치에 오느냐로 보이나, 사본 방향 자체가 죽었으므로 추적하지 않는다.

### 유일한 개선: `V82_ffn_up_tiles_12x5`

up/gate reduce 타일을 16/16/16/12 → **12×5**로 바꾸면 ffn 158,182 → **157,763** (−419).
makespan 기하평균 5.239 → **5.244**. down을 12×5로 하면 반대로 나빠진다(160,851). 이득이 실측 편차
(qkv 6%, ffn 1%)보다 작아 단독 제출로는 판정할 수 없으므로, 더 나은 구성이 나오면 함께 묶어 제출한다.

## V45 구현 시도 (2026-09-09) — lowering 한 단계 앞에서 중단

패치는 `scripts/dev/v45_post_norm_in_rows.py`에 그대로 있다(V50에 적용하면 재현된다).
`project_output`이 post-attention RMSNorm까지 자기 행 레이아웃에서 끝내고 residual을 직접 쓴다.

### 어디까지 됐나

Rust 타입 검사는 통과했고 툴체인 lowering까지 들어갔다. 가는 길에 확인된 것들:

- 60원소 행의 정석 패턴은 `geglu_split`이다: `fetch/collect m![H / 4 % 15], m![H % 4 # 8]` →
  `vector_narrow_split::<m![H / 4 % 15, 1 # 2], m![H % 4]>` → 이항연산들 → `vector_widen_concat`.
  **`vector_fp_binary`는 narrow 이전 상태(`IntraFirst`)에서는 존재하지 않는다.**
- **reduce 뒤에는 `vector_fp_div`와 `vector_widen_pad`만 온다.** eps 덧셈과 sqrt는 각각 별도 패스가
  필요하다(rmsnorm이 mean_square / reduced_mean_square / rms 셋으로 나눠 둔 이유가 이것이다).
- `vector_intra_slice_reduce`의 첫 인자는 **축 이름**이어야 한다(`H`). `m![H / 1920]` 같은 식은
  `AxisName`을 만족하지 않는다.
- `EPS`는 crate 루트, `H_F32`는 `rmsnorm` 전용 private.

### 남은 블로커 (정확히 한 곳)

`ms_slice` 패스(슬라이스별 제곱합)의 commit:

```
declared: [ Chip: [] | Cluster: [H_1920=2] | InSlice: [[[]+7]=8] ]
pipeline: [ Chip: [] | Cluster: [H_1920=2] | InSlice: [[[]+15]=16] ]
```

파이프라인이 슬라이스 패딩 16을 내보내는데 `HiddenRows`는 `m![H / 60 % 32, 1 # 8]`이다.
`1 # 16`으로 선언하면 클러스터당 32 × 16 = 512로 256을 넘고, 패킷을 `m![1 # 16]`으로 넓히면 f32
64 B라 `commit_trim` 규칙(8/16/24/32 B)에 걸린다. 원래 contraction의 transpose가
`m![H % 60 = 20 % 4 # 16]`으로 16-wide 레인을 쓰는 것과 관련이 있어 보인다.

### 중단 이유 — 이득이 노이즈보다 작다

기대 이득은 attn_out −1k(3.6%)인데 **attn_out의 실측 노이즈가 ±2~4%**다. 컴파일에 성공해도
작동을 입증할 수 없다. 위 §"측정 노이즈" 기준 (c)에 스스로 걸리므로 여기서 멈춘다.
**다시 열려면 먼저 필요한 것은 이 구현이 아니라 반복 측정 예산이다.**

## V44–V48 실현 가능성 조사 (2026-09-09, V50 위에서)

설계 슬롯 5개를 구현하려고 상류 API와 매핑 제약을 확인한 결과, **3개는 막혀 있고 2개는 구현 가능하지만
검증 수단이 없다.** 근거를 남긴다 — 다음 세션이 같은 벽을 다시 확인하지 않도록.

| 슬롯 | 기대 | 상태 | 근거 |
|---|---:|---|---|
| **V44** ffn scale 세그먼트 | ffn −6.0k | **막힘 (재검증)** | 아래 재분석 참조. |
| **V45** post-norm을 생산자 레이아웃에서 | attn_out −1k, ffn −1k | 구현 가능, **검증 불가** | 합산 순서(f32)가 바뀌므로 실측 검증이 필요한데 Arena 예산이 3회. |
| **V46** ffn 스칼라 broadcast 병합 | ffn −1.2k | **막힘** | 두 스칼라를 한 패킷에 담으려면 `m![Dummy2, 1 # 8]` f32 = **64 B**인데 collect 출력 패킷은 정확히 32 B여야 한다. `m![2 # 8]`(32 B)로 담으면 두 소비자가 레인을 따로 못 집는다. HBM 로드만 합치면 −521. |
| **V47** global scale을 eps로 접기 | ffn −0.4k | 구현 가능, **검증 불가** | `FpBinaryOp::DivF`와 VRF 피연산자 `vector_clip`은 존재한다(`attention.rs:284`, `projection.rs:211`). 다만 `DivF`의 피연산자 순서(`acc/operand` vs 반대)를 실측으로 확인해야 하고, eps 산술이 바뀌므로 정확도 검증이 필수. |
| **V48** qkv 인덱스 공유 | qkv −0.5~1k | **절반 막힘** | `dma_scatter_unscaled`가 상류에서 `todo!("unscaled dma_scatter (SPM-resident raw index) is not implemented yet")` — k/v scatter는 불가. gather 절반은 `dma_gather_unscaled`가 구현돼 있으나 `rope_offset`을 바이트 오프셋에서 **행 번호**로 바꿔야 해서 커널 입력의 의미를 건드린다(§4). |

### 이 조사가 남기는 판단

다섯 슬롯을 다 살려도 합계는 대략 **−2.4k**(기하평균 +1.5% 남짓)다. 반면 qkv의 x 로드 하나가
**18,400 cycle을 util 0.157로** 쓰고 있고(V50 스케줄), 이걸 절반만 회수해도 기하평균 +5% 이상이다.
**남은 레버리지는 V44–V48이 아니라 x 로드에 있다.**

그리고 이 조사에서 반복해서 걸린 것은 구현이 아니라 **검증**이다. CPU 에뮬레이터는 attn_out을
`carve` 패닉으로 아예 못 돌리고(V0조차), qkv의 k/v도 오판한다. 즉 **수치를 건드리는 변경은 Arena
없이는 판정할 수 없다.** V15~V41을 통째로 날린 것이 정확히 이 구멍이었으므로, Arena 예산이 적을 때는
**데이터 이동만 바꾸는 변경**(사본 수, 타일 형상)에 실험을 몰아야 한다 — makespan이 스크리닝해 주고
정확도 위험이 없다.

### V44 재분석 (2026-09-09, 독립 확인)

원 분석이 맞다. 숫자를 정확히 해두면: `up_weight_scale.to_dm()`은 **커널당 한 번**만 돌고(패스마다가
아니다 — 로드된 DM 텐서를 패스가 `tile`로 나눠 쓴다), 그 한 번이 9,774 cycle이다. 비용의 정체는
바이트가 아니라 **512 슬라이스 × 60행 = 30,720개의 120 B 세그먼트**다.

full row(240 B × 60행 = 14.4 KB 연속 = 슬라이스당 1 세그먼트)로 읽으면
548 + 7.4 MB/1.21 KB + 0.2×512 ≈ **6,766** (−3,008/행렬, up·gate 합쳐 **−6.0k**).

막히는 지점은 하나다: **슬라이스의 청크 인덱스가 scale의 열 절반을 골라야 하는데 `tile(start)`의
start는 전 슬라이스 공통이다.** 우회로 두 개도 다 막힌다.

- partials를 240열로 넓혀 나머지 절반을 0으로 두고 전폭 곱셈 → 그 0을 **어느 절반에 넣을지**가 다시
  슬라이스별 오프셋 문제다.
- VRF로 도망갈 수도 없다: scale VRF는 `$rows × 열수` f32이고 8 KB 상한이라, 240열이면 타일이 8행까지만
  가능하다(8 × 240 = 7,680 B). 타일을 늘려도 DMA는 한 번이므로 세그먼트 수는 그대로다.

**결론: 이 저장소의 매핑 언어로는 표현할 수 없다.** 상류가 슬라이스 좌표를 fetch 오프셋으로 쓰게 해주기
전에는 열리지 않는다. ffn DMA 큐의 18.5%가 여기 묶여 있다는 사실만 기록해 둔다.

## V49_hbm_copies_real_stores

분기점: `V41_x2_hbm_copies` (문서 계보의 끝; 코드는 V41의 사본 확대를 포함한다)

### 가설

HBM 사본은 좋은 생각이었지만 **한 번도 실제로 쓰이지 않았다**(위 §첫 RNGD 실측의 근본 원인). 사본마다
`to_hbm_view`를 명시적으로 호출하면 load 분산 이득은 그대로 두고 정확도를 되찾는다. store가 사본 수만큼
늘어나므로 사본 수는 다시 튜닝 대상이다.

### 대상 (4곳)

| 위치 | 현재 사본 | store 1개 비용 | 사본당 추가 store |
|---|---:|---:|---|
| `shared::mlp::stage_x_hi_lo_copies_hbm` (qkv x, f8 hi/lo) | 16 | ~434 | 2개 (hi, lo) |
| `shared::mlp::stage_x_hi_lo_hbm` (ffn up/gate) | 8 | ~377 | 2개 |
| `shared::mlp::stage_geglu_hi_lo_hbm` (ffn down) | 8 | ? | 2개 |
| `sliding::projection::project_output` (attn_out) | 8 | 377 | 2개 |

### 비용 모델 (V14→V15 실측 스케줄 기준)

사본 1개 → 8개로 load 18,400 → 5,367 (−13,033), store 434 × 7 추가 (+3,038) ⇒ 순 −9,995.
사본 수 c에 대해 load ≈ 18,400/c + 고정, store ≈ 434 × c × (조각 수)이므로 최적 c는 8보다 작을 수 있다.
**c = 1, 2, 4, 8, 16을 네 곳에서 각각 재보고 고른다.**

### 측정 (qkv, 사본 수 전수; lab 컴파일)

사본마다 `to_hbm_view`를 부르면 store는 실제로 나간다(16부 = store 32개, 합 17,696). 그런데
**store 1개마다 Core 디스크립터 명령이 3개씩 붙고 Core는 in-order 발행**이라 그게 임계경로가 된다.

| 사본 수 | qkv makespan | x load | store 합 | Core 합 |
|---:|---:|---:|---:|---:|
| **1 (사본 없음)** | **58,962** | 18,400 | 1,557 | 2,056 |
| 2 | 61,867 | 6,109 | 2,663 | 4,568 |
| 4 | 72,824 | 5,182 | 4,875 | 8,344 |
| 8 | 96,592 | 5,182 | 9,299 | 15,896 |
| 16 (head와 같은 사본 수) | 144,128 | 5,182 | 17,696 | 29,760 |

load는 18,400 → 6,109으로 실제로 3배 빨라지지만(사본이 원래 노렸던 것), Core 비용이 그보다 빨리
늘어 **어느 사본 수에서도 c=1을 못 이긴다**. 16부에서는 반복 하나가 5,942 cycle 간격으로 직렬화된다.

### 판정: **기각** (올바른 복제는 아끼는 load보다 비싸다)

사본이라는 아이디어 자체가 틀렸다. 남는 결론은 "사본을 없앤다"이고 그것이 V50이다.
단, **실측에서는 사본이 있는 쪽이 qkv를 더 줄인다**(V38 109,309 vs V50 149,244): 같은 주소를 512개
디스크립터가 읽는 부하를 정적 모델이 과소평가한다. 올바른 사본을 실측으로 A/B하는 것은 아직 열려 있다
(makespan은 c=1을 고르지만 실물은 c=2를 고를 수 있다).

## V50_drop_false_replication

분기점: `V38_post_norm_store_from_reducing` (V39~V49는 전부 기각/보류)

### 가설

**소스에 없는 축은 DMA write를 복제하지 않는다**(§첫 RNGD 실측, V49). 이 전제에 기댄 변경을 전부
되돌리면 계보의 성능 대부분을 지키면서 정확도가 돌아온다.

### 되돌린 두 곳

1. **qkv x의 HBM 사본** (`stage_x_hi_lo_copies_hbm` → `stage_x_hi_lo_qkv_hbm`, V15가 bf16 8부로 도입,
   V31이 f8 16부로 확대). 사본 1개만 기록되어 15/16의 슬라이스가 초기화되지 않은 HBM을 x로 읽었다.
   → qkv가 index 0부터 non-finite.
2. **V30의 RoPE 테이블 직접 gather** (`apply_rope_heads`). V30 커밋 메시지가 같은 전제를 명시한다:
   *"A cluster or slice axis absent from the table replicates"*. cos/sin을 두 클러스터 head 레이아웃으로
   곧장 gather했지만 기록되지 않은 슬라이스는 쓰레기를 읽는다. **RoPE를 거치지 않는 v만 통과**했다.
   V30 이전의 HBM 왕복(store 337 + load 555 × 2)을 복원했다.

### 두 번째 버그를 찾은 방법 (기록해 둘 가치가 있다)

1번만 고치고 제출하니(job 15358) v·attn_out·ffn은 PASS인데 q·k만 FAIL이고, **non-finite 위치가
index 0에서 q=641, k=385로 이동**했다. Ds=256이므로 641 = 2×256+129, 385 = 1×256+129 — 둘 다
**같은 채널 d=129, 같은 kv head n=1**. v만 통과한다는 것과 합치면 남는 경로는 RoPE뿐이고, RoPE는
Ds를 128씩 반으로 나누므로 d=129는 두 번째 half의 두 번째 원소다. 여기서 곧장 V30이 나왔다.
**실패 위치의 인덱스는 커널 디버깅에서 가장 값싼 단서다.**

### 측정

| | qkv | attn_out | ffn | 기하평균 |
|---|---:|---:|---:|---:|
| makespan | 58,962 | 27,424 | 158,182 | 5.310 |
| **RNGD 실측 (job 15359)** | 159,405 | 55,910 | 353,981 | **4.885** |
| **RNGD 실측 (job 15360, 커밋 확인)** | 149,244 | 56,522 | 354,825 | **4.972** |

qkv의 실행 편차가 6%로 세 커널 중 가장 크다(같은 바이너리, 같은 픽스처). attn_out·ffn은 1% 이내.
비용은 qkv뿐이다: 사본을 잃어 실측 109,309(V38, 무효) → 149,244, V30 복원으로 makespan +2,419.

### 판정: **채택 — 현재 SOTA**

V14의 3.936× → **4.972×** (+26%). 정확도가 확인된 첫 5×대 근처 구성이다.

### 다음 후보

- qkv가 이제 유일한 병목이다(실측 speedup 1.64로 나머지 두 커널의 7.2 / 10.4에 크게 못 미친다).
  기하평균이므로 여기가 레버리지다.
- 사본을 실측으로 A/B (V49의 결론은 makespan 기준. c=2를 실물에서 재볼 값어치가 있다).
- V30을 정직하게 다시 하기: cos/sin을 두 클러스터로 보내되 복제를 명시적으로 기록.
- V15 이후 계보에서 아직 실측되지 않은 최적화(V16~V37)는 대부분 정확도 위험이 없다. V50 위에서
  다시 쌓되, **채택 판정은 실측으로만** 한다.

## 2026-09-09 2차 체인: V52–V91 (스윕 가족, 설계됨)

첫 실측이 끝난 뒤 무인 체인을 다시 걸었다(§11, Arena는 `arena_off`로 꺼둠 — 항목은 `arena=pending`으로
남아 스위치를 되돌리면 큐 순서대로 자동 제출된다). 이 40개는 개별 가설이라기보다 **한 손잡이의 스윕**이므로
여기에 묶어 기록한다. 개별 브랜치는 `V{n}_{설명}`으로 push되어 있고 결과는 pod `auto/BOARD.md`에 쌓인다.

### 왜 이 손잡이인가 — V50 스케줄의 utilisation

DMA 큐가 세 커널 모두의 makespan이므로, **낮은 utilisation = 실제로 남은 여유분**이다.

| 커널 | 항목 | cycles | util | 성격 |
|---|---|---:|---:|---|
| qkv | x 로드 (`ops.rs:71`) | 18,400 | **0.157** | 512 슬라이스가 같은 7.5 KB를 읽음 |
| qkv | q / k·v weight | 13,512 / 7,030×2 | 0.86 / 0.82 | 바이트 한계, 손댈 수 없음 |
| ffn | block-scale 로드 ×3 | 9,774×3 | **0.277** | 120 B 세그먼트 30,720개 (V44) |
| ffn | weight ×2 | 29,427×2 | 0.74 | 바이트 한계 |
| attn_out | 출력 store | 2,510 | **0.002** | 64 디스크립터 (V45) |
| attn_out | x 로드 | 1,318 | **0.293** | 64 슬라이스가 같은 청크를 읽음 |

### 가족 1 — 사본 수를 **실측으로** 고른다 (V52–V80, 24개)

V49는 makespan으로 "사본 없음"을 골랐다. 그러나 qkv는 실측이 makespan의 **2.53배**(attn_out 2.06, ffn 2.24)로
셋 중 모델 오차가 가장 크고, 그 차이를 만드는 유일한 요소가 바로 이 같은-주소 로드다. V38(사본이 깨진 채로
있던 것)의 qkv 실측이 109,309인데 사본을 없앤 V50이 149,244인 것도 같은 방향을 가리킨다 —
**대부분의 사본이 쓰레기였는데도 로드는 실제로 빨랐다.**

| 브랜치 | 사본을 두는 곳 | 사본 수 |
|---|---|---|
| `V52`, `V54`, `V55`, `V56` | qkv x | 2, 4, 8, 16 |
| `V61`, `V62`, `V57` | attn_out x | 2, 4, 8 |
| `V63`, `V64`, `V58` | ffn up/gate x | 2, 4, 8 |
| `V65`, `V66`, `V59` | ffn down geglu | 2, 4, 8 |
| `V68`, `V67`, `V60` | 위 세 곳 동시 | 2, 4, 8 |
| `V69`–`V80` | qkv × 나머지 세 곳 격자 | qkv {2,4,8,16} × rest {2,4,8} |

격자가 필요한 이유: 네 지점이 **같은 DMA 큐**를 쓴다. 사본이 늘리는 store 디스크립터는 사본이 줄이려는
로드와 같은 큐에 얹히므로, 단일 지점 스윕으로는 최적점을 볼 수 없다.

### 가족 2 — ffn 행 타일 (V81–V91, 11개)

up 16/16/16/12와 down 16/16/16/8/4는 V12/V37/V40이 **makespan으로만** 고른 값이다. ffn은 실측이
makespan의 2.24배이고, 타일 수는 (a) 이전 타일의 contraction이 다음 weight 로드를 얼마나 가리는지와
(b) block-scale 로드가 몇 번 재발행되는지를 동시에 정한다.

| 브랜치 | up | down |
|---|---|---|
| `V81`–`V84` | 20×3 / 12×5 / 30×2 / 60×1 | (그대로) |
| `V85`–`V89` | (그대로) | 20×3 / 12×5 / 30×2 / 60×1 / 8×7+4 |
| `V90`, `V91` | 20×3 / 12×5 | 20×3 / 12×5 |

scale VRF 8 KB를 넘는 분할은 컴파일에서 죽을 것으로 예상한다 — 그것도 기록되는 결과다(§2.1).

### 아직 구현 안 된 것 (기대값 순)

- **V44** ffn scale 세그먼트 — 위 표에서 가장 큰 미착수 여유분(29,322 @ 0.277). pair가 행을 30/30으로
  나누고 partials를 ring-2 switch로 교환하면 scale이 1 세그먼트가 된다.
- **V45** attn_out post-norm을 생산자 레이아웃에서 — store 2,510 @ 0.002.
- **V48** qkv의 cos/sin gather·k/v scatter 인덱스 공유 — 3,726 @ ~0.004.
- V30을 복제까지 정직하게 다시 하기 (V50이 되돌린 −2,419).

## V48_qkv_shared_spm_index

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V39_x2_single_store`
- **가설:** `dma_gather_scaled`(cos·sin, 934씩)와 `dma_scatter`(k·v cache, 929씩)는 각각 HBM의 인덱스 텐서를 읽고 바이트
  오프셋으로 스케일한다. `*_unscaled` 변형은 SPM 상주 인덱스를 받으므로 rope_offset·kv_offset을 한 번씩 DM에 올려
  (`i32 × 512`) 4개 DMA가 공유하면 인덱스 로드/스케일 몫이 빠진다. 934/929 중 인덱스 몫이 얼마인지는 미지수 — 먼저 cos
  하나만 바꿔 측정.
- **변경 파일:** `src/device/sliding/rope.rs`, `src/ops.rs`
- **정확도:** 데이터 이동만 변경.
- **예상:** qkv −0.5k ~ −1k (불확실).

## V24_gather_before_hbm_store

- **상태:** makespan 측정 (2026-09-09)
- **분기점:** `V23_ffn_whole_scale_loads` (`fd20ecd`)
- **가설(원문):** V23 스케줄에서 HBM store가 디스크립터 비용에 묶여 있다(util 0.2~1%): ffn up/gate 결과 store
  4,535×2(슬라이스당 120 B × 256 디스크립터), ffn down·attn_out 출력 store 2,510(64 디스크립터). 단일 디스크립터
  store는 337~434. (a) 클러스터 안에서 switch ring gather로 슬라이스 1개에 모은 뒤 클러스터당 1 디스크립터로
  쓴다. (b) 이어지는 post-norm은 `Slice`로 로드(547) 후 `ReducingSlices`로 재배치(449)하던 것을 HBM에서
  ReducingSlices로 직접 로드한다.
- **변경 파일:** `src/device/shared/rmsnorm.rs`(`ReducingSlices` 공개, `load_reducing`, `normalize_reduced` /
  `normalize_add_reduced` / `normalize_add_gate_reduced` 추가; 기존 함수는 래퍼로), `src/device/shared/mlp.rs`
  (`project_down`·`feedforward`가 HBM/ReducingSlices 반환, global scale pass를 8슬라이스에서), `src/device/sliding/projection.rs`
  (`project_output`이 HBM 반환), `src/ops.rs`
- **공유 코드 영향:** rmsnorm 기존 함수 의미 불변(리팩터). mlp.rs 호출처는 `decoder_feedforward`뿐.

### 측정

| 단계 | qkv | attn_out | ffn | 비고 |
|---|---:|---:|---:|---|
| V23 | 51,631 | 30,487 | 181,625 | |
| (a) attn_out: `Broadcast1 { slice1: 32, slice0: 8 }` gather, 4원소 패킷 | | 33,050 | | switch 4,615 (ring 256 × Time 15), store 2,510 → 458 |
| (a) 12원소 패킷(Time 5, commit_trim 24 B) | | 30,483 | | switch 2,055 — store 절감분과 상쇄. **기각** |
| (b) 직접 로드 (세 커널) | **50,981** | **30,037** | **179,922** | qkv −650 (head의 relayout 449 + 로드 차), attn_out −450, ffn −1,703 (relayout 449×2 + global scale pass 1,241 → ~200 + 잔여) |
| **기하평균 (V0 대비 누적)** | | | | **5.180** |

- **정확도:** 수치 동일(로드 경로·pass 레이아웃만 변경; global scale 곱은 같은 f32 연산).
- **측정 방식:** makespan only.
- **컴파일 교훈:** `vector_inter_slice_reduce`는 innermost 슬라이스 축만 줄인다 — 바깥 축을 reduce하려는 레이아웃
  (`m![1 # 8, H / 60 % 32]`)은 visa 단계에서 `VRU reduce axes must be innermost in Partitioning`. `SwitchConfig`의
  `CustomBroadcast { ring_size }`는 패딩 축·순열을 지원하지만(std 테스트 참조) stride가 있는 live 집합은 ring 전체
  비용을 낸다.

### 판정: makespan 측정 (실측 대기)

- **배운 것:** HBM store 비용은 디스크립터 수와 레이아웃 슬롯 수에 묶이고, 그것을 줄이는 switch는 ring 크기에
  묶인다(둘 다 ~2k). hop을 줄이는 정답은 "모으기"가 아니라 **hop 자체를 없애거나(V25) 소비자 레이아웃으로 직접
  로드하기(V24b)**.

## V25_ffn_geglu_two_clusters

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V24_gather_before_hbm_store`
- **가설:** ffn DMA 165k 중 up/gate → geglu → down 사이의 hop이 13.9k: up/gate 결과 store 4,535×2(`m![L / 60 % 128, 1 # 2]`,
  256 디스크립터), geglu 입력 로드 1,328×2, geglu 출력 store 2,219. up과 gate는 같은 슬라이스에 같은 60행이 있으므로
  geglu를 그 레이아웃에서 직접 계산하면 앞의 네 DMA가 사라진다. 60 f32 = 7.5 패킷이라 8-wide 패킷에 안 맞는 문제는
  V18의 패딩 패킷 기법(`fetch::<m![L / 4 % 15], m![L % 4 # 8]>` → `narrow_split::<m![L / 4 % 15, 1 # 2], m![L % 4]>`)으로
  푼다. up/gate global scale은 contraction epilogue(inter-slice reduce 뒤 MulF scalar VRF)에 접어 넣어 geglu의
  scale pass 2개도 없앤다. geglu 출력 store는 256 디스크립터(4,535)로 남지만, 순이득 ≈ −9k. 2단계로 출력 앞에
  ring-4 pair gather(`Broadcast1 { slice1: 2, slice0: 2 }`, 비용 ~60+고정)를 붙여 128 디스크립터(2,219)로 줄이는 것을
  시도한다.
- **변경 파일:** `src/device/shared/mlp.rs`(`project_up_and_gate` 반환 레이아웃, `geglu_split`·`scale_split_rows` 추가, 구 `geglu` 삭제, `feedforward`)
- **공유 코드 영향:** mlp.rs 호출처는 `decoder_feedforward`뿐.
- **예상:** ffn −9k ~ −11k.

### 측정 (단계별, 브랜치 안에서 누적)

| 단계 | ffn | 비고 |
|---|---:|---|
| V24 | 179,922 | |
| (a) geglu_split + global scale을 contraction epilogue에 fold | 172,395 | 첫 컴파일 통과. DMA 165k → 158k. 그러나 contract pass 1,705 → 2,755 (epilogue narrow_split/MulF/widen이 12행 패킷에 1,050) → Main 122k → 132k |
| (b) scale을 gelu pass의 MulF로 | 컴파일 실패 | `13 is not available for op Binary(MulF)` — pass당 Mul0·Mul1 각 1회뿐이고 gelu pass가 둘 다 씀 |
| (c) scale을 별도 pass 2개(구 geglu와 동일 수치)로 | 172,479 | Main 122k 복귀. makespan은 DMA-bound라 (a)와 동일 |
| (d) 출력 store 앞 ring-4 gather (`Broadcast1 { slice1: 2, slice0: 2 }`) | 170,895 | switch 323, store 4,535 → 2,879 |
| (d) ring-8 | 170,594 | switch 383, store 2,206 |
| (d) **ring-16** | **170,158** | switch 503, store **1,870** |
| (d) ring-32 | 170,170 | switch 743, store 1,870 (바닥) |
| **기하평균 (V0 대비 누적)** | | **5.277** |

- **정확도:** (c) 채택본은 구 geglu와 연산·반올림 순서 동일(bf16(sum) × scale → bf16 → gelu/곱).
- **측정 방식:** makespan only. DMA busy 165k → 155k, Main 122k.
- **컴파일 교훈:** (1) 60원소 패킷은 `fetch::<m![L / 4 % 15], m![L % 4 # 8]>` → `narrow_split::<m![L / 4 % 15, 1 # 2], m![L % 4]>` →
  `widen_concat::<m![L / 4 % 15], m![L % 4 # 8]>` → `commit_trim::<m![L % 4]>`로 첫 시도에 통과(f32 출력 16 B, bf16 출력
  `cast::<bf16, m![L % 4 # 16]>` 후 8 B). (2) VE pass 하나에 `FpMulAlu::Mul0`·`Mul1` 각 1회. (3) stride-2 live 슬라이스의
  `Broadcast1 { slice1: g, slice0: 2 }` gather 비용 ≈ 260 + 2g × 15; HBM store는 디스크립터 수에 따라 256 → 4,535,
  128 → 2,879, 64 → 2,206, ≤32 → 1,870(바닥).

### 판정: makespan 측정 (실측 대기)

- **배운 것:** 큰 hop은 "모으기"가 아니라 소비자를 생산자 레이아웃으로 옮겨서 없앤다. 남은 ffn 임계 경로 = DMA 큐
  155k + 마지막 타일 뒤 직렬 tail 17k(dequant 6k + contract 1.7k + store 2.5k + hop 1.6k + norm 3k + 저장).
  스케줄러는 이제 gate scale 로드(9.8k)를 큐 선두에 두지만 DMA-bound라 makespan에 무해.
- **다음 후보:** 마지막 down 타일을 4행으로(V28 수정), scale 로드 29k(11 MB, 120 B 세그먼트 30,720개 — 레이아웃상
  불가피, 죽은 길 후보), post-norm을 두 클러스터 레이아웃에서(hop 1.6k + store 2.5k → 8 B 교환 + 최종 store 2.5k).

## V26_qkv_hsplit_x_halved

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V25_ffn_geglu_two_clusters`
- **가설:** qkv DMA 큐 47k 중 복제 x 로드 5.4k(3.9 MB)와 x_trf 스테이징 1.2k(임계 경로)는 슬라이스당 x 전체
  [H]를 필요로 해서 생긴다. 투영을 H/1920 열 반으로 나누면(Q: `m![Qs / 16 % 128, H / 1920]` 16행×1920열,
  K/V: `m![Ps / 8 % 128, H / 1920]` 8행×1920열, 2-way `vector_inter_slice_reduce`) 슬라이스당 x가 절반이라
  복제 로드 바이트가 절반(V15의 8부 사본 기법 유지: `m![Dummy256 / 16, Dummy8, H / 1920]`), x_trf도 절반.
  weight DMA는 바이트 동일, 행 세그먼트 2배(모델상 +0.2 cycle/세그먼트 ≈ +1.7k)라 순이득은 x 쪽에서 나온다.
  헤드 gather는 reduce 출력의 `1 # 2` 패딩이 섞인 ring-64(`Broadcast1 { slice1: 64, slice0: 2 }` → ring 128)가 된다.
- **변경 파일:** `src/device/sliding/projection.rs`, `src/ops.rs`
- **예상:** qkv −2k ~ −3k.

### 측정 (2026-09-09 밤, V39 위에 구현, 커밋 `f417b5c`): qkv 45,744 → **47,532** (+1,788)

- 첫 컴파일 통과(2-way `vector_inter_slice_reduce::<m![Qs / 16 % 128, 1 # 2]>`, `Broadcast1 { slice1: 64, slice0: 2 }` gather,
  `[Dummy256 / 16, H / 1920, Dummy2, H % 1920]` 스크래치에서 슬라이스당 1세그먼트 로드 모두 통과).
- DMA busy 41,910 → 44,447(+2.5k): x2 로드는 줄었지만 **weight 로드가 슬라이스당 1세그먼트(30 KB) → 16/8세그먼트(1920 B)로
  바뀌며 더 비싸졌다**. 즉 정적 모델은 1920 B 세그먼트에도 세그먼트 비용을 매긴다(V36의 "무관"은 30 KB급 단일 세그먼트끼리의
  비교였음).

| DMA 노드 | V38 | V26 | Δ | 세그먼트 Δ | cycle/세그먼트 |
|---|---:|---:|---:|---:|---:|
| Q weight (15.7 MB) | 13,512 | 15,950 | +2,438 | 512 → 8,192 | 0.32 |
| K weight (7.9 MB) | 7,030 | 8,250 | +1,220 | 512 → 4,096 | 0.34 |
| V weight (7.9 MB) | 7,030 | 8,250 | +1,220 | 512 → 4,096 | 0.34 |
| x2 복제 로드 | 5,182 (3.9 MB) | 2,841 (1.97 MB) | −2,341 | | |
| 합계 | | | **+2,537** | | |
- **정확도:** 합산 순서만 변경(f32 2-way reduce).

### 판정: **기각** (weight 세그먼트 비용 > x 절감)

- **배운 것:** 이 정적 모델에서 세그먼트 비용은 크기와 무관하게 개당 붙는다(≈0.2~0.3 cycle). x 복제 바이트를 줄이려고 weight
  행을 쪼개면 손해. qkv에서 x2 로드(5.2k)를 줄이는 유일한 길은 사본 수/채널 분산(V15 계열)뿐이고 그것도 16부에서 포화(V31).
  실물에서는 1920 B 세그먼트(256 B 비정렬)가 더 나쁠 수 있어 A/B 가치도 낮다.

## V27_attnout_scale_in_rmsnorm

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V26_qkv_hsplit_x_halved`
- **가설:** attn_out 타임라인 head 3.75k = x 청크 594 + `?` 838 + **채널 scale 로드 1,318**(64 디스크립터 × 120 B)
  뒤에야 첫 weight 타일이 시작된다. scale은 V18에서 contraction epilogue로 옮겨 tile0 pass의 소비자가
  됐기 때문에 DMA 큐 선두에 선다. scale 곱을 post-attn rmsnorm의 두 pass(mean_square 앞의 MulF, 마지막
  정규화 pass의 MulF)에 접어 넣으면 scale 로드는 ReducingSlices 8 디스크립터(~550)로 tail에서 마지막 타일
  pass와 겹치고, epilogue의 narrow_split/MulF/widen이 빠진다. rms(s⊙x)는 scale된 값으로 계산하므로 수치 동일.
- **변경 파일:** `src/device/sliding/projection.rs`, `src/device/shared/rmsnorm.rs`(`normalize_scaled_add_reduced` 추가), `src/ops.rs`
- **예상:** attn_out −1.3k.

## V28_ffn_tile_shapes

- **상태:** 설계됨 (2026-09-09; V25 타임라인을 보고 V26·V27보다 먼저 구현하기로 함)
- **분기점:** `V25_ffn_geglu_two_clusters`
- **가설:** (1) FFN DMA 155k 중 타일당 고정비(DmaLoad 고정 ~550 + `?` 838) × 15. V12에서 ROWS_PER_PASS=12가 scale VRF
  한도(8 KB)로 정해졌지만 16행 × 120 f32 = 7.7 KB도 들어간다. up/gate를 16/16/16/12(행렬당 4타일)로 하면 고정비 2개
  (≈2.8k)가 빠진다. (2) V25 타임라인: 마지막 down 타일 로드가 153.3k에 끝난 뒤 그 타일의 dequant 6,040 + contract 1,712가
  직렬로 남고 그 뒤에야 store·norm이 온다. down을 16/16/16/8/4로 하면 마지막 타일의 dequant가 ~2k라 tail이 ~5.5k 준다
  (타일 수는 5로 동일). transpose는 `= 16 / 4`, `= 8 / 4`, `= 4 / 4`.
- **변경 파일:** `src/device/shared/mlp.rs`(타일 헬퍼를 `macro_rules!`로 행 수별 생성, `project_up_and_gate`/`project_down`을
  `feedforward`에 통합, `geglu_split` 스칼라 fold)
- **공유 코드 영향:** mlp.rs 호출처는 `decoder_feedforward`뿐.
- **예상:** ffn −8k.

### 측정 (단계별)

| 단계 | ffn | 비고 |
|---|---:|---|
| V25 | 170,158 | |
| (a) up/gate 16/16/16/12, down 16/16/16/8/4 | 169,349 | DMA 155.5k → 152.7k(타일 3개 감소). 그러나 tail 불변: down 단계가 **VE-bound**(첫 down 타일 도착 117k 이후 dequant 30k + contract 8.6k + geglu가 직렬) |
| (b) down dequant를 up/gate pass 사이에 프로그램 순서로 인터리브 | 169,349 | **스케줄 완전 동일** — 스케줄러는 프로그램 순서를 전혀 보지 않는다(의존 그래프만) |
| (c) geglu 체인(scale·mul·gather)을 Sub 컨텍스트로 | 169,168 | Sub vector pass도 `VectorEngine`을 점유(공용 단일 자원). geglu는 global scale 소형 DMA(2×1,968, 큐 뒤 124.9k)를 기다림 |
| (d) global scale을 입력 없는 스칼라 준비 pass로 소비(로드가 큐 선두로), gelu·mul pass에 fold | 168,760 | 스칼라는 111k에 준비됐지만 gelu pass는 여전히 135k(VE가 dequant로 점유되고 스케줄러가 작은 pass를 뒤로) |
| (e) = (d) + geglu 체인을 Main으로 | **168,757** | 채택(수치 동일 계열: s_gate/√2, s_up·s_gate/2 f32 사전 곱) |
| **기하평균 (V0 대비 누적)** | | **5.292** |

- **측정 방식:** makespan only. DMA 152.7k / VE 121k / Main 121k.
- **컴파일 교훈:** (1) `macro_rules!`로 `m![L % 60 = $rows]`를 찍어내는 것은 문제없다. (2) `= 4 / 4` transpose(1패킷)도 통과.
  (3) **VectorEngine은 Main·Sub 공용 단일 자원**이고 dequant(4-lane f32 MulF, 3.8 elem/cycle)가 VE를 채우면 Sub의
  작은 vector pass도 못 낀다. (4) 스케줄러의 DMA 순서는 소비자 우선순위로 정해지며 프로그램 순서 인터리브는 무효.

### 판정: makespan 측정 (실측 대기)

- **배운 것:** ffn의 남은 구조적 한계는 dequant의 VE 비용(91k)이다. down 타일이 DMA 큐 뒤쪽(117k~)에 오는 한
  그 뒤의 VE 작업 40k가 tail을 만든다. Way8에는 FP 곱이 없고(`vector_fp_binary`는 Way4 전용), fxp 경로는
  파이프라인 순서(FpToFxp가 끝단)와 LUT 출력 타입(f8/bf16 고정)에 막힌다 → dequant 자체를 없애는 V29로.
- **다음 후보:** V29(블록 scale을 contraction 뒤로), post-norm hop 1.6k.

## V30_qkv_rope_tables_direct_gather

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V29_ffn_block_scale_after_contract`
- **가설:** `apply_rope_heads`는 cos/sin 행을 `Cluster, Slice`로 gather(934)한 뒤 HBM에 쓰고(337) 헤드 레이아웃으로 다시 읽는다(555).
  V15의 규칙("HBM에 없는 축을 슬라이스/클러스터 매핑에 쓰면 복제")이 gather에도 적용되면 `dma_gather_scaled`의 목적
  레이아웃을 `HeadClusters, HeadSlicesPerCluster, m![Ds]`로 두어 8개 live 슬라이스가 같은 512 B 행을 직접 받을 수 있다.
  DMA 큐에서 −1.8k, 그리고 cos gather(43.1k, V 로드 뒤)→store→load 체인이 tail에서 짧아진다.
- **변경 파일:** `src/device/sliding/rope.rs`
- **예상:** qkv −1.5k ~ −2.5k.

### 측정: qkv 50,981 → **48,638** (−2,343), 기하평균 5.408

- 첫 컴파일 통과. gather는 934 그대로(8 디스크립터가 같은 512 B 행을 읽어도 비용 불변), store 337×2·load 555×2 제거.
  DMA busy 46.5k → 44.7k. tail: V 로드 종료 42.6k → V contract 2,663 → 후처리 → scatter 2개 → 48.6k.
- **정확도:** 데이터 이동만 변경.

### 판정: makespan 측정 (실측 대기)

## V31_qkv_f8_contraction_no_lut

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V30_qkv_rope_tables_direct_gather`
- **가설:** qkv의 Q/K/V 가중치는 f8e4m3라 LUT(f8→bf16)를 거쳐 bf16 contraction을 한다: Q는 별도 LUT pass 5,063(Main), K/V는 융합
  contract 2,663(LUT-bound ~5.8 elem/cycle), 그리고 LUT pass마다 `?` DmaLoad 838(DMA-bound 커널에서 2.5k). V29의 기법으로 x를
  f8 두 조각(hi/lo of x·2^k)으로 TRF `m![Dummy2, H]`에 올리고 weight 패킷을 Dummy2 시간축으로 두 번 스트림하면 LUT 없이
  f8×f8 contraction(f32 누산)이 되고 `?`도 사라진다. 결과 q/k/v는 2^k배 커지지만 뒤따르는 head RMSNorm(q, k)과 value normalize가
  스케일 불변(rms(s·q) = s·rms(q); eps 항만 s²분의 1로 작아져 상대 1e-6 이하)이라 되돌릴 필요가 없다. 2^k는 head의
  ReducingSlices에서 max x²로(V29 head와 동일 체인). x 복제는 V15의 8부 사본을 유지하되 f8 2조각(바이트 동일).
- **변경 파일:** `src/device/sliding/projection.rs`, `src/ops.rs`, `src/device/shared/mlp.rs`의 스케일 체인 재사용 여부
- **정확도 리스크:** hi/lo 분해는 max|x|/4096 이상에서 exact; 그 아래는 ≤ 2^-10/s 절대오차(V29와 동일). rms 불변성은 eps에서만 깨짐.
- **예상:** qkv −3k ~ −4k.

### 측정: qkv 48,638 → 46,726 (8부 사본) → **46,541** (16부 사본), 기하평균 5.488

- 첫 컴파일 통과. DMA 44.7k → 42.7k(`?` 3개 제거, x2 store 553×2 추가, x 로드 5,367 → 5,182). K/V contract 2,663 → 1,225,
  Q LUT 5,063 사라짐, Q contract 2,185 그대로. 스케줄러는 이제 K 로드를 큐 선두에, Q 로드를 x 뒤에 둔다(DMA-bound라 무해).
  tail: V 로드 종료 42.1k → V contract 1,225 → 후처리 → scatter → 46.5k. 16·32부 사본은 동일(5,182).
- **정확도:** V29와 같은 hi/lo 분해; q/k/v는 2^k배로 커진 채 head RMSNorm에 들어가고 정규화에서 상쇄(eps 항만 2^-2k배).
  **실측 검증 필요.**

### 판정: makespan 측정 (실측 대기)

## V39_x2_single_store

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V38_post_norm_store_from_reducing`
- **가설:** DMA 비용은 개수에 묶인다(548 고정 + 디스크립터당 ~35 store). hi/lo를 HBM `[…, Dummy2, …]` 스크래치에 두 번 tile
  store하는 곳이 네 군데(qkv 553×2, attn_out 377×2, ffn head 553×2, ffn down 1,653×2). Dummy2 크기-1 tile로의 `commit_view`는
  lir ICE(V29)였으므로, 대신 **실제 축의 2배 버퍼** `m![H % 960]`(ReducingSlices) / `m![Qs % 1024]` / `m![L % 960]`에 hi pass는
  tile 0(`H % 960 = 480 #{!} 960`), lo pass는 tile 1로 `commit_view`하고 `unsafe reshape`로 `[Dummy2, H % 480]`을 만들어 store
  1회. 스트림 축(`H % 480`에서 파생)과 tile 축(`H % 960 = 480`)의 일치 여부가 관건(타입/segment 검사). hi를 DM에서 재로드하지
  않도록 절단 split(V35: `BitAnd 0xFFF00000` → ×s, lo = s·(x − trunc x), s는 2^k라 가환)을 qkv/ffn에도 쓴다.
- **변경 파일:** `src/device/shared/f8split.rs`(`hi_lo_trunc_pair_fns!`), `src/device/shared/mlp.rs`, `src/device/sliding/projection.rs`
- **정확도:** hi/lo 합은 여전히 exact(같은 바닥). qkv/ffn의 hi가 반올림 → 절단으로 바뀌어 f32 부분합 분포만 달라짐.
- **예상:** qkv −553, attn_out −377, ffn −2.2k (≈ 기하평균 +1.3%).

### 측정: 컴파일 실패 (qkv 프로브)

- `commit_view`(pair `m![H % 960]`의 tile `H % 960 = 480 #{!} 960`) ← 스트림(`x: m![H % 480]`에서 fetch) →
  `visa: Commit: cannot write an input axis into DM (StreamUnmatchedSegment)`. 타입은 통과하나 segment 검사에서 스트림 축
  (`H % 480` 파생)과 tile 축(`H % 960 = 480`)이 다른 구조로 판정된다. 통하는 경우(attn_out contraction 타일)는 **fetch 원본도
  같은 `= n` tile view**여서 스트림이 tile 축을 그대로 갖는다.
- 우회는 x(그리고 그것을 만드는 pass의 입력·VRF 피연산자)까지 2배 버퍼의 tile view로 타입을 바꿔야 해서(normalize 마지막
  pass → cast pass → hi/lo, gamma VRF 포함) 비용 대비 과하다. pair API(`vector_intra_slice_unzip` … `*_zip`)는 두 그룹을
  이항 연산으로 **하나로 합치는** 용도라 [hi, lo] 두 출력을 내지 못한다. `TagMode::AxisToggle`은 Ident라 device fn에서 불가.
- 코드는 되돌림(브랜치에 매크로 미커밋).

### 판정: **실패** (StreamUnmatchedSegment; 단일 store는 tile-view 타입 체인 없이는 불가)

- **배운 것:** `commit_view` tile의 축은 스트림이 **fetch한 view의 축과 같은 sub-axis 구조**여야 한다. HBM→DM tile-view 로드는
  이 제약이 없으므로, HBM에서 바로 오는 텐서(attn_out x)만 싸게 tile view로 만들 수 있다 — 그러나 f8 cast + commit_view
  tile은 V29e의 lir ICE 전례가 있어 별도 검증 필요.

## V38_post_norm_store_from_reducing

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V37_ffn_upgate_pass_a_big_tiles`
- **가설:** attn_out tail(V35): normalize pass 401 → switch pass 744 → store 434 → 600; ffn tail도 같은 switch 744(163,955)가
  마지막에 있다. switch는 [H]를 `Slice`(한 슬라이스) 레이아웃으로 옮겨 1 디스크립터 store를 하기 위한 것인데, store는
  ReducingSlices(8 슬라이스 × 960 B, 8 디스크립터)에서도 ~450이다(`load_reducing`의 역방향; qkv의 x2 store와 같은 급).
  normalize pass의 `vector_final()` 뒤에 `cast::<bf16, m![H % 8 # 16]>`를 붙여 bf16 ReducingSlices 텐서를 내고 그대로 store.
  switch config 로드(`?` 719)도 DMA 큐에서 빠진다.
- **변경 파일:** `src/device/shared/rmsnorm.rs`(`normalize_add_scaled_reduced`·`normalize_add_gate_reduced`가 ReducingSlices bf16 반환), `src/ops.rs`
- **정확도:** 데이터 이동만 변경(cast 위치 동일: f32 → bf16 1회).
- **예상:** attn_out −0.7k ~ −0.9k, ffn −0.7k ~ −1.4k.

### 측정: attn_out 27,954 → **27,424** (−530), ffn 158,712 → **158,182** (−530), 기하평균 5.779

- 첫 컴파일 통과. 두 커널 모두 switch pass 744가 빠지고 store가 434 → 546(8 디스크립터)으로 바뀌어 순 −530. `normalize_add_gate`
  래퍼(full 경로용)는 자기 switch pass를 유지.
- **정확도:** 데이터 이동만 변경.

### 판정: makespan 측정 (실측 대기)

- **배운 것:** 소비자 레이아웃(한 슬라이스 [H])이 HBM store를 위한 것이라면 8-슬라이스 레이아웃에서 바로 store하는 편이
  switch(744 + config 719)보다 싸다. tail의 pass 하나 = 그대로 makespan.
- **다음:** x_hi/x_lo store 병합(qkv·attn_out·ffn), qkv x 절반(V26), ffn up/gate 30행 full-H(scale 세그먼트 반감).

## V37_ffn_upgate_pass_a_big_tiles

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V36_qkv_scales_in_head_norms`
- **가설:** ffn 스케줄의 `?` DmaLoad 838 × 13은 **LUT pass마다 다시 로드되는 4 KB 룩업 테이블**(SRAM 4 KB → Sub 1,293 스테이징 →
  LUT contraction pass)이고, 타일 로드마다 DMA 고정비 548이 붙어 타일당 ≈ 1.4k(13타일 = 18k)가 DMA 큐에 있다. up/gate가
  16행 타일인 이유는 pass B의 scale VRF(16 × 120 f32 = 7.7 KB ≤ 8 KB)이지 pass A가 아니다. pass A(LUT + f8×f8 contract →
  f32 block partial)를 큰 타일로 돌려 60행 partials 버퍼(28.8 KB/슬라이스)에 `commit_view` tile로 쓰고, pass B는 그 버퍼의
  16/16/16/12행 tile view를 읽게 분리하면 up/gate 타일 8개 → 행렬당 60행 1타일이면 2개(−6 × 1.4k ≈ −8k), 30행이면 4개(−5.5k).
  down은 tail(마지막 4행 타일)이 걸려 있어 그대로. 위험: pass A의 시간축 길이(행 × 30 × 2)에 contraction 누산기 한도가 있다면
  (16 × 60 = 960 ≤ 1024?) 큰 타일이 막힌다 → 20/30/60을 순서대로 시도.
- **변경 파일:** `src/device/shared/mlp.rs`
- **정확도:** 연산 동일(같은 partial, 같은 pass B).
- **예상:** ffn −2.8k(20행) ~ −8k(60행).

### 측정 (변형별, ffn)

| 변형 | ffn | 비고 |
|---|---:|---|
| V36 | 165,733 | up/gate pass A·B 모두 16/16/16/12 |
| V37a (공유 60행 partials 버퍼, 같은 타일) | 166,380 | 통과. 버퍼 단위 의존성으로 pass B가 pass A 전부 뒤로 밀리고 DMA 순서가 바뀜(+647) |
| V37b (pass A 20/20/20) | 163,799 | 통과 — 누산기 한도 없음 |
| V37c (pass A 30/30) | 160,580 | |
| **V37d (pass A 60: 행렬당 1 pass)** | **158,712** | 채택. DMA 큐에서 타일 6개 × (548 + 838) 제거 |
| **기하평균 (V0 대비 누적)** | | **5.736** |

- 60행 pass A = LUT-bound ~19k Main/행렬. up 로드(31k) 동안 아무것도 못 하지만 gate 로드(31k) 동안 up pass A가 돌아
  DMA-bound 유지. pass B 8개(768)는 pass A 뒤에 몰린다.
- **정확도:** 연산 동일(같은 partial·pass B). LUT/contraction 결과는 타일 경계와 무관.
- **측정 방식:** makespan only.

### 판정: makespan 측정 (실측 대기)

- **배운 것:** (1) `contract_lane` Sequential·시간 무축약 pass A에 행 수 한도는 없다(60행 × 30 × 2 = 3,600 시간 스텝 통과);
  16행 한도는 pass B의 scale VRF(8 KB)뿐이었다. (2) 타일당 비용 = DMA 고정 548 + LUT 테이블 838 ≈ 1.4k. (3) down은
  tail이 걸려 있어 타일을 키우면 Main이 못 따라간다(pass A 316/행 vs 로드 481/행 + 548: 다음 타일이 앞 타일의 0.66배
  이상이어야 함) → 5타일 유지.
- **다음:** ffn down x2 store 2회 → 1회(−1.65k), attn_out post-norm switch pass 제거, qkv x 복제(V26).

## V36_qkv_scales_in_head_norms

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V35_attnout_trunc_split`
- **가설:** qkv makespan = DMA 큐 42.7k + 공백 2.2k + 1.6k. 큐의 비-weight 항목 중 `projection.rs:124` K/V weight-scale 로드가
  각 838(2048 bf16을 512 슬라이스 × 4원소 = 8 B 디스크립터 512개), Q scale 로드 792(16 B × 512)가 Q weight 뒤·V weight 앞에
  있다. head norm은 한 head가 한 슬라이스에 있는 레이아웃(HeadSlicesPerCluster, 클러스터당 4 슬라이스)이라 scale [Qs]/[Ps]를
  `[Ns, Gs, Ds]`/`[Ns, Ds]`로 보고 그 레이아웃에 로드하면 디스크립터 8개(gamma 로드와 같은 ~555). mean-square pass는
  `MulF(Mul1, scale)` → stash → `MulF(Mul0, Stash)`, normalize pass는 `MulF(Mul1, scale)` → DivF → MulF(Mul0, gamma)(V는 → div).
  투영의 scale pass(Main 345 × 3, V 것은 tail)도 사라진다. 소비자가 tail(head norm)이라 스케줄러가 로드를 V weight 뒤에
  둘 수 있다(그러면 −2.5k). 앞에 두더라도 −0.8k.
- **변경 파일:** `src/device/sliding/projection.rs`(scale 제거), `src/device/sliding/rmsnorm.rs`(`*_heads`에 channel_scale), `src/ops.rs`
- **정확도:** 지금은 bf16(f32(bf16 합)·scale) → norm; 바꾸면 bf16 합에 f32로 scale을 곱해 바로 norm(중간 bf16 반올림 1회 제거,
  오히려 정확). 실측 검증 필요.
- **예상:** qkv −0.8k ~ −2.5k.

### 측정: qkv 46,541 → **45,744** (−797), 기하평균 5.654

- 첫 컴파일 통과. DMA busy 42.7k → 41.9k: scale 로드 838+838+792 → 555+555+561. 스케줄러는 세 로드를 여전히 V weight 앞에
  둔다(k-scale 9,878, v-scale 12,977, q-scale 33,277 — 소비자 시점이 아니라 임계경로 길이 우선순위로 보임). V weight 로드 종료
  42.0k → 41.4k, tail 4.3k(contract 1,225 → switch 328 → norm 345 → sqrt → norm 345 → scatter 929 → 600) 그대로.
- **정확도:** 중간 bf16 반올림 1회 제거(더 정확). 실측 검증 필요.

### 판정: makespan 측정 (실측 대기)

- **배운 것:** (1) DMA 비용 모델(K/Q weight, attn_out 타일 3점 적합): **≈ 548 고정 + bytes / 1.21 KB, 세그먼트 수는 무관**.
  그래서 작은 로드는 크기와 무관하게 ~555이고, 타일 분할은 타일당 548을 더 낸다(V weight 2분할: 이득 612 − 548 ≈ 0).
  qkv 큐의 비-weight DMA 15개 × ~550 고정 = 8k가 구조적 하한. (2) 복제 로드(x, 3.9 MB, 512 디스크립터)는 모델 예측 3.8k보다
  1.4k 비싸다(같은 HBM 영역을 32 디스크립터가 읽는 비용?). attn_out x2 로드 0.5 MB는 1,318(예측 980).
- **다음:** DMA 개수 줄이기 — x_hi/x_lo store 2회 → 1회(plain-axis tile commit으로 `[Dummy2, H%480]` 조립; qkv −553, attn_out
  −377, ffn −2.2k), V26(x 절반: 복제 로드가 바이트 비례라면 −2.3k, 디스크립터 비례라면 0 — 먼저 ffn down x2 로드(2 MB)로 보정).

## V35_attnout_trunc_split

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V33_attnout_immediate_scale`
- **가설:** V33/V34에서 tile0 로드 발행(2,554)은 x_lo pass의 발행 시점과 일치한다. x_lo는 x_hi(cast f8 → DM) → x_hi VRF 재로드
  (327) → 지연 200을 거쳐야 발행된다. hi를 Cast Engine 반올림 대신 VE의 비트 마스크로 만들면(bf16 x의 유효 8비트 중 상위 4비트:
  `BitAnd 0xFFF00000`; s=16이 2의 거듭제곱이라 마스크와 스케일이 가환) hi는 e4m3에 정확히 들어가고 lo = 16·(x − trunc x)는
  나머지 ≤4비트라 역시 정확(|lo| ≥ 2^-9·… 구간; 바닥은 기존과 같은 max|x|/4096). 두 pass 모두 x만 읽으므로 x_lo가 x_hi 직후에
  발행되고(≈1,950) tile0 로드가 −600 앞당겨진다. VE 스테이지 순서(Tag → Logic → Narrow → Fp)상 마스크는 곱보다 앞에 온다.
- **변경 파일:** `src/device/shared/f8split.rs`(`hi_lo_trunc_fns!`), `src/device/sliding/projection.rs`
- **정확도:** hi + lo = bf16(x)·16 exact(동일 바닥). hi가 반올림이 아니라 절단이라 hi·w + lo·w의 f32 합산에서 부분합의 크기 분포만
  달라짐(결과 동일 수학). 마스크 즉치는 NaN 비트패턴(0xFFF00000)이라 컴파일러가 비트를 보존하는지 **실측 검증 필요**.
- **예상:** attn_out −0.5k ~ −0.7k.

### 측정 (변형별, attn_out)

| 변형 | attn_out | 비고 |
|---|---:|---|
| V33 | 28,002 | x_hi(Sub) → x_hi VRF(Sub) → x_lo(Sub) 2,554 → tile0 로드 2,554 |
| V35a (절단 split, x VRF·x_hi·x_lo 모두 Sub) | 28,066 | 컴파일 통과(Logic → Narrow → Fp 한 pass, NaN 마스크 즉치 OK). Sub가 x VRF를 x_hi **뒤에** 돌려 x_lo 2,618 |
| V35b (x_hi를 Main으로) | 28,066 | x VRF‖x_hi 1,538–1,947, x_lo 2,129. 그러나 tile0 로드 2,618 = weight-scale VRF pass 발행 시점 |
| **V35c (+ V34 fold: 투영에서 scale pass 제거)** | **27,954** | x_hi store 2,129–2,506 → tile0 2,506. 채택 |
| **기하평균 (V0 대비 누적)** | | **5.621** |

- **핵심 관찰 (스케줄러 모델 수정):** tile0 로드 발행 시점은 V33·V34에서 "세 번째 Sub pass의 시작"과, V35b에서는 "x_lo 종료 후
  scale VRF pass의 시작"과 일치한다. 즉 **PE core는 명령을 한 줄로 순서대로 발행하고, 바쁜 컨텍스트에 pass를 발행할 때
  막힌다**(in-order issue). DMA 로드도 그 줄의 한 자리라서, 앞에 선 Sub pass가 컨텍스트 대기로 막히면 로드도 같이 밀린다.
  줄의 순서는 스케줄러가 정한다(소스 순서 아님).
- 남은 head: DMA 고정 지연 1,003 + x 로드 535 + hi pass 409 + 발행 ~180 + x_hi store 377 = 2,506. 이제 x2 HBM 스테이징 자체
  (store 377×2, x2 로드 1,318 = 큐 2.1k)가 다음 대상.
- **정확도:** hi=16·trunc(x), lo=16·(x−trunc x), 합은 bf16(x)·16과 동일(바닥 max|x|/4096 동일). V34 fold로 bf16 반올림 지점이
  bf16(f32합)·scale로 바뀜(상대 2^-9, 동급). 마스크 즉치 0xFFF00000(NaN 패턴)의 비트 보존 **실측 검증 필요**.

### 판정: makespan 측정 (실측 대기)

- **배운 것:** (1) VE 스테이지는 단방향(Tag → Logic → Fxp → FxpToFp → Narrow → Fp → IntraReduce → FpDiv → Widen → …)이라
  비트 연산은 fp 연산 앞에만 온다; 2의 거듭제곱 스케일과는 가환이라 절단 split이 가능. (2) `SubF`는 stream − operand.
  (3) `vector_stash`는 Tag 단계(Way8)에서도 가능하지만 Way4 fp 읽기와 폭이 달라 VRF 사본을 썼다. (4) Sub 컨텍스트는 pass를
  스케줄러가 정한 순서로 직렬 실행하므로 독립 pass는 Main/Sub로 나눠야 겹친다.
- **다음:** x2 스테이징 제거(DM→DM 복사 또는 plain-axis tile commit으로 `[Dummy2, Qs%512]` 조립), post-norm tail의 switch pass
  (744) 제거(ReducingSlices에서 직접 store), qkv(가장 덜 개선).

## V34_attnout_scale_in_rmsnorm

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V33_attnout_immediate_scale` (V27 슬롯의 계획; V27은 V26 위에 예약돼 있어 새 번호로)
- **가설:** V33 큐 선두는 x 로드 535 → **weight-scale 로드 594** → x_hi store → tile0. scale의 소비자(Sub의 scale VRF pass)가
  의존성이 없어 일찍 잡히므로 로드가 앞에 선다. 투영 epilogue에서 scale 곱(narrow_split/MulF/widen, 타일 pass 956)을 빼고
  bf16 합만 내보내면 로드가 큐 선두에서 사라지고(−594) 마지막 타일 pass가 짧아진다. scale은 post-attn RMSNorm이
  ReducingSlices(8 디스크립터)로 tail에서 로드(DMA가 노는 구간)해 mean-square pass와 normalize pass에 `MulF(Mul1)`로 접는다
  (pass당 Mul0·Mul1 각 1회 규칙 안).
- **변경 파일:** `src/device/sliding/projection.rs`(`project_output`에서 scale 제거), `src/device/shared/rmsnorm.rs`
  (`normalize_add_scaled_reduced` 추가), `src/ops.rs`
- **정확도:** bf16 반올림 지점이 바뀐다: 지금은 bf16(f32합·scale), 바꾸면 bf16(f32합)·scale(f32). 상대 오차 크기 동일(2^-9),
  값은 비트 단위로 다를 수 있음. 실측 검증 필요.
- **예상:** attn_out −0.6k ~ −0.9k.

### 측정: attn_out 28,002 → 28,002 (동일)

- 큐 선두에서 scale 로드 594는 빠졌지만 tile0 로드는 여전히 2,554에 시작: **tile0 로드는 x_lo pass의 발행 시점에 묶여 있다**
  (V33·V34 모두 x_lo pass 시작 = tile0 로드 시작 = 2,554; x_hi store가 2,404에 끝나도 무관). 즉 리스트 스케줄러는 tile0
  contract의 다른 입력(x2 체인)이 발행될 때 tile0 로드를 발행한다. 큐의 나머지는 V33과 동일(tile2 로드 종료 18,862).
- tail: store 2,510 뒤 scale·gamma 로드가 hop 공백(1,600)을 채우고 gathered 로드는 22,380+1,600 = 23,980에 시작 → norm 체인
  24,526–26,968(pass 5개 직렬, reduce 뒤 549 공백) → store 434 → 28,002. 타일 pass는 epilogue를 빼도 956(contraction 스트림 시간).
- **정확도:** bf16 반올림 지점 변경(이득 없이 리스크만) → 기각, V33 유지.

### 판정: **기각** (makespan 동일)

- **배운 것:** (1) DMA 로드 발행 시점은 "큐가 비었을 때"가 아니라 소비자의 다른 입력 체인이 발행되는 시점이다. 큐 선두를 당기려면
  **x_lo pass의 발행을 앞당겨야** 한다(x_hi DM 왕복 없이 hi/lo 둘 다 x에서 직접: 절단 마스크 `BitAnd 0xFFF00000`). (2) tail은
  hop 1,600 + norm 5-pass 직렬 2.4k + switch 744가 전부 임계 경로.

## V33_attnout_immediate_scale

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V32_attnout_f8_contraction_no_lut`
- **가설:** V32c의 attn_out 스케줄 선두: x 로드 1,003–1,538 → max x² 413 → 상수 16 패킷 410 → VRF 327 → hi 410 → x_hi store
  3,395 → **tile0 로드가 3,922에야 시작**(DMA 큐 공백 2,697→3,922 = 1.2k). 스케줄러가 x_hi store를 tile0 로드 앞에 두기
  때문이다. s=16은 상수이므로 `hi_lo` pass의 `MulF(Mul0)`에 즉치 16을 쓰면(`hi_lo_const_fns!`) max x²·상수 패킷·VRF
  스테이징 세 pass(1.15k VE + 의존 지연)가 사라지고 x_hi store가 ~2.1k에 나와 큐 전체가 앞당겨진다.
- **변경 파일:** `src/device/shared/f8split.rs`(`hi_lo_const_fns!` 추가), `src/device/sliding/projection.rs`
- **정확도:** V32와 동일(같은 상수, 같은 연산).
- **예상:** attn_out −1.0k ~ −1.3k.

### 측정: attn_out 29,318 → **28,002** (−1,316), 기하평균 5.618

- 첫 컴파일 통과. DMA 큐: x 로드 1,003–1,538 → weight-scale 로드 594 → x_hi store 2,132 → **tile0 로드 2,554**(V32c 3,922).
  DMA busy 22,393 동일(바이트 불변), VE 7.0k → 5.9k. 큐 선두에 남은 것: weight-scale 로드 594(첫 타일보다 앞), x 로드 535.
- **정확도:** V32와 동일.

### 판정: makespan 측정 (실측 대기)

- **배운 것:** DMA-bound 커널에서는 큐 선두에 선 DMA의 *생산자 체인 길이*가 그대로 makespan에 더해진다. 스테이징 pass는
  바이트가 아니라 큐 공백으로 비용을 낸다.
- **다음:** weight-scale 로드(594)를 큐 선두에서 빼고 epilogue MulF를 없애는 V27 계획을 V33 위에서 실행(V34).

## V32_attnout_f8_contraction_no_lut

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V31_qkv_f8_contraction_no_lut`
- **가설:** attn_out(DMA 23.6k/30.0k)은 20행 타일 3개를 융합 LUT+contract(1,863, LUT-bound)로 처리하고 pass마다 `?` 838이 큐에
  선다(2.5k). x([Qs] bf16, 8 KB)를 작은 레이아웃(8슬라이스×512)에 올려 max x²·2^k·hi/lo를 만들고(V29 head 체인) HBM
  `[Qs / 512, Dummy2, Qs % 512]`에 쓴 뒤 슬라이스별 청크를 f8 두 조각으로 로드(지금의 x 청크 로드와 디스크립터 수 동일)하면
  타일 pass가 f8×f8 contraction(~1k)이 되고 `?`가 사라진다. 결과는 2^k배지만 post-attn RMSNorm이 흡수(eps만 변화).
- **변경 파일:** `src/device/sliding/projection.rs`, `src/device/shared/f8split.rs`(V29/V31의 스케일·hi/lo 매크로를 공유 모듈로 이동), `src/ops.rs`
- **예상:** attn_out −2k ~ −2.5k (DMA −2.5k `?` + 1.1k store; tail pass −0.9k).

### 측정 (변형별, attn_out)

| 변형 | attn_out | 비고 |
|---|---:|---|
| V31 | 30,037 | 융합 LUT 타일 3개, `?` 838×3 |
| V32a (동적 2^k: max x² intra/inter reduce → sqrt → DivF → BitAnd → DivF, hi/lo, HBM 2회 store) | 30,342 | **+0.3k.** DMA 23.6k → 22.4k인데 x 스테이징 체인(8슬라이스 VE pass 7개 + inter reduce + store 2 + load)이 타일 로드보다 앞에 서서 타일 contract 시작이 늦어짐 |
| V32b (상수 s=16을 `vector_narrow_trim`으로 만듦) | 실패 | `pruning valid value is not allowed`: 전부 live인 패킷은 narrow_trim 불가 |
| **V32c (상수 s=16을 max x² 패킷(`1 # 8`, 4 live)에서 MulF 0 → AddF 16으로; inter reduce·pow2 체인 제거)** | **29,318** | 채택. DMA busy 22,393. 기하평균 5.533 |

- **상수 스케일의 근거:** attention 출력 = Σ_j p_j v_j, Σ p_j = 1, p_j ≥ 0이고 v_j는 head RMSNorm(gamma 없음) 출력이라
  rms(v_j) ≈ 1 → 원소 절댓값 ≤ √Ds = 16(Ds = 256; 등호는 한 원소에 에너지가 몰릴 때). 따라서 |x·16| ≤ 256 < 448(e4m3 max),
  hi/lo 분해는 max|x|/4096 이상에서 exact. 일반적으로 |x| ≪ 16이라 s=16은 동적 2^k보다 작을 수 있어(정밀도 손실 구간이
  4096분의 1 → 절대오차 ≤ 2^-10/16 = 6e-5) **실측 정확도 검증이 필요**하다. 동적 스케일(V32a)이 필요하다면 +1k 비용.
- **컴파일러 교훈 (신규):** `vector_narrow_trim`은 live 원소를 잘라내지 못한다(패딩 lane만 제거). 상수 패킷은 기존 패킷에
  `MulF(Mul0, 0)` → `AddF c`로 만든다. `max_square_fns!`/`hi_lo_fns!`는 슬라이스당 원소 수를 인자로 받도록 일반화해
  `shared/f8split.rs`로 옮겼다(`#[macro_export]`).
- **정확도:** hi + lo = bf16(x)·16 exact(|x| ≥ max/4096 구간); post-attn RMSNorm이 16을 흡수(eps 항만 1/256). 실측 검증 필요.

### 판정: makespan 측정 (실측 대기)

- **배운 것:** 이 커널의 x 준비 체인은 타일 로드와 같은 DMA 큐 선두를 두고 경쟁한다. 스테이징 pass 수를 줄이는 게 DMA 바이트를
  줄이는 것보다 컸다(V32a→V32c −1k). 남은 것: store 2,510(HBM `[H]` 8 KB, 256 desc?) + `[H]` hop, weight-scale 로드 1,318 배치(V27).
- **다음 후보:** V27(weight-scale 로드를 큐 선두에서 빼기), attn_out tail(store + hop), V26 qkv H-split, ffn down x2 store 1회.

## V29_ffn_block_scale_after_contract

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V28_ffn_tile_shapes`
- **가설:** FFN Main/VE 121k 중 dequant pass가 91k(4-lane f32 MulF, 3.8 elem/cycle)이고 down 단계는 이 때문에
  VE-bound다. contraction engine의 Packet Reducer는 입력 패킷(32/64 B)을 접두 축약할 수 있으므로
  (`config_contract_packet`: `m![L % 32]` → `m![L / 16 % 2]`), 시간 축약 없이(`contract_time::<m![R, L / 32 % 60]>`)
  `contract_lane::<…, m![L / 16 % 2 # 8]>(Sequential)`로 **16열 블록별 partial sum**(f32)을 얻어
  `commit_trim::<m![L / 16 % 2]>`(8 B)로 DM에 조밀 저장한다(pass A: LUT f4→f8→bf16을 fetch에 융합, VE 없음).
  pass B는 partial `m![R, L / 128 % 15], m![L / 16 % 8]`(원소 수 1/16)을 fetch해 **기존 scale VRF 레이아웃 그대로**
  MulF → `vector_intra_slice_reduce::<L, …>` → widen_pad → inter-slice reduce(8 chunk) → bf16 transpose commit.
  수학적으로 Σ_b s_b Σ_{j∈b} w_j x_j = 현재 Σ_j (w_j s_b) x_j; bf16(w·s)는 유효 6비트라 현재도 exact이므로 차이는
  f32 합산 순서뿐.
- **예상:** VE 121k → ~45k. down 단계가 DMA-bound가 되어 makespan ≈ 160k (−8k).
- **변경 파일:** `src/device/shared/mlp.rs`(전면 재구성), `src/device/shared/rmsnorm.rs`(`normalize_reduced_f32` 추가), `src/ops.rs`

### 실제 설계 (구현하며 바뀐 것)

- fetch adapter는 f4→f8(paired LUT)과 f8→bf16(non-paired LUT)을 **한 fetch에 연달아 걸 수 없고**(`CanApplyFetchTableLookup`은
  첫 fetch 위치에서만), `FetchCast<bf16> for f8`도 없다 → bf16 스트림은 DM 사본을 거쳐야 하고 그러면 LUT pass가 2개(`?` 838
  DMA ×2). 대신 **f8 × f8 contraction**(`ContractionWeight<f8e4m3>`, f32 누산)을 쓴다: x = hi + lo, hi = f8e4m3(x·s),
  lo = f8e4m3(x·s − hi). bf16 x(유효 8비트)·s(2^k)는 4비트짜리 두 조각으로 정확히 분해된다(|x·s| ≥ 2^-5, 즉 max|x|/4096
  이상에서; 그 아래는 e4m3 subnormal이라 ≤ 2^-10·(1/s) 절대오차). TRF 원소는 `m![Dummy2, C % 1920]`, contraction fetch의
  시간축에 `Dummy2`를 넣어 weight 패킷을 두 번 스트림하면(`fetch::<m![R, C / 64 % 30, Dummy2], m![C % 64]>`) `contract_time`이
  Dummy2를 축약해 hi·w + lo·w를 f32로 더한다(accumulator 32 cell). LUT pass는 타일당 1개 그대로.
- **동적 스케일 s**: 픽스처의 geglu 출력은 ~1e-3라 e4m3(정상 최소 2^-6)에 그대로 넣으면 정밀도가 무너진다. s = 2^⌊log2(128/√max x²)⌋
  (max|x·s| ∈ (64, 128])를 벡터별로 계산: max x²(intra reduce Max → inter reduce Max), sqrt, `DivF Mode10 128`, `BitAnd +Inf`
  (지수만 남김), `DivF Mode10 1`. head는 rmsnorm의 ReducingSlices(8슬라이스, inter reduce 가능)에서, down 입력은 geglu 출력의
  gathered 레이아웃에서 슬라이스별 max를 ring-256 switch로 모아 클러스터별 s_c. 1/s는 geglu 스칼라(s_gate/(s√2), s_up·s_gate/(2s²))에,
  1/s_c는 down pass B의 MulF(Mul1)에 접는다(HBM `[L / 7680, 1 # 8]` → 슬라이스 2개 로드 → 순열 broadcast switch).
- pass A: `fetch(f4) → LUT f8 → collect 32 B → contract_outer(64 B 패킷) → contract_packet::<m![C / 16 % 4]> → contract_time(무축약, Dummy2만)
  → contract_lane::<…, m![C / 16 % 4 # 8]>(Sequential) → commit_trim 16 B` → f32 partial `m![R, C / 16 % 120]` 조밀. 16행 타일 5,063
  (LUT 12.1 elem/cycle, 스트림 2배). pass B: 기존 scale VRF 레이아웃 그대로 MulF → `intra_slice_reduce::<C>` → widen → inter-slice
  reduce → transpose commit (16행 822).
- x2 HBM 스크래치는 `[C / 1920, Dummy2, C % 1920]`로 두어 슬라이스당 1세그먼트 로드(`[Dummy2, C]`면 2세그먼트라 5,637).

### 측정 (변형별, ffn)

| 변형 | ffn | 비고 |
|---|---:|---|
| V28 | 168,757 | |
| V29b (첫 설계, DM에서 hi/lo 조립) | 161,356* | 스케줄은 나오나 lir ICE(`no entry found for key`) — Dummy2 크기-1 tile에 `commit_view` |
| V29e (HBM tile store로 조립, 스케일 없음) | 172,342* | lir ICE — Slice 레이아웃에서 f8 cast를 1920 tile에 commit |
| E6 (down만 새 경로, up/gate 구 경로) | 164,900 | 통과. down 단계 VE-bound 해소 확인 |
| V29f (head hi/lo를 ReducingSlices에서, 스케일 없음) | 166,587 | 통과. DMA +5.4k(x2 로드 2세그먼트 5,637, store 2회) |
| **V29h (동적 스케일, `[C/1920, Dummy2, C%1920]`, 스칼라 switch broadcast)** | **165,733** | 채택. DMA 158.0k / Main 76k / VE 20k |
| V29k (스칼라 2개를 DM tile-view DMA로 모아 switch 1회, inv_s 직접 로드) | 166,124 | DMA −1.1k인데 makespan +0.4k |
| V29l (V29h + inv_s 512-desc 직접 로드) | 166,733 | +1k |
| **기하평균 (V0 대비 누적)** | | **5.324** |

\* ICE 전에 덤프된 스케줄의 값(유효 커널 아님).

- **정확도:** 수학적으로 Σ_b s_b Σ_{j∈b} w_j x_j = 현재와 동일(f32 합산 순서만 다름). hi/lo 분해는 max|x|/4096 이상에서 exact,
  그 아래 원소는 ≤ 2^-10/s 절대오차(픽스처 x∈[0,1.7], geglu 출력 ~1e-3 모두 s가 흡수). **실측 정확도 검증 필요**(hard gate).
  s 계산의 padded 패킷(4/8 live)에서 padding lane이 0으로 채워진다는 가정(Max reduce)이 있다.
- **측정 방식:** makespan only. 이득이 예상(−8k)보다 작은 이유: switch pass마다 `?` DmaLoad 838이 붙고(스칼라 broadcast·cluster
  max·순열 broadcast 4개 = +3.4k), x2 store가 phase당 2회(head 553×2, down 1,653×2), 스칼라 store/load. DMA 152.7k → 158.0k.
- **컴파일러 교훈 (신규):** (1) `commit_view`를 크기-1 tile(`m![Dummy2], 1`)에 쓰면 lir ICE; 같은 tile로의 **HBM store/load와
  DM tile-view로의 DMA 로드는 된다**. (2) 다른 하위구조의 같은 축(`L % 3840 = 960` 스트림 → `L % 7680 = 960` tile)으로 commit 불가
  (`StreamUnmatchedSegment`). (3) `HbmTensor<[Dummy2, 1 # 8]>`의 tile store는 lowering에서 버퍼 크기 검사 실패. (4)
  `TagMode::AxisToggle`은 `Ident`(문자열 상수)라 device 함수에서 불가; pair API(`vector_intra_slice_unzip`)는 두 그룹을 zip해 하나로
  합치는 용도. (5) `contract_packet` 입력은 32/64 B, `contract_lane`은 [Lane, Packet] 누산기 1024 cell. (6) fetch 시간축에 텐서에
  없는 Dummy 축을 넣으면 복제 스트림. (7) `vector_fp_div_with_mode(BinaryArgMode::Mode10, c)` = c / stream;
  `vector_logic(LogicBinaryOpF32::BitAnd, f32::INFINITY)` = 지수 마스크. (8) switch pass 하나당 `?` DmaLoad 838.

### 판정: makespan 측정 (실측 대기)

- **배운 것:** VE 병목을 없애도 이 커널은 DMA 큐(158k)가 makespan을 정하고, 새 경로가 만든 소형 DMA(switch `?`, store 2회)가
  이득을 갉아먹는다. 다음은 DMA 바이트/디스크립터 자체(scale 29k, hop 3.3k+2.5k)와 tail 10k.
- **다음 후보:** down x2 store 2회 → 1회(조립 방법 필요), post-norm hop, 그리고 qkv(V26 H-split, 가장 뒤처진 커널).

## V13_ffn_dma_trims

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V12_ffn_rows_per_pass_12`
- **가설:** V12에서 down 구간은 타일당 DMA 9.5k(weight 6.3k + scale 2.4k + `?` 0.8k) vs Main 7.75k로 DMA-bound.
  DMA 큐에서 줄일 수 있는 것: (a) geglu 출력의 DM→DM relayout 11,687(mlp.rs:364, down 타일 로드 사이에 끼어
  Main을 5k 멈춤) → V7처럼 HBM에 30 KB 쓰고 ByColumns로 로드(예상 ~3k); (b) scale 타일 로드 20×2,716 + 10×2,394
  = 51k — 120~240 B 행이라 300~540 B/cycle. 행렬당 한 번(14 KB/슬라이스)이면 V0 실측 8.3k×2 + V6c 19k = 36k.
  단, 큰 로드가 첫 타일 앞에 오면 head가 늘 수 있다(V6d에서 4행 타일 때는 상쇄됨).
- **변경 파일:** `src/device/shared/mlp.rs`
- **공유 코드 영향:** vision/audio MLP 경로
- **예상:** (a) −7k, (b) −10k~−15k.

### 측정

| 단계 | ffn makespan | 판정 |
|---|---:|---|
| (a) geglu → HBM 스크래치 → ByColumns 로드 | 352,164 → **348,874** | 채택 |
| (b) + scale 행렬당 1회 로드 (`.view().tile()`로 pass별 참조) | 354,617 | **기각** — DMA busy 273k→258k인데 스케줄러가 8.3k scale 로드를 첫 타일 앞에 배치, Main 시작 15k→20.6k |

- **컴파일 교훈:** 이미 행 타일된 뷰에 열 타일을 다시 걸 때는 매핑에 바깥 패딩을 함께 적는다
  (`m![L % 60 = 12 # 60, H / 16 = 120 # 240]`), 아니면 `Output shape mismatch for IndexAccess`.
- **판정:** makespan 측정 (실측 대기), 누적 기하평균 2.866.

## V15_x_replicate_hbm_copies

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V14_two_clusters`
- **가설:** qkv의 `x_hbm.to_dm()`(Replicated, 512 슬라이스)는 7.7 MB를 18,400 cycle에 옮겨 420 B/cycle —
  같은 바이트 수의 down 타일 로드(930 B/cycle)의 절반이다. 디스크립터 수는 같으므로 원인은 **512개
  디스크립터가 전부 같은 7.5 KB HBM 구간을 읽는 것**(book: "channel interleaving: spread across 32 channels";
  7.5 KB는 채널 몇 개에만 걸친다)으로 추정. x를 `HbmTensor<bf16, Chip, m![Dummy8, H]>`에 8부 쓰고
  (8 디스크립터), DM 슬라이스 매핑 `m![Dummy32, Dummy8]`로 슬라이스마다 다른 사본을 읽게 하면 채널이
  분산된다.
- **변경 파일:** `src/ops.rs` (qkv 본문), 필요 시 `layout.rs`
- **리스크:** HBM 쪽 Dummy 축과 DM 쪽 Dummy 축의 대응을 DSL이 "사본 선택"으로 해석하는지 미지수.
- **예상:** qkv −9k (18.4k → ~9k).

### 측정

| 커널 | makespan (before → after) | RNGD cycles | speedup |
|---|---|---|---:|
| `sliding_project_qkv` | 73,445 → **60,412** | — | 1.216 |
| **기하평균 (V0 대비 누적)** | | | **4.434** |

- x 복제 DmaLoad **18,400 → 5,367** (util 0.157 → 0.539). `HbmTensor<bf16, Chip, m![Dummy8, H]>`에 8부 쓰고
  DM 슬라이스 매핑 `m![Dummy256 / 8, Dummy8]`로 로드(HBM에 있는 Dummy 축은 사본 선택, 없는 축은 복제) 후
  `unsafe reshape`로 `Replicated` 타입으로 되돌림. axes.rs는 채점에서 무시되므로 새 Dummy 축을 추가하지 않고
  `Dummy256 / 8`로 32를 만들었다.
- **정확도:** 수치 동일. **측정 방식:** makespan only.

### 판정: makespan 측정 (실측 대기)

- **배운 것:** 복제 로드의 병목은 디스크립터 수가 아니라 **같은 HBM 구간의 반복 읽기**(채널 집중)였다.
  attn_out(x 2k)과 ffn(x 3.4k)의 청크 로드도 같은 원리가 적용될 수 있으나 절대량이 작다.

## V17_qkv_hoist_weight_loads

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V16_rmsnorm_fused_residual`
- **동기:** 기하평균 점수에서 커널별 배율이 qkv 1.93× / attn_out 5.58× / ffn 9.06×로 qkv가 가장 뒤처진다
  (사용자 지시: 덜 개선된 커널에 집중).
- **가설:** V16 qkv 타임라인(60.4k): 0–6.6k rmsnorm, 6.7–12k x 복제 로드, **12–25.5k Q weight 로드**, 26–33k K,
  37–44k V, 이후 rope/rmsnorm tail 11k. weight 로드는 x와 무관한데 소스에서 `project_query` 안에서 발행되어
  x 뒤에 줄을 선다. 스케줄러는 소스 순서상 앞의 DMA를 뒤로 미루지 않으므로(V13b) 세 weight `to_dm`을
  rmsnorm 앞에서 발행하면 Q 로드 13.5k가 rmsnorm·x 스테이징(6.6k)과 겹치고 K/V도 연달아 흐른다.
- **변경 파일:** `src/device/sliding/projection.rs`(로더 함수 분리), `src/ops.rs`(qkv 본문 순서)
- **예상:** qkv 60.4k → ~50k.

### 측정

| 변형 | qkv | 비고 |
|---|---:|---|
| (a) 세 weight `to_dm`만 rmsnorm 앞으로 이동 | 60,412 | **불변** — 스케줄러는 발행 순서가 아니라 소비자 기준으로 DMA를 배치 |
| (b) Q만 LUT pass 분리(소비자를 rmsnorm 앞에 발행), contraction은 bf16에서 | **59,216** | Q 로드 3.6–17.1k로 선두, LUT 5k가 x 로드와 겹침. 그러나 K 로드는 여전히 32k 시작(7.7k 공백) |
| (c) K/V도 LUT 분리 + x_trf를 그 앞에 스테이징 | 60,375 | K/V 로드·LUT가 Q contract 뒤로 밀림 — Main도 프로그램 순서를 따르지 않음 |

- **채택:** (b). 기하평균 4.649.
- **배운 것:** 스케줄러는 DMA도 Main도 리스트 스케줄링(자체 우선순위)이며 소스 순서로 제어되지 않는다.
  DMA를 앞당기려면 **그 DMA를 소비하는 명령이 의존성 그래프상 일찍 필요해져야** 한다. qkv의 잔여
  59k는 DMA 36k(Q 13.5 + x 5.4 + K 7 + V 7 + 소량) + 마지막 로드 뒤 tail(V pass·normalize·scatter ≈ 8k)
  + 스케줄러 slack(~10k: q/k/v rmsnorm·rope 소형 pass들이 직렬). 하한은 ~50k.

## V19_qkv_tail_heads_layout

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V18_attnout_scale_in_epilogue`
- **동기:** qkv가 기하평균상 가장 뒤처짐(1.97×). V17 타임라인의 마지막 로드 뒤 tail 13k는 q/k/v 후처리 pass
  10여 개(각 0.3~1.3k)가 **슬라이스 1개**(Slice 레이아웃)에서 직렬로 도는 것 + K의 HBM hop이 V 로드 뒤에 막힌 것.
- **가설:** rope는 이미 `KvHeadsAcrossSlices = m![1 # 32, Ns]`(헤드당 슬라이스 1개)로 옮겨서 계산한다.
  투영 결과를 HBM hop에서 곧장 그 레이아웃으로 로드하고, q/k/v rmsnorm을 그 레이아웃용으로 다시 쓰고
  (`normalize_*_heads`; Ds 축약이 슬라이스 안에서 끝남), rope의 앞 InterTranspose(q 2.3k, k 1.3k)와 뒤 Broadcast1을
  없애며, q 저장·k/v scatter를 그 레이아웃에서 직접 하면 pass당 데이터가 1/8이 되고 전환 pass 4개가 사라진다.
- **부수 실험:** V19a — K/V 투영 순서를 바꿔 tail 길이를 조정하려 했으나 스케줄이 완전히 동일(스케줄러는 소스
  순서 무시). 기각.
- **변경 파일:** `src/device/layout.rs`(`HeadSlices`), `src/device/sliding/rmsnorm.rs`(함수 추가),
  `src/device/sliding/rope.rs`(`apply_rope_heads` 추가), `src/device/sliding/projection.rs`(반환 레이아웃), `src/ops.rs`
- **예상:** qkv −6k ~ −8k.

### 측정

| 커널 | makespan (before → after) | RNGD cycles | speedup |
|---|---|---|---:|
| `sliding_project_qkv` | 59,216 → **55,811** | — | 1.061 |
| **기하평균 (V0 대비 누적)** | | | **4.919** |

- 첫 컴파일 통과. 후처리 pass들이 1,304/1,287 → 345~409 cycle로 줄었고 InterTranspose 2개·Broadcast1 2개가
  사라졌다. 남은 tail(V 로드 종료 45.5k → 55.8k)은 V contract 2.7k + V scale 1k + **K/V의 HBM hop이 V weight
  로드 뒤에 DMA 큐에서 막혀** k 후처리가 48.4k에야 시작하는 것 + scatter 2개.
- **정확도:** 연산 동일(레이아웃만 변경).

### 판정: makespan 측정 (실측 대기)

## V20_qkv_tail_per_cluster

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V19_qkv_tail_heads_layout`
- **가설:** q/k/v 결과를 한 클러스터로 모으는 HBM hop(각 store+load ≈ 1.5k, 그리고 DMA 큐에서 큰 weight 로드
  뒤에 막힘)을 없앤다. 각 클러스터는 이미 4개 헤드(2048/1024 = 4 × Ds)를 갖고 있으므로, 클러스터 안에서
  ring-64 `Broadcast1{slice1: 64}` switch(64 슬라이스 × 8행 → 슬라이스당 1헤드, ~64 cycle)로 헤드/슬라이스
  레이아웃 `(m![Ns / 4], m![Ns % 4, 1 # 64])`을 만들고, `normalize_*_heads`·`apply_rope_heads`를 (C, S) 제네릭으로
  바꿔 두 클러스터에서 4헤드씩 병렬 처리한 뒤, q 저장·k/v scatter를 그 레이아웃에서 직접 한다. cos/sin은
  HBM 스크래치를 거쳐 양 클러스터에 복제.
- **변경 파일:** `src/device/sliding/{projection,rmsnorm,rope}.rs`, `src/ops.rs`
- **리스크:** 두 클러스터 레이아웃에서의 `dma_scatter`/`to_hbm_view`; padded `1 # 64` 슬라이스 축에서의 vector pass.
- **예상:** qkv −5k (~50k).

### 측정

| 커널 | makespan (before → after) | RNGD cycles | speedup |
|---|---|---|---:|
| `sliding_project_qkv` | 55,811 → **51,631** | — | 1.081 |
| **기하평균 (V0 대비 누적)** | | | **5.048** |

- 첫 컴파일 통과. ring-64 gather는 스케줄에 보이지 않을 만큼 작다. DMA 91% busy(47k) — qkv는 이제 DMA-bound.
  V 로드 종료(43.6k) 뒤 tail 8k: V contract 2.7k + cos/sin gather(43.7k, V 로드 뒤에 큐잉됨)를 기다리는 q/k rope
  + scatter 2개.
- **정확도:** 연산 동일. `dma_scatter`/`to_hbm_view`가 두 클러스터 레이아웃에서 컴파일됨(실측으로 확인 필요).

### 판정: makespan 측정 (실측 대기)

## V22_attnout_weight_tiles

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V21_qkv_rope_tables_early` (= V20 + rope 별칭 수정; V21 코드는 되돌림)
- **가설:** attn_out(31.1k)은 weight 로드 13.5k 뒤에 융합 LUT+contract 5k가 직렬로 붙는다. V10처럼 행을 타일로
  나눠 전부 선로드하면 타일 k의 pass가 타일 k+1의 로드와 겹친다. 두 클러스터 레이아웃(슬라이스당 60행)에서
  20행 × 3타일(4의 배수 ✓, `= 20 / 4` transpose). 타일당 `DmaLoad ?` 838이 2개 늘지만 DMA가 임계가 아니어서 흡수.
- **변경 파일:** `src/device/sliding/projection.rs`
- **예상:** attn_out −3k (~28k).

### 측정: attn_out 31,113 → **30,487** (−0.6k), 기하평균 5.082

- 타일 DMA 4,870×3 = 14.6k(통짜 13.5k보다 1.1k 많음) + 타일 사이 `DmaLoad ?` 838 ×2 → 겹침 이득 3.2k 중 2.6k 상쇄.
  채택하되 기대에 못 미침.

### 판정: makespan 측정 (실측 대기)

## V23_ffn_whole_scale_loads

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V22_attnout_weight_tiles`
- **가설:** V14 이후 FFN DMA 173k 중 scale 타일 15 × 2,394 = 36k(11 MB, 슬라이스당 12행 × 120 B 조각 → 300 B/cycle).
  행렬당 한 번(슬라이스당 60 × 120 B = 7.2 KB, 3.7 MB)이면 ~5k씩 3회 = 15k. V13b는 단일 클러스터·첫 타일 앞 배치로
  head가 8k 늘어 실패했지만, 지금은 첫 타일이 6.3k이고 up/gate 두 행렬의 scale 로드가 각 ~5k라 판단 재시도.
- **변경 파일:** `src/device/shared/mlp.rs`
- **예상:** ffn −10k ~ −20k (또는 head 증가로 무효).

### 측정: ffn 186,976 → **181,625** (−5.4k), 기하평균 5.131

- 행렬당 scale 로드는 9,774(3.7 MB → 380 B/cycle: 120 B 행 조각이라 여전히 느림) ×3 ≈ 29k vs 36k. 이번엔 첫
  타일(6.3k) 뒤에 배치되어 head 증가 없음. DMA busy 173k → 166k.

### 판정: makespan 측정 (실측 대기)



## V21_qkv_rope_tables_early

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V20_qkv_tail_per_cluster`
- **가설:** rope의 cos/sin gather(934×2)와 HBM hop이 V weight 로드 뒤에 DMA 큐잉되어 q/k rope가 46.6k에야
  시작한다. gather→hop→VRF 스테이징을 커널 맨 앞(Q LUT 뒤)에서 발행하면 Sub이 한가해 일찍 잡히고(V17에서
  Main 소비자가 DMA를 앞당긴 것과 같은 원리) q/k rope가 V 로드 중에 끝나 tail이 V 경로만 남는다.
- **변경 파일:** `src/device/sliding/rope.rs`(`stage_rope_tables`/`RopeTables`), `src/ops.rs`
- **예상:** qkv −2k ~ −3k.

### 측정: **51,631 → 51,631 (불변) — 기각**

- cos gather는 18k로 앞당겨졌으나 sin gather는 여전히 43.7k(V 로드 뒤). 순서를 바꿔도(sin 먼저) 첫 번째 것만
  앞당겨진다 — 스케줄러가 weight 로드 앞에 두는 소형 DMA를 하나로 제한하는 듯. 코드는 되돌렸다.
- **부수 발견:** device 함수 안에서 **사용자 struct 생성은 ICE**(`not yet implemented: RopeTables { .. }`) —
  여러 값을 돌려줄 때는 튜플을 쓴다.
- V20 커밋의 rope.rs에 `HeadSlices` import 정리로 생긴 컴파일 오류가 있었다 → 이 브랜치와 V20 브랜치에 수정 커밋.




## V18_attnout_scale_in_epilogue

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V17_qkv_hoist_weight_loads`
- **가설:** attn_out tail(13k/34.8k)에서 채널 scale pass가 1920 타일 2개(Sub 743 + Main 761 ×2 ≈ 3k)를 쓴다.
  contraction 체인은 `contract_lane → vector_init → inter_slice_reduce → vector_final`로 끝나는데, book이
  InterFirst(reducer 뒤 intra chain) 순서를 지원하므로 reduce 뒤에 `narrow_split → MulF(scale_vrf) →
  widen_concat`을 붙이면 slice당 60행의 scale 곱이 epilogue에서 끝나 tail pass가 사라진다. scale VRF는
  행당 1값을 `m![H % 60, 1 # 8]` 패딩 패킷으로 스테이징.
- **변경 파일:** `src/device/sliding/projection.rs`
- **리스크:** contract 출력 패킷(`1 # 8`)의 narrow_split 분해(`m![H % 60, 1 # 2], m![1 # 4]`)와 scale VRF
  매핑 일치가 미검증.
- **예상:** attn_out −3k (8.6%).

### 측정

| 커널 | makespan (before → after) | RNGD cycles | speedup |
|---|---|---|---:|
| `sliding_attention_output` | 34,776 → **31,113** | — | 1.118 |
| **기하평균 (V0 대비 누적)** | | | **4.822** |

- 첫 시도에 컴파일. scale VRF: DM `m![H % 60]`을 `fetch::<m![H % 60], m![1 # 8]>() → fetch_cast → collect`로
  행당 패딩 패킷 스테이징; epilogue `inter_slice_reduce → intra_slice_tag → narrow_split::<m![H % 60, 1 # 2],
  m![1 # 4]> → MulF → widen_concat::<m![H % 60], m![1 # 8]> → vector_final`. 같은 기법을 qkv의 Q/K/V scale
  pass(각 ~1k+)에도 적용 가능 → V19 후보.
- **정확도:** scale을 f32 단계에서 곱하므로 이전(bf16 재로드 후 곱)보다 정밀.

### 판정: makespan 측정 (실측 대기)



## V16_rmsnorm_fused_residual

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V15_x_replicate_hbm_copies`
- **가설:** attn_out tail 16k와 ffn tail에는 rmsnorm(ReducingSlices 레이아웃, Main pass 4개) 뒤에 residual add
  (1920 타일 2개: Sub 743 + Main 521 ×2 ≈ 2.5k)와 ffn의 layer gate pass(~1.2k)가 별도로 붙는다. rmsnorm의
  마지막 정규화 pass는 이미 vector 체인(DivF rms, MulF weight)이므로 residual VRF(480 f32 = 1.9 KB)를
  `vector_clip(Add)`로, gate를 `MulF(Mul1)`로 접어 넣으면 tail pass들이 사라진다. `normalize`는 그대로 두고
  `normalize_add`, `normalize_add_gate`를 추가(full/vision/audio 경로 무영향).
- **변경 파일:** `src/device/shared/rmsnorm.rs`(함수 추가), `src/ops.rs`(attn_out·ffn 본문)
- **예상:** attn_out −2.5k, ffn −3.5k.

### 측정

| 커널 | makespan (before → after) | RNGD cycles | speedup |
|---|---|---|---:|
| `sliding_attention_output` | 38,240 → **34,776** | — | 1.100 |
| `decoder_feedforward` | 191,122 → **186,976** | — | 1.022 |
| **기하평균 (V0 대비 누적)** | | | **4.618** |

- **컴파일 교훈:** `vector_clip`은 `vector_narrow_split` 이전(8-wide) 단계 전용 — 정규화 곱셈 뒤에는
  `vector_fp_binary(FpBinaryOp::AddF, &vrf)`를 쓴다(`no method named vector_clip ... Way4`).
- **정확도:** 연산 순서 동일(정규화 → ×w → +residual → ×gate), 중간 bf16 반올림이 한 번 줄어 오히려 정밀.
- **측정 방식:** makespan only

### 판정: makespan 측정 (실측 대기)



## V14_two_clusters

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V13_ffn_dma_trims`
- **가설:** `src/device/layout.rs`의 `Cluster = m![1 # 2]`는 클러스터 축 "1 live + 1 패딩"이다. 즉 세 커널
  모두 칩의 두 클러스터 중 하나(256 슬라이스, DM 128 MB, DMN 8개 = 1 KB/cycle)만 쓴다. 관측 DMA 최고치
  ~590 B/cycle(256 슬라이스 기준)이 1 KB/cycle 한도 아래에 있는 것과 부합. 행(또는 열)을 클러스터 축
  `m![H / 1920]` 같은 실제 분할로 바꾸면 슬라이스당 작업이 절반, DMA 대역폭 2배가 될 수 있다.
  프로브: attn_out을 Cluster `m![H / 1920]` × Slice `m![H / 60 % 32, Qs / 512]`(60행 × 512열, 8-way reduce)로.
- **변경 파일:** `src/device/sliding/projection.rs` (프로브), 성공 시 `layout.rs`·`mlp.rs`·rmsnorm 등 전반
- **리스크:** 클러스터 축의 실제 분할을 DSL/DMA가 지원하는지, 클러스터 간 gather(`to_dm` → `Cluster 1#2`)가
  되는지 미지수. `#[device(chip = 1)]`과 HBM 텐서의 `Chip` 매핑은 그대로.
- **예상:** 성공 시 attn_out weight DMA 29k → ~15k, Main 절반. 세 커널 전부에 적용 가능.

### 측정 (단계별, 브랜치 안에서 누적)

| 단계 | 변경 | qkv | attn_out | ffn |
|---|---|---:|---:|---:|
| V13 | (출발점) | 93,127 | 50,110 | 348,874 |
| attn_out | Cluster `H/1920` × Slice `H/60 % 32, Qs/512`, 60×512/슬라이스, 8-way reduce, HBM gather | | **38,240** | |
| ffn down | Cluster `H/1920` × Slice `H/60 % 32, L/1920`, 12행 타일 5개 선로드 | | | 291,679 |
| ffn up/gate | Cluster `L/7680` × Slice `L/60 % 128, H/1920`, 60×1920/슬라이스(dequant 1회), 2-way reduce, HBM으로 geglu 레이아웃 복귀; x는 슬라이스당 절반만 | | | **191,122** |
| qkv v1 | Q 8행·K/V 4행 두 클러스터, 결과를 512 슬라이스에서 직접 HBM 저장 | 176,882 (악화) | | |
| qkv v2 | x를 `BothClusters=m![Dummy2]`로 1회 로드해 reshape 공유; 클러스터 안 switch gather 후 HBM 2 디스크립터 | **73,445** | | |
| **기하평균 (V0 대비 누적)** | | | | **4.147** |

- **결정적 관측:** 같은 12행 down 타일 로드가 256 슬라이스 때 6,326 cycle, 512 슬라이스(2배 바이트) 때도
  6,326 cycle → **DMA 처리량이 정확히 2배**. attn_out weight 15.7 MB 전체가 13,512 cycle(util 0.86).
- **제약:** 클러스터 간 DM→DM DMA는 `synchronization_checker`가 거부(`T13 is used by DmaCommand#O8, while
  not synchronized from other clusters`); `DmTensor::to_dm`은 Cluster 타입을 못 바꾸고(`to_dm_view`만 가능).
  **HBM 경유 gather는 통과**한다. 작은 조각(슬라이스당 8~16 B)을 512 슬라이스에서 HBM으로 직접 쓰면
  디스크립터 비용으로 37k — 먼저 클러스터 안에서 switch로 모아 클러스터당 1개 조각으로 쓴다.
- **정확도:** 수치 동일(행 분할·partial 합산 f32). 전체 테스트 바이너리 빌드 통과.
- **측정 방식:** makespan only
- **지배 context:** qkv DMA 84% (x 복제 18.4k, Q 13.5k, K/V 7k×2) / attn_out DMA 56% + tail 16k /
  ffn DMA 90% (weight 타일 6.3k×15, scale 2.4k×15, HBM hop 4.5k×2+2.2k).

### 판정: makespan 측정 (실측 대기)

- **배운 것:** 하루 종일 싸운 "DMA-bound"의 절반은 칩의 절반만 쓰고 있었기 때문이었다. 베이스라인의
  `Cluster = m![1 # 2]`를 의심하지 않은 것이 가장 비싼 가정이었다.
- **다음 후보:** qkv x 복제(18.4k; Q를 H-split으로 바꾸면 절반), attn_out tail 16k(scale·rmsnorm·residual),
  ffn scale 타일 36k·`DmaLoad ?`, geglu(단일 클러스터 vector 작업)도 두 클러스터로.


## V12_ffn_rows_per_pass_12

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V11_residual_1920_tiles`
- **가설:** V6 이후 FFN은 DMA 83% busy이고, dequant pass마다 설명 없는 4 KB `DmaLoad ?`(838 cycle)가 하나씩
  붙어 90 pass × 838 ≈ 75k가 DMA 큐를 차지한다. 명령 수(스케줄러 오버헤드, Sub 프리로드 ×90)도 pass에 비례.
  `ROWS_PER_PASS`를 12로 올리면 pass가 30개로 줄어 이 고정비가 1/3이 된다. 12행이 막혔던 이유는 scale VRF
  (12 × 240 × 4 B = 11.5 KB > 8 KB)였는데, up/gate의 dequant를 1920열 절반씩 두 번 하면 scale이 5.8 KB로
  들어간다(LUT pass 수는 60→... 절반 분할 때문에 up/gate는 10×2=20, down 10 → 총 30). down은 L 청크가
  1920이라 12행 scale이 그대로 5.8 KB.
- **변경 파일:** `src/device/shared/mlp.rs`
- **공유 코드 영향:** vision/audio MLP 경로 동시 영향
- **리스크:** 12행 transpose `m![L % 60 = 12 / 4], m![L % 60 = 12 % 4 # 16]` 문법 미검증 → 실패 시 contraction만
  4행 서브체인 3개로. DM: 타일 4개 동시 상주 시 f4 92 KB + bf16 92 KB (up/gate) — 여유.
- **예상:** ffn −40k ~ −70k.

### 측정

| 커널 | makespan (before → after) | RNGD cycles | speedup |
|---|---|---|---:|
| `decoder_feedforward` | 408,566 → **352,164** | — | 1.160 |
| **기하평균 (V0 대비 누적)** | | | **2.857** |

- **정확도:** 수치 동일(행 묶음만 변경). 전체 테스트 바이너리 빌드 통과.
- **측정 방식:** makespan only. 명령 수 1,112 → 632.
- **지배 context:** DMA 79.6% / Main 67.9% / Vector 67.8%. 소스별: up/gate 타일 DmaLoad 10×10,271 / down 타일
  10×6,326 / 절반 dequant 20×6,040 / down dequant 10×6,040 / up·gate contract 10×3,145 / scale 타일 DMA 51k /
  `DmaLoad ?` 30×838 = 25k (V6의 75k에서) / x 복제 18.4k / geglu relayout 11.7k.
- **컴파일 교훈:** 열 타일 뷰(`H = 1920 # 3840`)는 fetch에서 `/ 32`로 분해 불가("live input axis is left unread")
  → 패킷 전체 `m![H = 1920]`로 fetch하고 collect에서 flit로 쪼갠다. 12행 transpose
  `m![L % 60 = 12 / 4], m![L % 60 = 12 % 4 # 16]`는 된다.

### 판정: makespan 측정 (실측 대기)

- **배운 것:** pass당 고정비가 실제로 컸다(−56k). dequant는 여전히 vector 엔진(≈3.8 elem/cycle)에 묶여
  Main 180k. 다음은 Sub 컨텍스트에 vector 작업을 나누는 것(LUT는 Main 전용이므로 f8 pass 별도, 12.6 elem/cycle).

## V11_residual_1920_tiles

- **상태:** makespan 측정 (2026-09-09)
- **분기점:** `V10_attn_weight_tiles_fused_lut`
- **가설:** attn_out과 ffn 모두 `shared::residual::add`로 끝나는데, 480 원소 × 8 타일마다 Sub 프리로드(383) +
  Main add(341)가 붙어 ~5.8k. 1920 × 2 타일(f32 7.5 KB < VRF 8 KB)이면 ~1.5k.
- **변경 파일:** `src/device/shared/residual.rs` (`add`만; `add_vision`은 그대로)
- **공유 코드 영향:** full attention 경로(`full_attention_output`)도 `residual::add`를 쓴다 — Stage 2에서 같이 이득.

### 측정

| 커널 | makespan (before → after) | RNGD cycles | speedup |
|---|---|---|---:|
| `sliding_project_qkv` | 93,127 → 93,127 | — | 1.000 |
| `sliding_attention_output` | 53,848 → **50,110** | — | 1.075 |
| `decoder_feedforward` | 412,304 → **408,566** | — | 1.009 |
| **기하평균 (V0 대비 누적)** | | | **2.716** |

- **정확도:** 수치 동일(타일 폭만 변경).
- **측정 방식:** makespan only

### 판정: makespan 측정 (실측 대기)

## V10_attn_weight_tiles_fused_lut

- **상태:** makespan 측정 (2026-09-09)
- **분기점:** `V6_ffn_upgate_overlap` (`14b428d`)
- **가설:** 위 요약 보드 참조. V5(LUT 융합)를 흡수.
- **변경 파일:** `src/device/sliding/projection.rs` (`project_output`, `project_query`, `project_one_kv_matrix`)
- **공유 코드 영향:** 없음

### 측정

| 커널 | makespan (before → after) | RNGD cycles | speedup |
|---|---|---|---:|
| `sliding_project_qkv` | 95,433 → **93,127** | — | 1.025 |
| `sliding_attention_output` | 58,015 → **53,848** | — | 1.077 |
| `decoder_feedforward` | 412,304 → 412,304 | — | 1.000 |
| **기하평균 (V0 대비 누적)** | | | **2.646** |

- **변형 비교 (attn_out):** 12행×5타일을 루프 안에서 로드 61,787(버퍼 재사용으로 DMA 직렬화) → 5타일
  선로드 + LUT 융합 **53,848** → 5타일 선로드 + LUT 분리 54,969. 융합 pass는 타일당 2,183 (분리 2,800).
- **변형 비교 (qkv):** Q 4×4행 + K/V 2×4행 타일 + 융합 = **97,866 (악화)** — DMA 93% busy라 타일당
  `DmaLoad ?` 838이 큐만 늘림. 융합만 적용 = 93,127.
- **정확도:** 수치 동일(LUT 결과를 DM에 쓰지 않고 바로 contraction).
- **측정 방식:** makespan only
- **지배 context:** attn_out DMA 71.8% (x 2.1k + 타일 5×5,785 + `?` 5×838) / Main 32%; qkv DMA 90.6%.

### 판정: makespan 측정 (실측 대기)

- **배운 것:** (1) `fetch → fetch_table_lookup::<bf16> → collect → contract_outer` 한 체인은 **컴파일된다**
  (V5 확인). (2) 타일화는 Main이 임계일 때만 이득; DMA-bound 커널에서는 타일당 고정 DMA 비용(838)이 손해.
  (3) attn_out tail 15k(relayout, scale, rmsnorm, residual)는 별도 표적 → V11.
- **다음 후보:** FFN ROWS_PER_PASS=12(scale VRF를 반으로 나눠 로드) · FFN scale pass Sub 오프로드 ·
  rmsnorm 경량화(공유) · qkv x 복제 18.4k.

## V6_ffn_upgate_overlap

- **상태:** makespan 측정 (2026-09-09). 브랜치 안에서 단계별로 누적.
- **분기점:** `V9_ffn_x_via_hbm` (`5258498`)
- **가설(원문):** up/gate 직렬화 해소 + ROWS_PER_PASS 튜닝. 실제로는 "FFN의 DMA와 Main을 겹치게
  만드는 모든 것"으로 확장됐다.
- **변경 파일:** `src/device/shared/mlp.rs` (`project_up_and_gate`, `project_down`, `feedforward`)
- **공유 코드 영향:** vision/audio MLP 경로 동시 영향 (Stage 1 세 커널 중 ffn만 변함, qkv/attn_out 불변 확인)

### 단계별 측정 (ffn makespan)

| 단계 | 변경 | ffn | Δ |
|---|---|---:|---:|
| V9 | (출발점) | 574,845 | |
| V6-b | 행렬 전체 f8 사본 제거 + `fetch_table_lookup → fetch_cast` 융합 체인, 두 행렬 동시 상주, 인터리브 | 578,083 | +3k (DMA 138k가 Main 앞에 통째로) |
| V6-(0,1,4) | weight를 pass별 4행 타일로 로드 + up/gate 인터리브 | 522,379 | −56k |
| V6-c | down: 반복당 타일 2개(더블버퍼) + geglu 출력 → ByColumns 직접 relayout + down scale 선발행 | 465,394 | −57k |
| V6-d | scale도 pass별 타일, 첫 반복 peel | 462,004 | −3k |
| V6-e | dequant/contract 분리, x_trf를 첫 dequant 뒤에 생성 (head 38k→8k) | 462,004 | 0 (임계 경로가 버퍼 사이클) |
| V6-f | 반복당 4타일 선로드 | 412,304 | |

### 컴파일러/스케줄러 제약 (추가 발견)

| 시도 | 결과 |
|---|---|
| 캐리 텐서 프리페치 (`next = load(i+1)` 루프 밖으로 전달, 마지막에 `DmTensor::new()` 더미) | `V1-Modulo failed for all operator schedule heuristics` |
| `Vec<DmTensor>` | `unsupported type in device function: *const u8` |
| `std::array::from_fn(\|i\| ...)` | `internal compiler error: not yet implemented: closure` |
| `for i in 1..PASSES` | `A loop was not rewritten to for` → `0..CONST`만 허용 (`const REST = PASSES-1; for k in 0..REST`) |
| `ROWS_PER_PASS = 12` | scale VRF 11,520 B > 8 KB. 60의 약수·4의 배수·VRF 한도 → 4만 가능 |
| `fetch → fetch_table_lookup::<f8> → fetch_cast::<f32> → vector` (f4 weight) | **컴파일됨.** f8 사본 pass(18k×2 + 26k) 제거 |
| DMA 발행 순서 | 스케줄러가 소비 시점 기준으로 재배치 — 소스에서 앞당겨 발행해도 무의미. 소비자를 앞당겨야 함 |
| 같은 DM 버퍼 재사용 | WAR로 다음 로드가 이전 contract 뒤로 밀림 → 반복 안에 타일 여러 개를 동시에 살려 다른 주소를 받게 함 |

### 판정: makespan 측정 (실측 대기)

- **정확도:** dequant 수식 동일(LUT→f32→×scale→bf16), 누산 순서 동일. geglu 직접 relayout은 데이터 이동만.
- **지배 context (V6-e):** DMA 74% / Main 56% / Vector 56%. DMA busy 342k 중 pass당 `DmaLoad ?`(설명 없음, 4 KB, 838 cycle)가
  90개 ≈ 75k — LUT/엔진 설정 테이블로 추정, pass 수에 비례.
- **배운 것:** FFN은 이제 DMA-bound. 남은 레버는 (1) 버퍼 깊이(4타일), (2) pass 수 자체(ROWS_PER_PASS 제약에 막힘),
  (3) Sub 컨텍스트로 scale pass 절반 이전(LUT는 Main 전용이라 LUT를 별도 pass로 되돌려야 함 — 순이득 불확실).
- **다음 후보:** V5(attn_out/qkv LUT 융합, tail 단축) · attn_out/qkv weight 타일화(tail 단축) · Sub 오프로드.

## V5_lut_in_contract_chain

- **상태:** 설계됨 (2026-09-09)
- **분기점:** 당시 SOTA
- **가설:** V0는 f8 weight를 `fetch → fetch_table_lookup::<bf16> → collect → commit`으로 DM에
  bf16 사본을 만든 뒤 다시 `fetch → collect → contract`한다. book(Fetch Adapter):
  "all stages preserve mapping and can route directly to Switch/Collect/Contraction
  without intermediate commits". 따라서 `fetch → fetch_table_lookup → collect → contract_outer`
  한 체인으로 LUT pass(qkv 20k, attn_out 78k→V2 후 ~10k, ffn up/gate 36k)와 bf16 DM 사본을
  제거할 수 있다. contraction 스트리밍 피연산자는 bf16(LUT 출력)이므로 TRF의 bf16 x와 같은 family.
- **변경 파일:** `src/device/sliding/projection.rs`, `src/device/shared/mlp.rs`
- **공유 코드 영향:** mlp.rs 부분은 vision/audio 영향.
- **리스크:** 빌더 타입 상태가 LUT 출력 → contract를 실제로 허용하는지는 컴파일해봐야 안다.
  f4→f8 LUT(ffn)는 그 뒤에 block scale 곱이 필요하므로 이 실험 범위 밖(f8 weight만).
- **예상:** qkv −15k, attn_out −8k(V2 후), ffn −30k.

## V4_attnout_qsplit_no_broadcast

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V2` (V2가 채택되면 그 위에)
- **가설:** V3와 같은 원리를 O projection에 적용. Qs=4096을 8슬라이스로 분할(512/슬라이스),
  행은 32그룹 × 120행. x는 512 bf16 = 32 flit만 각 슬라이스에 필요 → 복제 비용
  ring-8 스테이지 + 256×32 ≈ 10k (V0 66,311). 8-way inter-slice reduce로 partial 합산.
- **변경 파일:** `src/device/sliding/projection.rs` (`project_output`), `src/ops.rs` 본문
- **공유 코드 영향:** 없음 (`layout::broadcast_sliding_heads`는 사용 중단, 삭제는 하지 않음)
- **예상:** attn_out −55k.

## V3_qkv_hsplit_no_broadcast

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V0_baseline` (독립 A/B)
- **가설:** qkv makespan의 53%(62,215)가 `layout::broadcast_hidden` 한 노드다. 이 값은 switch
  브로드캐스트의 이론 하한(256 ring × 240 flit)이므로 switch 변형으로는 못 줄인다("죽은 길").
  대신 **reduction 축 H를 8슬라이스로 분할**한다: Q weight를 32행그룹(128행) × 8 H-청크(480열)로
  슬라이스에 배치하면 각 슬라이스는 x의 480개(960B = 30 flit)만 필요. x 복제 = 1→8 슬라이스
  청크 분배(ring 8 × 240 ≈ 2k) + 청크를 32그룹에 브로드캐스트(256 × 30 ≈ 7.7k) ≈ 10k.
  partial [128행]을 `vector_inter_slice_reduce`로 8-way 합산(project_down이 이미 쓰는 패턴).
  K/V(2048행)는 256행그룹... 8행×H → 32그룹(64행) × 8청크로 동일 적용.
- **변경 파일:** `src/device/sliding/projection.rs`, `src/ops.rs` 본문 (broadcast_hidden 호출 제거)
- **공유 코드 영향:** 없음 (`layout.rs`의 broadcast 함수는 full/vision 경로가 계속 사용 — 건드리지 않음)
- **리스크:** H-청크 480 f8 = 480B는 256B 정렬이 안 됨 → DmaLoad 2× 페널티 가능(현재 DMA는
  Main 뒤에 숨어 있고 util 0.06이라 여유 있음). weight 접근이 strided가 되어 HBM row
  switch 비용 증가 가능. 스케줄의 DMA util로 확인.
- **예상:** qkv 116k → ~60k.

## V8_weight_rows_interleaved_dma

- **상태:** 설계됨 (2026-09-09)
- **분기점:** `V7`
- **가설:** V7 이후 qkv(DMA 88%)와 attn_out(DMA 57%)은 weight DmaLoad가 임계 경로다. 실효
  580 B/cycle은 피크 2,048의 28%이고 util 0.06–0.11. book(Memory Performance): "DMN interleaving:
  alternate across 2 DMNs per cluster, else 50% loss", "Slice interleaving: spread across 32 slices
  per DMN". 현재 레이아웃은 슬라이스당 61 KB(qkv Q) / 60 KB(attn_out) **연속 블록**이라 한
  슬라이스·한 DMN에 순차 기록된다. 행을 4행 블록 단위로 슬라이스에 교차 배치
  (`m![H / 4 % 64, Qs / 1024]`, element `m![H / 256, H % 4, ...]`)하면 HBM 순차 읽기가 연속
  슬라이스로 번갈아 들어간다. 4행 블록인 이유: transpose 패킷(4행)과 `to_dm` permutation(8 B 단위)
  제약. 먼저 attn_out에서 프로브, 효과 있으면 qkv(Q/K/V)와 ffn(up/gate/down)에 확장.
- **변경 파일:** `src/device/sliding/projection.rs` (매핑만), 이후 `mlp.rs`
- **공유 코드 영향:** 1차는 없음
- **리스크:** DMA 엔진이 디스크립터를 어떻게 병렬화하는지 모른다 — 효과가 0일 수 있다.
  출력 벡터가 블록 permutation된 채 나오므로 `to_dm` relayout이 8 B 조각 960개를 옮겨야 한다.
- **예상:** attn_out weight DMA 26.7k → 10k 이하면 성공.

### 측정 (attn_out만 프로브)

| 커널 | makespan (before → after) | RNGD cycles | speedup |
|---|---|---|---:|
| `sliding_attention_output` | 58,015 → 59,225 | — | 0.980 |

- weight DmaLoad: **26,734 → 26,734, util 0.433 → 0.433 (완전히 동일)**. 출력 permutation 때문에
  `contraction.to_dm` relayout만 952 → 2,162로 늘었다.
- **측정 방식:** makespan only

### 판정: **기각** (스케줄러의 DMA 비용 모델은 슬라이스 배치 순서를 보지 않는다)

- **이유:** 정적 스케줄에서 DmaLoad 비용은 바이트 수와 디스크립터 형태로만 정해진다. 실물에서는
  DMN 인터리빙이 영향을 줄 수 있으나 검증 수단이 없다. Arena 실측이 되면 V7 vs V8 브랜치를 그대로
  A/B 제출할 가치는 있다(둘 다 push됨). 코드는 `V8_weight_rows_interleaved_dma` 브랜치에만 있다.
- **배운 것:** makespan 관점에서 DMA는 **바이트 수**와 **겹침**으로만 줄어든다.

## V7_qkv_x_replicate_via_hbm

- **상태:** makespan 측정 (2026-09-09)
- **분기점:** `V2_attnout_rows_over_256_slices` (`6403c3a`)
- **가설:** x 복제 비용 — switch 61k(하한), DM→DM DMA ~70 B/cycle — 대신 HBM→DM 로드.
  프로브 결과 커널 안에서 `HbmTensor::new()`가 **컴파일된다**. attn_out은 입력 `x`가 이미
  HBM `[Ns, Gs, Ds]`이므로 `unsafe { x.view().reshape() }`로 `HbmTensorView<m![Qs]>`를 만들어
  `project_output`이 슬라이스별 1024-청크를 직접 로드한다.
- **변경 파일:** `src/ops.rs` 본문 2곳, `src/device/sliding/projection.rs` (`project_output` 시그니처)
- **공유 코드 영향:** 없음 (`layout::broadcast_*`는 미사용으로 남음 — dead_code 경고만)

### 측정

| 커널 | makespan (before → after) | RNGD cycles | speedup (makespan) |
|---|---|---|---:|
| `sliding_project_qkv` | 116,583 → **95,433** | — | 1.222 |
| `sliding_attention_output` | 106,461 → **58,015** | — | 1.835 |
| `decoder_feedforward` | 609,223 → 609,223 | — | 1.000 |
| **기하평균 (V0 대비 누적)** | | | **2.250** |

- **정확도:** — (Arena 대기). 수치 변화 없음(복제 경로만 변경). qkv의 스크래치는 별도
  `HbmTensor::new()`라 `q_out`과 무관.
- **측정 방식:** makespan only
- **지배 context (변경 후):** qkv DMA 88.4% (Q 26,475 / x 18,400 / K,V 13,512×2 직렬) · Main 51.9%;
  attn_out DMA 57.1% (weight 26,734) · Main 35.2%.
- **HBM→DM Replicated 로드 실효:** 3.9 MB / 18,400 = 213 B/cycle (DM→DM 72 B/cycle의 3배,
  HBM 순차 로드 580 B/cycle의 1/3 — 같은 7.5 KB를 512번 읽는 패턴 한계로 추정).

### 판정: makespan 측정 (실측 대기)

- **배운 것:** (1) 커널 내 HBM 스크래치 할당 가능 → 레이아웃 변환의 자유도가 크게 늘었다.
  (2) 두 attention 커널 모두 이제 **weight DMA-bound** — 다음은 DMA 접근 패턴(V8).
  (3) FFN의 x→Replicated DmaStos 54k도 같은 기법으로 ~18k로 줄일 수 있다 (V9로 등록).
- **다음 후보:** V8(DMA 인터리빙) → V9(ffn x via HBM) → V6(up/gate 오버랩) → V5(LUT 융합).

## V2_attnout_rows_over_256_slices

- **상태:** makespan 측정 (2026-09-09)
- **분기점:** `V1_ffn_down_chunked_dequant` (`f588d2a`) — attn_out은 mlp.rs를 쓰지 않으므로 V0 분기와 동일
- **가설:** 원문은 위 요약 보드. 최종 구현은 15행×256이 아니라 **H/60 × Qs/1024 = 64 × 4 슬라이스**
  (슬라이스당 60행 × 1024열, 4-way `vector_inter_slice_reduce`). 8번의 컴파일 실패를 거쳐 도달했고
  그 제약들은 "컴파일러가 강제하는 매핑 제약" 표에 정리했다.
- **변경 파일:** `src/device/sliding/projection.rs` (`project_output` 재작성, `output_partial`·
  `add_partials` 삭제, `apply_output_channel_scale`은 Slice 레이아웃 1920-타일 2회로)
- **공유 코드 영향:** 없음

### 측정

| 커널 | makespan (before → after) | RNGD cycles | speedup (makespan) |
|---|---|---|---:|
| `sliding_project_qkv` | 116,583 → 116,583 | — | 1.000 |
| `sliding_attention_output` | 194,020 → **106,461** | — | **1.822** |
| `decoder_feedforward` | 609,223 → 609,223 | — | 1.000 |
| **기하평균 (V0 대비 누적)** | | | **1.723** |

- **정확도:** — (Arena 대기). 수치 변화: Qs 합산이 bf16 partial 3회 add(f32 왕복)에서 f32
  inter-slice reduce 1회로 → 오히려 정밀해짐.
- **측정 방식:** makespan only
- **지배 context (변경 후):** Main 81.4% / DMA 45.5% / Vector 9.9%. 잔여 상위:
  broadcast_sliding_heads 66,311 / weight DmaLoad 26,734 (브로드캐스트와 겹침) /
  x.to_dm 16,128 / LUT 9,863 / contract 4,105.

### 판정: makespan 측정 (실측 대기)

- **배운 것:** (1) live 슬라이스 수는 2의 거듭제곱, 행/슬라이스는 4의 배수 — H=3840에서는
  60·120만 가능. (2) 두 축을 함께 슬라이스에 나누고 inter-slice reduce로 합치는 것이
  "행 수가 안 나눠지는" 문제의 정답. (3) DM→DM 분배 DMA는 ~65-70 B/cycle — x 분배 16k는
  V7에서 HBM 경유로 줄일 것. (4) attn_out은 이제 x 분배(82k)가 makespan의 77%.
- **다음 후보:** V7 → V5(LUT 체인 융합; LUT 9.9k) → V6.

## V1_ffn_down_chunked_dequant

- **상태:** makespan 측정 (2026-09-09) — ffn 1,693,200 → **609,223** (2.779×). 첫 컴파일 통과.
  잔여 상위: x→Replicated DmaStos 54,432 / up·gate weight DmaLoad 49,157×2 / down scale DmaLoad 18,998 /
  down weight DmaLoad 30×2,473=74k / down scale pass 30×2,201=66k / up·gate scale pass 61,815×2.
  이제 ffn은 Main 52.9% / DMA 51.0% — DMA와 Main이 대등. 정확도: 누산 순서 불변(같은 8-way reduce).
- **분기점:** `V0_baseline`
- **가설:** ffn makespan 1.69M 중 **down projection이 ~1.2M**이다:
  - mlp.rs:393 scale pass 60 × 7,961 = 477,660 (28%) — 슬라이스당 2행 × L=15360 전체를 dequant.
  - mlp.rs:409/411 DMA 재배치 60 × 4,275 × 2 = 513,000 (30%) — `DownRows`(행 전체) →
    `DownRowsByColumns`(L을 8슬라이스로 분할)로 옮기는 DM→DM 복사.
  - mlp.rs:372 LUT pass 159,780. 정작 contraction(:419)은 22,560뿐.
  `DownRows = m![H/120, 1#8]`이라 8슬라이스 그룹이 같은 행 전체를 받아 dequant한 뒤 각자
  1/8만 쓴다 → **vector 작업이 필요량의 8배**. 처음부터 각 슬라이스가 자기 (4행 × L/1920)
  청크만 HBM에서 받아(`DownRowsByColumns` 레이아웃으로 직접 `to_dm`) 그 청크만 LUT·scale하면
  scale pass ≈ 60k, LUT ≈ 20k, 재배치 DMA 0.
- **변경 파일:** `src/device/shared/mlp.rs` (`project_down` 루프; `HALVES_PER_PASS` 제거)
- **공유 코드 영향:** `shared/mlp.rs` → vision/audio MLP 경로 동시 영향. 세 커널 makespan 기록.
- **리스크:** 행당 L/8 청크 = 960B(f4)는 256B 정렬이 안 됨 → HBM read 2× 페널티 가능. 현재
  down weight DmaLoad는 81k(4.8%)라 2배가 되어도 재배치 513k 제거가 압도. block scale
  `[H, L/16]`도 같은 방식으로 청크 로드(120 scale/행/슬라이스).
- **예상:** ffn 1.69M → 0.6~0.7M (**단독으로 기하평균 ~1.35×**).

## V0_baseline

- **상태:** makespan 측정 (2026-09-09). RNGD 실측은 Arena 승인 후.
- **분기점:** `main` (`4bf1bac`)
- **가설:** 없음 — 기준선. 커널 코드 원본 그대로.
- **변경 파일:** 없음 (문서·환경 파일만)
- **공유 코드 영향:** 없음

### 측정

| 커널 | makespan | instructions | RNGD cycles |
|---|---:|---:|---|
| `sliding_project_qkv` | 116,583 | 131 | — |
| `sliding_attention_output` | 194,020 | 163 | — |
| `decoder_feedforward` | 1,693,200 | 1,802 | — |

- **정확도:** — (Arena 승인 후 `./scripts/rngd_test.sh`)
- **측정 방식:** makespan only (`cargo furiosa-opt compile --exact --dump-schedule`, cargo-furiosa-opt 0.6.0)
- **지배 context:**
  - qkv: Main 95.9% / DMA 56.9% / Vector 17.4% / Sub 9.3%
  - attn_out: Main 93.8% / Vector 19.5% / DMA 18.5% / Sub 6.3%
  - ffn: Main 51.3% / DMA 49.8% / Vector 39.7% / Sub 7.7%

### 소스 라인별 cycle 분해 (상위)

**qkv (116,583)**

| cycle | n | %  | 무엇 |
|---:|---:|---:|---|
| 62,215 | 1 | 53.4 | `layout.rs:16` broadcast_hidden (switch ring 256) |
| 27,024 | 2 | 23.2 | `projection.rs:93` K/V weight DmaLoad |
| 26,475 | 1 | 22.7 | `projection.rs:24` Q weight DmaLoad |
| 10,126 | 2 | 8.7 | `projection.rs:94` K/V LUT pass |
| 9,863 | 1 | 8.5 | `projection.rs:25` Q LUT pass |
| 4,370 | 2 | 3.7 | `projection.rs:103` K/V contract |
| 4,105 | 1 | 3.5 | `projection.rs:34` Q contract |

**attn_out (194,020)**

| cycle | n | % | 무엇 |
|---:|---:|---:|---|
| 77,852 | 4 | 40.1 | `projection.rs:216` O weight LUT pass ×4 청크 |
| 66,311 | 1 | 34.2 | `layout.rs:32` broadcast_sliding_heads |
| 31,780 | 4 | 16.4 | `projection.rs:224` contract ×4 |
| 27,864 | 4 | 14.4 | `projection.rs:212` weight DmaLoad ×4 |

**ffn (1,693,200)**

| cycle | n | % | 무엇 |
|---:|---:|---:|---|
| 477,660 | 60 | 28.2 | `mlp.rs:393` down weight scale pass |
| 256,500 | 60 | 15.1 | `mlp.rs:409` down weight 재배치 DmaStos |
| 256,500 | 60 | 15.1 | `mlp.rs:411` down weight `to_dm_view` DmaStos |
| 159,780 | 60 | 9.4 | `mlp.rs:372` down LUT pass |
| 81,000 | 60 | 4.8 | `mlp.rs:368` down weight DmaLoad |
| 61,815 ×2 | 15 ×2 | 7.3 | `mlp.rs:57/:120` up/gate scale pass |
| 54,432 | 1 | 3.2 | `ops.rs:227` x → Replicated DmaStos |
| 49,157 ×2 | 1 ×2 | 5.8 | `mlp.rs:31/:94` up/gate weight DmaLoad |
| 22,560 | 30 | 1.3 | `mlp.rs:419` down contract |
| 18,263 ×2 | 1 ×2 | 2.2 | `mlp.rs:32/:95` up/gate LUT pass |

### 판정: **기준**

- **배운 것:**
  1. 세 커널 모두 **MainContext-bound**이며, 그 Main 시간의 대부분은 contraction이 아니라
     **dequant pass와 브로드캐스트**다. contraction 자체는 qkv 8.5k, attn_out 32k, ffn 60k 정도.
  2. HBM 실효 대역폭이 피크의 28%(util 0.06~0.11) — DMA는 Main 뒤에 숨어 있어 지금은
     병목이 아니지만 Main을 줄이면 곧 드러난다. 그때는 접근 패턴(정렬·strided)이 다음 과제.
  3. ffn의 down projection 구조(행 전체 복제 후 8-way 분할)가 전체 점수의 최대 레버.
- **다음 후보:** V1 → V2 → V5 → V3/V4 → V6 순.
