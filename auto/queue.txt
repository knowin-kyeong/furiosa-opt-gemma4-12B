# auto/queue.txt — the unattended chain's work list (scripts/dev/auto/driver.sh on the pod).
# One entry per line: BRANCH or BRANCH@COMMIT. Order = execution order. Lines starting with #
# are ignored. The driver re-reads this file after every entry and every idle poll (10 min), so
# entries may be appended while it runs. Entries with a final result are skipped.
#
# IMPORTANT: only add a line after the branch is pushed. A missing ref is recorded as
# `checkout-failed`, which is a FINAL result and is never retried.
#
# 2026-09-09, second run. Settled: V50_drop_false_replication is the measured SOTA at 4.972x
# (149,244 / 56,522 / 354,825, 3/3 PASS). qkv is the only bottleneck left: its measured speedup
# is 1.64 against attn_out 7.17 and ffn 10.45, and its real/makespan ratio (2.53) is the worst
# of the three, so the static model is least trustworthy exactly where the work now is.
V50_drop_false_replication
V52_qkv_x_copies_c2
V54_qkv_x_copies_c4
V55_qkv_x_copies_c8
V56_qkv_x_copies_c16
